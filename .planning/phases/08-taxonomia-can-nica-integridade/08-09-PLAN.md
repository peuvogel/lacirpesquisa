---
phase: 08-taxonomia-can-nica-integridade
plan: 09
type: execute
wave: 6
depends_on: [08-07]
files_modified:
  - src/routes/mapas/MeasureDiseasePicker.tsx
  - src/routes/mapas/MeasureDiseasePicker.test.tsx
  - src/features/catalog/diseaseAliases.ts
  - src/features/catalog/diseaseAliases.test.ts
  - src/routes/variaveis/VariaveisPage.tsx
  - src/routes/variaveis/VariaveisPage.test.tsx
autonomous: true
requirements: [TAX-05]

must_haves:
  truths:
    - "O estudante digita AVC no seletor de Mapas e recebe as 3 categorias canonicas corretas, selecionaveis (TAX-05)"
    - "As N categorias aparecem como linhas normais, com uma tira explicativa acima — nunca uma linha agregada que nao existe no dado (D-18)"
    - "O rotulo exibido, selecionado e copiado e sempre o oficial da Lista Morb, identico por qualquer caminho de busca; nenhum badge de apelido (D-19)"
    - "aterosclerose acha Arteroesclerose sem entrada curada, porque a dobra de acento e o fuzzy entram no picker junto (D-17)"
    - "A busca de Variaveis tambem deixa de perder AVC apos a canonizacao, pelo campo aliases que o filterCatalog ja consome"
    - "Nenhum apelido vira chave de dado em nenhum dos dois caminhos"
  artifacts:
    - path: "src/routes/mapas/MeasureDiseasePicker.tsx"
      provides: "Busca por apelido plugada em diseaseMatches e a tira explicativa do D-18"
      contains: "corresponde a"
    - path: "src/routes/mapas/MeasureDiseasePicker.test.tsx"
      provides: "Cobertura de comportamento da busca por apelido no componente real"
      min_lines: 60
  key_links:
    - from: "src/routes/mapas/MeasureDiseasePicker.tsx"
      to: "src/features/catalog/diseaseAliases.ts"
      via: "import de matchDiseases e labelMatchesQuery dentro de diseaseMatches"
      pattern: "diseaseAliases"
    - from: "src/routes/variaveis/VariaveisPage.tsx"
      to: "src/features/catalog/diseaseAliases.ts"
      via: "enriquece CatalogEntry.aliases antes de chamar filterCatalog"
      pattern: "withDiseaseAliases"
---

<objective>
Plugar a funcao pura de apelidos no seletor de agravos de Mapas, com a tira explicativa do D-18, e decidir a questao em aberto sobre a busca de Variaveis.

Purpose: a plan 08-07 entregou o matcher e o dicionario, mas nenhum estudante os alcanca ainda. Sem esta plan, TAX-05 nao existe na interface e a canonizacao piora a busca em vez de melhorar: antes da migracao "avc" achava o agravo porque o id **era** `avc`; depois, nenhum dos tres ids do AVC verdadeiro contem essa substring, e os rotulos oficiais tambem nao.

Output: `diseaseMatches` delegando ao matcher, a tira explicativa acima da lista, cobertura de comportamento no componente real, e a cobertura de Variaveis resolvida com justificativa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md
@src/routes/mapas/MeasureDiseasePicker.tsx
@src/features/catalog/filterCatalog.ts
@.planning/phases/08-taxonomia-can-nica-integridade/08-07-SUMMARY.md

<interfaces>
<!-- Contratos entregues pela 08-07 e pontos de plugue ja mapeados. Nao explorar o componente. -->

`src/features/catalog/diseaseAliases.ts` exporta:
- `foldAccents(texto)` — NFKD, remocao de marcas combinantes, minusculas
- `labelMatchesQuery(label, query)` — AND entre tokens da query, substring, e Levenshtein menor ou igual a 2 somente quando os dois tokens tem 6 ou mais caracteres
- `resolveAliasTerm(query, aliases)` — casamento exato do termo inteiro, devolve a entrada curada ou nulo
- `matchDiseases(diseases, query, aliases)` — devolve os agravos casados mais, quando houver, o termo de apelido acionado e quantas categorias ele resolveu

`src/features/catalog/aliases.json` — 4 entradas (`avc`, `tvp`, `ait`, `varizes`), cada uma com termos, categorias com `tabnetCode` e `labelEsperado`, e motivo. Chaveado por codigo, nunca por slug.

Pontos de plugue confirmados em `src/routes/mapas/MeasureDiseasePicker.tsx`:
- linhas 49-51: `normalizeCidQuery(q)` — a normalizacao de CID, que continua como esta
- linhas 53-60: `diseaseMatches(disease, q)` — funcao pura, sem estado, que hoje casa `disease.label.toLowerCase().includes(lower)`, `disease.id.includes(lower)` e depois o CID normalizado
- linha 92-104: o `useMemo` `visibleDiseases`, cujo unico ponto de filtro e `DISEASES.filter((d) => diseaseMatches(d, q))` na linha 94; ele ja separa selecionados, com pack e sem pack
- linhas 249-262: o `input` de busca com `aria-label` "Buscar doença" e, logo abaixo, o paragrafo de contagem que hoje mostra `${visibleDiseases.length} resultado(s)` ou `${DISEASES.length} categorias · busque pelo nome ou pelo CID-10`
- linha 263 em diante: o `ul` `role="list"` com as linhas de agravo, cada uma com `input type="checkbox"`, `Badge` de CID e o `disease.label`
- multi-selecao ja existe: `selectedDiseaseSet` e `onToggleDisease`/`toggleDisease`. **Nao ha UI de selecao nova a construir**

`src/routes/variaveis/VariableList.tsx` **nao faz busca propria** — recebe `entries: CatalogEntry[]` ja filtrados. Quem filtra e `src/routes/variaveis/VariaveisPage.tsx` na linha 90, com `filterCatalog(variables, filters)`.
`src/features/catalog/filterCatalog.ts`, funcao `matchesQuery` (linhas 12-30), ja monta o haystack com `entry.label`, `entry.id`, `entry.sourceSystem`, `entry.sourceName`, `entry.domain`, `entry.tableOrIndicator`, `entry.period`, `entry.methodologyNotes` e `...(entry.aliases ?? [])`, exigindo que todo token da query esteja presente. O campo opcional `aliases?: string[]` ja existe em `CatalogEntry` (`src/features/catalog/types.ts:31`) e ja e consumido — **e um ponto de extensao pronto, nao um campo novo**.

Estado pos-flip relevante: `DISEASES` tem 331 entradas. Os codigos das entradas curadas que possuem pack fisico hoje sao `178` e `179` (dois dos tres do AVC) e `185` (TVP); os demais codigos curados nao tem pack.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plugar o matcher em diseaseMatches, de forma aditiva</name>

  <read_first>
    - `src/routes/mapas/MeasureDiseasePicker.tsx` — linhas 1-110, com atencao a `normalizeCidQuery` (49-51), `diseaseMatches` (53-60) e o `useMemo` `visibleDiseases` (92-104)
    - `src/features/catalog/diseaseAliases.ts` — as quatro exportacoes e o que cada uma devolve
    - `src/features/catalog/aliases.json` — as 4 entradas
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-17 e D-19
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 6, que confirma o ponto de plugue e que a funcao ja e pura
  </read_first>

  <files>src/routes/mapas/MeasureDiseasePicker.tsx</files>

  <action>
Trocar, dentro de `diseaseMatches`, o casamento de rotulo por `labelMatchesQuery(disease.label, q)`. E uma troca aditiva em capacidade: o `includes` atual e substring simples e sensivel a acento; `labelMatchesQuery` mantem o casamento por substring, acrescenta dobra de acento, AND entre tokens da query e fuzzy acima de 6 caracteres. Manter as duas outras condicoes como estao — `disease.id.includes(lower)` e o ramo de CID com `normalizeCidQuery`. O ramo de `id` deixa de casar apelido por acidente depois da canonizacao, o que e justamente o motivo desta plan existir, mas continua util para query em forma de slug.

Calcular o apelido uma vez por query, nao por agravo: no `useMemo` `visibleDiseases`, chamar `matchDiseases(DISEASES, q, ALIASES)` e usar o conjunto de agravos que ele devolve unido ao resultado de `diseaseMatches`. O `pool` da linha 94 passa a ser a uniao dos dois, sem duplicar. Preservar intacta a ordenacao que ja existe abaixo: selecionados primeiro, depois os com pack, depois os sem pack.

Expor tambem, do mesmo `useMemo` ou de um segundo, o resultado do apelido — termo acionado e numero de categorias — porque a Task 2 precisa dele para a tira. Nao usar estado novo: a informacao e derivada da query, exatamente como `visibleDiseases`.

Nao reescrever o componente. As unicas alteracoes sao: dois imports, o corpo de `diseaseMatches`, a composicao do `pool`, e a exposicao do resultado de apelido. `selectedDiseaseSet`, `onToggleDisease`, `toggleDisease`, `preferredMeasureId` e toda a arvore de render continuam como estao — a multi-selecao que o D-18 usa ja existe.

Atualizar o texto do paragrafo de contagem que hoje diz `${DISEASES.length} categorias`: o numero passa a ser 331 automaticamente por vir de `DISEASES.length`, entao nao ha literal a mudar. Conferir que nao existe nenhum `330` escrito a mao no componente.
  </action>

  <verify>
    <automated>npm run typecheck && npx vitest run src/routes/mapas</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "diseaseAliases" src/routes/mapas/MeasureDiseasePicker.tsx` retorna pelo menos `1`
    - `grep -c "label.toLowerCase().includes" src/routes/mapas/MeasureDiseasePicker.tsx` retorna `0`
    - `grep -c "normalizeCidQuery" src/routes/mapas/MeasureDiseasePicker.tsx` continua retornando pelo menos `2` — o ramo de CID nao foi removido
    - `grep -c "330" src/routes/mapas/MeasureDiseasePicker.tsx` retorna `0`
    - `grep -c "useState" src/routes/mapas/MeasureDiseasePicker.tsx` continua igual ao valor anterior a esta task — nenhum estado novo
    - `matchDiseases` e chamado uma vez por query, dentro de `useMemo`, e nao dentro de `DISEASES.filter`
    - `npm run typecheck` sai com codigo 0
    - `npx vitest run src/routes/mapas` continua verde
  </acceptance_criteria>

  <done>A busca do seletor passa por apelido, dobra de acento e fuzzy medido, sem que nenhuma outra parte do componente tenha sido reescrita.</done>
</task>

<task type="auto">
  <name>Task 2: Tira explicativa do D-18 e cobertura de comportamento</name>

  <read_first>
    - `src/routes/mapas/MeasureDiseasePicker.tsx` — linhas 249-300: o input de busca, o paragrafo de contagem e o inicio do `ul` com as linhas de agravo
    - `src/routes/mapas/SharedDiseasePanel.test.tsx` — o padrao de teste de componente ja usado nesta pasta
    - `src/test/flowHelpers.ts` — os helpers de fluxo criados na Fase 7
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-18 e D-19, incluindo a alternativa rejeitada
    - `src/features/catalog/aliases.json` — os termos e a contagem de categorias de cada entrada
  </read_first>

  <files>src/routes/mapas/MeasureDiseasePicker.tsx, src/routes/mapas/MeasureDiseasePicker.test.tsx</files>

  <action>
Acrescentar a tira explicativa entre o paragrafo de contagem e o `ul` da lista, renderizada somente quando um apelido foi acionado. Para apelido que resolve varias categorias, o texto e o termo em maiusculas seguido de "corresponde a", o numero de categorias, "categorias da Lista Morb CID-10 — selecione as que quiser comparar ou somar." Para apelido que resolve uma unica categoria, a mesma forma no singular, terminando em "categoria da Lista Morb CID-10" seguida do rotulo oficial dessa categoria. PT-BR didatico, no mesmo registro do resto da interface.

A tira e um elemento **acima da lista**, nunca dentro de uma linha. As N categorias continuam sendo linhas normais, selecionaveis pelos mesmos checkboxes de sempre. Foi rejeitada explicitamente a alternativa de mostrar uma linha agregada tipo "AVC (3 categorias)": criaria uma pseudo-entidade que nao existe no dado e tomaria em silencio uma decisao estatistica pelo estudante.

D-19 e a restricao dura desta task: o apelido vive **so no indice de busca**. Nenhum badge de apelido, nenhum rotulo que dependa do caminho de busca. O rotulo exibido, selecionado e copiado e sempre o oficial da Lista Morb — o mesmo que o estudante reencontra no TabNet e cita no trabalho. A tira e informacao de contexto sobre a busca, nao rotulo de dado.

Dar a tira um papel acessivel que a associe a lista de resultados, por exemplo `role="status"`, para que o leitor de tela anuncie a mudanca quando o termo dispara o apelido.

Criar `src/routes/mapas/MeasureDiseasePicker.test.tsx` — o componente nao tem teste hoje. Casos:
- digitar `avc` no campo com `aria-label` "Buscar doença" faz aparecer exatamente 3 linhas de agravo, com os rotulos oficiais de `Hemorragia intracraniana`, `Infarto cerebral` e `Acid vascular cerebr nao espec hemorrag ou isquem`, lidos de `aliases.json` e nao digitados no teste
- a tira aparece com o texto do D-18 citando 3 categorias
- as 3 linhas sao selecionaveis: clicar num checkbox chama `onToggleDisease` com o **id canonico**, nunca com o termo `avc`
- digitar `infarto cerebral` acha a mesma linha, e o texto da linha e **identico** ao obtido pelo caminho `avc` — a prova de D-19, de que o rotulo nao varia por caminho de busca
- nenhum elemento renderizado contem o texto `avc` fora do proprio campo de busca — nada de badge de apelido
- digitar `tvp` mostra 1 categoria e a tira no singular
- digitar `aterosclerose` acha a linha cujo rotulo oficial e `Arteroesclerose`, sem que exista entrada curada para esse termo — o caso que prova que a regra automatica cobre o que alcanca
- digitar um termo sem resultado mostra `0 resultado(s)` e nenhuma tira
  </action>

  <acceptance_criteria>
    - `npx vitest run src/routes/mapas/MeasureDiseasePicker.test.tsx` passa com pelo menos 8 casos
    - O caso de `avc` afirma exatamente 3 linhas e os 3 rotulos oficiais derivados de `aliases.json`
    - O caso de igualdade de rotulo entre os caminhos `avc` e `infarto cerebral` esta presente e verde
    - O caso que afirma ausencia do texto `avc` fora do campo de busca esta presente e verde
    - `grep -c "corresponde a" src/routes/mapas/MeasureDiseasePicker.tsx` retorna pelo menos `1`
    - `grep -ci "badge" src/routes/mapas/MeasureDiseasePicker.tsx` nao aumentou em relacao ao valor anterior a esta task — nenhum badge novo
    - A tira so renderiza quando ha apelido acionado: buscar `pneumonia` nao renderiza nenhuma tira
    - `npm run gate` verde
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run src/routes/mapas/MeasureDiseasePicker.test.tsx</automated>
    <human-check>Abrir Mapas, ir ao seletor de doenca, digitar "avc": confirmar que aparecem as 3 categorias como linhas normais selecionaveis, que a tira acima explica a correspondencia, e que o rotulo mostrado e sempre o oficial da Lista Morb. Repetir com "tvp", "ait" e "aterosclerose".</human-check>
  </verify>

  <done>O estudante que digita um termo da liga chega as categorias canonicas certas, entende por que sao varias, e nunca ve um rotulo que so existe dentro do produto.</done>
</task>

<task type="auto">
  <name>Task 3: Decidir e implementar a cobertura de apelido na busca de Variaveis</name>

  <read_first>
    - `src/routes/variaveis/VariaveisPage.tsx` — linhas 1-40 e a linha 90, onde `filterCatalog(variables, filters)` e chamado
    - `src/features/catalog/filterCatalog.ts` — linhas 12-30, `matchesQuery`, e o consumo ja existente de `entry.aliases`
    - `src/features/catalog/types.ts` — linha 31, o campo `aliases?: string[]` em `CatalogEntry`
    - `src/routes/variaveis/VariableList.tsx` — confirmar que ele nao faz busca propria, so agrupa e ordena o que ja chegou filtrado
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — a secao `deferred`, onde esta questao esta registrada como "levantada e nao discutida, decidir no planejamento"
  </read_first>

  <files>src/features/catalog/diseaseAliases.ts, src/features/catalog/diseaseAliases.test.ts, src/routes/variaveis/VariaveisPage.tsx, src/routes/variaveis/VariaveisPage.test.tsx</files>

  <action>
Resolver a questao que o CONTEXT deixou em aberto, com a decisao e o motivo registrados no SUMMARY.

**Decisao: sim, a busca de Variaveis recebe a camada de apelido.** O motivo e concreto e mensuravel, nao preferencia: dois dos tres codigos do AVC (178 e 179) e o codigo da TVP (185) tem pack fisico e portanto tem entradas em `variables.json`. Antes da canonizacao, buscar "avc" em Variaveis achava essas entradas porque o `id` delas era `sih.avc.*`. Depois do flip, os ids sao `sih.infarto_cerebral.*` e `sih.acid_vascular_cerebr_nao_espec_hemorrag_ou_isquem.*`, e nenhum rotulo contem "AVC" — buscar "avc" em Variaveis passaria a devolver zero. Deixar de cobrir seria a canonizacao piorando a busca numa segunda tela, que e exatamente o que TAX-05 existe para impedir.

`VariableList.tsx` **nao** recebe alteracao: ele nao faz busca propria, so agrupa e ordena o que ja chegou filtrado. O ponto unico e `VariaveisPage.tsx` na linha 90.

A implementacao usa o ponto de extensao que ja existe, sem tocar em nenhum artefato gerado: `filterCatalog.matchesQuery` ja inclui `...(entry.aliases ?? [])` no haystack, e `CatalogEntry.aliases` ja e opcional no tipo. Acrescentar a `diseaseAliases.ts` uma funcao pura `withDiseaseAliases(entries, diseases, aliases)` que, para cada entrada com `packId`, descobre o agravo daquele pack, e se o `tabnetCode` dele aparecer em alguma categoria do dicionario, acrescenta os termos daquela entrada ao array `aliases` da entrada, preservando os apelidos que ja existirem. Aplicar em `VariaveisPage` dentro de um `useMemo` sobre `variables`, antes de `filterCatalog`.

Fazer assim, e nao gravando os termos dentro de `public/data/catalog/variables.json`, e deliberado: `variables.json` e artefato gerado e esta sob os invariantes B e D2; injetar dado de apresentacao nele criaria uma lista mantida em paralelo com a fonte canonica, que e exatamente o que TAX-06 proibe. O apelido continua vivendo so no indice de busca, em memoria, coerente com D-19.

Acrescentar casos a `src/features/catalog/diseaseAliases.test.ts` cobrindo `withDiseaseAliases`: uma entrada cujo pack e do codigo 178 recebe os termos de `avc`; uma entrada de pack sem codigo curado nao recebe nenhum termo; uma entrada que ja tinha `aliases` preserva os antigos e ganha os novos; entrada sem `packId` passa intacta.

Acrescentar a `src/routes/variaveis/VariaveisPage.test.tsx` um caso de comportamento: digitar "avc" na busca de Variaveis devolve pelo menos uma entrada, e o rotulo dessa entrada e o oficial, sem nenhuma mencao a "AVC".

Registrar no SUMMARY a decisao completa e o que ficou fora: **busca fuzzy sobre o campo CID continua adiada** — foi levantada junto com esta questao no CONTEXT, nao tem caso de uso medido, e o campo CID ja e casado por prefixo e substring normalizados no picker.
  </action>

  <verify>
    <automated>npx vitest run src/features/catalog/diseaseAliases.test.ts src/routes/variaveis/VariaveisPage.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c withDiseaseAliases src/routes/variaveis/VariaveisPage.tsx` retorna pelo menos `1`
    - `git diff --stat src/routes/variaveis/VariableList.tsx` nao mostra alteracao
    - `git diff --stat public/data/catalog/variables.json` nao mostra alteracao — nenhum apelido gravado em artefato gerado
    - `git diff --stat src/features/catalog/filterCatalog.ts` nao mostra alteracao — o ponto de extensao ja existia
    - `npx vitest run src/features/catalog/diseaseAliases.test.ts` passa com os 4 casos novos de `withDiseaseAliases`
    - O caso de comportamento em `VariaveisPage.test.tsx` afirma resultado nao vazio para "avc" e rotulo oficial na entrada devolvida
    - `npm run catalog:validate` continua saindo com codigo 0 — os invariantes B e D2 nao foram perturbados
    - `npm run gate` verde
    - O SUMMARY registra a decisao sobre Variaveis e a manutencao do adiamento do fuzzy sobre CID
  </acceptance_criteria>

  <done>A segunda tela que consome a taxonomia tambem encontra o agravo pelo termo da liga, usando um campo que ja existia, sem escrever apelido em nenhum artefato gerado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query digitada pelo estudante para render | Texto livre atravessa o matcher e vira lista de agravos e uma tira de texto na tela |
| Camada de apresentacao para chave de dado | O id que sai do picker vira `disease_id` na sessao de analise e no handoff para Estatistica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Spoofing | Rotulo que varia por caminho de busca, fazendo o estudante citar no trabalho um nome que nao existe no TabNet | mitigate | D-19: apelido so no indice de busca; teste afirma que a linha alcancada por `avc` tem texto identico a alcancada por `infarto cerebral`, e que `avc` nao aparece fora do campo de busca |
| T-08-09-02 | Tampering | Apelido virando chave de dado ao selecionar | mitigate | `onToggleDisease` recebe o id canonico; teste afirma o argumento exato |
| T-08-09-03 | Spoofing | Linha agregada tipo "AVC (3 categorias)" criando pseudo-entidade inexistente no dado | mitigate | D-18 rejeitou explicitamente essa forma; a tira e elemento separado acima da lista, nunca uma linha |
| T-08-09-04 | Tampering | Apelido gravado em `variables.json`, criando lista mantida em paralelo com a fonte canonica | mitigate | `withDiseaseAliases` enriquece em memoria; criterio de aceite verifica que o artefato gerado nao mudou |
| T-08-09-05 | Denial of Service | Matcher rodando por agravo a cada tecla | mitigate | `matchDiseases` chamado uma vez por query dentro de `useMemo`, nunca dentro do `filter` |
| T-08-09-06 | Tampering | Query com metacaractere quebrando o matcher | mitigate | Tokenizacao por classe de caractere; nenhuma `RegExp` construida a partir da query, nem no picker nem em `withDiseaseAliases` |
| T-08-09-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao nesta fase (RESEARCH secao Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run typecheck` sai com codigo 0
- `npx vitest run src/routes/mapas/MeasureDiseasePicker.test.tsx` verde com pelo menos 8 casos
- `npx vitest run src/features/catalog/diseaseAliases.test.ts src/routes/variaveis/VariaveisPage.test.tsx` verde
- `npm run catalog:validate` sai com codigo 0
- `npm run gate` verde
- Verificacao manual registrada em `08-VALIDATION.md`: abrir Mapas, digitar "avc", confirmar as 3 categorias como linhas normais com a tira acima e rotulo oficial
</verification>

<success_criteria>
- Buscar "avc" no seletor de Mapas devolve as 3 categorias canonicas, selecionaveis, com a tira explicativa acima
- Buscar "tvp", "ait" e "varizes" devolve a categoria curada correspondente
- Buscar "aterosclerose" acha "Arteroesclerose" sem entrada curada
- O rotulo exibido e identico por qualquer caminho de busca, sem badge de apelido
- Selecionar uma linha entrega o id canonico, nunca o termo
- A busca de Variaveis cobre os apelidos pelo campo `aliases` ja existente, sem alterar artefato gerado
- Decisao sobre `VariableList.tsx` e sobre o fuzzy de CID registrada com justificativa
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-09-SUMMARY.md` when done
</output>
