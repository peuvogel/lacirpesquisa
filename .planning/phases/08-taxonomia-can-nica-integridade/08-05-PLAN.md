---
phase: 08-taxonomia-can-nica-integridade
plan: 05
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - supabase/config.toml
  - supabase/migrations/
  - supabase/rollback/
  - supabase/verify/
  - scripts/catalog/generateRenameMigration.mjs
  - src/features/catalog/renameMigration.test.ts
  - .gitignore
autonomous: true
requirements: [TAX-03, TAX-04]

must_haves:
  truths:
    - "O repositorio passa a ter a estrutura supabase/ com o schema real de producao capturado como migracao baseline (D-05)"
    - "O SQL da migracao e gerado de rename-map.json, nunca transcrito — regenerar devolve byte a byte o arquivo commitado"
    - "A renomeacao acontece em duas passadas com ids temporarios, num statement por passada agrupando as tres tabelas com CTEs de escrita — o padrao reproduzido empiricamente em RESEARCH secao 4.2"
    - "O agravo do codigo 330 e inserido na mesma transacao e removido pelo mesmo --down (D-25/D-03)"
    - "A prova de integridade D-04 roda dentro da transacao e aborta por RAISE EXCEPTION se qualquer soma agregada por agravo divergir"
    - "Zero DDL: nenhum ALTER TABLE, e nenhum ON UPDATE CASCADE (D-01/D-08)"
    - "O --down devolve o banco ao estado inicial e e o que TAX-04 chama de reversivel (D-03)"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Amarra o diretorio local ao projeto hmfbxqemububjyhdckrj"
    - path: "supabase/migrations/"
      provides: "Baseline do schema real de producao mais a migracao de renomeacao"
    - path: "supabase/rollback/"
      provides: "Script --down testado, fora do caminho de db push"
    - path: "supabase/verify/contagens.sql"
      provides: "Verificacao de contagens absolutas e de particao de metrica"
    - path: "scripts/catalog/generateRenameMigration.mjs"
      provides: "Gerador determinístico do up, do down e do verify a partir do mapa"
  key_links:
    - from: "scripts/catalog/generateRenameMigration.mjs"
      to: "scripts/catalog/rename-map.json"
      via: "le renames e added para emitir as VALUES das duas passadas"
      pattern: "rename-map.json"
    - from: "src/features/catalog/renameMigration.test.ts"
      to: "supabase/migrations/"
      via: "regenera em memoria e compara byte a byte com o arquivo commitado"
      pattern: "rename_disease_ids"
---

<objective>
Introduzir a estrutura `supabase/` no projeto e gerar, a partir de `rename-map.json`, o SQL de migracao (up), o de reversao (down) e o de verificacao — com a prova de integridade D-04 embutida na propria transacao.

Purpose: e a mudanca de maior raio de dano do milestone. A mecanica ja deixou de ser hipotese — RESEARCH secao 4.2 reproduziu as duas passadas com CTEs multiplas contra um Postgres 17 real, com schema identico ao de producao e os ciclos reais do ground truth, testando up e down. O que falta e instanciar com os 21 pares reais, sem digitar nenhum deles, e embutir a verificacao que impede um embaralhamento silencioso.

Output: `supabase/config.toml`, baseline do schema real, migracao gerada, rollback gerado, verificacao gerada, e teste que impede edicao manual do SQL.

**Nenhuma migracao e aplicada nesta plan.** O ensaio local e a plan 08-08; producao e a 08-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md
@docs/SUPABASE-CATALOG.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-02-SUMMARY.md

<interfaces>
<!-- Schema REAL de producao, obtido por supabase db dump em RESEARCH secao 4.1.
     docs/SUPABASE-CATALOG.md esta desatualizado e diverge disto. Usar estes nomes, nunca os do doc. -->

Tabelas: `public.sih_disease` (colunas `id` text NOT NULL, `label`, `filter_kind`, `tabnet_code`, `def_path` default `sih/cnv/nibr.def`, `created_at` timestamptz default now()), `public.sih_metric_uf`, `public.sih_metric_muni` (ambas com `disease_id` text NOT NULL).

Constraints reais:
- `sih_disease_pkey` PRIMARY KEY (id)
- `sih_metric_uf_pkey` PRIMARY KEY (disease_id, uf_codigo, ano)
- `sih_metric_muni_pkey` PRIMARY KEY (disease_id, municipio_codigo, ano)
- `sih_metric_uf_disease_id_fkey` FOREIGN KEY (disease_id) REFERENCES sih_disease(id) **ON DELETE CASCADE**
- `sih_metric_muni_disease_id_fkey` FOREIGN KEY (disease_id) REFERENCES sih_disease(id) **ON DELETE CASCADE**
- Ambas as FK confirmadas **NOT DEFERRABLE**; nenhuma clausula ON UPDATE (ou seja, NO ACTION) — consistente com D-08

Indices reais: `sih_metric_muni_disease_uf_ano` sobre (disease_id, uf_codigo, ano) e `sih_metric_uf_disease_ano` sobre (disease_id, ano). RLS habilitada nas tres tabelas com policy de SELECT para `anon` e `authenticated`.

**Armadilha do ON DELETE CASCADE:** qualquer estrategia que apague e reinsira uma linha de `sih_disease` apaga em cascata todas as metricas daquele agravo, sem erro. As duas passadas com UPDATE nunca deletam a linha pai e por isso nao acionam o gatilho. Qualquer variante futura do SQL precisa saber disso.

Contagens de producao medidas ao vivo (PostgREST, chave anon, 2026-08-03):
- `sih_disease` 330 (alvo pos-migracao: **331**)
- `sih_metric_uf` 30.313 (nao muda)
- `sih_metric_muni` 1.099.403 (nao muda)
- Linhas efetivamente tocadas pela renomeacao: **4.195** em `sih_metric_uf` e **200.636** em `sih_metric_muni` — ou seja ~205 mil linhas por passada, nao 1,13M. Isso responde a suposicao A3 do RESEARCH: os 21 ids sao uma fracao do total
- **237 dos 330 agravos ja hoje nao tem nenhuma linha em `sih_metric_uf`** (so 93 tem). E a lacuna de cobertura da coleta, diagnosticada em PROJECT.md e escopo da Fase 9 — nao e algo que esta fase possa afirmar de outro jeito. Ver a nota sobre D-25 na Task 2

Padrao de migracao confirmado por reproducao empirica (RESEARCH secao 4.2): UPDATEs separados por tabela falham nos dois sentidos; o que funciona e um unico statement com multiplas CTEs de escrita agrupando pai e filhas, porque a checagem de FK NOT DEFERRABLE acontece ao fim do statement inteiro.

Convencao de nome de migracao (observada em 133 arquivos do projeto irmao Mneuma): `YYYYMMDDHHMMSS_descricao_em_snake_case.sql`. O Supabase CLI envolve cada arquivo de migracao numa transacao implicita — nao escrever BEGIN/COMMIT no arquivo.

`scripts/catalog/rename-map.json`: `renames` (21, com `tabnetCode`, `old`, `canonical`, `label`), `added` (1, codigo 330, com `id`, `label`, `filterKind`, `def`), `tombstones` (21).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-requisito Docker e scaffold supabase/ com o schema real como baseline</name>

  <read_first>
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 3 inteira: CLI ja instalado e autenticado, `supabase link` sem senha do banco, e a exigencia de Docker rodando
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-VALIDATION.md` — o item de Wave 0 sobre Docker
    - `docs/SUPABASE-CATALOG.md` — o contrato documentado, para saber o que esta desatualizado
    - `.gitignore` — o estado atual, antes de acrescentar as entradas do Supabase
  </read_first>

  <files>supabase/config.toml, supabase/migrations/, .gitignore</files>

  <action>
Comecar confirmando o pre-requisito de ambiente, porque ele nao e obvio e ja surpreendeu na pesquisa: `supabase db pull` e `supabase db dump` **nao** usam o `pg_dump` do host — o CLI baixa e roda um container `supabase/postgres:17.6.1.147` e executa `pg_dump` de dentro dele. Docker Desktop precisa estar **rodando**, nao so instalado, e nesta maquina ele nao sobe por padrao. Verificar com `docker info`; se falhar, abrir o Docker Desktop e esperar ate `docker info` responder antes de seguir. Comandos de Management API (`projects list`, `link`) funcionam sem Docker; qualquer coisa que toque schema, nao.

O CLI ja esta instalado em `/opt/homebrew/bin/supabase` na versao 2.90.0 e ja esta autenticado nesta maquina — nao ha login a fazer, e `supabase link` nao pede a senha do banco (o CLI provisiona um papel de login Postgres temporario e escopado via Management API).

Rodar, na raiz do repo: `supabase init` para criar `supabase/config.toml`; `supabase link --project-ref hmfbxqemububjyhdckrj` para amarrar ao projeto "Site bioestatistica"; e `supabase db pull` para capturar o schema real de producao como migracao baseline em `supabase/migrations/`. O `db pull` tambem sincroniza a tabela de historico de migracoes no remoto, o que faz um `db push` futuro **nao** tentar reaplicar o baseline — e por isso que ele e preferivel a um `db dump` solto.

Se o `db pull` falhar ou pedir interacao que nao da para suprimir, o caminho alternativo e: `supabase db dump --linked -s public -f supabase/migrations/<timestamp>_remote_schema.sql` seguido de `supabase migration repair --status applied <timestamp>`, que marca o baseline como ja aplicado no remoto. Registrar no SUMMARY qual dos dois caminhos foi usado.

Conferir o arquivo baseline gerado contra o que a pesquisa mediu: precisa conter os cinco nomes de constraint reais, o `ON DELETE CASCADE` nas duas FK, os dois indices e as policies de RLS. Se divergir, a pesquisa e que esta desatualizada e a divergencia vai para o SUMMARY.

Acrescentar ao `.gitignore` as entradas que o Supabase CLI cria e que nao devem ser versionadas: `supabase/.branches/`, `supabase/.temp/`, e tambem `.local/` — diretorio que a plan 08-08 vai usar para o dump de dados de producao, que tem centenas de MB e nunca pode entrar no repo.

Criar os diretorios `supabase/rollback/` e `supabase/verify/` com um `.gitkeep`. Eles ficam **fora** de `supabase/migrations/` de proposito: `supabase db push` aplica tudo que estiver em `migrations/`, e um script de reversao aplicado por engano desfaria a migracao em producao.
  </action>

  <verify>
    <automated>test -f supabase/config.toml && ls supabase/migrations/*_remote_schema.sql >/dev/null && grep -q "hmfbxqemububjyhdckrj" supabase/config.toml && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `docker info` responde sem erro antes de qualquer comando de schema
    - `supabase/config.toml` existe e tem `project_id` igual a `hmfbxqemububjyhdckrj`
    - Existe exatamente um arquivo em `supabase/migrations/` e o nome dele casa `^[0-9]{14}_.*\.sql$`
    - `grep -c "sih_metric_uf_disease_id_fkey" supabase/migrations/*_remote_schema.sql` retorna pelo menos `1`
    - `grep -c "ON DELETE CASCADE" supabase/migrations/*_remote_schema.sql` retorna `2`
    - `grep -c "sih_metric_muni_disease_uf_ano" supabase/migrations/*_remote_schema.sql` retorna pelo menos `1`
    - `grep -c "ON UPDATE CASCADE" supabase/migrations/*_remote_schema.sql` retorna `0` — confirma D-08 contra o estado real
    - `.gitignore` contem `supabase/.temp`, `supabase/.branches` e `.local/`
    - `git status` nao lista nada sob `supabase/.temp/` nem `.local/`
    - `supabase/rollback/` e `supabase/verify/` existem e estao versionados via `.gitkeep`
  </acceptance_criteria>

  <done>O repo tem a estrutura `supabase/` amarrada ao projeto real e o schema de producao capturado como baseline, com os nomes de constraint reais no lugar dos que o doc supunha.</done>
</task>

<task type="auto">
  <name>Task 2: Gerar up, down e verificacao a partir de rename-map.json</name>

  <read_first>
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 4.2 completa (o SQL das duas passadas, testado e funcionando, incluindo o ciclo real) e secao 4.5 (o INSERT do codigo 330 e a excecao de invariante que ele exige)
    - `scripts/catalog/rename-map.json` — as 21 renomeacoes e a adicao
    - `scripts/catalog/metricless-diseases.json` — o registro do agravo sem metrica
    - `supabase/migrations/*_remote_schema.sql` — os nomes reais de tabela, coluna e constraint capturados na Task 1
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-01, D-03, D-04, D-08 e as consequencias de D-25
    - `.agents/skills/supabase-postgres-best-practices/references/lock-short-transactions.md` — para dimensionar o custo de lock das duas passadas
  </read_first>

  <files>scripts/catalog/generateRenameMigration.mjs, supabase/migrations/, supabase/rollback/, supabase/verify/contagens.sql</files>

  <action>
Criar `scripts/catalog/generateRenameMigration.mjs` no molde CLI-mais-export. Ele le `rename-map.json` e `metricless-diseases.json` e emite tres arquivos. O timestamp da migracao e uma constante declarada no proprio gerador, para que regenerar seja deterministico e o teste da Task 3 possa comparar byte a byte.

**Arquivo up** — `supabase/migrations/<timestamp>_rename_disease_ids.sql`. Sem `BEGIN`/`COMMIT` (o CLI ja envolve em transacao). Sem nenhum `ALTER TABLE`: zero DDL, conforme D-01, e sem `ON UPDATE CASCADE`, conforme D-08 — que e o oposto do que a nota antiga do ROADMAP mandava. Sequencia:

Primeiro, tabelas temporarias `ON COMMIT DROP` guardando o retrato agregado **antes**: por `disease_id`, `count(*)` e a soma de `internacoes`, `obitos`, `valor_total` e `dias_permanencia`, uma para `sih_metric_uf` e outra para `sih_metric_muni`, mais a contagem de `sih_disease`. E o retrato que o D-04 exige: contagem sozinha nao detectaria embaralhamento entre os 21, porque as tres contagens ficariam identicas e a integridade referencial passaria.

Depois, uma temporaria com o mapa das 21 duplas `old` e `canonical`, emitida a partir de `rename-map.json`.

Passada 1: um unico statement com tres CTEs de escrita (uma por tabela) movendo os 21 ids para a forma temporaria com prefixo `__mig_`, cada CTE fazendo `UPDATE ... FROM` contra a temporaria do mapa e um `RETURNING`, e um `SELECT` final agregando as contagens de linhas afetadas. Passada 2: mesma forma, movendo de `__mig_` para o id canonico. Agrupar as tres tabelas no mesmo statement e o que faz funcionar — separado por tabela falha nos dois sentidos, verificado empiricamente.

Terceira operacao, na mesma transacao: `INSERT INTO sih_disease` do agravo do codigo 330, com `id`, `label`, `filter_kind`, `tabnet_code` e `def_path` vindos da entrada `added` do mapa. Nao precisa de CTE multipla — esse agravo nao tem nenhuma linha filha, entao o INSERT nao dispara checagem de FK.

Por fim, um bloco `DO` com a prova de integridade que **aborta a transacao com `RAISE EXCEPTION`** se qualquer uma destas falhar:
- para cada `disease_id` do retrato anterior, a tupla agregada depois, lida sob o id traduzido pelo mapa, e identica a de antes (comparar com `IS DISTINCT FROM`, para que NULL conte como NULL e nao passe batido)
- o total de linhas de `sih_metric_uf` e de `sih_metric_muni` nao mudou
- `count(*)` de `sih_disease` e exatamente o anterior mais 1
- nenhum id em nenhuma das tres tabelas ainda comeca com `__mig_`
- todo `canonical_id` do mapa existe em `sih_disease`
- o conjunto de agravos sem nenhuma linha em `sih_metric_uf`, traduzido de volta pelo mapa, e igual ao conjunto de antes acrescido exatamente dos ids listados em `metricless-diseases.json`

**Nota factual importante sobre a ultima checagem.** D-25 foi escrita supondo que o agravo do codigo 330 seria o unico agravo canonico sem metrica. Medicao ao vivo em producao mostra que **237 dos 330 agravos ja hoje nao tem linha em `sih_metric_uf`** — e a lacuna de cobertura da coleta, ja diagnosticada em PROJECT.md e escopo da Fase 9. Afirmar "exatamente um agravo sem metrica" seria falso contra o banco real. A formulacao acima preserva a intencao de D-25 (excecao registrada como dado com motivo escrito, nunca allowlist silenciosa, e um caso novo falha alto) numa forma que e verdadeira: **o conjunto de agravos orfaos de metrica nao pode crescer alem do que esta registrado em `metricless-diseases.json`**. Isso pega tanto um agravo novo sem coleta quanto uma renomeacao que orfanasse um agravo que tinha dado — que e o modo de falha real que a migracao pode introduzir. Registrar essa correcao no SUMMARY.

**Arquivo down** — `supabase/rollback/<timestamp>_rename_disease_ids_down.sql`, fora de `migrations/` para nao ser aplicado por `db push`. Mesma tecnica na direcao inversa, com prefixo temporario `__down_`, mais `DELETE FROM sih_disease` do agravo do codigo 330, mais a mesma prova de integridade invertida, incluindo `count(*)` de `sih_disease` voltando a ser o anterior menos 1. E isso que D-03 chama de reversivel: script testado, nao restauracao manual.

**Arquivo de verificacao** — `supabase/verify/contagens.sql`, que checa contagens absolutas e falha com `RAISE EXCEPTION` se divergirem: `sih_disease` igual a 331, `sih_metric_uf` igual a 30.313 e `sih_metric_muni` igual a 1.099.403. Esses tres numeros ficam numa constante do gerador com comentario citando a origem (medicao ao vivo por PostgREST em 2026-08-03, RESEARCH secao 3.2). O arquivo tambem lista os agravos sem metrica que nao estao em `metricless-diseases.json`, para inspecao no ensaio.

Rodar o gerador uma vez e commitar os tres arquivos. **Nao aplicar nenhum deles.**
  </action>

  <verify>
    <automated>node scripts/catalog/generateRenameMigration.mjs && git diff --quiet supabase/ && echo "geracao deterministica OK"</automated>
  </verify>

  <acceptance_criteria>
    - Existem exatamente dois arquivos em `supabase/migrations/`: o baseline e `<timestamp>_rename_disease_ids.sql`
    - `grep -v "^--" supabase/migrations/*_rename_disease_ids.sql | grep -ci "alter table"` retorna `0`
    - `grep -v "^--" supabase/migrations/*_rename_disease_ids.sql | grep -ci "on update cascade"` retorna `0`
    - `grep -v "^--" supabase/migrations/*_rename_disease_ids.sql | grep -ci "^begin;"` retorna `0`
    - `grep -c "__mig_" supabase/migrations/*_rename_disease_ids.sql` retorna pelo menos `6` — tres CTEs por passada, duas passadas
    - O arquivo up contem 21 duplas no mapa temporario e exatamente um `INSERT INTO sih_disease`
    - `grep -ci "raise exception" supabase/migrations/*_rename_disease_ids.sql` retorna pelo menos `6` — uma por checagem do D-04
    - `grep -v "^--" supabase/rollback/*_down.sql | grep -c "__down_"` retorna pelo menos `6`, e o arquivo contem exatamente um `DELETE FROM sih_disease`
    - Nenhum arquivo de `supabase/rollback/` ou `supabase/verify/` esta em `supabase/migrations/`
    - `supabase/verify/contagens.sql` cita os tres numeros `331`, `30313` e `1099403`
    - Rodar o gerador uma segunda vez deixa `git status` limpo
  </acceptance_criteria>

  <done>O SQL das duas direcoes existe, gerado do mapa, com zero DDL e com a prova de integridade agregada embutida na propria transacao — e nao foi aplicado em lugar nenhum.</done>
</task>

<task type="auto">
  <name>Task 3: Teste que impede edicao manual do SQL de migracao</name>

  <read_first>
    - `scripts/catalog/generateRenameMigration.mjs` — as funcoes de render criadas na Task 2
    - `src/features/catalog/renameMap.test.ts` — o padrao de teste de recomputacao byte-identica ja escrito na 08-02
    - `src/features/catalog/catalogAnalysisData.test.ts` — linhas 1-24, como ler arquivo real do repo dentro de um teste
  </read_first>

  <files>src/features/catalog/renameMigration.test.ts</files>

  <action>
Criar `src/features/catalog/renameMigration.test.ts` sob `src/`, importando as funcoes de render do gerador por caminho relativo de 3 niveis.

Casos:
- regerar o up em memoria e comparar **string com string** contra o arquivo commitado em `supabase/migrations/`; idem para o down e para o verify. E o mesmo mecanismo do teste de `rename-map.json`: qualquer edicao manual no SQL fica vermelha na suite, o que importa porque este e o arquivo que vai tocar 1,13M linhas de producao
- afirmar que o texto do up nao contem `alter table`, `on update cascade` nem `begin;` fora de comentario
- afirmar que o up contem os 21 pares do mapa e que cada `old` e cada `canonical` aparece exatamente onde deveria — derivando a lista de `rename-map.json`, nunca digitando
- afirmar que o up contem o `INSERT` do id de `added` e que o down contem o `DELETE` do mesmo id
- afirmar que up e down sao simetricos: o conjunto de pares no down e o inverso exato do conjunto no up
- afirmar que a prova de integridade cobre as quatro medidas nominalmente: `internacoes`, `obitos`, `valor_total` e `dias_permanencia` aparecem no bloco de agregacao. Contagem sozinha nao detecta embaralhamento entre os 21 — este caso e o que impede alguem simplificar a verificacao para so `count(*)`
  </action>

  <verify>
    <automated>npx vitest run src/features/catalog/renameMigration.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/features/catalog/renameMigration.test.ts` passa com pelo menos 6 casos
    - Trocar um caractere no arquivo de migracao commitado deixa o teste vermelho; reverter depois de comprovar
    - Remover a soma de `valor_total` do bloco de agregacao deixa o teste vermelho; reverter depois de comprovar
    - `grep -c "'avc'" src/features/catalog/renameMigration.test.ts` retorna `0`
    - `npm run test:run` verde e `npm run catalog:validate` sai com codigo 0
  </acceptance_criteria>

  <done>O SQL que vai tocar producao so pode mudar mudando o mapa que o gera — editar o arquivo a mao passa a ser uma falha de suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositorio para banco de producao | `supabase db push` aplica em `hmfbxqemububjyhdckrj` o que estiver em `supabase/migrations/` |
| Supabase CLI para Management API | O CLI provisiona um papel Postgres temporario com senha efemera para dump e push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | Migracao editada a mao embaralhando os 21 ids de forma consistente e invisivel | mitigate | SQL gerado do mapa; teste de recomputacao byte-identica em `renameMigration.test.ts`; prova D-04 por soma agregada por agravo e medida, nao por contagem |
| T-08-05-02 | Tampering | Estrategia de DELETE mais INSERT em `sih_disease` apagaria metricas em cascata sem erro | mitigate | `ON DELETE CASCADE` documentado em `<interfaces>` e nos comentarios do SQL gerado; as duas passadas so fazem UPDATE, nunca deletam a linha pai |
| T-08-05-03 | Denial of Service | Script de rollback aplicado por engano em producao por estar em `migrations/` | mitigate | `supabase/rollback/` fora do caminho varrido por `db push`; criterio de aceite verifica |
| T-08-05-04 | Denial of Service | Lock longo em `sih_metric_muni` durante a migracao | mitigate | Medicao ao vivo mostra 200.636 linhas tocadas em muni e 4.195 em uf, nao 1,13M; o ensaio da plan 08-08 mede o tempo real antes de tocar producao |
| T-08-05-05 | Information disclosure | Senha efemera do papel `cli_login_postgres` impressa por `db dump --dry-run` | mitigate | Nao usar `--dry-run` em log compartilhado; nunca colar a saida em SUMMARY nem em commit |
| T-08-05-06 | Information disclosure | Dump de dados de producao gravado no diretorio de trabalho | mitigate | `.local/` no `.gitignore` nesta plan, antes de a 08-08 gerar o dump |
| T-08-05-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao nesta fase; o Supabase CLI ja esta instalado via Homebrew e nao entra no `package.json` (RESEARCH secao Package Legitimacy Audit) |
</threat_model>

<verification>
- `node scripts/catalog/generateRenameMigration.mjs` roda duas vezes deixando `git status` limpo
- `npx vitest run src/features/catalog/renameMigration.test.ts` verde
- `npm run gate` verde
- Nenhuma migracao aplicada: `supabase migration list` mostra a de renomeacao como pendente no remoto
</verification>

<success_criteria>
- `supabase/config.toml` amarrado a `hmfbxqemububjyhdckrj` e baseline do schema real capturado
- `.gitignore` cobre `supabase/.temp`, `supabase/.branches` e `.local/`
- Up, down e verify gerados de `rename-map.json`, deterministicos e commitados
- Up com zero DDL, sem `ON UPDATE CASCADE`, duas passadas com CTEs multiplas, INSERT do codigo 330 e prova D-04 por soma agregada
- Down simetrico, fora de `migrations/`
- Teste impede edicao manual do SQL e impede simplificar a verificacao para so contagem
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-05-SUMMARY.md` when done
</output>
