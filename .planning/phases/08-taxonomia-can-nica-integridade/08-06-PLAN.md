---
phase: 08-taxonomia-can-nica-integridade
plan: 06
type: execute
wave: 4
depends_on: [08-03, 08-04]
files_modified:
  - scripts/catalog/diseases.json
  - scripts/catalog/columnMap.json
  - scripts/catalog/sql/
  - scripts/catalog/paths.mjs
  - scripts/catalog/syncColumnMap.mjs
  - scripts/catalog/syncPackImports.mjs
  - scripts/catalog/build.mjs
  - scripts/catalog/validate.mjs
  - src/features/catalog/diseases.lista.json
  - src/features/catalog/taxonomy.ts
  - src/features/catalog/catalogAnalysisData.ts
  - src/routes/mapas/mockAnalysisData.ts
  - src/test/noTombstoneLiterals.test.ts
  - public/data/catalog/variables.json
  - public/data/catalog/manifest.json
  - public/data/catalog/packs/
autonomous: true
requirements: [TAX-01, TAX-02, TAX-06]

must_haves:
  truths:
    - "Nenhum agravo exibe dados de outra doenca: os 21 ids passam a nomear o codigo TabNet que realmente serviram"
    - "A taxonomia commitada tem 331 agravos, com o codigo 330 incluido e CID nao-nulo em todos os lista_morb"
    - "catalog:validate roda os invariantes A, B, C, D e D2 e falha fechado se qualquer um divergir (TAX-02)"
    - "O invariante F varre os arquivos versionados e falha se qualquer id-tombstone aparecer como literal ancorado (D-23)"
    - "Packs, variables.json, seeds SQL, columnMap e a copia no bundle sao gerados da taxonomia canonica — nenhuma lista mantida a mao em paralelo (TAX-06)"
    - "Renomeacao e invariantes entram no mesmo commit, entao o gate nunca fica vermelho e o invariante nasce fail-closed, sem allowlist (D-24)"
    - "Os rotulos que o estudante le em variables.json descrevem o codigo que a linha realmente serve"
  artifacts:
    - path: "scripts/catalog/diseases.json"
      provides: "Taxonomia canonica de 331 agravos, gerada do snapshot"
      contains: "todas_as_outras_causas_externas"
    - path: "src/features/catalog/diseases.lista.json"
      provides: "Copia no bundle, regenerada, 331 entradas"
    - path: "src/test/noTombstoneLiterals.test.ts"
      provides: "Invariante F — guarda estrutural varrendo os arquivos versionados"
    - path: "public/data/catalog/packs/"
      provides: "10 packs, 9 renomeados para o id canonico, com packId interno coerente"
  key_links:
    - from: "scripts/catalog/validate.mjs"
      to: "scripts/catalog/sync-lista-morb.mjs"
      via: "main() chama checkRegenerationFromDisk, que regenera do snapshot e compara"
      pattern: "checkRegenerationFromDisk"
    - from: "scripts/catalog/paths.mjs"
      to: "scripts/catalog/tombstones.mjs"
      via: "assertNotTombstone ao montar PACK_SOURCES"
      pattern: "assertNotTombstone"
    - from: "src/test/noTombstoneLiterals.test.ts"
      to: "scripts/catalog/tombstones.mjs"
      via: "usa findTombstoneHits, a mesma funcao que o motor de renomeacao usou"
      pattern: "findTombstoneHits"
---

<objective>
O flip: executar os geradores e o motor, regenerar todo artefato derivado da taxonomia canonica, ligar os invariantes ao gate e acrescentar a guarda estrutural — tudo num **unico commit**.

Purpose: e aqui que a fase entrega o goal. Depois desta plan, nenhum agravo exibe dado de outra doenca e a validacao impede que volte. O commit e grande e quase todo gerado: `rename-map.json` dirige a renomeacao, entao o que precisou de revisao humana foi o mapa, nao as 50 linhas de `variables.json`.

Output: taxonomia de 331 agravos, 9 packs renomeados, `variables.json`/`columnMap.json`/seeds SQL/copia no bundle regenerados, invariantes A-E ligados ao `catalog:validate`, invariante F como teste vitest, guarda de tombstone no ponto de entrada de build.

**Restricao de execucao (D-24): esta plan tem uma task so, de proposito.** O executor do GSD commita depois de cada task, sem excecao — `~/.claude/agents/gsd-executor.md`, bloco `<task_commit_protocol>`: "After each task completes (verification passed, done criteria met), commit immediately"; e `~/.claude/get-shit-done/references/git-integration.md`, bloco `<commit_points>`: "Task completed | YES | Atomic unit of work (1 commit per task)". **Nao existe mecanismo de supressao de commit no framework** — o adiamento por segmentos (Pattern B / `segment_execution`) so liga em plan que contenha task `checkpoint:*`, e esta nao tem nenhuma.

Fatiar o flip em varias tasks produziria varios commits, e o primeiro deles traria a taxonomia inteira renomeada **antes** de qualquer invariante existir: renomeacao e invariante em commits separados, que e exatamente o que D-24 proibe. Com uma task so, o commit unico do D-24 vira consequencia deterministica do framework, e nao depende de o executor honrar uma instrucao em prosa que ele nao tem como honrar.

A task e grande, e e assim que tem que ser. A alternativa rejeitada no D-24 era ligar o invariante antes com uma allowlist temporaria para os 21, o que seria uma anotacao de "falha conhecida" — proibida pela Fase 7 em QA-03 — e mascararia um 22o id caso existisse. O commit e grande mas quase todo gerado: `rename-map.json` dirige a renomeacao, entao o que precisou de revisao humana foi o mapa, nao as 50 linhas de `variables.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-03-SUMMARY.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-04-SUMMARY.md

<interfaces>
<!-- Tudo abaixo ja existe, entregue pelas plans 08-01 a 08-04. -->

Comandos disponiveis:
- `node scripts/catalog/sync-lista-morb.mjs` — escreve `diseases.json` (331) e `diseases.lista.json` (331) a partir do snapshot
- `node scripts/catalog/applyRenameMap.mjs --apply` — aplica a renomeacao em packs, manifest, variables.json, columnMap e codigo-fonte
- `node scripts/catalog/applyRenameMap.mjs --check` — varre o escopo e sai diferente de zero se sobrar tombstone
- `node scripts/catalog/generateDiseaseSeeds.mjs` — reescreve `scripts/catalog/sql/*.sql`
- `node scripts/catalog/syncColumnMap.mjs` — preenche colunas faltantes em `columnMap.json` (merge incremental, nunca remove)
- `node scripts/catalog/syncPackImports.mjs` — reescreve o bloco de imports e `PACKS` em `catalogAnalysisData.ts` a partir dos arquivos em `public/data/catalog/packs/`

Funcoes ja escritas e ainda **nao** ligadas ao `main()` de `validate.mjs`:
`checkSlugConsistency`, `checkRegeneration`, `checkRegenerationFromDisk`, `checkPartition`, `checkColumnMapKeys`.
Ja ligada: `checkSnapshotIntegrity` (invariante C, desde a 08-01).

`scripts/catalog/tombstones.mjs`: `TOMBSTONES`, `CANONICAL_BY_OLD`, `tombstonePatterns`, `findTombstoneHits`, `assertNotTombstone`.

Medicao ancorada do estado atual (feita no planejamento, com os mesmos padroes de `tombstonePatterns`): **485 ocorrencias em 39 arquivos** versionados sob `src/`, `scripts/` e `public/`. Distribuicao: `columnMap.json` 128, `variables.json` 97, `diseases.lista.json` 42, `diseases.json` 21, `lista-morb-cid.json` 21, `sql/2.sql` 21, `sync-lista-morb.mjs` 21, `catalogAnalysisData.ts` 21, `manifest.json` 9, `taxonomy.ts` 9, `mockAnalysisData.ts` 6, `syncPackImports.mjs` 3, `build.mjs` 2, `paths.mjs` 2, `syncColumnMap.mjs` 1, 9 packs com 1 cada, e **15 arquivos de teste** sob `src/`.
Tres desses caem a zero antes do flip por trabalho ja feito: `sync-lista-morb.mjs` (o `KNOWN_BY_CODE` saiu na 08-02) e `lista-morb-cid.json` (re-chaveado por codigo na 08-01). `manifest.json` cai a 9 menos os `sourceDir`, que ficam.

`scripts/catalog/build.mjs` linhas 212 e 274 tem `'sih.embolia_trombose_uf'` hardcoded — achado do planejamento, nao citado por nome em RESEARCH nem em PATTERNS. A forma e ancorada (`sih.<old>_uf`), entao o motor alcanca.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip completo — regenerar a taxonomia, propagar a renomeacao, ligar os invariantes e fechar o gate (D-24)</name>

  <read_first>
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-04-SUMMARY.md` — o relatorio de dry-run do motor, que diz exatamente o que vai mudar
    - `scripts/catalog/rebuildAfterScrape.mjs` — as 4 linhas da cadeia de regeneracao, para saber o que **nao** rodar
    - `scripts/catalog/paths.mjs` — linhas 58-91 (`multiPackSource` monta o caminho do corpus a partir do `disease.id`, que e por que `build.mjs` nao pode ser rodado) e 80-91 (o loop que monta `PACK_SOURCES`)
    - `scripts/catalog/syncPackImports.mjs` — linhas 23-31 (a ordenacao com `rank` que cita dois packIds) e 73-79 (o template com `VARIABLE_ID_ALIASES` hardcoded)
    - `scripts/catalog/validate.mjs` — o `main()` atual (linhas 290-320) e as cinco funcoes escritas na 08-03
    - `scripts/catalog/tombstones.mjs` — `findTombstoneHits` e `assertNotTombstone`
    - `package.json` — a definicao de `gate`, `test:run` e `pretest`
    - `.githooks/pre-commit` e `.githooks/pre-push` — o que roda automaticamente no commit
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 4 inteira
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 5, com a divisao CLI versus vitest e a ressalva sobre a varredura precisar excluir a si mesma
    - `.planning/phases/07-baseline-verde/07-CONTEXT.md` — a decisao de guarda por ausencia estrutural em vez de convencao documentada
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-24 e a decisao da Fase 7 sobre nao existir falha conhecida
  </read_first>

  <files>scripts/catalog/diseases.json, src/features/catalog/diseases.lista.json, scripts/catalog/columnMap.json, scripts/catalog/sql/, scripts/catalog/paths.mjs, scripts/catalog/syncColumnMap.mjs, scripts/catalog/syncPackImports.mjs, scripts/catalog/build.mjs, scripts/catalog/validate.mjs, src/features/catalog/taxonomy.ts, src/features/catalog/catalogAnalysisData.ts, src/routes/mapas/mockAnalysisData.ts, src/test/noTombstoneLiterals.test.ts, public/data/catalog/variables.json, public/data/catalog/manifest.json, public/data/catalog/packs/</files>

  <action>
Esta task inteira produz **um unico commit**, criado pelo protocolo normal de commit-por-task do executor ao fim dela. E assim que D-24 se cumpre: renomeacao e invariantes no mesmo commit, sem depender de nenhuma instrucao de adiamento.

**Parte 1 — executar os geradores e o motor, nesta ordem exata.**

Primeiro `node scripts/catalog/sync-lista-morb.mjs`, que reescreve `scripts/catalog/diseases.json` com 331 entradas e `src/features/catalog/diseases.lista.json` com as mesmas 331. A partir deste passo o working tree fica transitoriamente inconsistente e o gate vermelho — e esperado, e e a razao de tudo estar numa task so.

Segundo `node scripts/catalog/applyRenameMap.mjs --apply`, que renomeia os 9 packs com `git mv`, reescreve `packId` interno, `manifest.json`, as 50 entradas de `variables.json`, move e poda as 21 chaves de `columnMap.json` regenerando as folhas das medidas padrao, e reescreve o codigo-fonte pelos padroes ancorados — incluindo `taxonomy.ts`, `catalogAnalysisData.ts`, `mockAnalysisData.ts`, `build.mjs`, `syncPackImports.mjs`, os 15 arquivos de teste, e as duas guardas literais duplicadas de `paths.mjs:88` e `syncColumnMap.mjs:54`.

Terceiro `node scripts/catalog/generateDiseaseSeeds.mjs`, que reescreve `scripts/catalog/sql/*.sql` a partir da taxonomia canonica: 5 arquivos, 80 mais 80 mais 80 mais 80 mais 11.

Quarto `node scripts/catalog/syncColumnMap.mjs`, que so acrescenta o que falta — a entrada do agravo novo do codigo 330 com as 5 medidas padrao. A saida precisa dizer `packs=331`. Rodar uma segunda vez precisa reportar `+0 column slots`, provando idempotencia.

Quinto `node scripts/catalog/syncPackImports.mjs`, que reescreve o bloco de imports e o objeto `PACKS` em `catalogAnalysisData.ts` a partir dos nomes de arquivo ja renomeados. Como o template com `VARIABLE_ID_ALIASES` foi corrigido no passo dois, agora ele escreve os ids canonicos. Conferir depois que `getDefaultCatalogVariableId()` tambem ficou canonico — ele mora **fora** do bloco que a regex do script substitui, entao quem o corrigiu foi o motor, nao este script.

Sexto `node scripts/catalog/applyRenameMap.mjs --check`, que precisa sair com codigo 0.

**Nao rodar `npm run catalog:rebuild` nem `node scripts/catalog/build.mjs`, em hipotese nenhuma.** `build.mjs` reconstroi os packs lendo os CSVs do corpus atraves de `PACK_SOURCES`, que agora monta o caminho a partir do id **canonico** — e os 341 diretorios de coleta continuam com os nomes antigos por decisao explicita (D-22, escopo da Fase 9). Rodar o build agora nao encontraria fonte para os 9 packs renomeados e os apagaria em silencio, derrubando `variables.json` de 71 para cerca de 21 entradas. Os packs desta fase sao **renomeados**, nao reconstruidos: o dado dentro deles ja esta correto para o codigo consultado; so o nome estava errado.

Rodar `npm run typecheck` ao fim desta parte: se `catalogAnalysisData.ts` ficou com import de arquivo inexistente, e aqui que aparece.

**Parte 2 — ligar os invariantes ao gate e acrescentar o invariante F.**

Ligar ao `main()` de `validate.mjs` as quatro funcoes que ainda estao soltas: `checkSlugConsistency` (invariante A), `checkRegenerationFromDisk` (invariante B), `checkPartition` (invariante D) e `checkColumnMapKeys` (extensao D2). O invariante C ja esta ligado desde a 08-01. Todos os erros vao para o mesmo array que hoje vem de `validateCatalogDir`, mantendo a saida atual com previa de 25 mensagens e o exit code 1 em caso de falha. Prefixar as mensagens de cada bloco de forma que o operador saiba qual invariante falhou.

Ligar `assertNotTombstone` em `scripts/catalog/paths.mjs`, no loop que monta `PACK_SOURCES`: cada `disease.id` lido de `diseases.json` e afirmado como nao-tombstone. E o ponto de entrada de build que D-06 exige, ao lado do ponto de escrita ja fechado em `uploadSihToSupabase.mjs`. So agora e possivel ligar: antes da Parte 1, `diseases.json` estava cheio de tombstones e o assert derrubaria o proprio `catalog:validate`.

Criar `src/test/noTombstoneLiterals.test.ts` — o **invariante F** (D-23). Ele lista os arquivos versionados com `git ls-files`, filtra o escopo, le cada um e usa `findTombstoneHits` de `tombstones.mjs` — a mesma funcao que o motor de renomeacao usou para reescrever. Usar a mesma funcao nos dois lados e o que garante que varredura e reescrita nunca discordem sobre o que conta como tombstone; uma varredura com regra propria poderia dar verde sobre um arquivo que o motor nunca tocou.

Escopo varrido: `src/`, `scripts/` e `public/`. **Fora do escopo:** `.planning/` (documentacao historica, que precisa continuar citando os ids antigos — a nota de ground truth e o registro do defeito), `trabalhos datasus/` (os 341 diretorios de coleta e os scripts Python, que D-22 mantem para a Fase 9), `docs/`, e `supabase/` (a migracao e o rollback sao literalmente a lista dos 21 pares).

Allowlist de arquivo, com motivo explicito em comentario no proprio teste:
- `scripts/catalog/rename-map.json` — o unico local legitimo, conforme D-23
- `src/test/fixtures/taxonomy/diseases.pre-migracao.json` — a fixture congelada do estado corrompido, que **precisa** conter os 21 para provar TAX-02 sob D-24; ela ja tem invariante proprio, a igualdade byte a byte com o `diseases.json` do commit anterior, afirmada em `renameMap.test.ts`
- o proprio arquivo de teste, por precaucao, ainda que ele derive os tombstones de `rename-map.json` e nao contenha nenhum literal

A mensagem de falha precisa citar arquivo, offset e forma casada, para que quem quebrar o invariante saiba exatamente onde. Um caso adicional afirma que o teste **falharia** se um tombstone fosse plantado: escrever um arquivo temporario sob `src/` com um literal ancorado, rodar a funcao de varredura sobre uma lista que inclua esse arquivo, afirmar a deteccao, e apagar o arquivo. Sem esse caso, o invariante F pode passar por nunca ter olhado para nada.

**Parte 3 — fechar o gate e commitar.**

Rodar `npm run gate` inteiro — `catalog:validate`, `vitest run` e `build`. Precisa ficar verde. Se algum teste dos 15 arquivos renomeados falhar, corrigir o teste e nao o dado: os testes foram reescritos por substituicao ancorada e uma falha ali significa que alguma asserçao dependia do id antigo de uma forma que o padrao nao alcancou.

Conferir que o diff **nao** contem nada fora do previsto: nenhum arquivo sob `trabalhos datasus/`, nenhum nome de coluna de CSV legado alterado, `manifest.json` com `sourceDir` intacto, e nenhum arquivo sob `.planning/` alem do SUMMARY — o commit e grande por construcao, mas nao pode arrastar edicao incidental de documento de planejamento junto.

O commit desta task e o commit unico do D-24. A mensagem precisa dizer que renomeacao e invariantes entram juntos por D-24, e o corpo cita: 331 agravos, 21 ids renomeados, 1 agravo inserido (codigo 330), 9 packs renomeados, 50 entradas de `variables.json` atualizadas, invariantes A a F ativos.

Registrar no SUMMARY, para a Fase 9 encontrar: os 341 diretorios de `coleta_sih_multi/` seguem com nome antigo por D-22 e agora o upload falha alto neles (D-06); `manifest.json` mantem `packs[].sourceDir` apontando para o diretorio legado (por exemplo `coleta_sih_multi/avc` sob o pack `sih.outras_doencas_do_olho_e_anexos_uf`, que agora se chama `outras_doencas_do_olho_e_anexos`), o que nao e inconsistencia e sim proveniencia verdadeira, porque aquele diretorio realmente contem o dado do codigo 163 — renomear ali afirmaria um caminho que nao existe; e `build.mjs` nao pode ser rodado ate a Fase 9 re-chavear o corpus.
  </action>

  <verify>
    <automated>npm run gate</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/catalog/diseases.json` tem 331 entradas e contem uma com `tabnetCode` `330` e id `todas_as_outras_causas_externas`
    - `src/features/catalog/diseases.lista.json` tem 331 entradas e nenhuma entrada `lista_morb` com `cid` nulo
    - `ls public/data/catalog/packs/ | wc -l` retorna `10`, e `ls public/data/catalog/packs/ | grep -c "sih.avc_uf"` retorna `0`
    - `git status` mostra os 9 packs como renomeados (`R`), preservando historico
    - Cada pack renomeado tem o campo `packId` interno igual ao nome do arquivo
    - `public/data/catalog/variables.json` continua com 71 entradas e `manifest.json` continua com `catalogEntryCount` 71
    - `grep -c "AVC / acidente vascular cerebral" public/data/catalog/variables.json` retorna `0` — o rotulo que o estudante lia sob o codigo 163 sumiu
    - `scripts/catalog/columnMap.json` tem 331 chaves de nivel 1 e nenhuma na forma `sih.<tombstone>_uf`
    - `ls scripts/catalog/sql/*.sql | wc -l` retorna `5`, e o total de linhas de valores nos cinco arquivos e `331`
    - `node scripts/catalog/syncColumnMap.mjs` imprime `packs=331` e, na segunda execucao, `+0 column slots`
    - `node scripts/catalog/applyRenameMap.mjs --check` sai com codigo 0
    - `npm run typecheck` sai com codigo 0
    - `npm run catalog:validate` sai com codigo 0 e imprime `catalog:validate OK`
    - Alterar um caractere de um `label` em `scripts/catalog/diseases.json` faz `catalog:validate` sair 1 citando os invariantes A e B; reverter depois de comprovar
    - Remover a chave `331` de `scripts/catalog/exclusions.json` faz `catalog:validate` sair 1 citando o invariante D; reverter depois de comprovar
    - Duplicar uma chave antiga em `scripts/catalog/columnMap.json` faz `catalog:validate` sair 1 citando D2; reverter depois de comprovar
    - `grep -c assertNotTombstone scripts/catalog/paths.mjs` retorna pelo menos `1`
    - `npx vitest run src/test/noTombstoneLiterals.test.ts` passa, e a varredura visita mais de 200 arquivos (afirmar a contagem no proprio teste para que ele nao possa passar varrendo lista vazia)
    - A allowlist do teste tem exatamente 3 caminhos, cada um com comentario de motivo
    - `npm run gate` sai com codigo 0
    - `git log --oneline -1` mostra exatamente um commit novo para esta plan
    - `git show --stat HEAD` inclui os 9 packs como renomeacao, `diseases.json`, `diseases.lista.json`, `columnMap.json`, `variables.json`, `manifest.json`, os 5 `sql/*.sql`, `validate.mjs`, `paths.mjs`, `taxonomy.ts`, `catalogAnalysisData.ts`, `mockAnalysisData.ts`, `src/test/noTombstoneLiterals.test.ts` e os 15 arquivos de teste
    - `git show --stat HEAD` **nao** inclui nada sob `trabalhos datasus/`
    - `git grep -c "internacoes_embolia_trombose_arteriais" -- scripts/catalog/parseCsv.mjs` continua retornando pelo menos `1` — o nome de coluna legado sobreviveu, como decidido
    - Rodar o invariante A contra `src/test/fixtures/taxonomy/diseases.pre-migracao.json` continua devolvendo 21 mensagens, e contra o `diseases.json` do commit, zero
    - `npm run catalog:validate` sai com codigo 0 num clone limpo do commit
  </acceptance_criteria>

  <done>A taxonomia canonica e todos os derivados existem, o gate passa a barrar `id`/`tabnetCode`/`label` divergentes, derivado dessincronizado, particao incompleta e literal de id morto — e tudo isso entrou num commit so, com o gate verde nele.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artefato de catalogo para tela do estudante | `variables.json` e `diseases.lista.json` entram no bundle e viram rotulo lido em aula |
| Codigo-fonte versionado para runtime | Um literal de id morto sobrevivente aponta em silencio para o agravo errado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Spoofing | Rotulo clinico convincente servindo codigo errado — o defeito que a fase existe para corrigir | mitigate | Invariante A ligado ao `catalog:validate`, com prova contra a fixture pre-migracao; rotulos de `variables.json` e `columnMap.json` regenerados do rotulo canonico |
| T-08-06-02 | Tampering | Literal de id morto sobrevivendo em codigo de producao | mitigate | Invariante F varrendo `git ls-files` com a mesma funcao de deteccao usada pelo motor, com allowlist de 3 caminhos justificados |
| T-08-06-03 | Denial of Service | Rodar `catalog:rebuild` apagaria os 9 packs em silencio por nao achar o corpus sob o id canonico | mitigate | Proibicao explicita na task com o motivo; packs sao renomeados, nunca reconstruidos, nesta fase |
| T-08-06-04 | Repudiation | Commit gigante impossivel de revisar | mitigate | Quase todo o diff e gerado e reproduzivel: `rename-map.json` foi revisado antes, e os invariantes B e a recomputacao do mapa permitem regerar e comparar |
| T-08-06-05 | Elevation of privilege | `paths.mjs` construindo caminho de corpus a partir de id tombstone | mitigate | `assertNotTombstone` no loop de `PACK_SOURCES` |
| T-08-06-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao nesta fase (RESEARCH secao Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run gate` verde
- `node scripts/catalog/applyRenameMap.mjs --check` sai com codigo 0
- `npm run catalog:validate` falha fechado ao adulterar `diseases.json`, `exclusions.json` ou `columnMap.json`
- Um unico commit criado, com os 9 packs registrados como renomeacao
</verification>

<success_criteria>
- Taxonomia de 331 agravos commitada, gerada do snapshot, com o codigo 330 incluido
- 9 packs, 50 entradas de `variables.json`, `columnMap.json`, seeds SQL e copia no bundle coerentes com a taxonomia canonica
- Zero ocorrencia ancorada de id-tombstone em `src/`, `scripts/` e `public/`, fora dos 3 caminhos allowlistados
- Invariantes A, B, C, D e D2 ligados ao `catalog:validate`; invariante F ativo na suite
- `assertNotTombstone` nos dois pontos de entrada: build (`paths.mjs`) e escrita (`uploadSihToSupabase.mjs`)
- Um unico commit, gate verde nele
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-06-SUMMARY.md` when done
</output>
