---
phase: 08-taxonomia-can-nica-integridade
plan: 04
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - scripts/catalog/tombstones.mjs
  - scripts/catalog/applyRenameMap.mjs
  - scripts/catalog/generateDiseaseSeeds.mjs
  - scripts/catalog/uploadSihToSupabase.mjs
  - src/features/catalog/tombstones.test.ts
  - package.json
autonomous: true
requirements: [TAX-06]

must_haves:
  truths:
    - "A renomeacao dos 32 arquivos contaminados e executada por um motor dirigido por rename-map.json, nunca por edicao manual de lista de ids (D-12)"
    - "O motor e a varredura do invariante F compartilham o mesmo construtor de padrao, entao reescrita e deteccao nunca discordam"
    - "Nome de coluna de CSV legado e nome de diretorio de coleta nao sao renomeados — sao camada distinta de disease.id (D-22 e RESEARCH Open Question 2)"
    - "As seeds SQL passam a ser geradas da taxonomia canonica: rodar o gerador contra a taxonomia de hoje reproduz byte a byte os sql/*.sql commitados (TAX-06)"
    - "O ponto de entrada de escrita no Supabase recusa ruidosamente qualquer id-tombstone, inclusive os dois que hoje sao id canonico de outra doenca (D-06)"
    - "Packs, os 10 imports estaticos e as 50 entradas de variables.json entram no alcance do motor agora, apesar de a Fase 10 substituir os packs (D-21)"
    - "A auditoria da Edge Function sih-ingest esta fechada sem acao de codigo: o fonte foi baixado e nao tem nenhum disease_id fixado (D-07, RESEARCH secao 7). Ela e a terceira superficie de escrita, e a guarda de tombstone fica do lado do pipeline Node, nao dentro da funcao"
    - "Nada no repositorio foi renomeado ainda: o motor roda em dry-run e o gate segue verde ate o flip"
  artifacts:
    - path: "scripts/catalog/tombstones.mjs"
      provides: "Fonte unica de tombstones, padroes ancorados e guarda de escrita"
      exports: ["TOMBSTONES", "CANONICAL_BY_OLD", "tombstonePatterns", "findTombstoneHits", "assertNotTombstone"]
    - path: "scripts/catalog/applyRenameMap.mjs"
      provides: "Motor de renomeacao com dry-run, apply e check"
    - path: "scripts/catalog/generateDiseaseSeeds.mjs"
      provides: "Gerador das seeds sql/0.sql..N.sql a partir de diseases.json"
    - path: "src/features/catalog/tombstones.test.ts"
      provides: "Cobertura dos padroes ancorados e da fidelidade do gerador de seeds"
  key_links:
    - from: "scripts/catalog/applyRenameMap.mjs"
      to: "scripts/catalog/tombstones.mjs"
      via: "usa tombstonePatterns para reescrever"
      pattern: "tombstonePatterns"
    - from: "scripts/catalog/uploadSihToSupabase.mjs"
      to: "scripts/catalog/tombstones.mjs"
      via: "assertNotTombstone no nome de diretorio e no disease_id de cada linha"
      pattern: "assertNotTombstone"
---

<objective>
Construir a maquinaria que executa a renomeacao: um motor dirigido por `rename-map.json`, um gerador de seeds SQL, e a guarda de tombstone no ponto de escrita do Supabase.

Purpose: o raio de contaminacao reconfirmado e de 32 arquivos e 153 ocorrencias, das quais 15 sao arquivos de teste e 3 sao codigo de producao. Renomear isso a mao e a mesma operacao que produziu o bug original — uma tabela de 21 linhas escrita a mao passou por revisao humana e ainda assim errou. Nesta fase, listas de ids nao se escrevem a mao: derivam do mapa e a recomputacao e afirmada.

Output: `tombstones.mjs` (fonte unica de padroes), `applyRenameMap.mjs` (motor), `generateDiseaseSeeds.mjs` (seeds), guarda D-06 no upload, e teste de cobertura.

**O motor nao e aplicado nesta plan.** Roda so em `--dry-run`, e o relatorio dele e o criterio de aceite. A aplicacao acontece na plan 08-06, no mesmo commit dos invariantes (D-24).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md
@scripts/catalog/uploadSihToSupabase.mjs
@scripts/catalog/paths.mjs
@.planning/phases/08-taxonomia-can-nica-integridade/08-02-SUMMARY.md

<interfaces>
<!-- Contratos ja entregues. Usar direto. -->

`scripts/catalog/rename-map.json`: `renames` (array de `tabnetCode`, `old`, `canonical`, `label`), `added` (1 entrada, codigo 330), `removed` (vazio), `tombstones` (21 ids).

Formato exato das seeds `scripts/catalog/sql/<n>.sql`, medido byte a byte:
- linha 1: `insert into sih_disease (id,label,filter_kind,tabnet_code,def_path) values`
- uma linha por agravo: `('<id>','<label>','<filterKind>','<tabnetCode>','<def>'),` — a ultima linha do arquivo **sem** virgula
- linha final: `on conflict (id) do update set label=excluded.label, filter_kind=excluded.filter_kind, tabnet_code=excluded.tabnet_code, def_path=excluded.def_path;`
- **sem newline no fim do arquivo**
- 80 agravos por arquivo; hoje sao 5 arquivos: `0.sql`..`3.sql` com 80 e `4.sql` com 10 (total 330). Pos-flip: `4.sql` fica com 11 (total 331)
- ordem = ordem de `diseases.json`; nenhum rotulo atual contem apostrofo, mas o gerador precisa dobrar apostrofo por seguranca

Shape de `public/data/catalog/manifest.json`: `version`, `generatedAt`, `catalogEntryCount`, `packs[]` com `packId`, `grain`, `sourceDir`, `rowCount`, `years`, `keys`.
Shape de um pack: `packId`, `grain`, `keys`, `metricKeys`, `rows[]`.
Shape de uma entrada de `variables.json`: `id`, `label`, `variableType`, `domain`, `sourceSystem`, `sourceName`, `tableOrIndicator`, `period`, `officialUrl`, `methodologyNotes`, `loadable`, `packId`, `columnKey`, `grain`, `unit`, `yearsAvailable`.

`scripts/catalog/uploadSihToSupabase.mjs` linhas 63-76: itera `readdirSync` sobre `trabalhos datasus/outputs/coleta_sih_multi`, usa o **nome do diretorio** como `disease_id` de cada linha enviada. E este o caminho de escrita que D-06 fecha.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tombstones.mjs — padroes ancorados e guarda de escrita (D-06)</name>

  <read_first>
    - `scripts/catalog/rename-map.json` — a fonte dos tombstones
    - `scripts/catalog/uploadSihToSupabase.mjs` — arquivo inteiro (106 linhas), com atencao ao loop das linhas 63-104
    - `scripts/catalog/parseCsv.mjs` — linhas 23-25: os nomes de coluna do CSV legado (`internacoes_embolia_trombose_arteriais` e irmaos) que **nao** podem casar com nenhum padrao
    - `scripts/catalog/paths.mjs` — linhas 58-91: `LEGACY_PACKS` com `sourceDir` `outputs/coleta_embolia_trombose_uf`, que tambem **nao** pode casar
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-06 (por que um id antigo passa pela FK e grava no agravo errado) e D-22
  </read_first>

  <files>scripts/catalog/tombstones.mjs, scripts/catalog/uploadSihToSupabase.mjs, package.json</files>

  <action>
Criar `scripts/catalog/tombstones.mjs` como fonte unica de verdade sobre id morto. Exportar `TOMBSTONES` (array lido de `rename-map.json`, nunca literal), `CANONICAL_BY_OLD` (mapa derivado de `renames`), `tombstonePatterns(oldId)`, `findTombstoneHits(text, tombstones)` e `assertNotTombstone(id, contexto)`.

`tombstonePatterns(oldId)` devolve os padroes **ancorados** que representam o id como identidade de agravo, e somente eles: `sih.<old>_uf` (packId), `sih.<old>.` (prefixo de id de variavel de catalogo), e o id nu delimitado por aspas simples ou duplas. Nenhum padrao pode ser substring solta.

Essa ancoragem e a parte de maior consequencia da task, porque tres coisas no repo contem o texto de um tombstone sem serem o agravo: `internacoes_embolia_trombose_arteriais` e irmaos em `parseCsv.mjs` (nomes de coluna do CSV historico), `outputs/coleta_embolia_trombose_uf` em `paths.mjs` e em `mergeMultiIntoLegacyCsv.mjs` (nome de diretorio do corpus, que D-22 mantem), e `base_embolia_trombose_arteriais_uf_2013_2025.csv` (nome de arquivo do CSV). **Nenhum dos tres pode ser renomeado nesta fase** — sao camada distinta de `disease.id`, e mexer neles significaria reprocessar os CSVs brutos, que e escopo da Fase 9. Os padroes ancorados os deixam de fora por construcao.

`findTombstoneHits` devolve as ocorrencias com id, forma casada e offset. Ela e usada tanto pelo motor (para reescrever) quanto pelo invariante F (para detectar) — a mesma funcao nos dois lados garante que reescrita e varredura nunca discordem sobre o que conta como tombstone.

`assertNotTombstone(id, contexto)` **lanca** com mensagem em PT-BR citando o id, o contexto e o canonico correspondente. E a unica funcao deste modulo que lanca, porque e guarda de caminho de escrita, nao checagem de validacao.

Ligar a guarda em `scripts/catalog/uploadSihToSupabase.mjs` em dois pontos: antes de montar as linhas, chamar `assertNotTombstone(diseaseId, ...)` para cada nome de diretorio lido do corpus; e antes de cada `upsert`, afirmar que nenhum `disease_id` das linhas e tombstone. Os dois ids que sao **ao mesmo tempo** tombstone e canonico de outra doenca (`hemorroidas` e `embolia_pulmonar`, pelos dois ciclos) sao recusados do mesmo jeito — e exatamente esse o caso que a FK deixa passar sem erro e grava no agravo errado, e o motivo de D-06 existir. A consequencia pratica e que subir dado de um diretorio legado passa a falhar alto ate a Fase 9 re-chavear o pipeline; e o comportamento correto e o que a Fase 9 quer encontrar (D-22).

**Nao** ligar guarda em `scripts/catalog/paths.mjs` nesta plan: `paths.mjs` le `diseases.json`, que ainda esta corrompido, e um assert ali deixaria `catalog:validate` vermelho antes do flip. Essa ligacao acontece na plan 08-06.

Acrescentar em `package.json` os scripts `catalog:apply-rename` (`node scripts/catalog/applyRenameMap.mjs`) e `catalog:seeds` (`node scripts/catalog/generateDiseaseSeeds.mjs`), sem encadear em `pretest` nem `gate`.
  </action>

  <verify>
    <automated>npm run catalog:validate && npm run test:run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "'avc'" scripts/catalog/tombstones.mjs` retorna `0` — a lista vem de `rename-map.json`
    - `TOMBSTONES` tem 21 entradas; `CANONICAL_BY_OLD` tem 21 chaves
    - `findTombstoneHits` sobre o texto de `scripts/catalog/parseCsv.mjs` devolve zero ocorrencias
    - `findTombstoneHits` sobre o texto de `scripts/catalog/paths.mjs` devolve ocorrencias apenas nas formas `sih.<old>_uf` e id nu entre aspas — nunca no `sourceDir` `outputs/coleta_embolia_trombose_uf`
    - `findTombstoneHits` sobre `src/features/catalog/taxonomy.ts` devolve ocorrencias (o arquivo tem 7 hoje)
    - `assertNotTombstone` lanca para `hemorroidas` e para `embolia_pulmonar` — os dois ids de ciclo — com mensagem citando o canonico
    - `grep -c assertNotTombstone scripts/catalog/uploadSihToSupabase.mjs` retorna pelo menos `2`
    - `grep -c assertNotTombstone scripts/catalog/paths.mjs` retorna `0` nesta plan
    - `npm run catalog:validate` sai com codigo 0 e `npm run test:run` verde
  </acceptance_criteria>

  <done>Existe uma definicao unica e ancorada do que e um id morto, o caminho de escrita no Supabase a recusa ruidosamente, e nome de coluna e nome de diretorio legado ficam intocados por construcao.</done>
</task>

<task type="auto">
  <name>Task 2: applyRenameMap.mjs — o motor de renomeacao, em dry-run</name>

  <read_first>
    - `scripts/catalog/rename-map.json` e `scripts/catalog/tombstones.mjs` (Task 1)
    - `scripts/catalog/columnMap.json` — as chaves `sih.avc_uf` (5 colunas padrao com rotulo clinico legado errado) e `sih.embolia_trombose_uf` (10 colunas, nomes de CSV historico mais `cnes.*`/`sidra.*` compartilhadas)
    - `scripts/catalog/syncColumnMap.mjs` — linhas 14-50: `MEASURES` e `standardColumns`, a forma canonica de uma folha regenerada
    - `public/data/catalog/manifest.json` e um arquivo de `public/data/catalog/packs/` — os shapes citados em `<interfaces>`
    - `public/data/catalog/variables.json` — as 10 entradas de `sih.embolia_trombose_uf` e as 5 de `sih.avc_uf`, para ver a diferenca entre coluna padrao e coluna legada
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 4 completa (cadeia de regeneracao, os dois hardcodes que sobrevivem ao rebuild) e a tabela de blast radius no fim
  </read_first>

  <files>scripts/catalog/applyRenameMap.mjs</files>

  <action>
Criar `scripts/catalog/applyRenameMap.mjs` com tres modos: `--dry-run` (padrao, so relata), `--apply` (muta) e `--check` (varre e sai diferente de zero se sobrar tombstone no escopo). Toda decisao vem de `rename-map.json` e dos padroes de `tombstones.mjs`; nenhum id literal no arquivo.

Escopo de arquivos: `src/**/*.ts`, `src/**/*.tsx`, `scripts/catalog/*.mjs`, `scripts/catalog/columnMap.json`, `public/data/catalog/**`. **Fora do escopo, deliberadamente:** `.planning/**` (documentacao historica, incluindo a nota de ground truth, que precisa continuar citando os ids antigos), `trabalhos datasus/**` (os 341 diretorios de coleta e os scripts Python — D-22, Fase 9), `docs/**`, e `scripts/catalog/rename-map.json` (unico lugar legitimo dos ids antigos, D-23).

Regras por artefato:

**Packs** — para cada `rename` cujo arquivo `public/data/catalog/packs/sih.<old>_uf.json` existir: renomear o arquivo para `sih.<canonical>_uf.json` usando `git mv` (preserva historico) e reescrever o campo `packId` interno. Sao 9 dos 10 packs; `sih.amputacao_mmii_uf.json` nao entra em nenhum rename.

**`public/data/catalog/manifest.json`** — reescrever `packs[].packId`. **Preservar `packs[].sourceDir` como esta**: ele aponta para o diretorio real do corpus, que D-22 mantem com o nome antigo; renomear ali seria afirmar um caminho que nao existe. Registrar essa excecao como comentario no proprio motor.

**`public/data/catalog/variables.json`** — para cada entrada: reescrever `packId` na forma `sih.<old>_uf` e `id` na forma `sih.<old>.`; quando o `columnKey` for uma das cinco medidas padrao (`internacoes`, `obitos`, `valor_total`, `dias_permanencia`, `taxa_mortalidade`), regenerar o `label` como o prefixo padrao daquela medida seguido de travessao e o **rotulo canonico** do agravo, porque e ali que mora o rotulo enganoso que o estudante le (hoje `sih.avc.internacoes` aparece como "Internacoes — AVC / acidente vascular cerebral" servindo o codigo 163). Para colunas fora das cinco padrao, so reescrever `id`/`packId` e deixar o rotulo curado como esta — os rotulos do pack legado ja descrevem corretamente o codigo 183. **Nao alterar o campo `domain`**: mudar `vascular` para `sih_lista_morb` reagruparia a tela de Variaveis sem que nada estivesse mentindo, e isso e escopo de outra decisao.

**`scripts/catalog/columnMap.json`** — renomear a chave de nivel 1 de `sih.<old>_uf` para `sih.<canonical>_uf` e **remover a antiga** (poda). A poda e obrigatoria: `syncColumnMap.mjs` faz merge incremental e nunca remove chave, entao sem esse passo o arquivo ficaria com as duas versoes lado a lado e a extensao D2 acusaria. Dentro da entrada movida, regenerar por completo as folhas das cinco medidas padrao com a forma de `standardColumns(canonicalId, canonicalLabel)`, e nas demais folhas so reescrever o prefixo de `id`.

**Codigo-fonte** (`src/**/*.ts`, `src/**/*.tsx`, `scripts/catalog/*.mjs`) — substituicao textual usando exclusivamente os padroes ancorados de `tombstonePatterns`. Isso cobre `taxonomy.ts` (`catalogIdFor` e `CATALOG_ID_ALIASES`), `catalogAnalysisData.ts` (imports, `VARIABLE_ID_ALIASES`, `getDefaultCatalogVariableId`), `mockAnalysisData.ts` (`MOCK_LABEL_TO_ID` e o argumento de `getCatalogTimeSeriesYears`), os 15 arquivos de teste, e as duas guardas literais duplicadas `disease.id === 'embolia_trombose'` que vivem em `paths.mjs:88` e `syncColumnMap.mjs:54`. Essas duas sao codigo de producao, sao identicas, e se so uma for atualizada a outra passa a comparar contra um id que nao existe mais e nunca mais casa — tratando em silencio um pack legado como generico ou o contrario. O motor atualiza as duas na mesma passada por serem a mesma forma ancorada.

**`syncPackImports.mjs`** merece atencao especial e nao e resolvido por rodar o script: o bloco `VARIABLE_ID_ALIASES` que ele escreve em `catalogAnalysisData.ts` esta hardcoded como string literal **dentro do proprio gerador** (linhas 73-79). A substituicao ancorada alcanca esse literal porque ele esta na forma `sih.<old>.`; conferir no relatorio de dry-run que `syncPackImports.mjs` aparece na lista de arquivos alterados. Se nao aparecer, o padrao esta errado.

O relatorio de `--dry-run` imprime, por artefato: arquivos que seriam alterados, numero de ocorrencias por forma, packs a renomear, chaves de `columnMap` a mover e a podar, e — apos simular todas as substituicoes em memoria — quantas ocorrencias de tombstone sobrariam no escopo. Esse ultimo numero tem que ser zero; se nao for, o relatorio lista arquivo e offset de cada sobra.

`--apply` faz as mesmas mutacoes em disco. **Nao rodar `--apply` nesta plan.**
  </action>

  <verify>
    <automated>node scripts/catalog/applyRenameMap.mjs --dry-run && npm run catalog:validate && npm run test:run</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/catalog/applyRenameMap.mjs --dry-run` sai com codigo 0 e imprime `sobras: 0`
    - O relatorio lista 9 packs a renomear e nenhum deles e `sih.amputacao_mmii_uf`
    - O relatorio lista 50 entradas de `variables.json` a alterar (10 do pack legado mais 5 vezes 8 packs padrao) e nao lista as 16 entradas sem `packId`
    - O relatorio lista 21 chaves de `columnMap.json` a mover e 21 a podar
    - O relatorio inclui, entre os arquivos de codigo, `src/features/catalog/taxonomy.ts`, `src/features/catalog/catalogAnalysisData.ts`, `src/routes/mapas/mockAnalysisData.ts`, `scripts/catalog/paths.mjs`, `scripts/catalog/syncColumnMap.mjs` e `scripts/catalog/syncPackImports.mjs`
    - O relatorio inclui 15 arquivos `.test.ts`/`.test.tsx`
    - O relatorio **nao** inclui `scripts/catalog/parseCsv.mjs`, `scripts/catalog/mergeMultiIntoLegacyCsv.mjs`, nenhum caminho sob `.planning/`, `trabalhos datasus/` ou `docs/`, nem `scripts/catalog/rename-map.json`
    - `git status` limpo depois do dry-run — o modo padrao nao muta nada
    - `grep -c "'embolia_trombose'" scripts/catalog/applyRenameMap.mjs` retorna `0`
    - `npm run catalog:validate` sai com codigo 0 e `npm run test:run` verde
  </acceptance_criteria>

  <done>O motor sabe exatamente o que vai mudar em cada um dos artefatos contaminados, prova que nao sobra tombstone, e nao tocou em nada ainda.</done>
</task>

<task type="auto">
  <name>Task 3: generateDiseaseSeeds.mjs — seeds SQL geradas, com prova de fidelidade</name>

  <read_first>
    - `scripts/catalog/sql/0.sql` e `scripts/catalog/sql/4.sql` — o formato exato: header, linhas de valores, ultima sem virgula, linha `on conflict`, sem newline final
    - `scripts/catalog/diseases.json` — a ordem das entradas, que e a ordem das seeds
    - `src/test/fixtures/taxonomy/diseases.pre-migracao.json` — a fixture congelada, contra a qual o gerador prova fidelidade
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 3 (convencoes) e a linha da tabela sobre `sql/0.sql`..`4.sql`
  </read_first>

  <files>scripts/catalog/generateDiseaseSeeds.mjs, src/features/catalog/tombstones.test.ts</files>

  <action>
Criar `scripts/catalog/generateDiseaseSeeds.mjs` no molde CLI-mais-export: exporta `renderSeedChunks(diseases, chunkSize)` devolvendo um array de strings (uma por arquivo), e o `main()` escreve `scripts/catalog/sql/<n>.sql`.

A saida tem que reproduzir o formato atual byte a byte, incluindo o detalhe de nao haver newline no fim do arquivo e de a ultima linha de valores nao levar virgula. Escapar apostrofo dobrando, mesmo que nenhum rotulo atual tenha um.

Antes de escrever qualquer arquivo, o `main()` remove os `sql/*.sql` existentes e reescreve o conjunto inteiro, para que reduzir a contagem de agravos nunca deixe um arquivo orfao para tras.

Prova de fidelidade — esta e a razao de a task existir: `renderSeedChunks` aplicado a fixture pre-migracao (330 entradas, chunk 80) precisa produzir strings **byte-identicas** aos cinco `sql/*.sql` commitados hoje. As seeds foram mantidas a mao ate agora; so vale substitui-las por um gerador se o gerador reproduzir exatamente o que ja existe. Se divergir, o formato do gerador esta errado — nao os arquivos.

Criar `src/features/catalog/tombstones.test.ts` sob `src/`, cobrindo esta task e a Task 1:
- `renderSeedChunks(fixturePreMigracao, 80)` devolve 5 strings byte-identicas aos cinco `sql/*.sql` lidos do repo
- `renderSeedChunks(generateFromSnapshot().diseases, 80)` devolve 5 strings, a ultima com 11 linhas de valores (331 agravos), e a ultima entrada e `amputacao_mmii`
- nenhuma string gerada contem qualquer tombstone (usar `findTombstoneHits`)
- `tombstonePatterns` nao casa nas tres formas legadas que ficam: nome de coluna `internacoes_embolia_trombose_arteriais`, diretorio `outputs/coleta_embolia_trombose_uf` e arquivo `base_embolia_trombose_arteriais_uf_2013_2025.csv`
- `tombstonePatterns` casa nas tres formas de agravo: `sih.avc_uf`, `sih.avc.internacoes` e o id entre aspas
- `assertNotTombstone` lanca para os dois ids de ciclo e nao lanca para `amputacao_mmii`

**Nao rodar `main()` nesta plan** — reescrever `sql/*.sql` agora produziria seeds identicas as atuais (porque `diseases.json` ainda esta corrompido) e so poluiria o diff; a geracao real acontece no flip.
  </action>

  <verify>
    <automated>npx vitest run src/features/catalog/tombstones.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/features/catalog/tombstones.test.ts` passa com pelo menos 7 casos
    - O caso de fidelidade compara as 5 strings com o conteudo lido de `scripts/catalog/sql/0.sql`..`4.sql` e passa sem nenhuma normalizacao de espaco ou newline
    - O caso canonico produz 5 chunks somando 331 linhas de valores
    - `git status` nao mostra alteracao em `scripts/catalog/sql/`
    - `grep -c "lista_morb'" scripts/catalog/generateDiseaseSeeds.mjs` retorna `0` — o `filterKind` vem do dado, nao de literal no gerador
    - `npm run test:run` verde e `npm run catalog:validate` sai com codigo 0
  </acceptance_criteria>

  <done>As seeds SQL deixam de ser lista mantida a mao e passam a ser artefato gerado, com prova de que o gerador reproduz exatamente o que existia antes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline offline (service role) para Supabase | `uploadSihToSupabase.mjs` escreve com a service role key; o `disease_id` que ele envia vem do nome de um diretorio do sistema de arquivos |
| `rename-map.json` para 32 arquivos do repo | Um mapa errado renomeia errado em massa, de forma consistente e portanto invisivel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | Upload gravando dado sob id antigo que hoje e canonico de outra doenca (os dois ciclos) | mitigate | `assertNotTombstone` no nome de diretorio e no `disease_id` de cada linha, antes de qualquer `upsert`; recusa inclusive `hemorroidas` e `embolia_pulmonar` (D-06) |
| T-08-04-02 | Tampering | Substituicao textual larga demais renomeando nome de coluna de CSV ou diretorio de corpus | mitigate | Padroes ancorados em `tombstones.mjs`, com teste explicito das tres formas legadas que precisam sobreviver |
| T-08-04-03 | Tampering | Reescrita e varredura discordarem sobre o que e tombstone, deixando sobra invisivel | mitigate | Motor e invariante F consomem a mesma `findTombstoneHits`; dry-run reporta `sobras: 0` |
| T-08-04-04 | Denial of Service | `--apply` rodado por engano antes do flip deixaria o repo meio-renomeado e o gate vermelho | mitigate | `--dry-run` e o modo padrao; criterio de aceite exige `git status` limpo |
| T-08-04-05 | Information disclosure | `SUPABASE_SERVICE_ROLE_KEY` lido de ambiente no script de upload | accept | Comportamento existente, offline-only por PROJECT.md; nenhuma credencial nova nesta fase. O `INGEST_SECRET` em texto plano em `scrape_upload_sih.py:33` fica registrado e **nao** e corrigido aqui — rotacao e Fase 9 |
| T-08-04-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao nesta fase (RESEARCH secao Package Legitimacy Audit) |
</threat_model>

<verification>
- `node scripts/catalog/applyRenameMap.mjs --dry-run` sai com codigo 0 e reporta `sobras: 0`
- `npm run catalog:validate` sai com codigo 0
- `npm run test:run` verde
- `npm run gate` verde
- `git status` limpo tirando os arquivos novos previstos — nenhum artefato de catalogo renomeado ainda
</verification>

<success_criteria>
- `tombstones.mjs` e a fonte unica de tombstones e de padroes ancorados, sem id literal
- Upload no Supabase recusa ruidosamente qualquer id-tombstone, incluindo os dois de ciclo
- Motor de renomeacao cobre packs, manifest, variables, columnMap e codigo-fonte, e prova em dry-run que nao sobra tombstone
- Nome de coluna de CSV legado, diretorio de corpus e `.planning/` ficam intocados por construcao
- Gerador de seeds reproduz byte a byte os `sql/*.sql` atuais quando alimentado com a taxonomia pre-migracao
- Nada foi renomeado; gate verde
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-04-SUMMARY.md` when done
</output>
