---
phase: 08-taxonomia-can-nica-integridade
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - scripts/catalog/validate.mjs
  - src/features/catalog/taxonomyInvariants.test.ts
autonomous: true
requirements: [TAX-01, TAX-02, TAX-06]

must_haves:
  truths:
    - "O invariante A acusa exatamente os 21 registros corrompidos quando roda contra a taxonomia pre-migracao — a prova de TAX-02 possivel sob D-24"
    - "O invariante A passa limpo contra a taxonomia canonica regenerada, com allowlist justificada de uma entrada so (amputacao_mmii)"
    - "O invariante B pega edicao a mao dos dois artefatos gerados, cobrindo diseases.json e diseases.lista.json (D-10/D-16)"
    - "O invariante D acusa o codigo 330 caindo fora sem motivo registrado — o segundo bug da linha 80"
    - "columnMap.json fica coberto por uma extensao do invariante D, porque e dado e o invariante F so alcanca codigo-fonte"
    - "Nenhum invariante novo esta ligado ao main() ainda: a taxonomia commitada segue corrompida e o gate segue verde ate o flip (D-24)"
  artifacts:
    - path: "scripts/catalog/validate.mjs"
      provides: "Invariantes A, B, D e D2 como funcoes puras exportadas no molde de checkEntry"
      exports: ["checkSlugConsistency", "checkRegeneration", "checkRegenerationFromDisk", "checkPartition", "checkColumnMapKeys"]
    - path: "src/features/catalog/taxonomyInvariants.test.ts"
      provides: "Prova de TAX-02 contra a fixture pre-migracao mais cobertura de A, B, D e D2"
      min_lines: 90
  key_links:
    - from: "src/features/catalog/taxonomyInvariants.test.ts"
      to: "src/test/fixtures/taxonomy/diseases.pre-migracao.json"
      via: "leitura da fixture congelada para provar que A pega os 21"
      pattern: "diseases.pre-migracao.json"
    - from: "src/features/catalog/taxonomyInvariants.test.ts"
      to: "scripts/catalog/rename-map.json"
      via: "deriva a lista esperada de ids corrompidos do mapa, nunca digitada"
      pattern: "rename-map.json"
---

<objective>
Escrever os invariantes A (semantico), B (regeneracao byte-identica), D (particao completa) e a extensao D2 (`columnMap.json`) como funcoes puras no molde ja existente de `validate.mjs`, e provar que o invariante A barra o bug real rodando contra a taxonomia pre-migracao como fixture.

Purpose: TAX-02 pede explicitamente que "o bug avc para 163 seria barrado". D-24 poe renomeacao e invariantes no mesmo commit, entao o invariante nunca e observado falhando contra o repo — a unica prova possivel e a fixture congelada. Esta plan produz essa prova.

Output: cinco funcoes exportadas em `validate.mjs` e um arquivo de teste sob `src/` que roda cada uma contra a fixture corrompida e contra a taxonomia canonica.

**Nenhuma delas e chamada no `main()` nesta plan.** A, B, D e D2 falham contra o estado atual do repo por construcao — sao elas que descrevem o bug. Ligar ao CLI acontece na plan 08-06, no mesmo commit da renomeacao (D-24).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md
@scripts/catalog/validate.mjs
@.planning/phases/08-taxonomia-can-nica-integridade/08-02-SUMMARY.md

<interfaces>
<!-- Contratos entregues pelas plans 08-01 e 08-02. Usar direto. -->

`scripts/catalog/sync-lista-morb.mjs`:
- `generateFromSnapshot()` devolve `{ diseases, runtime, diseasesText, listaText }`
- `buildDiseases({ options, exclusions, extras })`, `buildRuntimeLista(diseases, cidByCode)`
- `renderDiseasesJson(diseases)`, `renderListaJson(runtime)` — as strings exatas gravadas em disco

`scripts/catalog/listaMorbSource.mjs`:
- `slugify(label, code)`, `parseListaMorbOptions(html)`, `loadSnapshot()`, `checkSnapshotIntegrity()`

`scripts/catalog/rename-map.json`:
campos `version`, `generatedBy`, `before`, `after`, `renames` (array de `tabnetCode`, `old`, `canonical`, `label`), `added` (array de `tabnetCode`, `id`, `label`, `filterKind`, `def`), `removed` (vazio) e `tombstones` (array de 21 ids).

`scripts/catalog/extra-diseases.json` — array de 1 entrada com `id`, `label`, `filterKind`, `tabnetCode`, `def`, `reason`. A presenca de `reason` e o que qualifica um id para a allowlist do invariante A: allowlist justificada por dado, nunca lista solta em codigo.

`scripts/catalog/exclusions.json` — chaves `TODAS_AS_CATEGORIAS__`, `331`, `332`, `333`; valores sao motivos em PT-BR.

Molde obrigatorio de `validate.mjs` (`checkEntry`, linhas 42-112; `main()`, 290-320): funcao exportada devolve `string[]`, nunca lanca, nunca chama `process.exit`; mensagem no formato `<contexto>: descricao curta`; so `main()` decide exit code, atras de `isDirectRun`.

Shape de `scripts/catalog/columnMap.json`: objeto de objetos. Nivel 1 chaveado por `packId` na forma `sih.<diseaseId>_uf`; nivel 2 por nome de coluna; cada folha tem `id` na forma `sih.<diseaseId>.<sufixo>`, mais `label`, `variableType`, `domain`, `unit`, `sourceSystem`, `sourceName`, `tableOrIndicator`, `sourceKey`.
As cinco colunas padrao geradas por `syncColumnMap.mjs` sao `internacoes`, `obitos`, `valor_total`, `dias_permanencia` e `taxa_mortalidade`, com sufixos de id `internacoes`, `obitos`, `custo`, `dias_permanencia`, `taxa_mortalidade`, prefixos de rotulo `Internacoes`, `Obitos`, `Custo (valor total)`, `Dias de permanencia`, `Taxa de mortalidade hospitalar`, e `domain` igual a `sih_lista_morb`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invariantes A, B, D e D2 como funcoes puras em validate.mjs</name>

  <read_first>
    - `scripts/catalog/validate.mjs` — arquivo inteiro (320 linhas). Molde e `checkEntry` (42-112), agregacao e `checkCatalog` (122-155), CLI e `main()` mais `isDirectRun` (290-320)
    - `scripts/catalog/sync-lista-morb.mjs` — assinaturas das funcoes puras criadas na 08-02
    - `scripts/catalog/columnMap.json` — inspecionar as chaves `sih.avc_uf` (pack padrao, 5 colunas, rotulos clinicos legados errados) e `sih.embolia_trombose_uf` (pack legado, colunas com nome de CSV historico mais colunas compartilhadas `cnes.*`/`sidra.*`)
    - `scripts/catalog/syncColumnMap.mjs` — linhas 34-68: `standardColumns` (a forma canonica de uma folha) e o loop de merge que nunca remove chave
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 5 (tabela de invariantes A-F e divisao CLI vs vitest)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 1 e o paragrafo da secao 4 sobre merge incremental
  </read_first>

  <files>scripts/catalog/validate.mjs</files>

  <action>
Acrescentar cinco funcoes exportadas a `scripts/catalog/validate.mjs`, todas no molde de `checkEntry`: recebem dado, devolvem array de mensagens, nunca lancam, nunca saem do processo. Nenhuma entra no `main()` nesta plan.

`checkSlugConsistency(diseases, allowlist)` — **invariante A** (D-10). Para cada entrada afirma que `slugify(entry.label, entry.tabnetCode)` e igual a `entry.id`. Ids presentes em `allowlist` sao pulados. A `allowlist` e derivada de `extra-diseases.json`: o conjunto dos `id` cujas entradas tem `reason` nao vazio. Allowlist explicita e justificada por dado, nunca array literal dentro da funcao. Mensagem no formato que cita o id, o slug esperado e o `tabnetCode`.

`checkRegeneration(committed, generated)` — **invariante B** (D-10/D-16). O primeiro argumento traz os textos lidos do repo, o segundo os textos que `generateFromSnapshot()` produziu. Compara **string com string**, um erro por artefato divergente, citando o offset do primeiro caractere diferente e um trecho curto dos dois lados. Cobrir os dois artefatos e obrigatorio: `scripts/catalog/diseases.json` (indentado com 2 espacos) e `src/features/catalog/diseases.lista.json` (minificado numa linha). Um `JSON.stringify` com a indentacao trocada em qualquer um dos dois falha byte a byte mesmo com dado identico — a assimetria e intencional e load-bearing.

`checkRegenerationFromDisk()` — wrapper fino que le os dois arquivos e delega para `checkRegeneration`. E ele que o `main()` vai chamar na plan 08-06; aqui so existe.

`checkPartition(input)` — **invariante D** (D-14), recebendo `options` do snapshot, `exclusions`, `diseases` e `extras`. Toda opcao do snapshot ou tem o `code` presente como `tabnetCode` em `diseases`, ou tem o `code` como chave de `exclusions` com motivo nao vazio, nunca as duas coisas. Todo `tabnetCode` de `diseases` ausente do snapshot precisa vir de `extras` — e o caso de `amputacao_mmii`, codigo 3331, procedimento fora da Lista Morb. Quatro mensagens distintas: codigo ausente dos dois lados (particao incompleta), codigo nos dois lados ao mesmo tempo, codigo em `diseases` sem estar no snapshot nem em `extras`, e motivo vazio em `exclusions`.

`checkColumnMapKeys(columnMap, diseases)` — **extensao D2**. Existe por dois motivos concretos: `columnMap.json` e **dado**, e o invariante F (varredura do repositorio) so alcanca codigo-fonte, entao sem D2 as chaves antigas ficariam como tombstone residual sem ninguem olhando; e `syncColumnMap.mjs` faz merge incremental e **nunca remove chave** (confirmado por leitura completa do script), entao rodar o sync depois da renomeacao deixa as chaves velhas ao lado das novas. Checar: toda chave de nivel 1 casa `sih.<id>_uf` com `<id>` sendo id de agravo presente em `diseases`; nenhum agravo `lista_morb` fica sem chave; para cada uma das cinco colunas padrao presentes, a folha tem `id` igual a `sih.<diseaseId>.<sufixo>` e `label` igual ao prefixo padrao seguido do rotulo canonico do agravo. Colunas fora das cinco padrao sao checadas so quanto ao prefixo de `id`: se comecar com `sih.`, o segmento do meio tem que ser o id do agravo daquela chave.

Depois de escrever, rodar `npm run catalog:validate`: precisa continuar saindo com codigo 0 e imprimindo `catalog:validate OK`. Se sair 1, alguma das cinco foi ligada ao CLI por engano e o gate ficaria vermelho antes do flip.
  </action>

  <verify>
    <automated>npm run catalog:validate && npm run test:run</automated>
  </verify>

  <acceptance_criteria>
    - Um import ES do modulo expoe as cinco funcoes `checkSlugConsistency`, `checkRegeneration`, `checkRegenerationFromDisk`, `checkPartition` e `checkColumnMapKeys` como `function`
    - Nenhuma das cinco aparece dentro do corpo de `main()` — conferir lendo o bloco `main()` inteiro
    - Nenhuma das cinco contem `throw`, `process.exit` ou `console.` no corpo
    - `grep -c "amputacao_mmii" scripts/catalog/validate.mjs` retorna `0` — a allowlist do invariante A vem de `extra-diseases.json`, nao de literal
    - `npm run catalog:validate` sai com codigo 0 e imprime `catalog:validate OK`
    - `npm run test:run` continua verde
  </acceptance_criteria>

  <done>Os quatro invariantes de taxonomia existem como funcoes puras testaveis, no mesmo molde fail-closed da Fase 5, e o CLI continua verde porque nenhuma delas foi ligada ainda.</done>
</task>

<task type="auto">
  <name>Task 2: Prova de TAX-02 — invariantes rodando contra a fixture pre-migracao</name>

  <read_first>
    - `src/features/catalog/validateCatalogEntry.test.ts` — linhas 1-23: import cross-tree de 3 niveis e fabrica de fixture com overrides
    - `src/features/catalog/catalogAnalysisData.test.ts` — linhas 1-24: como ler arquivo real do repo dentro de um teste com `node:fs`/`node:path`, sem mock
    - `src/test/fixtures/taxonomy/diseases.pre-migracao.json` — a fixture congelada na 08-02
    - `scripts/catalog/rename-map.json` — de onde a lista esperada de ids corrompidos e derivada
    - `scripts/catalog/validate.mjs` — as cinco funcoes escritas na Task 1
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-VALIDATION.md` — a restricao bloqueante de localizacao de arquivo de teste
  </read_first>

  <files>src/features/catalog/taxonomyInvariants.test.ts</files>

  <action>
Criar `src/features/catalog/taxonomyInvariants.test.ts` — obrigatoriamente sob `src/`, porque o `include` do vitest e `src/**/*.{test,spec}.{ts,tsx}` e um teste em `scripts/` passaria por nunca ter executado, que e exatamente o modo de falha que esta fase existe para eliminar.

Casos do **invariante A**:

O caso central de TAX-02 — rodar `checkSlugConsistency` sobre a fixture pre-migracao com a allowlist derivada de `extra-diseases.json` e afirmar que devolve exatamente 21 mensagens, e que o conjunto de ids citados nas mensagens e igual ao array `tombstones` de `rename-map.json`. A lista esperada e **derivada do mapa**, nunca digitada. Afirmar tambem que a mensagem referente ao id `avc` cita o slug esperado `outras_doencas_do_olho_e_anexos` — e a formulacao literal de TAX-02, "o bug avc para 163 seria barrado".

Rodar `checkSlugConsistency` sobre `generateFromSnapshot().diseases` com a mesma allowlist e afirmar array vazio.

Rodar sobre a taxonomia canonica com allowlist vazia e afirmar exatamente uma mensagem, citando `amputacao_mmii` — prova que a allowlist e necessaria, e que e de uma entrada so.

Casos do **invariante B**: comparar os textos gerados contra eles mesmos e afirmar array vazio; mutar um caractere do texto de `diseases.json` e afirmar uma mensagem citando `diseases.json`; mutar um caractere do texto de `diseases.lista.json` e afirmar uma mensagem citando `diseases.lista.json`; re-serializar o runtime com indentacao de 2 espacos em vez de minificado e afirmar que B pega — e o caso que prova que a assimetria de formatacao esta coberta.

Casos do **invariante D**: rodar contra snapshot, `exclusions.json`, taxonomia canonica e `extra-diseases.json` reais e afirmar array vazio; rodar contra a **taxonomia pre-migracao** e afirmar que aparece mensagem citando o `tabnetCode` 330 (o codigo que caia fora sem motivo registrado); com um `exclusions` sintetico sem a chave `331`, afirmar mensagem citando 331; com uma opcao sintetica de codigo inexistente adicionada ao snapshot, afirmar mensagem citando esse codigo — e o caso de "codigo novo no TabNet falha alto em vez de entrar ou sair em silencio".

Casos da **extensao D2**: montar um `columnMap` sintetico com uma chave `sih.<id-inexistente>_uf` e afirmar mensagem; montar um com um agravo `lista_morb` faltando e afirmar mensagem; montar um cuja folha `internacoes` tem rotulo de outra doenca e afirmar mensagem — este ultimo e o que pega os rotulos clinicos enganosos que hoje moram em `columnMap.json` (a chave `sih.avc_uf` diz "Internacoes — AVC / acidente vascular cerebral" enquanto serve o codigo 163).

Nao transcrever nenhuma lista de ids: tudo que for esperado vem de `rename-map.json`, de `extra-diseases.json` ou da regeneracao. As unicas strings literais permitidas sao `avc`, `outras_doencas_do_olho_e_anexos`, `amputacao_mmii` e o codigo `330`, que sao asserçoes de comportamento especifico citadas nominalmente em TAX-02 e D-25.
  </action>

  <verify>
    <automated>npx vitest run src/features/catalog/taxonomyInvariants.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/features/catalog/taxonomyInvariants.test.ts` passa com pelo menos 12 casos
    - O caso central afirma comprimento `21` e igualdade de conjunto com `tombstones` de `rename-map.json`
    - O arquivo vive sob `src/` e importa de `../../../scripts/catalog/validate.mjs` e `../../../scripts/catalog/sync-lista-morb.mjs`
    - `grep -c "'ait'" src/features/catalog/taxonomyInvariants.test.ts` retorna `0` — nenhuma lista dos 21 transcrita
    - Reintroduzir temporariamente uma entrada do antigo `KNOWN_BY_CODE` no gerador faz o caso "canonica passa limpo" ficar vermelho; reverter depois de comprovar
    - `npm run test:run` verde de ponta a ponta e `npm run catalog:validate` sai com codigo 0
  </acceptance_criteria>

  <done>Existe prova executavel de que a validacao barra o bug real: o invariante A, rodado contra a taxonomia de hoje, acusa exatamente os 21 registros que o `rename-map.json` lista.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artefato gerado para runtime do app | `diseases.lista.json` entra no bundle e vira `DISEASES`; se divergir da fonte canonica, o estudante le rotulo errado |
| Dado de catalogo para rotulo exibido | `columnMap.json` alimenta `variables.json`, que e onde moram os rotulos que o estudante le |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | Edicao a mao de `diseases.json` ou `diseases.lista.json` | mitigate | Invariante B compara string com string contra a regeneracao do snapshot, cobrindo os dois artefatos com as duas formatacoes |
| T-08-03-02 | Spoofing | Rotulo clinico convincente servindo o codigo errado (o bug original) | mitigate | Invariante A com allowlist justificada por dado; D2 estende a checagem de rotulo ate `columnMap.json` |
| T-08-03-03 | Tampering | Chave tombstone sobrevivendo em `columnMap.json` apos o rename | mitigate | Extensao D2 — o invariante F nao alcanca dado, so codigo-fonte |
| T-08-03-04 | Denial of Service | Ligar os invariantes ao CLI antes do flip deixaria o gate vermelho e bloquearia todo commit | mitigate | As cinco funcoes ficam fora do `main()` nesta plan; criterio de aceite verifica `catalog:validate` saindo 0 |
| T-08-03-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao nesta fase (RESEARCH secao Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run catalog:validate` sai com codigo 0
- `npm run test:run` verde
- `npm run gate` verde
- `git diff --stat` mostra somente `scripts/catalog/validate.mjs` e o arquivo de teste novo
</verification>

<success_criteria>
- Cinco funcoes de invariante exportadas de `validate.mjs`, todas puras, nenhuma ligada ao `main()`
- Invariante A devolve exatamente 21 mensagens contra a fixture pre-migracao, conjunto identico aos `tombstones` do mapa
- Invariante A devolve zero mensagens contra a taxonomia canonica e exatamente uma sem allowlist
- Invariante B pega mutacao de um caractere em cada um dos dois artefatos e pega troca de formatacao
- Invariante D acusa o codigo 330 na taxonomia pre-migracao e acusa codigo novo no snapshot
- D2 acusa chave tombstone, agravo faltante e rotulo de outra doenca
- Gate verde ao fim da plan
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-03-SUMMARY.md` when done
</output>
