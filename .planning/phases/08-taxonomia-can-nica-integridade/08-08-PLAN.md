---
phase: 08-taxonomia-can-nica-integridade
plan: 08
type: execute
wave: 6
depends_on: [08-05, 08-06]
files_modified:
  - .planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md
autonomous: true
requirements: [TAX-03, TAX-04]

must_haves:
  truths:
    - "A migracao foi exercitada contra uma copia real de producao, com PK, FK, ON DELETE CASCADE e RLS reais, antes de tocar no banco vivo (D-02)"
    - "O ciclo up, verificacao, down, verificacao devolveu o banco local ao estado inicial — e isso que TAX-04 chama de reversivel (D-03)"
    - "A prova de integridade agregada por agravo e medida rodou e passou: nenhuma soma de internacoes, obitos, valor_total ou dias_permanencia mudou de dono (D-04)"
    - "O custo e o perfil de lock em volume real estao medidos, nao estimados — era a unica pergunta que a pesquisa deixou em aberto"
    - "As contagens alvo 331, 30.313 e 1.099.403 foram observadas num banco restaurado, nao so afirmadas em documento (TAX-03)"
    - "Nada de producao foi tocado nesta plan: o dump e leitura, o container e descartavel"
  artifacts:
    - path: ".planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md"
      provides: "Registro das medicoes de tempo, linhas tocadas e resultado do ciclo up/down — insumo do go/no-go da plan 08-10"
      contains: "tempo"
  key_links:
    - from: "ensaio local"
      to: "supabase/migrations/*_rename_disease_ids.sql"
      via: "psql --single-transaction contra o Postgres do container"
      pattern: "rename_disease_ids"
    - from: "ensaio local"
      to: "supabase/rollback/*_down.sql"
      via: "mesma invocacao na direcao inversa, provando D-03"
      pattern: "_down.sql"
---

<objective>
Ensaiar a migracao inteira contra um Postgres local restaurado de um `pg_dump` de producao: aplicar o up, rodar a prova de integridade, aplicar o down, e confirmar o retorno ao estado inicial — medindo tempo e volume real.

Purpose: e o ensaio obrigatorio do D-02, e a mudanca de maior raio de dano do milestone nao pode acontecer sem ele. **Este ensaio mede, nao revalida a mecanica.** RESEARCH secao 4.2 ja reproduziu de ponta a ponta as duas passadas com CTEs multiplas contra um Postgres 17 real, com schema identico ao de producao e os ciclos reais do ground truth, testando up e down com retorno exato ao estado original. O que ficou em aberto — e e o unico proposito desta plan — e **custo e locking em volume de producao**.

Output: um banco local restaurado, o ciclo completo exercitado, e um SUMMARY com as medicoes que a plan 08-10 vai citar antes de tocar producao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-05-SUMMARY.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-06-SUMMARY.md

<interfaces>
<!-- Ambiente e numeros ja confirmados. Nao redescobrir. -->

Supabase CLI: `/opt/homebrew/bin/supabase`, versao 2.90.0, **ja autenticado nesta maquina**. `supabase link` nao pede senha do banco — o CLI provisiona um papel de login Postgres temporario e escopado (`cli_login_postgres.<ref>`) via Management API, com senha efemera. Projeto: `hmfbxqemububjyhdckrj` ("Site bioestatistica").

**Docker precisa estar rodando, nao so instalado.** `supabase db dump` nao usa o `pg_dump` do host: o CLI baixa e executa `pg_dump` de dentro de um container `supabase/postgres:17.6.1.147`, para garantir compatibilidade de versao com o servidor remoto. Nesta maquina o Docker Desktop nao sobe por padrao — foi preciso `open -a Docker` e esperar cerca de 40 segundos. A imagem `supabase/postgres:17.6.1.147` ja foi baixada pela plan 08-05 ao rodar `db pull`.

Essa mesma imagem serve de banco do ensaio: ela ja traz os papeis `anon`, `authenticated`, `service_role` e `supabase_admin`, entao as policies de RLS do schema baseline aplicam sem erro — coisa que um `postgres:17` puro nao faz. E ela ja tem `psql` dentro, entao nao e preciso `psql` no host: usar `docker exec -i`.

Arquivos gerados pelas plans anteriores:
- `supabase/migrations/<ts0>_remote_schema.sql` — schema real de producao capturado por `db pull`
- `supabase/migrations/<ts1>_rename_disease_ids.sql` — o up, sem BEGIN/COMMIT (o CLI envolve; no ensaio quem envolve e `psql --single-transaction`)
- `supabase/rollback/<ts1>_rename_disease_ids_down.sql` — o down, fora de `migrations/` para nunca ser aplicado por `db push`
- `supabase/verify/contagens.sql` — contagens absolutas com `RAISE EXCEPTION` em divergencia

Contagens de producao medidas ao vivo por PostgREST em 2026-08-03:
- `sih_disease` 330, alvo pos-migracao **331**
- `sih_metric_uf` 30.313, nao muda
- `sih_metric_muni` 1.099.403, nao muda

**Volume real da migracao, medido no planejamento** — a renomeacao toca **4.195 linhas em `sih_metric_uf`** e **200.636 linhas em `sih_metric_muni`**, mais as 21 linhas de `sih_disease`. Ou seja cerca de **205 mil linhas por passada**, duas passadas, cerca de 410 mil versoes de linha no total — **nao** 1,13M. Isso responde a suposicao A3 do RESEARCH: os 21 ids sao uma fracao do total, nao a tabela inteira. Use esse numero como expectativa: um up que leve dezenas de segundos e normal; um que passe de poucos minutos e sinal de que algo esta diferente do previsto e precisa ser investigado antes de producao, nao depois.

Cada linha renomeada toca uma coluna que faz parte da PK composta, entao nunca e HOT update: gera nova versao de linha e atualiza todos os indices que tocam a coluna. Os indices envolvidos sao `sih_metric_uf_pkey`, `sih_metric_uf_disease_ano`, `sih_metric_muni_pkey` e `sih_metric_muni_disease_uf_ano`.

Armadilha registrada: ambas as FK tem `ON DELETE CASCADE`. Qualquer variante que apague e reinsira linha de `sih_disease` apagaria em cascata todas as metricas daquele agravo, sem erro. As duas passadas so fazem UPDATE e nunca deletam a linha pai — mas se algo der errado no ensaio, **nao** improvisar um caminho com DELETE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-requisitos, dump de producao e restauracao local</name>

  <read_first>
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 3.4 inteira: o que `supabase db dump` realmente faz, a exigencia de Docker e o papel temporario
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-05-SUMMARY.md` — qual caminho de scaffold foi usado e o nome exato do arquivo baseline
    - `supabase/migrations/` — os dois arquivos, para saber os timestamps reais
    - `.gitignore` — confirmar que `.local/` ja esta ignorado (foi acrescentado na 08-05)
  </read_first>

  <files>.local/supabase/prod_data.sql</files>

  <action>
Confirmar o pre-requisito antes de qualquer outra coisa: rodar `docker info`. Se falhar, abrir o Docker Desktop e esperar ate `docker info` responder. Sem Docker, nem o dump nem o container do ensaio funcionam, e a falha aparece no meio da operacao em vez de no comeco.

Confirmar tambem que `.local/` esta em `.gitignore` antes de gerar qualquer arquivo ali. O dump de dados tem centenas de MB e nunca pode entrar no repositorio.

Gerar o dump de dados de producao: `supabase db dump --linked --data-only -s public -f .local/supabase/prod_data.sql`. E leitura pura — nenhuma escrita em producao. Sao 1,13M linhas, entao a operacao demora; nao interromper. Se a flag combinada `--data-only` com `-s public` for rejeitada pela versao do CLI, rodar so com `--data-only` e registrar a diferenca no SUMMARY.

Subir o banco do ensaio como container descartavel, usando a mesma imagem que o CLI ja baixou: `docker run --rm -d --name lacir-ensaio -e POSTGRES_PASSWORD=postgres -p 55432:5432 supabase/postgres:17.6.1.147`. Escolher essa imagem e nao um `postgres:17` puro nao e detalhe: ela ja traz os papeis `anon`, `authenticated` e `service_role`, sem os quais as policies de RLS do schema baseline falhariam ao aplicar, e o ensaio deixaria de exercitar RLS real — que e metade da razao de o D-02 existir. Esperar o banco aceitar conexao antes de seguir.

Aplicar o schema baseline: `docker exec -i lacir-ensaio psql -U postgres -v ON_ERROR_STOP=1 -f -` alimentado pelo conteudo de `supabase/migrations/<ts0>_remote_schema.sql`. Se algum statement de nivel de projeto falhar (extensao indisponivel, `ALTER ... OWNER` para papel inexistente), registrar quais falharam e aplicar o subconjunto necessario: as tres tabelas, as tres PK, as duas FK com `ON DELETE CASCADE`, os dois indices e as policies de RLS. O ensaio precisa desses objetos, e so deles, para ser fiel.

Aplicar os dados: mesma invocacao alimentada por `.local/supabase/prod_data.sql`. Tambem demora.

Registrar as contagens de partida com uma consulta direta: `sih_disease`, `sih_metric_uf` e `sih_metric_muni`. Precisam bater 330, 30.313 e 1.099.403. Se nao baterem, o dump ficou incompleto e o ensaio nao vale — parar aqui e refazer o dump, nunca seguir com um retrato parcial.

Registrar tambem, com uma consulta agregada por `disease_id`, quantas linhas de cada tabela de metrica pertencem aos 21 ids que serao renomeados. O planejamento mediu 4.195 em `sih_metric_uf` e 200.636 em `sih_metric_muni`; confirmar contra o dump restaurado. Divergencia grande aqui significa que a distribuicao mudou desde a medicao e o custo previsto mudou junto.
  </action>

  <verify>
    <automated>docker exec -i lacir-ensaio psql -U postgres -tAc "select (select count(*) from sih_disease), (select count(*) from sih_metric_uf), (select count(*) from sih_metric_muni)"</automated>
  </verify>

  <acceptance_criteria>
    - `docker info` respondeu sem erro antes de qualquer comando de dump
    - `.local/` consta em `.gitignore` e `git status` nao lista nada sob ele
    - `.local/supabase/prod_data.sql` existe com tamanho maior que 10 MB
    - O container `lacir-ensaio` esta rodando com a imagem `supabase/postgres:17.6.1.147`
    - As tres contagens no banco local sao exatamente `330`, `30313` e `1099403`
    - As duas FK existem no banco local com `ON DELETE CASCADE` — conferir por consulta a `pg_constraint`, com `contype` igual a `f` e `confrelid` apontando para `sih_disease`
    - RLS esta habilitada nas tres tabelas no banco local
    - A contagem de linhas pertencentes aos 21 ids esta registrada, e as duas medicoes ficam dentro de 10 por cento de 4.195 e 200.636 — divergencia maior fica registrada como achado
    - Nenhuma escrita foi feita em producao: os unicos comandos remotos foram `supabase db dump`
  </acceptance_criteria>

  <done>Existe um Postgres local com PK, FK, ON DELETE CASCADE e RLS reais, carregado com a copia integral dos dados de producao e com as contagens conferidas.</done>
</task>

<task type="auto">
  <name>Task 2: Ciclo up, verificacao, down, verificacao — com medicao de tempo</name>

  <read_first>
    - `supabase/migrations/<ts1>_rename_disease_ids.sql` — o arquivo inteiro, com as duas passadas, o INSERT do codigo 330 e o bloco de prova D-04
    - `supabase/rollback/<ts1>_rename_disease_ids_down.sql` — o arquivo inteiro
    - `supabase/verify/contagens.sql` — o que ele afirma e como falha
    - `scripts/catalog/rename-map.json` — para escolher agravos de amostra na conferencia manual
    - `.agents/skills/supabase-postgres-best-practices/references/monitor-explain-analyze.md` — como ler o plano e o tempo real
    - `.agents/skills/supabase-postgres-best-practices/references/lock-short-transactions.md` — o que caracteriza uma transacao longa demais
  </read_first>

  <files>.planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md</files>

  <action>
Antes do up, capturar o retrato de referencia do banco local: por `disease_id`, `count(*)` mais a soma de `internacoes`, `obitos`, `valor_total` e `dias_permanencia`, para `sih_metric_uf` e para `sih_metric_muni`, gravando o resultado num arquivo sob `.local/`. E o mesmo retrato que a prova D-04 monta dentro da transacao, mas guardado **fora** dela, para poder comparar depois do down. Contagem sozinha nao serve: se os 21 fossem embaralhados entre si, as tres contagens ficariam identicas e a integridade referencial passaria — foi por isso que o D-04 pediu soma por agravo e por medida.

Aplicar o up medindo o tempo de parede: `docker exec -i lacir-ensaio psql -U postgres -v ON_ERROR_STOP=1 --single-transaction -f -` alimentado pelo arquivo de migracao, com `\timing on` ativado no inicio da sessao. O `--single-transaction` faz aqui o papel que o Supabase CLI faz sozinho no `db push`, e e o que garante que as temporarias `ON COMMIT DROP` e o bloco de prova vivam na mesma transacao das duas passadas.

Registrar: tempo total, tempo de cada passada, e o numero de linhas afetadas que cada `SELECT` final de CTE reportou. Expectativa vinda da medicao do planejamento: cerca de 205 mil linhas por passada, sendo 21 em `sih_disease`, 4.195 em `sih_metric_uf` e 200.636 em `sih_metric_muni`. Dezenas de segundos e normal; alguns minutos ja e sinal para investigar antes de producao. Cada linha toca coluna de PK composta, entao nunca ha HOT update: cada uma gera nova versao de linha e atualiza `sih_metric_uf_pkey`, `sih_metric_uf_disease_ano`, `sih_metric_muni_pkey` e `sih_metric_muni_disease_uf_ano`.

Medir tambem o perfil de lock, sem sujar o resultado: numa transacao separada, rodar `EXPLAIN (ANALYZE, BUFFERS)` sobre o statement da passada 1 e dar `ROLLBACK` no fim. Registrar o plano e o tempo. E essa medicao que responde a suposicao A5 do RESEARCH, sobre a janela de manutencao ser aceitavel.

Se o up abortar com `RAISE EXCEPTION`, **e sinal correto, nao acidente**: a prova de integridade pegou algo. Registrar a mensagem exata, nao contornar, nao remover a checagem, e parar a plan. Um up que aborta aqui e infinitamente melhor que um que passa em producao.

Depois do up bem sucedido, rodar `supabase/verify/contagens.sql` no mesmo banco local. Ele precisa passar sem `RAISE EXCEPTION`, com `sih_disease` igual a 331, `sih_metric_uf` igual a 30.313 e `sih_metric_muni` igual a 1.099.403.

Conferir por amostra dirigida os dois ciclos, que sao a razao de a migracao ter duas passadas: o agravo que agora se chama `hemorroidas` precisa carregar as somas que antes estavam sob `outras_doencas_veias`, e o que agora se chama `embolia_pulmonar` precisa carregar as que estavam sob `doencas_reumaticas_cronicas`. Comparar contra o retrato de referencia. Um `UPDATE` sequencial teria violado a PK justamente aqui.

Conferir tambem que o agravo do codigo 330 existe em `sih_disease` com o id canonico e **zero** linhas em ambas as tabelas de metrica, e que ele e o unico agravo cuja ausencia de metrica apareceu depois do up — o conjunto de agravos sem metrica so pode ter crescido pelo que esta registrado em `scripts/catalog/metricless-diseases.json`. Lembrar que 237 dos 330 agravos ja nao tinham linha em `sih_metric_uf` antes da migracao: isso e a lacuna de cobertura da coleta, escopo da Fase 9, e nao pode ser confundido com efeito da renomeacao.

Aplicar o down pela mesma invocacao, com `--single-transaction`, alimentado por `supabase/rollback/<ts1>_rename_disease_ids_down.sql`, medindo o tempo tambem.

Depois do down, recomputar o retrato agregado e comparar com o arquivo de referencia guardado antes do up: precisa ser **identico**, agravo por agravo, medida por medida. Contagens de volta a 330, 30.313 e 1.099.403, e o agravo do codigo 330 ausente. E isso, e so isso, que TAX-04 chama de reversivel — script testado, nao restauracao manual.
  </action>

  <verify>
    <automated>docker exec -i lacir-ensaio psql -U postgres -tAc "select count(*) from sih_disease"</automated>
  </verify>

  <acceptance_criteria>
    - O up aplicou sem erro, dentro de uma unica transacao, e o bloco de prova D-04 nao levantou excecao
    - As linhas afetadas reportadas pelas CTEs somam 21 em `sih_disease`, cerca de 4.195 em `sih_metric_uf` e cerca de 200.636 em `sih_metric_muni`, por passada
    - `supabase/verify/contagens.sql` passou apos o up, com 331, 30.313 e 1.099.403
    - Nenhum id em nenhuma das tres tabelas comeca com `__mig_` depois do up
    - O agravo que agora se chama `hemorroidas` tem exatamente as somas que o retrato de referencia registrava para `outras_doencas_veias`, e o mesmo vale para o par `embolia_pulmonar` e `doencas_reumaticas_cronicas`
    - O agravo do codigo 330 existe em `sih_disease` e tem zero linhas em `sih_metric_uf` e em `sih_metric_muni`
    - O conjunto de agravos sem metrica cresceu exatamente pelos ids listados em `scripts/catalog/metricless-diseases.json`
    - O down aplicou sem erro e as contagens voltaram a 330, 30.313 e 1.099.403
    - O retrato agregado apos o down e identico ao de antes do up, agravo por agravo e medida por medida — comparacao por diff de arquivo, sem tolerancia
    - Tempo total do up, tempo do down e a saida de `EXPLAIN (ANALYZE, BUFFERS)` da passada 1 estao registrados no SUMMARY
  </acceptance_criteria>

  <done>O par up e down foi exercitado contra dados reais, a prova de integridade agregada passou, o banco voltou exatamente ao estado inicial, e o custo esta medido em vez de estimado.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar as medicoes e destruir o ambiente do ensaio</name>

  <read_first>
    - As saidas capturadas na Task 2 — tempos, linhas afetadas, plano de execucao
    - `$HOME/.claude/get-shit-done/templates/summary.md` — a forma do SUMMARY
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-VALIDATION.md` — a linha da tabela de verificacoes manuais sobre custo e lock em volume de producao
  </read_first>

  <files>.planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md</files>

  <action>
Escrever o SUMMARY com as medicoes em forma de tabela, porque a plan 08-10 vai cita-las antes de tocar producao e o desenvolvedor precisa poder decidir com numero na mao: tempo do up, tempo do down, linhas afetadas por tabela e por passada, tempo do `EXPLAIN ANALYZE` da passada 1, e as contagens observadas em cada etapa.

Registrar tambem, explicitamente, o veredito: o ciclo passou por completo ou nao. Se qualquer etapa falhou, o SUMMARY diz qual e a plan 08-10 nao pode comecar — o `[BLOCKING]` dela cita este documento.

Registrar como achado, se aplicavel: divergencia entre a contagem de linhas dos 21 ids medida agora e a medida no planejamento (4.195 e 200.636); qualquer statement do schema baseline que nao aplicou no container e por que; e o tempo que o dump de dados levou, que e informacao util para a Fase 9.

Destruir o ambiente: parar o container com `docker stop lacir-ensaio` (ele sobe com `--rm`, entao para e se remove). Apagar `.local/supabase/prod_data.sql` **somente depois** de o SUMMARY estar escrito — se algo precisar ser reconferido, refazer o dump custa outra rodada longa contra producao. Deixar registrado no SUMMARY que o arquivo foi apagado.

Confirmar ao final que `git status` esta limpo tirando o SUMMARY: esta plan nao altera nenhum arquivo de codigo nem de dado do repositorio.
  </action>

  <verify>
    <automated>docker ps --filter name=lacir-ensaio --format "{{.Names}}" | grep -c lacir-ensaio | grep -qx 0 && test -f .planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - O SUMMARY contem uma tabela com tempo do up, tempo do down e linhas afetadas por tabela
    - O SUMMARY declara em uma frase se o ciclo passou por completo
    - O container `lacir-ensaio` nao aparece mais em `docker ps`
    - `.local/supabase/prod_data.sql` foi apagado e o SUMMARY registra isso
    - `git status` mostra apenas o SUMMARY como alteracao
    - `npm run gate` continua verde — esta plan nao tocou em codigo
  </acceptance_criteria>

  <done>As medicoes do ensaio estao registradas em forma citavel e o ambiente descartavel sumiu, sem deixar dump de producao no disco nem sujeira no repositorio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Producao para disco local | `supabase db dump --data-only` traz 1,13M linhas de dado publico do SUS para o sistema de arquivos do desenvolvedor |
| Supabase CLI para Management API | O CLI provisiona papel Postgres temporario com senha efemera para executar o dump |
| Container descartavel para host | O Postgres do ensaio expoe a porta 55432 com senha trivial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Information disclosure | Dump de producao commitado por engano | mitigate | `.local/` no `.gitignore` desde a 08-05; criterio de aceite verifica `git status`; arquivo apagado ao fim da plan |
| T-08-08-02 | Information disclosure | Senha efemera do papel `cli_login_postgres` aparecendo em log ou SUMMARY | mitigate | Nao usar `db dump --dry-run`; nunca colar saida de conexao no SUMMARY nem em commit |
| T-08-08-03 | Elevation of privilege | Container do ensaio com `POSTGRES_PASSWORD=postgres` exposto na porta 55432 | accept | Container efemero, ligado com `--rm`, destruido na Task 3; dado e publico do SUS; acesso via `docker exec`, sem necessidade de expor a porta a rede |
| T-08-08-04 | Tampering | Ensaio rodado contra dump incompleto daria falso verde antes de producao | mitigate | Contagens de partida conferidas contra 330, 30.313 e 1.099.403 antes do up; ensaio para se nao baterem |
| T-08-08-05 | Denial of Service | Comando de escrita disparado contra producao por engano no lugar do container | mitigate | Todo comando de escrita passa por `docker exec -i lacir-ensaio`; o unico comando remoto da plan e `db dump`, que e leitura |
| T-08-08-06 | Tampering | Contornar um `RAISE EXCEPTION` do bloco D-04 para fazer o ensaio passar | mitigate | Task instrui explicitamente a parar e registrar; a checagem nunca e removida nem afrouxada |
| T-08-08-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao nesta fase; a imagem `supabase/postgres:17.6.1.147` e a mesma que o proprio CLI ja usa (RESEARCH secao Package Legitimacy Audit) |
</threat_model>

<verification>
- Contagens locais de partida iguais a 330, 30.313 e 1.099.403
- Up aplicado numa unica transacao, prova D-04 sem excecao, `contagens.sql` passando com 331
- Down aplicado e retrato agregado identico ao de antes do up, por diff sem tolerancia
- Tempos e plano de execucao registrados no SUMMARY
- Container destruido, dump apagado, `git status` limpo tirando o SUMMARY
- `npm run gate` verde

**Manual, fora do gate automatico:** o ciclo inteiro desta plan e manual por natureza — o CI nao tem Postgres populado, e por isso `08-VALIDATION.md` ja o lista como verificacao manual. O que entra no gate e o codigo; o que entra aqui e a medicao.
</verification>

<success_criteria>
- Postgres local restaurado de producao com PK, FK, `ON DELETE CASCADE` e RLS reais
- Up aplicado sem erro, com a prova de integridade agregada passando dentro da transacao
- Contagens pos-up iguais a 331, 30.313 e 1.099.403
- Os dois ciclos de renomeacao conferidos por amostra contra o retrato de referencia
- Agravo do codigo 330 presente e sem metrica, e nenhum outro agravo orfanado pela migracao
- Down aplicado e estado inicial restaurado de forma identica
- Tempo do up, tempo do down e linhas afetadas medidos e registrados
- Ambiente descartavel destruido e dump de producao removido do disco
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md` when done
</output>
