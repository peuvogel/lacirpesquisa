---
phase: 08-taxonomia-can-nica-integridade
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - scripts/catalog/sync-lista-morb.mjs
  - scripts/catalog/buildRenameMap.mjs
  - scripts/catalog/rename-map.json
  - src/test/fixtures/taxonomy/diseases.pre-migracao.json
  - src/features/catalog/renameMap.test.ts
  - package.json
autonomous: true
requirements: [TAX-01, TAX-02]

must_haves:
  truths:
    - "O gerador produz o slug canonico para todo codigo TabNet: KNOWN_BY_CODE deixou de existir (causa raiz removida)"
    - "O codigo 330 deixa de cair fora por acidente: o filtro passa a ser code === TODAS_AS_CATEGORIAS__, nunca uma regex sobre o rotulo (D-25)"
    - "A geracao le o snapshot commitado e nunca a rede (D-09)"
    - "O mapa old para canonical foi computado do diff, nunca transcrito da nota de ground truth (D-12)"
    - "Recomputar o mapa a partir da fixture congelada devolve exatamente o arquivo commitado — provado por teste"
    - "O mapa tem 21 renomeacoes, 1 adicao (codigo 330), 0 remocoes, e os dois ciclos verificados aparecem nele"
    - "A nota de ground truth, ja emendada para 21 ids (D-15), serve de ponto de conferencia humana do computo — nunca de fonte da migracao"
    - "A taxonomia commitada ainda nao mudou: esta plan entrega o gerador, nao o resultado (o gate segue verde)"
  artifacts:
    - path: "scripts/catalog/sync-lista-morb.mjs"
      provides: "Gerador canonico com funcoes puras exportadas, sem dicionario de ids e sem fetch"
      exports: ["buildDiseases", "buildRuntimeLista", "renderDiseasesJson", "renderListaJson", "generateFromSnapshot"]
    - path: "src/test/fixtures/taxonomy/diseases.pre-migracao.json"
      provides: "Estado corrompido congelado, unica fonte para provar TAX-02 depois do D-24"
      min_lines: 2000
    - path: "scripts/catalog/rename-map.json"
      provides: "Mapa old para canonical chaveado por tabnetCode, fonte unica das duas direcoes da migracao (D-03/D-12)"
      contains: "tombstones"
    - path: "scripts/catalog/buildRenameMap.mjs"
      provides: "Computo do mapa a partir do diff fixture vs regeneracao"
  key_links:
    - from: "scripts/catalog/sync-lista-morb.mjs"
      to: "scripts/catalog/listaMorbSource.mjs"
      via: "import de parseListaMorbOptions/slugify/loadSnapshot"
      pattern: "listaMorbSource"
    - from: "scripts/catalog/buildRenameMap.mjs"
      to: "src/test/fixtures/taxonomy/diseases.pre-migracao.json"
      via: "leitura do estado anterior para o diff"
      pattern: "diseases.pre-migracao.json"
    - from: "src/features/catalog/renameMap.test.ts"
      to: "scripts/catalog/buildRenameMap.mjs"
      via: "import relativo de 3 niveis, recomputa e compara"
      pattern: "buildRenameMap"
---

<objective>
Corrigir a causa raiz (`KNOWN_BY_CODE`) e o bug irmao do filtro por rotulo, e computar o mapa de renomeacao a partir do diff — sem ainda escrever a taxonomia nova.

Purpose: esta e a plan que decide **quais** sao os ids canonicos. Todo o resto da fase e dirigido por `rename-map.json`. A ordem "computar do diff, depois congelar" (D-12) foi escolhida em resposta direta ao 21o id que a nota de ground truth nao tinha — nesta fase, listas de ids nunca se escrevem a mao.

Output: `sync-lista-morb.mjs` reescrito como modulo puro com CLI, fixture pre-migracao congelada sob `src/test/fixtures/taxonomy/`, `rename-map.json` computado e commitado, e um teste que garante que recomputar da o mesmo arquivo.

**O gerador nao e executado nesta plan.** Escrever `diseases.json`/`diseases.lista.json` agora deixaria o repo num estado meio-renomeado e o gate vermelho — a escrita acontece na plan 08-06 (o flip), junto com os invariantes, conforme D-24.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md
@.planning/notes/2026-07-28-taxonomia-corrompida-ground-truth.md
@scripts/catalog/sync-lista-morb.mjs
@.planning/phases/08-taxonomia-can-nica-integridade/08-01-SUMMARY.md

<interfaces>
<!-- Contratos entregues pela plan 08-01. Usar direto, sem explorar. -->

`scripts/catalog/listaMorbSource.mjs`:
- `parseListaMorbOptions(html)` devolve `Array<{ code: string, label: string }>` na ordem do documento
- `slugify(label, code)` — o gerador canonico, preservado byte a byte da versao original
- `loadSnapshot()` devolve `{ html, extract }`, com `extract.options` sendo o array acima
- `checkSnapshotIntegrity()` devolve `string[]`

Arquivos de entrada criados na 08-01:
- `scripts/catalog/exclusions.json` — objeto `{ [code: string]: motivo }`, 4 chaves: `TODAS_AS_CATEGORIAS__`, `331`, `332`, `333`
- `scripts/catalog/extra-diseases.json` — array de 1 entrada com `id`, `label`, `filterKind`, `tabnetCode`, `def`, `reason`
- `scripts/catalog/lista-morb-cid.json` — objeto `{ [tabnetCode: string]: cid }`, 330 chaves

Formato exato dos dois artefatos gerados (confirmado por leitura de bytes — assimetria e load-bearing para o invariante B):
- `scripts/catalog/diseases.json` = `JSON.stringify(out, null, 2)` mais `"\n"`
- `src/features/catalog/diseases.lista.json` = `JSON.stringify(runtime)` **minificado, uma linha so**, mais `"\n"`

Shape do objeto runtime que o gerador monta hoje (`sync-lista-morb.mjs:109-117`), na ordem de chaves do literal:
`{ id, label, filterKind, tabnetCode, cid, packId, domain }` — `cid` so para `lista_morb`, `packId` sempre `sih.<id>_uf`, `domain` `sih_lista_morb` ou `vascular`.
Atencao: o `diseases.lista.json` **commitado hoje** tem `cid` como ultima chave, ou seja foi produzido por uma versao anterior do gerador. Regenerar vai mudar a ordem das chaves nesse arquivo — comportamento esperado, nao regressao.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever sync-lista-morb.mjs como modulo puro sobre o snapshot</name>

  <read_first>
    - `scripts/catalog/sync-lista-morb.mjs` — arquivo inteiro (123 linhas). Linhas 15-37 `KNOWN_BY_CODE` (deletar), 39 `SKIP_CODES`, 67-73 fetch e parse, 77-91 loop, 93-99 push de `amputacao_mmii`, 101-121 escrita dos dois artefatos
    - `scripts/catalog/listaMorbSource.mjs` — o modulo criado na 08-01, para importar em vez de duplicar
    - `scripts/catalog/validate.mjs` — linhas 290-320, o molde `main()`/`isDirectRun` a repetir (o arquivo hoje nao exporta nada nem tem funcao alguma: e top-level await puro)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 2 completa (excertos linha a linha do que sai e do que fica)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-25 (o filtro estrito) e a secao `root_cause`
  </read_first>

  <files>scripts/catalog/sync-lista-morb.mjs, package.json</files>

  <action>
Reescrever `scripts/catalog/sync-lista-morb.mjs` no molde CLI-mais-export de `validate.mjs`. Nenhum top-level await, nenhuma escrita fora do `main()`.

Remover `KNOWN_BY_CODE` por completo (linhas 15-37). A atribuicao de id passa a ser apenas `slugify(label, code)` — e a correcao que faz a taxonomia se autocorrigir; os 308 agravos que ja caiam no slugify permanecem identicos.

Substituir o filtro da linha 80. Hoje ele e `!code || SKIP_CODES.has(code) || /^todas/i.test(label) || code.startsWith('TODAS')`. Passa a ser: pular apenas quando `code` esta vazio ou quando `code` e uma chave de `exclusions.json`. A pseudo-opcao entra em `exclusions.json` como `TODAS_AS_CATEGORIAS__` e por isso e coberta pela mesma checagem estrita. **Nenhuma condicao pode olhar para `label`.** O casamento frágil contra o rotulo e a mesma classe de defeito do `KNOWN_BY_CODE` e e o que faz o codigo 330 ("Todas as outras causas externas") cair fora hoje por acidente (D-25). Deletar tambem a constante `SKIP_CODES` — a informacao dela agora vive em `exclusions.json`.

Trocar o `fetch` por leitura do snapshot: usar `loadSnapshot()` de `listaMorbSource.mjs`, iterar sobre `extract.options` (D-09). Manter `--refresh` como responsabilidade exclusiva de `listaMorbSource.mjs`; este arquivo nunca faz rede.

Substituir o `out.push` hardcoded de `amputacao_mmii` por leitura de `extra-diseases.json`, emitindo somente os cinco campos que o `diseases.json` carrega (`id`, `label`, `filterKind`, `tabnetCode`, `def`) — o campo `reason` fica de fora do artefato gerado.

Trocar a leitura do mapa CID de `cidById[d.id]` para `cidByCode[d.tabnetCode]` (D-11).

Endurecer a desambiguacao de slug: hoje a linha 82 faz `id = id + '_' + code` em silencio quando dois rotulos produzem o mesmo slug. Ground truth confirma que esse caminho nunca e exercitado nos 330 atuais. Trocar por `throw` com os dois codigos e os dois rotulos na mensagem — uma colisao de slug e uma decisao que precisa de humano, nao de sufixo automatico. Silencio aqui e exatamente o padrao de defeito que o milestone existe para eliminar.

Exportar as funcoes puras: `buildDiseases({ options, exclusions, extras })` devolvendo o array de `diseases.json`; `buildRuntimeLista(diseases, cidByCode)` devolvendo o array runtime na ordem de chaves atual do literal; `renderDiseasesJson(diseases)` e `renderListaJson(runtime)` devolvendo **exatamente** as duas strings que hoje sao escritas em disco (indentada com newline final, e minificada com newline final, respectivamente); e `generateFromSnapshot()` que carrega snapshot, exclusions, extras e mapa CID e devolve `{ diseases, runtime, diseasesText, listaText }`. Essas quatro sao o que os invariantes B e o rename-map vao consumir sem tocar em disco.

`main()`, atras de `isDirectRun`, chama `generateFromSnapshot()` e escreve os dois arquivos. **Nao rodar `main()` nesta plan** — nem manualmente, nem via npm script. Rodar agora deixaria o repo meio-renomeado e o gate vermelho; a escrita e da plan 08-06 (D-24).

Acrescentar em `package.json` o script `catalog:sync-lista-morb` apontando para `node scripts/catalog/sync-lista-morb.mjs`, sem encadear em `pretest` nem `gate`.
  </action>

  <verify>
    <automated>node --input-type=module -e "import {generateFromSnapshot} from './scripts/catalog/sync-lista-morb.mjs'; const r=generateFromSnapshot(); if(r.diseases.length!==331) process.exit(1); console.log('331 OK')" && npm run catalog:validate && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c KNOWN_BY_CODE scripts/catalog/sync-lista-morb.mjs` retorna `0`
    - `grep -c SKIP_CODES scripts/catalog/sync-lista-morb.mjs` retorna `0`
    - `grep -c "fetch(" scripts/catalog/sync-lista-morb.mjs` retorna `0`
    - `grep -v "^\s*\*" scripts/catalog/sync-lista-morb.mjs | grep -c "test(label)"` retorna `0` — nenhuma condicao de filtro olha para o rotulo
    - `generateFromSnapshot().diseases` tem comprimento `331`
    - Nesse array, existe exatamente uma entrada com `tabnetCode` `330`, `id` `todas_as_outras_causas_externas`, `label` `Todas as outras causas externas` e `filterKind` `lista_morb`
    - Nao existe entrada com `tabnetCode` em 331, 332 ou 333, nem com `code` `TODAS_AS_CATEGORIAS__`
    - Existe exatamente uma entrada com `filterKind` `procedimento`, id `amputacao_mmii`, e ela **nao** tem o campo `reason`
    - Em `generateFromSnapshot().runtime`, nenhuma entrada com `filterKind` `lista_morb` tem `cid` nulo (330 entradas com CID nao-nulo)
    - `renderDiseasesJson` devolve string terminando em `\n` e com indentacao de 2 espacos; `renderListaJson` devolve string de uma unica linha terminando em `\n`
    - `scripts/catalog/diseases.json` e `src/features/catalog/diseases.lista.json` **nao aparecem** em `git status` — o gerador nao foi executado
    - `npm run catalog:validate` sai com codigo 0 e `npx vitest run` continua verde
  </acceptance_criteria>

  <done>O gerador produz 331 agravos canonicos em memoria a partir do snapshot, sem dicionario de ids, sem rede e sem filtro por rotulo — e o repo continua exatamente como estava.</done>
</task>

<task type="auto">
  <name>Task 2: Congelar a fixture pre-migracao e computar rename-map.json do diff</name>

  <read_first>
    - `scripts/catalog/diseases.json` — o estado corrompido atual, 330 entradas; e ele que vira fixture
    - `.planning/notes/2026-07-28-taxonomia-corrompida-ground-truth.md` — tabela das 21 linhas e a secao de ciclos. **Ponto de conferencia, nunca fonte**: o mapa e computado, e foi o computo que achou o 21o id que essa tabela nao tinha
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-12 (computar do diff, congelar), D-03 (mapa e fonte unica das duas direcoes), D-06 (tombstones permanentes)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 3, convencao de shape de arquivo de dado
  </read_first>

  <files>src/test/fixtures/taxonomy/diseases.pre-migracao.json, scripts/catalog/buildRenameMap.mjs, scripts/catalog/rename-map.json, package.json</files>

  <action>
Copiar `scripts/catalog/diseases.json` byte a byte para `src/test/fixtures/taxonomy/diseases.pre-migracao.json`. Precisa morar sob `src/` porque o vitest so enxerga `src/**` e essa fixture e a unica prova possivel de TAX-02 depois de D-24 — com renomeacao e invariante no mesmo commit, o invariante nunca e visto falhando contra o repo. A copia tem que ser feita **antes** de qualquer regeneracao; a partir daqui ela e imutavel.

Criar `scripts/catalog/buildRenameMap.mjs` no molde CLI-mais-export: exporta `computeRenameMap({ before, after })` e `renderRenameMap(map)`, e o `main()` escreve `scripts/catalog/rename-map.json`.

`computeRenameMap` indexa os dois lados por `tabnetCode` e produz: `renames` (codigo presente nos dois lados com `id` diferente, cada entrada com `tabnetCode`, `old`, `canonical` e `label` canonico), `added` (codigo so no lado canonico, com `tabnetCode`, `id`, `label`, `filterKind`, `def`), `removed` (codigo so no lado antigo) e `tombstones` (a lista deduplicada dos `old`, derivada de `renames`, jamais digitada). Ordenar `renames` e `added` por `tabnetCode` numerico crescente, e `tombstones` na mesma ordem dos `renames`.

Fazer a funcao abortar com mensagem explicita se: dois codigos colidirem no mesmo lado; `removed` nao for vazio (perder um codigo seria perda de categoria, nao renomeacao); ou algum `canonical` aparecer duas vezes.

`main()` le a fixture congelada como `before` e `generateFromSnapshot().diseases` como `after`, escreve `rename-map.json` com `JSON.stringify(x, null, 2)` mais newline final, e imprime um resumo com as contagens. Acrescentar tambem no arquivo os campos de proveniencia `version`, `generatedBy`, `before` e `after` (caminho da fixture e descricao da regeneracao) — o arquivo e revisado por humano no PR, entao precisa dizer de onde veio.

Rodar `main()` uma vez e commitar `rename-map.json` congelado.

Acrescentar em `package.json` o script `catalog:rename-map` apontando para `node scripts/catalog/buildRenameMap.mjs`.

Conferir o resultado contra a tabela do ground truth como **checagem de sanidade humana**, nao como fonte: se o computo divergir da tabela, o computo vence e a divergencia entra no SUMMARY (foi exatamente assim que o codigo 180 apareceu).
  </action>

  <verify>
    <automated>node --input-type=module -e "import m from './scripts/catalog/rename-map.json' with {type:'json'}; const ok=m.renames.length===21&&m.added.length===1&&m.added[0].tabnetCode==='330'&&(m.removed??[]).length===0&&new Set(m.tombstones).size===21; if(!ok) process.exit(1); console.log('mapa OK')" && npm run catalog:validate</automated>
  </verify>

  <acceptance_criteria>
    - `src/test/fixtures/taxonomy/diseases.pre-migracao.json` e byte-identico ao `scripts/catalog/diseases.json` do commit anterior (`git show HEAD:scripts/catalog/diseases.json | diff - src/test/fixtures/taxonomy/diseases.pre-migracao.json` nao imprime nada)
    - `rename-map.json` tem `renames` com exatamente 21 entradas, `added` com 1 entrada de `tabnetCode` `330`, `removed` vazio e `tombstones` com 21 ids unicos
    - O par `tabnetCode` `163` mapeia `avc` para `outras_doencas_do_olho_e_anexos`
    - O par `tabnetCode` `180` esta presente, mapeando `outras_doencas_arteriais` para `outras_doencas_cerebrovasculares` (o 21o id, ausente da versao original da nota)
    - Os dois ciclos aparecem: `186` leva `hemorroidas` a `veias_varicosas_das_extremidades_inferiores` e `187` leva `outras_doencas_veias` a `hemorroidas`; `173` leva `doencas_reumaticas_cronicas` a `embolia_pulmonar` e `182` leva `embolia_pulmonar` a `outras_doencas_vasculares_perifericas`
    - `tombstones` contem `hemorroidas` e `embolia_pulmonar` mesmo eles sendo ids canonicos de outra doenca — comportamento exigido por D-06
    - `amputacao_mmii` (codigo `3331`) **nao** aparece em `renames` nem em `tombstones`
    - Rodar `npm run catalog:rename-map` uma segunda vez deixa `git status` limpo (saida deterministica)
    - `npm run catalog:validate` sai com codigo 0
  </acceptance_criteria>

  <done>O mapa das duas direcoes da migracao existe, computado do diff e congelado, e a fixture do estado corrompido esta preservada sob `src/` para provar TAX-02 depois.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de recomputacao do mapa (D-12)</name>

  <read_first>
    - `src/features/catalog/validateCatalogEntry.test.ts` — linhas 1-23: o padrao de import cross-tree de 3 niveis e a fabrica de fixture com overrides
    - `src/features/catalog/catalogAnalysisData.test.ts` — linhas 1-24: como um teste le arquivo real do repo com `node:fs`/`node:path` em vez de mockar
    - `scripts/catalog/buildRenameMap.mjs` — a assinatura de `computeRenameMap` e `renderRenameMap` criada na Task 2
    - `scripts/catalog/rename-map.json` — o arquivo congelado
  </read_first>

  <files>src/features/catalog/renameMap.test.ts</files>

  <action>
Criar `src/features/catalog/renameMap.test.ts` importando `computeRenameMap`, `renderRenameMap` de `scripts/catalog/buildRenameMap.mjs` e `generateFromSnapshot` de `scripts/catalog/sync-lista-morb.mjs`, ambos por caminho relativo de 3 niveis.

Casos:

O caso central de D-12 — recomputar o mapa a partir da fixture congelada e da regeneracao do snapshot produz uma string **byte-identica** ao `rename-map.json` commitado. Comparar strings, nao objetos: e a comparacao de string que pega reordenacao e mudanca de formatacao.

Contagens e forma: 21 `renames`, 1 `added` de codigo 330, 0 `removed`, 21 `tombstones` unicos.

Os dois ciclos verificados: afirmar os quatro pares por `tabnetCode` (186, 187, 173, 182), lendo do arquivo commitado. Estes sao os pares que quebrariam um `UPDATE` sequencial e por isso sao a justificativa da migracao de duas passadas.

Tombstone que tambem e canonico: afirmar que `hemorroidas` e `embolia_pulmonar` estao em `tombstones` **e** aparecem como `canonical` de outro codigo. E a situacao que a FK sozinha nao cobre e que D-06 existe para tratar.

Deteccao de perda: chamar `computeRenameMap` com um `after` sintetico ao qual falta um `tabnetCode` presente no `before` e afirmar que a funcao lanca (nao devolve mapa parcial).

Deteccao de colisao: chamar com um `after` sintetico onde dois codigos produzem o mesmo `canonical` e afirmar que lanca.

Nenhum dos 21 ids pode ser digitado literalmente no teste, exceto os quatro dos ciclos e os dois tombstones-canonicos citados acima, que sao asserçoes de comportamento especifico e nao uma lista transcrita. Todo o resto e derivado do arquivo.
  </action>

  <verify>
    <automated>npx vitest run src/features/catalog/renameMap.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/features/catalog/renameMap.test.ts` passa com 6 casos
    - O caso de recomputacao compara strings e falha se `rename-map.json` for editado a mao (verificar trocando um caractere do arquivo, confirmar o vermelho e reverter)
    - O teste importa de `../../../scripts/catalog/` — nao ha copia de logica dentro do teste
    - `grep -c "'avc'" src/features/catalog/renameMap.test.ts` retorna `0` — a lista dos 21 nao e transcrita
    - `npm run test:run` verde de ponta a ponta
  </acceptance_criteria>

  <done>Recomputar o mapa passa a ser uma condicao de commit: qualquer edicao manual em `rename-map.json` fica vermelha na suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot para taxonomia | O extrato commitado decide quais 331 agravos existem |
| `rename-map.json` para migracao de 1,13M linhas | O mesmo arquivo dirige o SQL de producao e a renomeacao de todo artefato do repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | `rename-map.json` editado a mao depois de congelado | mitigate | Teste de recomputacao byte-identica em `renameMap.test.ts`, dentro de `test:run` e do gate |
| T-08-02-02 | Tampering | Colisao de slug resolvida em silencio por sufixo de codigo | mitigate | Caminho de sufixo substituido por `throw` com os dois codigos e rotulos; colisao vira decisao humana |
| T-08-02-03 | Repudiation | Mapa sem proveniencia impede auditar de onde veio cada par | mitigate | Campos `version`, `generatedBy`, `before` e `after` gravados no proprio arquivo |
| T-08-02-04 | Elevation of privilege | Um `removed` silencioso apagaria uma categoria clinica do catalogo | mitigate | `computeRenameMap` aborta se `removed` nao for vazio; teste dedicado cobre |
| T-08-02-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao nesta fase (RESEARCH secao Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run catalog:validate` sai com codigo 0
- `npm run test:run` verde
- `npm run gate` verde
- `git status` mostra apenas os arquivos previstos; `scripts/catalog/diseases.json` e `src/features/catalog/diseases.lista.json` intocados
</verification>

<success_criteria>
- `KNOWN_BY_CODE`, `SKIP_CODES` e `fetch` sumiram de `sync-lista-morb.mjs`; nenhum filtro olha para o rotulo
- `generateFromSnapshot()` devolve 331 agravos com o codigo 330 incluido e CID nao-nulo em todos os `lista_morb`
- Fixture pre-migracao congelada e byte-identica ao `diseases.json` anterior
- `rename-map.json` congelado com 21 renomeacoes, 1 adicao, 0 remocoes, 21 tombstones
- Teste de recomputacao byte-identica verde e sensivel a edicao manual
- A taxonomia commitada ainda nao mudou — o gate segue verde
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-02-SUMMARY.md` when done
</output>
