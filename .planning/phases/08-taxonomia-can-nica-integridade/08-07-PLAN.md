---
phase: 08-taxonomia-can-nica-integridade
plan: 07
type: execute
wave: 5
depends_on: [08-06]
files_modified:
  - src/features/catalog/aliases.json
  - src/features/catalog/diseaseAliases.ts
  - src/features/catalog/diseaseAliases.test.ts
  - scripts/catalog/validate.mjs
autonomous: false
requirements: [TAX-05]

must_haves:
  truths:
    - "Buscar AVC depois da canonizacao devolve as 3 categorias corretas — sem apelidos, devolveria zero (TAX-05)"
    - "O apelido vive so no indice de busca: o rotulo exibido, selecionado e copiado e sempre o oficial da Lista Morb (D-19)"
    - "Nenhum apelido e gravado como chave de dado — a funcao devolve id canonico, nunca o termo"
    - "O que a regra automatica alcanca fica automatico: dobra de acento e fuzzy resolvem aterosclerose para Arteroesclerose sem entrada curada (D-17)"
    - "Cada entrada curada cita codigo e rotulo esperado, e a validacao afirma que o rotulo daquele codigo bate (D-20)"
    - "Fuzzy so acima de 6 caracteres e AND entre tokens da query — o limiar medido que zera falso positivo em busca multi-palavra"
  artifacts:
    - path: "src/features/catalog/aliases.json"
      provides: "Dicionario curado minimo, com codigo e rotulo esperado por entrada"
    - path: "src/features/catalog/diseaseAliases.ts"
      provides: "Funcao pura de matching: dobra de acento, tokenizacao, Levenshtein com piso, AND entre tokens, resolucao de apelido"
      exports: ["foldAccents", "labelMatchesQuery", "resolveAliasTerm", "matchDiseases"]
  key_links:
    - from: "scripts/catalog/validate.mjs"
      to: "src/features/catalog/aliases.json"
      via: "invariante E confere codigo e rotulo esperado contra a taxonomia canonica"
      pattern: "checkAliases"
    - from: "src/features/catalog/diseaseAliases.ts"
      to: "src/features/catalog/aliases.json"
      via: "import do dicionario curado"
      pattern: "aliases.json"
---

<objective>
Construir a camada de apelidos clinicos: dicionario curado minimo mais matching automatico, com validacao que impede o dicionario de repetir o defeito do `KNOWN_BY_CODE`.

Purpose: `MeasureDiseasePicker.diseaseMatches` casa contra `disease.label`, `disease.id` e `disease.cid`. Hoje "avc" acha o agravo porque o **id literalmente e** `avc`. Depois da canonizacao os ids do AVC verdadeiro sao `hemorragia_intracraniana`, `infarto_cerebral` e `acid_vascular_cerebr_nao_espec_hemorrag_ou_isquem` — nenhum contem a substring "avc", e os rotulos tambem nao. **Sem apelidos, buscar AVC depois da migracao retorna zero.** TAX-05 e o que impede a canonizacao de piorar a busca.

Output: `aliases.json`, funcao pura de matching, invariante E ligado ao `catalog:validate`, e um checkpoint humano sobre as 4 entradas curadas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md
@src/features/catalog/filterCatalog.ts
@.planning/phases/08-taxonomia-can-nica-integridade/08-06-SUMMARY.md

<interfaces>
<!-- Contratos e medicoes ja disponiveis. -->

`src/features/catalog/diseases.lista.json` (pos-flip, 331 entradas), shape de cada entrada:
`{ id, label, filterKind, tabnetCode, cid, packId, domain }`

Codigos e rotulos canonicos relevantes para as entradas curadas (pos-flip):
- `177` — Hemorragia intracraniana — `hemorragia_intracraniana`
- `178` — Infarto cerebral — `infarto_cerebral`
- `179` — Acid vascular cerebr nao espec hemorrag ou isquem — `acid_vascular_cerebr_nao_espec_hemorrag_ou_isquem`
- `180` — Outras doencas cerebrovasculares — `outras_doencas_cerebrovasculares`
- `185` — Flebite tromboflebite embolia e trombose venosa — `flebite_tromboflebite_embolia_e_trombose_venosa`
- `186` — Veias varicosas das extremidades inferiores — `veias_varicosas_das_extremidades_inferiores`
Ler os rotulos exatos, com acento, de `src/features/catalog/diseases.lista.json` — nunca digitar de memoria.

Tabela de expectativa do CONTEXT (medida em 2026-08-03, busca por substring no rotulo):
| termo | espera | acha hoje sem apelido |
| `embolia pulmonar` | 173 | acha |
| `hemorroidas` | 187 | acha |
| `insuficiencia cardiaca` | 175 | acha |
| `avc` | 177, 178, 179 | nao acha |
| `ait` | 180 | nao acha |
| `tvp` | 185 | nao acha |
| `varizes` | 186 | nao acha |
| `aterosclerose` | 181 | nao acha |

Algoritmo medido e recomendado (RESEARCH secao 6.2, medicao programatica contra os 330 rotulos reais):
- piso de 6 caracteres para aplicar distancia de edicao — tokens menores exigem casamento exato ou substring, nunca fuzzy. Sem o piso, "ait" produz **18** matches espurios
- Levenshtein com distancia menor ou igual a 2
- AND entre tokens da query para busca multi-palavra
- com essas duas regras, a taxa de falso positivo medida em queries multi-palavra realistas foi **0 em 8 termos**; `aterosclerose` continua achando "Arteroesclerose" (distancia 2)
- falso positivo residual conhecido e aceito: `varizes` tambem casa "Transtornos dos nervos raizes e plexos nervosos" via "raizes". E aceitavel exatamente porque D-18 mostra as N linhas normais em vez de redirecionar em silencio — uma linha extra visivel e descartavel nao leva o estudante a dado errado

Implementacao de referencia do AND entre tokens ja no repo — `src/features/catalog/filterCatalog.ts:12-30`, `matchesQuery`: monta um haystack e exige que todo token da query esteja presente. Copiar a forma, nao a substancia.

Regra herdada da Fase 5: **zero dependencias novas** no pipeline de catalogo. Levenshtein a mao, sem biblioteca.

Molde de `validate.mjs`: funcao exportada devolve array de mensagens, nunca lanca, so `main()` decide exit code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: aliases.json curado e invariante E ligado ao gate</name>

  <read_first>
    - `src/features/catalog/diseases.lista.json` — os rotulos canonicos exatos dos codigos 177, 178, 179, 180, 185 e 186, com acento
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-17, D-19, D-20 e a tabela de expectativa
    - `.planning/notes/2026-07-28-taxonomia-corrompida-ground-truth.md` — a secao de apelidos clinicos
    - `scripts/catalog/validate.mjs` — o `main()` ja com os invariantes A a D2 ligados, e o molde de funcao pura
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 3, convencao de shape de arquivo de dado
  </read_first>

  <files>src/features/catalog/aliases.json, scripts/catalog/validate.mjs</files>

  <action>
Criar `src/features/catalog/aliases.json` com **quatro** entradas curadas e nenhuma a mais: `avc`, `tvp`, `ait` e `varizes`. Sao exatamente os termos que a medicao mostrou inalcancaveis por regra — siglas ausentes do texto oficial e um sinonimo popular. Antes de acrescentar qualquer quinta entrada, verificar se a regra automatica ja resolve; o vies do usuario e derivacao automatica sempre que a regra alcance, curadoria so para o residuo.

O arquivo mora sob `src/features/catalog/` porque e consumido pelo bundle; `validate.mjs` o le por caminho relativo, do mesmo jeito que ja le `public/data/catalog/`.

Cada entrada carrega: os termos de busca que a acionam (normalizados: minusculas, sem acento), a lista de categorias com `tabnetCode` **e** `labelEsperado`, e um `motivo` em PT-BR explicando por que a regra automatica nao alcanca. Citar codigo e rotulo esperado e o mecanismo do D-20 e nao e formalidade: este dicionario tem exatamente a forma do `KNOWN_BY_CODE` que produziu o bug original — uma tabela escrita a mao mapeando termo para codigos. Revisao humana ja falhou nessa forma uma vez, com 21 linhas plausiveis e nenhuma conferida contra o rotulo real.

Copiar os `labelEsperado` **lendo** de `diseases.lista.json`, nunca digitando de memoria, e conferindo contra o `tabnetCode`.

Acrescentar `checkAliases(aliases, diseases)` a `validate.mjs` — o **invariante E** — e liga-la ao `main()` junto com as demais. Ela verifica: todo `tabnetCode` citado existe na taxonomia; o `labelEsperado` de cada categoria e **exatamente igual** ao `label` canonico daquele codigo; nenhum termo esta vazio, e todo termo esta em minusculas e sem acento; nenhum termo se repete entre entradas; todo `motivo` e string nao vazia. E o unico invariante que pode ser ligado sem depender do flip, porque e chaveado por codigo e os codigos nunca estiveram errados — mas ligue-o aqui, junto do dicionario que ele valida.
  </action>

  <verify>
    <automated>npm run catalog:validate</automated>
  </verify>

  <acceptance_criteria>
    - `src/features/catalog/aliases.json` tem exatamente 4 entradas: `avc`, `tvp`, `ait`, `varizes`
    - A entrada `avc` lista 3 categorias, com `tabnetCode` `177`, `178` e `179`
    - As entradas `tvp`, `ait` e `varizes` listam 1 categoria cada, com `tabnetCode` `185`, `180` e `186`
    - Todo `labelEsperado` e igual, caractere a caractere incluindo acento, ao `label` daquele `tabnetCode` em `src/features/catalog/diseases.lista.json`
    - Nenhum termo contem acento ou maiuscula; nenhum termo aparece em duas entradas
    - Todo `motivo` e string nao vazia em portugues
    - `npm run catalog:validate` sai com codigo 0
    - Trocar um caractere de um `labelEsperado` faz `catalog:validate` sair 1 citando o invariante E; reverter depois de comprovar
    - Trocar um `tabnetCode` para um codigo inexistente faz `catalog:validate` sair 1; reverter depois de comprovar
    - `grep -c "id" src/features/catalog/aliases.json` nao encontra nenhum id de agravo — o dicionario e chaveado por codigo, nao por slug, para nao quebrar se um id mudar de novo
  </acceptance_criteria>

  <done>O dicionario curado existe com 4 entradas, cada uma amarrada a codigo e rotulo, e a validacao afirma essa amarracao a cada `catalog:validate`.</done>
</task>

<task type="auto">
  <name>Task 2: diseaseAliases.ts — funcao pura de matching com fuzzy medido</name>

  <read_first>
    - `src/features/catalog/filterCatalog.ts` — linhas 12-30: `matchesQuery`, a implementacao de referencia do AND entre tokens
    - `src/routes/mapas/MeasureDiseasePicker.tsx` — linhas 49-60: `normalizeCidQuery` e `diseaseMatches`, a funcao que a plan 08-09 vai plugar
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 6.2 inteira, com as duas medicoes e os numeros de falso positivo
    - `src/features/catalog/aliases.json` — o dicionario da Task 1
    - `src/features/catalog/diseases.lista.json` — o corpus de 331 rotulos contra o qual o matching roda
  </read_first>

  <files>src/features/catalog/diseaseAliases.ts, src/features/catalog/diseaseAliases.test.ts</files>

  <action>
Criar `src/features/catalog/diseaseAliases.ts` como modulo puro, sem estado e sem dependencia nova.

Exportar `foldAccents(texto)` fazendo normalizacao NFKD, remocao de marcas combinantes e minusculizacao. Sem essa dobra, "aterosclerose" e "Arteroesclerose" nem chegam a ser comparadas com o acento neutralizado.

Exportar `labelMatchesQuery(label, query)` implementando o algoritmo medido: dobrar acento dos dois lados, tokenizar por nao-alfanumerico, e exigir que **todo** token da query case algum token do rotulo. Um token casa quando e substring do token do rotulo, ou — somente se os dois tokens tiverem 6 ou mais caracteres — quando a distancia de Levenshtein entre eles for menor ou igual a 2. O piso de 6 e o que elimina por construcao o ruido de siglas curtas: sem ele, "ait" produz 18 matches espurios contra o corpus real. Implementar Levenshtein a mao, por programacao dinamica.

Exportar `resolveAliasTerm(query, aliases)` devolvendo a entrada do dicionario quando a query normalizada casa exatamente um dos termos dela, ou nulo. Casamento exato do termo inteiro, nao substring: um apelido nao deve disparar por acidente dentro de uma busca maior.

Exportar `matchDiseases(diseases, query, aliases)` devolvendo os agravos casados **e** a informacao de que veio de apelido, na forma de um objeto com a lista de agravos e, quando houver, o termo de apelido acionado e quantas categorias ele resolveu — e essa segunda parte que alimenta a tira explicativa do D-18 na plan 08-09. Quando um apelido dispara, os agravos vem dos `tabnetCode` da entrada; quando nao dispara, vem de `labelMatchesQuery`. As duas fontes podem ser unidas, sem duplicar.

A funcao devolve **agravos canonicos**, sempre. O apelido nunca aparece como id, nunca e devolvido como rotulo, e nunca vira chave de nada (D-19): o rotulo exibido, selecionado e copiado continua sendo o oficial da Lista Morb, o mesmo que o estudante reencontra no TabNet e cita no trabalho.

Criar `src/features/catalog/diseaseAliases.test.ts` rodando a bateria medida contra os 331 rotulos reais lidos de `diseases.lista.json`:
- `embolia pulmonar` casa exatamente 1 agravo, o de codigo `173`
- `insuficiencia cardiaca` casa exatamente 1, codigo `175`
- `aterosclerose` casa o codigo `181`, cujo rotulo oficial e "Arteroesclerose" — o caso que motivou o D-17
- `diabetes` e `pneumonia` como controles, casando o esperado
- `avc` devolve exatamente os 3 codigos da entrada curada, e o resultado indica apelido acionado com 3 categorias
- `ait` e `tvp` devolvem 1 codigo cada, via apelido
- `varizes` inclui o codigo `186` no resultado; afirmar a **presenca** do alvo, nao a exclusividade, porque o falso positivo residual de "raizes" e conhecido, medido e aceito
- sem o dicionario, `avc` devolve zero agravos — o teste que prova a afirmacao central do TAX-05, de que a canonizacao sem apelidos quebraria a busca
- nenhum retorno de `matchDiseases` contem o termo de apelido como id
- casos de unidade do piso de 6: dois tokens de 3 caracteres a distancia 2 nao casam; dois de 7 a distancia 2 casam
  </action>

  <verify>
    <automated>npx vitest run src/features/catalog/diseaseAliases.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/features/catalog/diseaseAliases.test.ts` passa com pelo menos 12 casos
    - O caso "sem dicionario, avc devolve zero" esta presente e verde
    - `grep -c "from '" src/features/catalog/diseaseAliases.ts` nao lista nenhuma dependencia de `node_modules` — zero deps novas
    - `npm run typecheck` sai com codigo 0
    - Baixar o piso de 6 para 3 no codigo faz o caso de `ait` sem dicionario ficar vermelho por excesso de matches; reverter depois de comprovar
    - `npm run test:run` verde
  </acceptance_criteria>

  <done>Existe uma funcao pura que resolve termo clinico para agravo canonico, com o limiar de fuzzy que a medicao mostrou nao produzir falso positivo em busca multi-palavra.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Conferencia clinica das 4 entradas curadas</name>

  <read_first>
    - `src/features/catalog/aliases.json` — as 4 entradas com codigo, rotulo esperado e motivo
    - `.planning/notes/2026-07-28-taxonomia-corrompida-ground-truth.md` — a tabela com CID de cada codigo
  </read_first>

  <what-built>
Dicionario curado de 4 apelidos (`avc`, `tvp`, `ait`, `varizes`), cada um amarrado a codigo TabNet e rotulo oficial, com o invariante E ja afirmando por maquina que o rotulo citado bate com o rotulo canonico daquele codigo. A funcao de matching resolve cada termo para os agravos canonicos correspondentes, e a bateria medida esta verde.

O invariante E prova que a entrada **cita o rotulo certo daquele codigo**. Ele nao prova, e nao tem como provar, que **aquele codigo e a categoria clinica certa para aquele termo**. Essa parte e a conferencia humana.
  </what-built>

  <how-to-verify>
Rodar `node -e` ou abrir `src/features/catalog/aliases.json` e conferir as quatro linhas abaixo contra o CID de cada codigo, disponivel na tabela da nota de ground truth:

1. `avc` para os codigos 177 (Hemorragia intracraniana, I60-I62), 178 (Infarto cerebral, I63) e 179 (Acid vascular cerebr nao espec, I64) — as tres formas de AVC. Confirmar que sao as tres que a liga espera ver ao buscar "AVC", e decidir se o codigo 180 (Outras doencas cerebrovasculares, I65-I69) deveria ou nao entrar tambem.
2. `tvp` para o codigo 185 (Flebite tromboflebite embolia e trombose venosa, I80-I82). TVP e I80.2, dentro do intervalo.
3. `varizes` para o codigo 186 (Veias varicosas das extremidades inferiores, I83).
4. `ait` para o codigo 180 (Outras doencas cerebrovasculares, I65-I69). **Este e o ponto que precisa de decisao explicita:** AIT, ataque isquemico transitorio, e G45 na CID-10, que fica no capitulo de doencas do sistema nervoso, nao em I65-I69. A expectativa de mapear `ait` para 180 vem da tabela de medicao do CONTEXT. Confirmar se e isso mesmo que a liga quer, ou se o apelido deve apontar para outra categoria, ou se deve sair do dicionario.

Se qualquer uma das quatro mudar, a alteracao e no `aliases.json` e o invariante E revalida sozinho no `catalog:validate`.
  </how-to-verify>

  <acceptance_criteria>
    - As quatro entradas foram lidas em voz alta contra o CID correspondente
    - A decisao sobre `ait` para 180 esta registrada por escrito no SUMMARY, seja mantendo, alterando ou removendo
    - A decisao sobre incluir ou nao o codigo 180 em `avc` esta registrada
    - Depois de qualquer alteracao, `npm run catalog:validate` sai com codigo 0 e `npx vitest run src/features/catalog/diseaseAliases.test.ts` continua verde
  </acceptance_criteria>

  <action>
Apresentar as quatro entradas de `src/features/catalog/aliases.json` lado a lado com o CID de cada codigo, pausar e aguardar a resposta do desenvolvedor. Aplicar em `aliases.json` a correcao que ele indicar, se indicar, e rodar `npm run catalog:validate` e a suite de apelidos de novo. Registrar a decisao de cada uma das quatro entradas no SUMMARY, inclusive as mantidas sem alteracao.
  </action>

  <verify>
    <automated>npm run catalog:validate && npx vitest run src/features/catalog/diseaseAliases.test.ts</automated>
  </verify>

  <resume-signal>Responder "aprovado" ou descrever a correcao por entrada</resume-signal>

  <done>As quatro entradas curadas foram conferidas por um humano contra o CID, e a duvida clinica do `ait` foi decidida e registrada em vez de assumida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query digitada pelo estudante para funcao de busca | Texto livre entra num matcher que percorre 331 rotulos |
| Dicionario curado a mao para resultado de busca | A mesma forma de tabela manual que produziu o defeito original |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Spoofing | Entrada curada apontando termo clinico para a categoria errada — o defeito do `KNOWN_BY_CODE` reencarnado | mitigate | Invariante E amarra codigo e rotulo por maquina; checkpoint humano confere codigo contra CID; dicionario chaveado por codigo, nunca por slug |
| T-08-07-02 | Tampering | Apelido gravado como chave de dado | mitigate | `matchDiseases` devolve so agravo canonico; teste afirma que nenhum retorno contem o termo; D-19 mantem o apelido no indice de busca |
| T-08-07-03 | Denial of Service | Levenshtein sobre corpus grande a cada tecla | accept | 331 rotulos e algumas centenas de tokens; medido em milissegundos em JS puro. O produto ja renderiza as 331 linhas sem virtualizacao por decisao registrada |
| T-08-07-04 | Tampering | Query com caractere de controle ou regex quebrando o matcher | mitigate | Tokenizacao por classe de caractere, sem construir RegExp a partir da query |
| T-08-07-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao; Levenshtein a mao por regra herdada da Fase 5 (RESEARCH secao Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run catalog:validate` sai 0 e falha fechado ao adulterar `labelEsperado` ou `tabnetCode`
- `npx vitest run src/features/catalog/diseaseAliases.test.ts` verde, incluindo o caso "sem dicionario, avc devolve zero"
- `npm run gate` verde
- Checkpoint humano respondido e registrado
</verification>

<success_criteria>
- `aliases.json` com 4 entradas, cada uma citando codigo e rotulo esperado, sem id de agravo
- Invariante E ligado ao `catalog:validate`
- Matcher puro com dobra de acento, AND entre tokens e fuzzy so acima de 6 caracteres
- `avc` resolve para 3 categorias canonicas; `ait`, `tvp` e `varizes` para 1 cada
- `aterosclerose` acha "Arteroesclerose" sem entrada curada
- Zero dependencia nova
- Conferencia clinica humana registrada, com a duvida do `ait` decidida
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-07-SUMMARY.md` when done
</output>
