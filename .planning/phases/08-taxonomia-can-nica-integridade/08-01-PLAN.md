---
phase: 08-taxonomia-can-nica-integridade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/catalog/listaMorbSource.mjs
  - scripts/catalog/snapshot/lista-morb.nibr.html
  - scripts/catalog/snapshot/lista-morb.extract.json
  - scripts/catalog/exclusions.json
  - scripts/catalog/extra-diseases.json
  - scripts/catalog/metricless-diseases.json
  - scripts/catalog/lista-morb-cid.json
  - scripts/catalog/validate.mjs
  - src/features/catalog/snapshotIntegrity.test.ts
  - package.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true
requirements: [TAX-01, TAX-06]

must_haves:
  truths:
    - "A geracao da taxonomia deixa de depender da rede: o HTML da Lista Morb esta versionado e catalog:validate roda offline (D-09)"
    - "O snapshot guarda HTML bruto mais extrato codigo-rotulo, e o invariante C prova que um foi derivado do outro (D-13)"
    - "As exclusoes 331/332/333 e a pseudo-opcao TODAS_AS_CATEGORIAS__ sao dado com motivo escrito, nao SKIP_CODES em codigo (D-14)"
    - "amputacao_mmii deixa de ser literal hardcoded no gerador e vira segunda fonte de entrada (D-14)"
    - "lista-morb-cid.json e chaveado por tabnetCode, imune a renomeacao dos ids (D-11)"
    - "O unico agravo canonico sem metrica (codigo 330) esta registrado com motivo escrito, e um segundo caso futuro nao tem onde se esconder (D-25)"
    - "REQUIREMENTS.md, ROADMAP.md e PROJECT.md dizem 331 agravos, e o ROADMAP deixa de mandar adicionar ON UPDATE CASCADE (D-08/D-25)"
  artifacts:
    - path: "scripts/catalog/snapshot/lista-morb.nibr.html"
      provides: "Evidencia arquivada do que a fonte oficial dizia na data da captura (D-13)"
    - path: "scripts/catalog/snapshot/lista-morb.extract.json"
      provides: "Extrato codigo-rotulo revisavel no diff, entrada da geracao"
      contains: "TODAS_AS_CATEGORIAS__"
    - path: "scripts/catalog/listaMorbSource.mjs"
      provides: "Parser puro, slugify e captura --refresh, exportados"
      exports: ["parseListaMorbOptions", "slugify", "loadSnapshot", "checkSnapshotIntegrity"]
    - path: "scripts/catalog/exclusions.json"
      provides: "Particao explicita: codigo fora da taxonomia com motivo em PT-BR"
    - path: "scripts/catalog/extra-diseases.json"
      provides: "Segunda fonte de entrada para agravos fora da Lista Morb (amputacao_mmii)"
    - path: "scripts/catalog/metricless-diseases.json"
      provides: "Registro do agravo canonico sem metrica coletada (D-25)"
  key_links:
    - from: "scripts/catalog/validate.mjs"
      to: "scripts/catalog/listaMorbSource.mjs"
      via: "import de checkSnapshotIntegrity chamado no main()"
      pattern: "checkSnapshotIntegrity"
    - from: "src/features/catalog/snapshotIntegrity.test.ts"
      to: "scripts/catalog/listaMorbSource.mjs"
      via: "import relativo de 3 niveis"
      pattern: "scripts/catalog/listaMorbSource.mjs"
---

<objective>
Congelar a fonte oficial da Lista Morb CID-10 como snapshot versionado e transformar em **dado com motivo escrito** tudo que hoje e decisao embutida em codigo: exclusoes, agravo extra e o mapa CID.

Purpose: a geracao da taxonomia hoje faz `fetch()` ao TabNet a cada execucao (`sync-lista-morb.mjs:12,67`), o que torna `catalog:validate` — encadeado em `pretest`/`test:run`/`gate` — dependente da rede. Sem snapshot, os invariantes B e C sao impossiveis. Esta plan nao muda nenhum id ainda: prepara as entradas.

Output: `scripts/catalog/snapshot/` (HTML bruto + extrato), `listaMorbSource.mjs` (parser puro), `exclusions.json`, `extra-diseases.json`, `metricless-diseases.json`, `lista-morb-cid.json` re-chaveado, invariante C ligado ao `catalog:validate`, e a emenda 330 para 331 nos documentos de projeto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md
@scripts/catalog/sync-lista-morb.mjs
@scripts/catalog/validate.mjs

<interfaces>
<!-- Contratos que o executor precisa. Nao explorar o repo para descobri-los. -->

Formato atual de `scripts/catalog/diseases.json` (330 entradas, ordem de chaves fixa):
`{ "id": string, "label": string, "filterKind": "lista_morb" | "procedimento", "tabnetCode": string, "def": string }`

Formato atual de `scripts/catalog/lista-morb-cid.json` (329 chaves, chaveado por `id` — o que esta plan re-chaveia):
`{ "<diseaseId>": "<intervalo CID como string livre>" }` — ex.: `"colera": "A00"`, `"outras_doencas_infecciosas_intestinais": "A02, A04-A05, A07-A08"`

Molde fail-closed ja existente em `validate.mjs` (repetir, nao recriar):
`export function checkX(...args)` devolve `string[]` — nunca lanca, nunca chama `process.exit`, sempre devolve array de mensagens no formato `"<contexto>: descricao curta"`. So `main()` decide exit code, atras do guarda `isDirectRun`.

Import cross-tree confirmado (`src/features/catalog/validateCatalogEntry.test.ts:1-5`):
`import { checkEntry } from '../../../scripts/catalog/validate.mjs';`

Config vitest (`vite.config.ts`): `include: ['src/**/*.{test,spec}.{ts,tsx}']` — um `.test.mjs` em `scripts/` **nao roda**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot versionado da Lista Morb + parser puro + invariante C</name>

  <read_first>
    - `scripts/catalog/sync-lista-morb.mjs` — linhas 41-91: `slugify`, `decodeEntities`, regex do `<select>` e do `<option>`. E este codigo que sai daqui para o modulo novo
    - `scripts/catalog/validate.mjs` — linhas 290-320 (molde `main()` e `isDirectRun`) e 42-112 (formato de retorno `string[]`)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 1.1 (encoding ISO-8859-1, 334 opcoes, 1 pseudo mais 333 reais) e secao 1.2 (o que sao 330/331/332/333)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 1 (molde de validate.mjs) e secao 3 (convencao de shape dos JSON)
  </read_first>

  <files>scripts/catalog/listaMorbSource.mjs, scripts/catalog/snapshot/lista-morb.nibr.html, scripts/catalog/snapshot/lista-morb.extract.json, scripts/catalog/validate.mjs, src/features/catalog/snapshotIntegrity.test.ts, package.json</files>

  <action>
Criar `scripts/catalog/listaMorbSource.mjs` movendo para la, sem alterar comportamento, as funcoes `slugify` e `decodeEntities` e as duas regex de extracao de `sync-lista-morb.mjs` (o select `SLista_Morb__CID-10` e o `<option value=...>`). Exportar `slugify(label, code)`, `decodeEntities(text)`, `parseListaMorbOptions(html)` devolvendo array de `{ code, label }` na ordem do documento com rotulo decodificado e espacos colapsados, `loadSnapshot()` lendo os dois arquivos de `scripts/catalog/snapshot/`, e `checkSnapshotIntegrity()` devolvendo `string[]` no molde de `checkEntry`.

O HTML tem que ser lido com encoding `latin1` — a fonte e ISO-8859-1 declarada no meta e confirmada por `file` em RESEARCH secao 1.1. Ler como `utf8` corrompe todo rotulo acentuado em silencio.

Acrescentar ao modulo um modo de captura acionado por `--refresh`, atras de `isDirectRun`: faz `fetch` de `http://tabnet.datasus.gov.br/cgi/deftohtm.exe?sih/cnv/nibr.def`, grava os bytes crus em `scripts/catalog/snapshot/lista-morb.nibr.html`, e grava `scripts/catalog/snapshot/lista-morb.extract.json` com os campos `source`, `capturedAt`, `sha256` e `options` — `sha256` calculado sobre o conteudo bruto do HTML via `node:crypto`, `options` sendo o retorno de `parseListaMorbOptions`. Se o `fetch` falhar, se o HTML nao contiver o select esperado, ou se o parse render menos de 300 opcoes, sair com codigo diferente de zero e mensagem explicita — nunca gravar snapshot vazio ou parcial. E a mesma classe de defeito do PIPE-01 que o milestone existe para eliminar.

Rodar a captura uma vez para produzir os dois arquivos e commita-los. Extrato com `JSON.stringify(x, null, 2)` mais newline final, conforme a convencao do pipeline.

`checkSnapshotIntegrity()` implementa o **invariante C** (D-13): recalcula o sha256 do HTML e compara com `extract.sha256`; re-parseia o HTML e compara com `extract.options` par a par (mesmo comprimento, mesma ordem, mesmo `code`, mesmo `label`). Divergencia vira mensagem em PT-BR no formato `snapshot: <descricao>`.

Ligar `checkSnapshotIntegrity()` ao `main()` de `validate.mjs`: os erros entram no mesmo array que hoje vem de `validateCatalogDir`, antes do bloco de impressao, de forma que `npm run catalog:validate` ja falhe se o snapshot for adulterado. O invariante C passa verde imediatamente porque o extrato acabou de ser derivado do HTML. Nenhum outro invariante entra nesta task.

Criar `src/features/catalog/snapshotIntegrity.test.ts` — obrigatoriamente sob `src/`, porque vitest nao enxerga `scripts/` — importando `parseListaMorbOptions` e `checkSnapshotIntegrity` por caminho relativo de 3 niveis, com tres casos: `checkSnapshotIntegrity()` devolve array vazio contra o snapshot commitado; `parseListaMorbOptions` sobre um HTML sintetico de duas opcoes com entidades `&ccedil;` e `&atilde;` devolve os rotulos decodificados; o extrato commitado tem 334 opcoes, a primeira sendo `TODAS_AS_CATEGORIAS__` e os codigos reais cobrindo 1 a 333 sem buraco.

Acrescentar em `package.json` o script `catalog:snapshot` apontando para `node scripts/catalog/listaMorbSource.mjs --refresh`. Nao encadear em `pretest` nem em `gate` — a captura e comando explicito de atualizacao de fonte (D-09), nunca automatica.
  </action>

  <verify>
    <automated>npm run catalog:validate && npx vitest run src/features/catalog/snapshotIntegrity.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `file scripts/catalog/snapshot/lista-morb.nibr.html` reporta `ISO-8859`
    - Um script node de uma linha lendo `lista-morb.extract.json` imprime `options.length === 334`
    - A primeira opcao do extrato tem `code` igual a `TODAS_AS_CATEGORIAS__`
    - Os 333 codigos restantes do extrato, convertidos para numero, sao exatamente 1..333 sem buraco
    - A opcao de `code` `330` tem `label` exatamente `Todas as outras causas externas`
    - `grep -c ccedil scripts/catalog/snapshot/lista-morb.extract.json` retorna `0` (rotulos gravados decodificados, nao com entidades)
    - `npm run catalog:validate` sai com codigo 0 e a saida contem `catalog:validate OK`
    - Adulterar um rotulo no extrato faz `npm run catalog:validate` sair com codigo 1 imprimindo mensagem iniciada por `snapshot:`; reverter a adulteracao depois de comprovar
    - `npx vitest run src/features/catalog/snapshotIntegrity.test.ts` passa com 3 casos
    - `grep -v "^\s*\*" scripts/catalog/listaMorbSource.mjs | grep -c "fetch("` retorna `1` — so o caminho `--refresh` acessa a rede
    - `node -e "const p=require('./package.json');process.exit(p.scripts['catalog:snapshot']&&!/catalog:snapshot/.test(p.scripts.pretest+p.scripts.gate)?0:1)"` sai com 0
  </acceptance_criteria>

  <done>O HTML da Lista Morb e seu extrato estao versionados, o parser e puro e importavel, e `catalog:validate` ja falha fechado se os dois divergirem — sem tocar em nenhum id de agravo.</done>
</task>

<task type="auto">
  <name>Task 2: Exclusoes, agravo extra, mapa CID re-chaveado e registro de agravo sem metrica — tudo como dado</name>

  <read_first>
    - `scripts/catalog/sync-lista-morb.mjs` — linha 39 (`SKIP_CODES`), linha 80 (o filtro), linhas 93-99 (o push hardcoded de `amputacao_mmii`), linhas 104-114 (leitura de `lista-morb-cid.json` por id)
    - `scripts/catalog/lista-morb-cid.json` — as 329 chaves atuais, chaveadas por id
    - `scripts/catalog/diseases.json` — o par id/tabnetCode atual, que e a chave da conversao
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 1.2 (o que 331/332/333 sao, com evidencia de duas fontes DATASUS) e secao 1.3 (o intervalo CID do codigo 330, item 1.103 de `mxcid10lm.htm`)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-PATTERNS.md` — secao 3 (objeto plano, motivo como string livre em PT-BR, indentacao 2 mais newline final)
  </read_first>

  <files>scripts/catalog/exclusions.json, scripts/catalog/extra-diseases.json, scripts/catalog/metricless-diseases.json, scripts/catalog/lista-morb-cid.json</files>

  <action>
Criar `scripts/catalog/exclusions.json` como objeto plano de codigo para motivo em PT-BR, com exatamente 4 chaves: `TODAS_AS_CATEGORIAS__` (pseudo-opcao de interface do TabNet para selecionar tudo, nao e codigo de categoria), `331`, `332` e `333` (os tres sao marcador de qualidade do dado subjacente — AIH sem CID preenchido, AIH anterior a CID-10, subcategoria CID inexistente — e nao categoria clinica; aparecem na tabela oficial `mxcid10lm.htm` sem codigo proprio de Lista Morb). O codigo **330 nao entra aqui** (D-25): e categoria clinica genuina e passa a fazer parte da taxonomia.

Criar `scripts/catalog/extra-diseases.json` como array com uma entrada, transcrevendo o push hardcoded de `sync-lista-morb.mjs:93-99` com os cinco campos que o gerador emite (`id`, `label`, `filterKind`, `tabnetCode`, `def`) mais um campo `reason` explicando que e procedimento SIH (`sih/cnv/qibr.def`), fora da Lista Morb CID-10, e por isso legitimamente sem `slugify(label) === id` — e a allowlist justificada do invariante A prevista em D-10. O campo `reason` e metadado do arquivo de entrada e nao pode aparecer no `diseases.json` gerado.

Criar `scripts/catalog/metricless-diseases.json` como objeto plano de id canonico para motivo, com exatamente uma chave: `todas_as_outras_causas_externas`, motivo registrando que o codigo 330 entrou na taxonomia por D-25 sem nenhuma coleta associada e que a cobertura dele e decisao da Fase 9. Este arquivo e o que permite ao D-04 e ao invariante de particao tolerarem **exatamente um** agravo sem linha de metrica sem abrir excecao silenciosa — um segundo caso futuro nao esta listado e falha alto.

Re-chavear `scripts/catalog/lista-morb-cid.json` de `id` para `tabnetCode` (D-11). A conversao e **computada**, nunca transcrita: escrever um script descartavel que le `diseases.json`, monta o mapa de id para tabnetCode, e reemite o mapa CID com as chaves trocadas; abortar se algum id do mapa nao existir em `diseases.json` ou se duas chaves colidirem no mesmo codigo. Depois da conversao, acrescentar a chave `330` com o intervalo `W20-W64, W75-W99, X10-X39, X50-X59, Y10-Y89`, que RESEARCH secao 1.3 obteve do item 1.103 da tabela oficial `mxcid10lm.htm` — sem essa entrada o codigo 330 entraria na taxonomia com `cid` nulo e o invariante do D-11 falharia. Manter chaves em ordem numerica crescente, `JSON.stringify(x, null, 2)` mais newline final.

Nenhum destes quatro arquivos e lido por codigo ainda — `sync-lista-morb.mjs` so passa a consumi-los na plan 08-02. Por isso esta task e neutra para o gate.
  </action>

  <verify>
    <automated>npm run catalog:validate && npx vitest run src/features/catalog/snapshotIntegrity.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `exclusions.json` tem exatamente 4 chaves e **nao** contem a chave `330`
    - As 3 chaves numericas de `exclusions.json` sao `331`, `332` e `333`, e cada motivo e string nao vazia em portugues
    - `extra-diseases.json` e um array de 1 entrada com `tabnetCode` igual a `3331`, `filterKind` igual a `procedimento` e campo `reason` nao vazio
    - `metricless-diseases.json` tem exatamente 1 chave, `todas_as_outras_causas_externas`
    - `lista-morb-cid.json` tem exatamente 330 chaves, todas numericas, e nenhuma chave e um id de agravo
    - `lista-morb-cid.json["330"]` comeca com `W20-W64`
    - Toda chave de `lista-morb-cid.json` de 1 a 329 corresponde a um `tabnetCode` presente em `diseases.json`, e nenhum valor CID foi alterado em relacao ao arquivo anterior (comparar o multiset de valores antes e depois: identico)
    - `npm run catalog:validate` continua saindo com codigo 0
  </acceptance_criteria>

  <done>As quatro decisoes que hoje vivem como literal no gerador viraram dado versionado com motivo escrito, e o mapa CID esta chaveado por codigo — imune a renomeacao.</done>
</task>

<task type="auto">
  <name>Task 3: Emenda 330 para 331 nos documentos de projeto (D-25) e correcao do ROADMAP sobre ON UPDATE CASCADE e sobre os 21 ids</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` — linhas 32-37 (TAX-01..TAX-06) e linha 34 (contagem de `sih_disease`)
    - `.planning/ROADMAP.md` — linhas 75-91 (bloco inteiro da Fase 8: Goal, Success Criteria 1-6, Notes)
    - `.planning/PROJECT.md` — linhas 13, 17 e 57 (as afirmacoes de contagem e a descricao da taxonomia canonica)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-25 (a lista exata de documentos a emendar) e D-08 (a decisao contraria ao ROADMAP)
    - `.planning/notes/2026-07-28-taxonomia-corrompida-ground-truth.md` — Emenda 2026-08-03 (sao 21 ids, nao 20)
  </read_first>

  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md</files>

  <action>
Emendar a redacao dos tres documentos para a contagem correta pos-D-25, sempre registrando que **330 continua sendo o numero de agravos com dado coletado** — a mudanca e da taxonomia, nao da coleta.

Em `.planning/REQUIREMENTS.md`: TAX-01 passa a citar explicitamente os **331 agravos** da taxonomia canonica (330 categorias `lista_morb` da Lista Morb CID-10 mais o procedimento `amputacao_mmii`), com a nota de que 330 e a contagem com dado coletado; TAX-03 passa a exigir `sih_disease` com **331** linhas, mantendo `sih_metric_uf` em 30.313 e `sih_metric_muni` em 1.099.403 (o codigo 330 nao tem dado coletado, entao as tabelas de metrica nao mudam). Acrescentar em TAX-03 a referencia a D-25 como origem da emenda.

Em `.planning/ROADMAP.md`, bloco da Fase 8: Success Criterion 1 passa a dizer 331 agravos; Success Criterion 2 passa a dizer **21** registros corrompidos (hoje diz 20, numero refutado pela emenda datada de 2026-08-03 na nota de ground truth); Success Criterion 3 passa a dizer que `sih_disease` tem 331 linhas. No bloco Notes da Fase 8, remover a instrucao de adicionar `ON UPDATE CASCADE` e substituir por registro de que D-08 decidiu explicitamente **nao** adiciona-la, com o motivo (depois desta fase renomear `disease_id` deixa de ser operacao esperada; CASCADE tornaria facil fazer em silencio exatamente o que a fase existe para tornar dificil) — a nota atual instrui o contrario da decisao travada e por isso e uma armadilha para quem executar. Ainda em Notes, registrar que o codigo 330 entra na taxonomia por D-25.

Em `.planning/PROJECT.md`: no bullet de Taxonomia canonica (Target features) e no item correspondente de Active, registrar 331 agravos na taxonomia canonica com a mesma nota de "330 com dado coletado".

**Fora de escopo desta emenda, deliberadamente:** as ocorrencias de "330 agravos" em DATA-01, DATA-02, MAPA-01 e nos blocos das Fases 9/10/11 do ROADMAP. Elas descrevem **cobertura de coleta**, nao tamanho de taxonomia, e a cobertura do codigo 330 e decisao da Fase 9 (registrada em `metricless-diseases.json`). Nao alterar essas linhas; nao inventar numero para elas.
  </action>

  <verify>
    <automated>grep -c "20 registros corrompidos" .planning/ROADMAP.md | grep -qx 0 && grep -q "331" .planning/REQUIREMENTS.md && grep -q "D-08" .planning/ROADMAP.md && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "TAX-01" .planning/REQUIREMENTS.md` mostra a linha contendo `331`
    - `grep -n "TAX-03" .planning/REQUIREMENTS.md` mostra `sih_disease` com `331` e mantem `30.313` e `1.099.403` inalterados
    - `grep -c "20 registros corrompidos" .planning/ROADMAP.md` retorna `0`
    - `grep -c "21 registros corrompidos" .planning/ROADMAP.md` retorna `1`
    - `grep -n "ON UPDATE CASCADE" .planning/ROADMAP.md` retorna apenas linhas que tambem citam `D-08` ou a palavra `nao`
    - As tres ocorrencias emendadas contem a expressao `com dado coletado` para desambiguar 330 de 331
    - `git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md` mostra alteracao nos tres arquivos e em nenhum outro
    - Nenhuma linha de DATA-01, DATA-02 ou MAPA-01 aparece no diff
  </acceptance_criteria>

  <done>Os documentos de projeto param de afirmar um numero que a fase vai tornar falso, o ROADMAP para de instruir o oposto de D-08, e a contagem de ids corrompidos bate com o ground truth emendado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TabNet (HTTP sem TLS) para o repo | O HTML da Lista Morb chega por `http://`, sem autenticidade de origem; a partir daqui vira fonte da taxonomia inteira |
| Arquivo de dado versionado para pipeline | `exclusions.json`, `extra-diseases.json`, `lista-morb-cid.json` passam a decidir o que entra na taxonomia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | snapshot HTML obtido por HTTP puro | mitigate | `sha256` gravado no extrato, invariante C no `catalog:validate`, e o HTML entra no diff do PR — adulteracao posterior falha fechado; captura so por `--refresh` explicito |
| T-08-01-02 | Denial of Service | `fetch` do TabNet dentro do gate | mitigate | D-09: o gate passa a ler o snapshot commitado; o unico caminho de rede fica atras de `--refresh`, que nao esta em `pretest` nem `gate` |
| T-08-01-03 | Tampering | `lista-morb-cid.json` re-chaveado a mao poderia embaralhar CID por codigo | mitigate | Conversao computada de `diseases.json` com aborto em id ausente ou colisao; multiset de valores CID identico antes e depois |
| T-08-01-04 | Information disclosure | HTML de terceiro versionado no repo | accept | Conteudo publico do DATASUS, sem PII, sem credencial |
| T-08-01-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao nesta fase — RESEARCH secao Package Legitimacy Audit registra "nao aplicavel"; regra herdada da Fase 5 de zero dependencias novas no pipeline |
</threat_model>

<verification>
- `npm run catalog:validate` sai com codigo 0
- `npx vitest run` continua verde (nenhum id de agravo mudou nesta plan)
- `npm run gate` verde
- `git status` limpo apos o commit, sem arquivo de scratch da conversao do mapa CID deixado para tras
</verification>

<success_criteria>
- Snapshot HTML e extrato commitados, com 334 opcoes e o codigo 330 presente com o rotulo real
- Invariante C ativo no `catalog:validate` e coberto por teste vitest sob `src/`
- `exclusions.json` (4 chaves, sem 330), `extra-diseases.json` (1 entrada), `metricless-diseases.json` (1 chave) criados
- `lista-morb-cid.json` com 330 chaves numericas, incluindo o intervalo CID do codigo 330
- REQUIREMENTS.md, ROADMAP.md e PROJECT.md emendados para 331 agravos, com a nota "330 com dado coletado"
- ROADMAP nao instrui mais adicionar `ON UPDATE CASCADE` e diz 21 registros corrompidos
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-01-SUMMARY.md` when done
</output>
