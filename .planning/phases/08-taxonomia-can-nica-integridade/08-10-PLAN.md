---
phase: 08-taxonomia-can-nica-integridade
plan: 10
type: execute
wave: 7
depends_on: [08-08, 08-09]
files_modified:
  - docs/SUPABASE-CATALOG.md
  - .planning/phases/08-taxonomia-can-nica-integridade/08-10-SUMMARY.md
autonomous: false
requirements: [TAX-03, TAX-04]

must_haves:
  truths:
    - "A migracao foi aplicada em producao so depois do ensaio local ter passado, com as medicoes na mesa (D-02)"
    - "sih_disease tem 331 linhas, sih_metric_uf tem 30.313 e sih_metric_muni tem 1.099.403, conferidas no banco vivo (TAX-03)"
    - "Nenhum id em producao comeca com __mig_ e todo id canonico do mapa existe em sih_disease"
    - "O conjunto de ids de sih_disease e identico ao conjunto das seeds geradas da taxonomia canonica — banco e repositorio contam a mesma historia (TAX-06)"
    - "Existe um caminho de reversao escrito, testado no ensaio e pronto para uso, e ele nao esta em supabase/migrations (D-03/TAX-04)"
    - "docs/SUPABASE-CATALOG.md deixa de divergir do schema real: nomes de constraint, ON DELETE CASCADE e indices documentados como sao"
  artifacts:
    - path: "docs/SUPABASE-CATALOG.md"
      provides: "Contrato do banco atualizado para o schema real e para a contagem 331"
      contains: "sih_metric_uf_disease_id_fkey"
    - path: ".planning/phases/08-taxonomia-can-nica-integridade/08-10-SUMMARY.md"
      provides: "Registro do push, das contagens observadas e do caminho de reversao"
  key_links:
    - from: "supabase/migrations/*_rename_disease_ids.sql"
      to: "producao hmfbxqemububjyhdckrj"
      via: "supabase db push"
      pattern: "db push"
    - from: "supabase/verify/contagens.sql"
      to: "producao hmfbxqemububjyhdckrj"
      via: "verificacao pos-push das contagens absolutas"
      pattern: "contagens.sql"
---

<objective>
Aplicar a migracao em producao e provar, no banco vivo, que as contagens e a integridade referencial batem — com o caminho de reversao pronto e escrito.

Purpose: e a operacao de maior raio de dano do milestone. Ate aqui tudo foi reversivel por `git`; a partir do `db push` o dado de 1,13M linhas muda de nome em producao. O ensaio da plan 08-08 ja exercitou o ciclo completo contra uma copia real e mediu o custo — esta plan executa, confere e documenta.

Output: producao migrada, contagens conferidas, `docs/SUPABASE-CATALOG.md` alinhado ao schema real, e o caminho de reversao registrado.

**Esta plan e nao-autonoma por decisao.** Ela abre com um checkpoint de decisao humana, com as medicoes do ensaio na mesa, e a aplicacao so acontece depois da autorizacao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md
@docs/SUPABASE-CATALOG.md

<interfaces>
<!-- Ambiente, numeros e schema real. Nao redescobrir. -->

Projeto de producao: `hmfbxqemububjyhdckrj` ("Site bioestatistica", regiao West US Oregon). CLI `supabase` 2.90.0 em `/opt/homebrew/bin/supabase`, ja autenticado nesta maquina; `link` ja feito na plan 08-05. Docker precisa estar rodando para qualquer operacao de schema.

Arquivos relevantes:
- `supabase/migrations/<ts0>_remote_schema.sql` — baseline, ja marcado como aplicado no remoto pelo `db pull`
- `supabase/migrations/<ts1>_rename_disease_ids.sql` — o unico pendente; e ele que `db push` vai aplicar
- `supabase/rollback/<ts1>_rename_disease_ids_down.sql` — **fora** de `migrations/` de proposito, para nunca ser aplicado por `db push`
- `supabase/verify/contagens.sql` — contagens absolutas com `RAISE EXCEPTION` em divergencia, mais a lista de agravos sem metrica que nao constam de `scripts/catalog/metricless-diseases.json`
- `scripts/catalog/sql/0.sql`..`4.sql` — seeds geradas da taxonomia canonica, 331 ids no total

Contagens alvo pos-migracao: `sih_disease` **331**, `sih_metric_uf` **30.313**, `sih_metric_muni` **1.099.403**. As duas ultimas nao mudam — o codigo 330 entra sem dado coletado.

Volume tocado pela renomeacao, medido: 21 linhas em `sih_disease`, 4.195 em `sih_metric_uf`, 200.636 em `sih_metric_muni`, por passada.

Schema real (RESEARCH secao 4.1, obtido por `db dump`), que diverge de `docs/SUPABASE-CATALOG.md`:
- Constraints: `sih_disease_pkey`, `sih_metric_uf_pkey` (disease_id, uf_codigo, ano), `sih_metric_muni_pkey` (disease_id, municipio_codigo, ano), `sih_metric_uf_disease_id_fkey` e `sih_metric_muni_disease_id_fkey`, ambas com **ON DELETE CASCADE**, ambas NOT DEFERRABLE, nenhuma com ON UPDATE
- Indices: `sih_metric_uf_disease_ano` sobre (disease_id, ano) e `sih_metric_muni_disease_uf_ano` sobre (disease_id, uf_codigo, ano) — o doc chamava este ultimo de `sih_metric_muni_uf_ano` e o dava como intencao nao confirmada; ele **existe** e esta aplicado
- `sih_disease` tem coluna `created_at` timestamptz default now(), nao documentada
- RLS habilitada nas tres tabelas, com policy de SELECT para `anon` e `authenticated`

Leitura de conferencia sem credencial nova: `VITE_SUPABASE_ANON_KEY` de `.env.local` e chave publica de leitura, a mesma que o app usa. PostgREST responde `content-range` com o total quando a requisicao leva `Prefer: count=exact`. E assim que as contagens de producao foram medidas em 2026-08-03.

Aviso de escopo herdado: `INGEST_SECRET` em texto plano em `scrape_upload_sih.py:33` **nao** e corrigido aqui. A rotacao e da Fase 9, por decisao registrada. Se aparecer no caminho, registrar sem corrigir.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Autorizacao para aplicar em producao</name>

  <read_first>
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md` — o veredito do ensaio e a tabela de medicoes: tempo do up, tempo do down, linhas afetadas por tabela
    - `supabase/migrations/<ts1>_rename_disease_ids.sql` — o arquivo que sera aplicado
    - `supabase/rollback/<ts1>_rename_disease_ids_down.sql` — o caminho de volta
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-CONTEXT.md` — D-02 e D-03
  </read_first>

  <decision>Aplicar agora, em producao, a migracao de renomeacao dos 21 ids de agravo mais a insercao do agravo do codigo 330, em `hmfbxqemububjyhdckrj`.</decision>

  <context>
Ate este ponto tudo foi reversivel por `git`. Depois do `db push`, o dado de producao muda de nome: 21 ids renomeados em tres tabelas e um agravo novo inserido. A reversao passa a depender do script `--down`, nao de um `git revert`.

O que ja esta feito e verificavel antes de decidir:
- a mecanica das duas passadas com CTEs multiplas foi reproduzida empiricamente contra um Postgres 17 real, com schema identico ao de producao e os dois ciclos reais do ground truth, testando up e down com retorno exato ao estado original (RESEARCH secao 4.2)
- o ensaio da plan 08-08 aplicou o up, a prova de integridade agregada por agravo e por medida, o down e a conferencia de retorno, contra uma copia integral de producao restaurada de `pg_dump`
- o SQL e gerado de `rename-map.json` e um teste de suite impede que ele seja editado a mao
- a prova D-04 roda **dentro** da transacao: se qualquer soma de `internacoes`, `obitos`, `valor_total` ou `dias_permanencia` mudar de dono, a transacao aborta sozinha e nada e aplicado
- o volume real e de cerca de 205 mil linhas por passada, nao 1,13M

Apresentar ao desenvolvedor, antes de perguntar: o tempo do up medido no ensaio, o tempo do down, e o veredito registrado em `08-08-SUMMARY.md`.
  </context>

  <options>
    <option id="aplicar-agora">
      <name>Aplicar agora</name>
      <pros>Fecha TAX-03 e TAX-04; desbloqueia a Fase 9, que e chaveada por `disease_id` e precisa dos ids canonicos antes da coleta longa; o repositorio ja esta migrado desde a plan 08-06, entao banco e bundle voltam a concordar</pros>
      <cons>Operacao irreversivel por `git`; se algo falhar fora da transacao, a volta e pelo script `--down` aplicado a mao</cons>
    </option>
    <option id="adiar-para-janela">
      <name>Adiar para uma janela sem aula</name>
      <pros>Evita qualquer risco de indisponibilidade durante uso didatico</pros>
      <cons>O repositorio fica com a taxonomia canonica e o banco com a antiga, entao o handoff do mapa para Estatistica erra o agravo enquanto durar a janela; adiar aumenta o tempo nesse estado inconsistente</cons>
    </option>
    <option id="nao-aplicar">
      <name>Nao aplicar e reabrir a decisao</name>
      <pros>Nenhuma mudanca em producao</pros>
      <cons>TAX-03 e TAX-04 ficam abertos, a Fase 9 fica bloqueada, e o defeito que a fase existe para corrigir continua vivo no banco</cons>
    </option>
  </options>

  <acceptance_criteria>
    - O veredito do ensaio em `08-08-SUMMARY.md` foi lido em voz alta e diz que o ciclo passou por completo
    - Tempo do up e tempo do down medidos foram apresentados antes da pergunta
    - Se o veredito do ensaio nao for de aprovacao, a plan para aqui e nenhuma opcao de aplicacao e oferecida
    - A escolha esta registrada por escrito no SUMMARY, com data e hora
  </acceptance_criteria>

  <action>
Ler `.planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md`, extrair o veredito do ensaio e a tabela de medicoes, e apresentar tempo do up, tempo do down e linhas afetadas por tabela antes de fazer a pergunta. Se o veredito nao for de aprovacao, parar a plan sem oferecer nenhuma opcao de aplicacao. Pausar, aguardar a escolha do desenvolvedor, e registra-la no SUMMARY com data e hora. Nenhum comando de escrita contra producao pode ser executado nesta task.
  </action>

  <verify>
    <automated>test -f .planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md</automated>
  </verify>

  <resume-signal>Escolher: aplicar-agora, adiar-para-janela ou nao-aplicar</resume-signal>

  <done>A decisao de tocar producao foi tomada por um humano, com o custo medido na mesa, e nao por inercia do fluxo de execucao.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar a migracao em producao com supabase db push</name>

  <read_first>
    - `supabase/config.toml` — confirmar `project_id` igual a `hmfbxqemububjyhdckrj` antes de qualquer comando
    - `supabase/migrations/` — os dois arquivos e seus timestamps
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 3, sobre o CLI ja autenticado e a exigencia de Docker
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-08-SUMMARY.md` — o tempo esperado do up, para saber se a execucao esta se comportando como no ensaio
  </read_first>

  <files>supabase/migrations/</files>

  <action>
**Esta task e bloqueante e so comeca depois da autorizacao da Task 1.** Nenhuma parte da fase pode ser dada por verificada sem ela: `typecheck`, `test:run` e `build` passam sem o push, porque os tipos vem da configuracao e nao do banco vivo — o que cria exatamente o falso verde que esta fase existe para eliminar.

Antes de aplicar, conferir tres coisas. Primeiro, `docker info` responde. Segundo, `supabase migration list` mostra o baseline como aplicado tanto local quanto remotamente, e `<ts1>_rename_disease_ids` como pendente **apenas** no remoto — se houver qualquer terceira migracao pendente, ou se o baseline aparecer como nao aplicado no remoto, parar e investigar; aplicar um baseline por cima de um schema existente e um caminho para dano. Terceiro, `git status` limpo e `HEAD` no commit do flip ou depois dele, para que banco e repositorio fiquem coerentes ao fim.

Aplicar com `supabase db push`. Se o comando pedir confirmacao interativa que nao dá para suprimir, confirmar manualmente — a task ja e nao-autonoma justamente por isso. Registrar o tempo de parede e compará-lo com o do ensaio: uma execucao muito mais longa que a medida em 08-08 e sinal de que algo esta diferente e merece investigacao imediata, antes de qualquer outro comando.

Se a migracao abortar com `RAISE EXCEPTION` vinda do bloco de prova D-04, **e o comportamento correto e nada foi aplicado** — a transacao inteira reverte. Registrar a mensagem exata, parar a plan, e nao tentar contornar, afrouxar ou remover a checagem para "conseguir aplicar". Um push que aborta e infinitamente melhor que um que passa e embaralha 200 mil linhas.

Nao aplicar, em nenhuma circunstancia, nada de `supabase/rollback/` nem de `supabase/verify/` por meio de `db push`. O primeiro desfaz a migracao, e os dois estao fora de `migrations/` exatamente para nao serem varridos.

Depois do push, confirmar por `supabase migration list` que as duas migracoes aparecem como aplicadas no remoto.

**Caminho de reversao, para registrar no SUMMARY mesmo que nao seja usado:** aplicar `supabase/rollback/<ts1>_rename_disease_ids_down.sql` em producao. Como `db push` so aplica o que esta em `migrations/`, a reversao e feita executando o conteudo desse arquivo pelo SQL Editor do dashboard do projeto, numa unica execucao para que fique numa transacao so. O arquivo ja foi exercitado de ponta a ponta no ensaio da plan 08-08 e devolveu o banco ao estado inicial de forma identica. Depois de uma reversao, seria preciso tambem marcar a migracao como nao aplicada com `supabase migration repair`, para que o historico volte a bater.
  </action>

  <verify>
    <automated>supabase migration list</automated>
  </verify>

  <acceptance_criteria>
    - A Task 1 foi respondida com `aplicar-agora` antes desta task comecar
    - `docker info` respondeu sem erro
    - Antes do push, `supabase migration list` mostrava exatamente uma migracao pendente no remoto
    - `supabase db push` terminou com codigo 0 e sem `RAISE EXCEPTION`
    - Depois do push, `supabase migration list` mostra as duas migracoes aplicadas local e remotamente
    - O tempo de parede do push esta registrado e comparado com o tempo do ensaio
    - Nenhum arquivo de `supabase/rollback/` ou `supabase/verify/` foi aplicado
    - `git status` continua limpo — o push nao altera o repositorio
  </acceptance_criteria>

  <done>Producao esta migrada: os 21 ids nomeiam o codigo TabNet que realmente servem, e o agravo do codigo 330 existe.</done>
</task>

<task type="auto">
  <name>Task 3: Verificacao pos-push no banco vivo</name>

  <read_first>
    - `supabase/verify/contagens.sql` — o que ele afirma e como falha
    - `scripts/catalog/rename-map.json` — os 21 canonicos e o id adicionado
    - `scripts/catalog/metricless-diseases.json` — o unico agravo sem metrica registrado
    - `scripts/catalog/sql/0.sql`..`4.sql` — as seeds, fonte do conjunto de ids esperado
    - `.env.local` — a chave anon publica usada para leitura, a mesma que o app usa
  </read_first>

  <files>.planning/phases/08-taxonomia-can-nica-integridade/08-10-SUMMARY.md</files>

  <action>
Conferir no banco vivo, por leitura, e registrar cada numero observado no SUMMARY.

Contagens absolutas por PostgREST com a chave anon e cabecalho `Prefer: count=exact`, lendo o `content-range` de cada tabela: `sih_disease` precisa dar **331**, `sih_metric_uf` **30.313** e `sih_metric_muni` **1.099.403**. E a leitura pelo mesmo caminho que o app usa, sem credencial nova e sem escrita.

Rodar tambem `supabase/verify/contagens.sql` pelo SQL Editor do dashboard, numa unica execucao. Ele repete as tres contagens com `RAISE EXCEPTION` em divergencia e lista os agravos sem metrica que nao constam de `metricless-diseases.json` — essa lista precisa vir vazia. Lembrar que 237 dos 330 agravos ja nao tinham linha em `sih_metric_uf` antes da migracao: isso e a lacuna de cobertura da coleta, escopo da Fase 9, e o que a verificacao afirma e que esse conjunto **nao cresceu** alem do que esta registrado.

Conferir a coerencia entre banco e repositorio, que e a prova de TAX-06 do lado do dado: ler todos os `id` de `sih_disease` por PostgREST, paginando, e comparar como conjunto com os ids extraidos das cinco seeds `scripts/catalog/sql/*.sql`. Os dois conjuntos precisam ser **identicos**, com 331 elementos. Divergencia aqui significa que banco e repositorio contam historias diferentes, que e literalmente o defeito que a fase corrige.

Conferir que nenhum id em `sih_disease` comeca com `__mig_` nem com `__down_`, e que o agravo `todas_as_outras_causas_externas` existe com `tabnet_code` igual a `330`, `filter_kind` igual a `lista_morb` e zero linhas nas duas tabelas de metrica.

Conferir por amostra os dois ciclos, que sao a razao da migracao de duas passadas: o agravo `hemorroidas` agora precisa carregar as somas que antes estavam sob `outras_doencas_veias` (codigo 187), e `embolia_pulmonar` as que estavam sob `doencas_reumaticas_cronicas` (codigo 173). Comparar contra os valores registrados no ensaio da plan 08-08.

Por fim, rodar `npm run gate` e confirmar verde. O repositorio ja estava migrado desde a plan 08-06; o que se confirma aqui e que banco e bundle voltaram a concordar.
  </action>

  <verify>
    <automated>npm run gate</automated>
  </verify>

  <acceptance_criteria>
    - `content-range` de `sih_disease` termina em `/331`
    - `content-range` de `sih_metric_uf` termina em `/30313` e o de `sih_metric_muni` em `/1099403`
    - `supabase/verify/contagens.sql` executou sem `RAISE EXCEPTION` e a lista de agravos sem metrica fora do registro veio vazia
    - O conjunto de ids de `sih_disease` e identico ao conjunto de ids das seeds, com 331 elementos, conferido por comparacao de conjunto e nao por amostragem
    - Nenhum id comeca com `__mig_` ou `__down_`
    - `todas_as_outras_causas_externas` existe com `tabnet_code` `330` e zero linhas em ambas as tabelas de metrica
    - As somas agregadas dos dois pares de ciclo batem com os valores registrados no ensaio
    - `npm run gate` sai com codigo 0
    - Todos os numeros observados estao no SUMMARY, nao so a conclusao
  </acceptance_criteria>

  <done>As contagens e a integridade referencial estao conferidas no banco vivo, e o conjunto de agravos do banco e o do repositorio sao o mesmo.</done>
</task>

<task type="auto">
  <name>Task 4: Alinhar docs/SUPABASE-CATALOG.md ao schema real e registrar o handoff da Fase 9</name>

  <read_first>
    - `docs/SUPABASE-CATALOG.md` — o documento inteiro, para saber exatamente o que diverge
    - `supabase/migrations/<ts0>_remote_schema.sql` — o schema real capturado por `db pull`
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-RESEARCH.md` — secao 4.1, a tabela de divergencias entre o doc e a producao
    - `.planning/ROADMAP.md` — a tabela de Riscos conhecidos, com o item do indice de `sih_metric_muni` dado como nao confirmado
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-06-SUMMARY.md` — as consequencias registradas para a Fase 9
  </read_first>

  <files>docs/SUPABASE-CATALOG.md, .planning/phases/08-taxonomia-can-nica-integridade/08-10-SUMMARY.md</files>

  <action>
Atualizar `docs/SUPABASE-CATALOG.md` para descrever o banco como ele e, nao como se supunha. O documento e referencia canonica para as Fases 9 e 10 e hoje diverge da producao em pontos que ja custaram tempo nesta fase.

Corrigir: os nomes reais das constraints (`sih_disease_pkey`, `sih_metric_uf_pkey`, `sih_metric_muni_pkey`, `sih_metric_uf_disease_id_fkey`, `sih_metric_muni_disease_id_fkey`); o **`ON DELETE CASCADE`** em ambas as FK, que nao estava documentado em lugar nenhum do projeto e e uma armadilha real — qualquer estrategia futura que apague e reinsira uma linha de `sih_disease` apaga em cascata todas as metricas daquele agravo, sem erro; a ausencia de `ON UPDATE` em ambas, registrando que isso e deliberado por D-08; os nomes reais dos indices, `sih_metric_uf_disease_ano` e `sih_metric_muni_disease_uf_ano` — este ultimo aparecia no doc como `sih_metric_muni_uf_ano` e no ROADMAP como intencao nao confirmada, e ele **existe e esta aplicado**; a coluna `created_at` de `sih_disease`; e a contagem de `sih_disease`, que passa de 330 para **331**, com a nota de que 330 segue sendo o numero de agravos com dado coletado.

Acrescentar ao doc uma secao curta sobre a estrutura `supabase/` introduzida nesta fase: `migrations/` e o que `db push` aplica, `rollback/` guarda o `--down` testado e fica fora do caminho de `db push` de proposito, e `verify/` guarda a verificacao de contagens.

Registrar no SUMMARY o handoff para a Fase 9, em forma de lista curta e acionavel: os 341 diretorios de `trabalhos datasus/outputs/coleta_sih_multi/` seguem com os nomes antigos por D-22, e agora `uploadSihToSupabase.mjs` **falha alto** ao encontrar um deles, por D-06 — e o comportamento pretendido, e e o que a Fase 9 vai encontrar ao re-chavear o pipeline; `manifest.json` mantem `sourceDir` legado como proveniencia verdadeira; `catalog:build` nao pode ser rodado ate o corpus ser re-chaveado; a rotacao do `INGEST_SECRET` em `scrape_upload_sih.py:33` continua pendente e continua sendo escopo da Fase 9, registrada aqui sem correcao; e a cobertura de coleta do agravo do codigo 330, hoje registrada em `scripts/catalog/metricless-diseases.json`, e decisao da Fase 9.
  </action>

  <verify>
    <automated>grep -c "sih_metric_uf_disease_id_fkey" docs/SUPABASE-CATALOG.md && grep -c "ON DELETE CASCADE" docs/SUPABASE-CATALOG.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "sih_metric_muni_disease_uf_ano" docs/SUPABASE-CATALOG.md` retorna pelo menos `1`
    - `grep -c "sih_metric_muni_uf_ano" docs/SUPABASE-CATALOG.md` retorna `0` — o nome errado sumiu
    - `grep -c "ON DELETE CASCADE" docs/SUPABASE-CATALOG.md` retorna pelo menos `2`
    - `grep -n "ON UPDATE" docs/SUPABASE-CATALOG.md` retorna apenas linhas que citam `D-08`
    - O documento cita `331` para `sih_disease` e traz a nota "330 com dado coletado"
    - O documento cita `created_at` em `sih_disease`
    - O documento tem uma secao descrevendo `supabase/migrations`, `supabase/rollback` e `supabase/verify`
    - O SUMMARY tem uma lista de handoff para a Fase 9 com pelo menos os cinco itens citados na acao
    - `npm run gate` continua verde
  </acceptance_criteria>

  <done>O contrato do banco documentado descreve o banco que existe, e a Fase 9 comeca sabendo o que herdou em vez de descobrir por acidente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositorio para banco de producao | `supabase db push` aplica em `hmfbxqemububjyhdckrj` o que estiver em `supabase/migrations/`, com 1,13M linhas do outro lado |
| Supabase CLI para Management API | O CLI provisiona papel Postgres temporario com senha efemera para o push |
| Leitura anonima para conferencia | A `VITE_SUPABASE_ANON_KEY` le as tres tabelas via PostgREST sob RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10-01 | Tampering | Migracao embaralhando os 21 agravos em producao de forma consistente e invisivel | mitigate | Prova D-04 por soma agregada de `internacoes`, `obitos`, `valor_total` e `dias_permanencia` roda dentro da transacao e aborta com `RAISE EXCEPTION`; a task proibe explicitamente contornar a checagem |
| T-08-10-02 | Denial of Service | Script de rollback aplicado por engano como se fosse migracao | mitigate | `supabase/rollback/` e `supabase/verify/` ficam fora de `migrations/`; a task proibe aplica-los por `db push`; conferencia de `migration list` antes do push exige exatamente uma pendente |
| T-08-10-03 | Denial of Service | Lock longo em `sih_metric_muni` durante uso didatico | mitigate | Volume real medido em 200.636 linhas em muni e 4.195 em uf, e o tempo foi medido no ensaio; o checkpoint da Task 1 oferece adiar para janela sem aula como opcao explicita |
| T-08-10-04 | Repudiation | Push aplicado sem registro de quem autorizou nem com que numeros | mitigate | Checkpoint de decisao com as medicoes do ensaio na mesa; escolha, data e hora registradas no SUMMARY |
| T-08-10-05 | Tampering | Banco e repositorio divergindo em silencio depois do push | mitigate | Comparacao de conjunto entre os ids de `sih_disease` e os ids das seeds geradas, com 331 elementos dos dois lados |
| T-08-10-06 | Information disclosure | Senha efemera do papel `cli_login_postgres` aparecendo em log, SUMMARY ou commit | mitigate | Nao usar `--dry-run` em log compartilhado; nunca colar saida de conexao |
| T-08-10-07 | Information disclosure | `INGEST_SECRET` em texto plano em `scrape_upload_sih.py:33` | accept | Registrado sem correcao por decisao explicita: a rotacao e escopo da Fase 9. O repositorio nao tem remote, entao nada foi publicado |
| T-08-10-08 | Elevation of privilege | Uso da service role key para conferencia pos-push | mitigate | Conferencia feita com a chave anon publica sob RLS, a mesma que o app usa; nenhuma credencial de escrita entra nesta plan |
| T-08-10-SC | Tampering | instalacao de pacotes npm/pip/cargo | accept | Nenhuma instalacao nesta fase (RESEARCH secao Package Legitimacy Audit) |
</threat_model>

<verification>
- Checkpoint de decisao respondido e registrado antes de qualquer comando de escrita
- `supabase db push` com codigo 0 e as duas migracoes aplicadas em `supabase migration list`
- `content-range` de producao terminando em `/331`, `/30313` e `/1099403`
- `supabase/verify/contagens.sql` sem `RAISE EXCEPTION` e sem agravo orfao fora do registro
- Conjunto de ids de `sih_disease` identico ao das seeds, 331 elementos
- `npm run gate` verde
- `docs/SUPABASE-CATALOG.md` sem os nomes errados de constraint e indice
</verification>

<success_criteria>
- Migracao aplicada em producao apos autorizacao humana explicita, com o ensaio aprovado
- `sih_disease` com 331 linhas, `sih_metric_uf` com 30.313 e `sih_metric_muni` com 1.099.403, conferidas no banco vivo
- Nenhum id com prefixo temporario sobrevivente; agravo do codigo 330 presente e sem metrica
- Conjunto de agravos do banco identico ao das seeds geradas da taxonomia canonica
- Caminho de reversao escrito, testado no ensaio e registrado, fora de `supabase/migrations/`
- `docs/SUPABASE-CATALOG.md` alinhado ao schema real, incluindo o `ON DELETE CASCADE` nao documentado e a contagem 331
- Handoff da Fase 9 registrado com os itens que ela herda
</success_criteria>

<output>
Create `.planning/phases/08-taxonomia-can-nica-integridade/08-10-SUMMARY.md` when done
</output>
