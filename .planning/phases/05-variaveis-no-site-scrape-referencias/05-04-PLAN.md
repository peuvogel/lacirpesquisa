---
phase: 05-variaveis-no-site-scrape-referencias
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - src/routes/variaveis/VariaveisPage.tsx
  - src/routes/variaveis/VariableFilters.tsx
  - src/routes/variaveis/VariableList.tsx
  - src/routes/variaveis/VariableDetailPanel.tsx
  - src/routes/variaveis/VariaveisPage.test.tsx
  - src/app/router.test.tsx
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
must_haves:
  truths:
    - "/variaveis shows search + filters + scrollable list + detail panel (no PlaceholderShell / Em breve)"
    - "Selected variable always renders full D-05 provenance block; never orphan"
    - "Detail panel shows suggested test hint from suggestTestForVariable / SuggestedTestCard"
    - "Visual language matches LACIR dark/teal list+detail composition — not card-heavy dashboard"
  artifacts:
    - path: "src/routes/variaveis/VariaveisPage.tsx"
      provides: "Catalog page composition"
    - path: "src/routes/variaveis/VariableDetailPanel.tsx"
      provides: "Provenance + hint + selection actions shell"
    - path: "src/routes/variaveis/VariaveisPage.test.tsx"
      provides: "CAT-01/02/03 UI coverage"
  key_links:
    - from: "VariaveisPage.tsx"
      to: "filterCatalog / loadCatalog"
      via: "load on mount + filter state"
      pattern: "loadCatalog|filterCatalog"
    - from: "VariableDetailPanel.tsx"
      to: "suggestTestForVariable"
      via: "resolveHint + SuggestedTestCard"
      pattern: "SuggestedTestCard|resolveHint"
---

<objective>
Wave 4 Variáveis UI: replace PlaceholderShell with search/filter/list/detail catalog page showing mandatory provenance and test hints (D-11, D-12, D-13).

Purpose: CAT-01 browse/search, CAT-02 provenance UI, CAT-03 hint display.
Output: Real /variaveis page + tests; router Em breve expectation updated. Load action wiring completes in 05-06 (buttons may be present but can be disabled until handoff plan if needed — prefer enabling Estatística load here using buildSessionDataset since 05-03 exists; Mapas navigate in 05-06 after catalogAnalysisData).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-CONTEXT.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-PATTERNS.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-03-SUMMARY.md
@src/routes/mapas/MapasPage.tsx
@src/routes/mapas/SuggestedTestCard.tsx
@src/components/PlaceholderShell.tsx
@src/app/router.test.tsx
</context>

<interfaces>
Page chrome (Mapas/PlaceholderShell outer):
div.mx-auto.max-w-[1520px].px-6.py-8 + h1.text-display "Variáveis"

Provenance labels (PT-BR): Fonte/sistema, Nome da fonte, Tabela/indicador, Período, URL oficial (anchor), Notas metodológicas, Tipo, Domínio, Carregável.

Do not touch MetaAnalisePage PlaceholderShell.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Variáveis page shell, filters, and list</name>
  <files>src/routes/variaveis/VariaveisPage.tsx, src/routes/variaveis/VariableFilters.tsx, src/routes/variaveis/VariableList.tsx, src/app/router.test.tsx</files>
  <read_first>
    - `05-CONTEXT.md` D-11, D-12
    - `05-PATTERNS.md` VariaveisPage + page chrome
    - `src/routes/mapas/MapasPage.tsx` shell density
    - `src/routes/mapas/VariableCheckboxList.tsx` list interaction cues
    - `src/app/router.test.tsx` Em breve case for /variaveis
  </read_first>
  <action>
    1. Replace PlaceholderShell in VariaveisPage with list+detail composition: load catalog via loadCatalog; local state for query + filters (sourceSystem, variableType, domain, loadable) + selectedId + multi-select Set for loadable ids.

    2. VariableFilters: search input + filter controls using existing shadcn primitives; PT-BR labels; no new card grid dashboard.

    3. VariableList: scrollable list of filterCatalog results; show label, variableType badge, loadable indicator, short sourceSystem; keyboard-selectable rows.

    4. Update router.test.tsx: /variaveis must NOT expect Em breve; assert heading Variáveis and that catalog UI mounts (e.g. search textbox or list). Keep /meta-analise Em breve test unchanged.

    Commit hint: feat(05-04): Variáveis catalog search and list UI
  </action>
  <verify>
    <automated>npm run test:run -- src/app/router.test.tsx</automated>
  </verify>
  <done>Placeholder retired; browse/search shell mounts at /variaveis</done>
</task>

<task type="auto">
  <name>Task 2: Detail panel with provenance + test hint</name>
  <files>src/routes/variaveis/VariableDetailPanel.tsx, src/routes/variaveis/VariaveisPage.test.tsx, src/routes/variaveis/VariaveisPage.tsx</files>
  <read_first>
    - `05-CONTEXT.md` D-05, D-06, D-13
    - `src/routes/mapas/SuggestedTestCard.tsx`
    - `src/routes/mapas/ReviewAnalysisDialog.tsx` (hint + actions layout)
  </read_first>
  <action>
    1. VariableDetailPanel always renders full provenance block for the selected CatalogEntry (all D-05 fields). Render methodologyNotes and labels as React text children only — never dangerouslySetInnerHTML (T-05 XSS). officialUrl as external link with rel noopener.

    2. Show resolveHint → SuggestedTestCard (reuse from Mapas). Empty selection: EmptyState PT-BR didactic copy.

    3. Multi-select checkboxes for loadable entries (prepare for 05-06 actions). Show disabled/ghost placeholders for "Carregar na Estatística" / "Usar no mapa" only if wiring deferred — preferred: enable Estatística button calling buildSessionDataset + setDataset + navigate('/estatistica' or '/') with state.activeTestId from hint when selection compatible (D-14); leave Mapas button wired in 05-06 if catalogAnalysisData not ready. If Estatística wired here, cover in VariaveisPage.test with SessionProvider + MemoryRouter.

    4. VariaveisPage.test.tsx: filter narrows list; selecting row shows provenance fields (sourceSystem, tableOrIndicator, period, officialUrl); hint testId present; no Em breve.

    Commit hint: feat(05-04): provenance detail panel and test hints
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/variaveis/VariaveisPage.test.tsx src/app/router.test.tsx</automated>
  </verify>
  <done>CAT-01/02/03 visible in UI with automated coverage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog JSON → React detail panel | methodologyNotes/URLs rendered to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering / XSS | VariableDetailPanel methodologyNotes | mitigate | Text nodes only; no dangerouslySetInnerHTML |
| T-05-09 | Spoofing | Provenance UI | mitigate | Refuse to render incomplete entries (dev assert); pipeline already fails orphans |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- npm run test:run -- src/routes/variaveis/ src/app/router.test.tsx
- Manual spot: /variaveis is list+detail teal shell without card dashboard
</verification>

<success_criteria>
- PlaceholderShell removed from Variáveis only
- Every visible variable shows D-05 provenance + CAT-03 hint
</success_criteria>

<output>
Create `.planning/phases/05-variaveis-no-site-scrape-referencias/05-04-SUMMARY.md` when done
</output>
