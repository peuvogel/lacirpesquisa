---
phase: 05-variaveis-no-site-scrape-referencias
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/catalog/paths.mjs
  - scripts/catalog/parseCsv.mjs
  - scripts/catalog/columnMap.json
  - scripts/catalog/reference-seed.json
  - scripts/catalog/build.mjs
  - scripts/catalog/README.md
  - trabalhos datasus/scripts/coleta_embolia_trombose_municipio_uf.py
  - trabalhos datasus/scripts/coleta_vascular_amputacao.py
  - public/data/catalog/manifest.json
  - public/data/catalog/variables.json
  - public/data/catalog/packs/sih.embolia_trombose_uf.json
  - public/data/catalog/packs/sih.amputacao_mmii_uf.json
autonomous: true
requirements: [CAT-05]
must_haves:
  truths:
    - "Offline build packages both embolia and amputação UF×ano CSVs into committed pack JSON (351 rows each)"
    - "variables.json includes one loadable entry per metric column from columnMap plus curated reference-only rows"
    - "No invented numeric cells; empty CSV cells become JSON null"
    - "coleta_*.py resolve BASE_DIR relative to repo trabalhos datasus/ (regenerate path)"
  artifacts:
    - path: "scripts/catalog/build.mjs"
      provides: "Normalize corpus → public/data/catalog/*"
    - path: "scripts/catalog/columnMap.json"
      provides: "packId → columnKey → CatalogEntry field seeds"
    - path: "public/data/catalog/manifest.json"
      provides: "version, generatedAt, packs[], catalogEntryCount"
    - path: "public/data/catalog/packs/sih.embolia_trombose_uf.json"
      provides: "Wide UF×ano pack for embolia/CNES/pop"
  key_links:
    - from: "scripts/catalog/build.mjs"
      to: "trabalhos datasus/outputs/coleta_*"
      via: "paths.mjs allowlisted resolve"
      pattern: "coleta_embolia_trombose_uf|coleta_vascular_amputacao"
    - from: "build.mjs"
      to: "public/data/catalog/"
      via: "atomic write"
      pattern: "manifest\\.json|variables\\.json"
---

<objective>
Wave 1 offline pipeline: scaffold `scripts/catalog/`, package existing serious coletas into `public/data/catalog/`, curate reference-seed rows, and port coleta absolute paths for reproducible regenerate (D-01, D-03, D-08, D-17).

Purpose: CAT-05 versioned assets must exist before app modules or UI can load real data.
Output: build scripts + committed manifest/variables/packs + regenerate path on coleta_*.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-CONTEXT.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-RESEARCH.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-PATTERNS.md
@scripts/fetch-geo-assets.mjs
@trabalhos datasus/outputs/coleta_embolia_trombose_uf/metadata.json
@trabalhos datasus/outputs/coleta_vascular_amputacao/metadata.json
</context>

<interfaces>
From 05-RESEARCH.md recommended pack shape (discretion: wide rows):
- PackFile: packId, grain "uf_ano", keys ["uf_codigo","uf","ano"], metricKeys[], rows[] with metric values as number|null
- Manifest: version, generatedAt, catalogEntryCount, packs[{packId, grain, sourceDir, rowCount, years, keys}]
- Loadable pack IDs: sih.embolia_trombose_uf, sih.amputacao_mmii_uf
- Shared CNES/pop catalog ids emitted once (primary packId = embolia); amputação pack may still carry columns for join
- Skip text-only columns as loadables: lista_morb_cid10, metodo_sih, populacao_fonte, cnes_competencia
- lacir_projetos: reference-only only (no pack) per D-01.3
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold catalog scripts, columnMap, reference-seed, and port coleta paths</name>
  <files>scripts/catalog/paths.mjs, scripts/catalog/parseCsv.mjs, scripts/catalog/columnMap.json, scripts/catalog/reference-seed.json, scripts/catalog/README.md, trabalhos datasus/scripts/coleta_embolia_trombose_municipio_uf.py, trabalhos datasus/scripts/coleta_vascular_amputacao.py</files>
  <read_first>
    - `05-CONTEXT.md` D-01, D-03, D-04, D-05, D-08, D-17
    - `05-RESEARCH.md` § Recommended Catalog JSON Schema, Loadable column → entry map, Reference curation
    - `scripts/fetch-geo-assets.mjs` (ROOT via import.meta.url pattern)
    - Both coleta `metadata.json` + CSV headers
    - Both `coleta_*.py` BASE_DIR lines
  </read_first>
  <action>
    1. Create scripts/catalog/paths.mjs: resolve repo root from import.meta.url; export CORPUS_DIR = join(root, "trabalhos datasus"); allowlist only outputs under coleta_embolia_trombose_uf and coleta_vascular_amputacao (and catalogos/ for seed curation reads). Never hardcode /Users/... Desktop paths (Pitfall 4).

    2. Create scripts/catalog/parseCsv.mjs: readCsvUtf8Sig — strip BOM (U+FEFF), split on commas for these delimiter-simple bases, empty cell → null, coerce ano to number and known metric columns to number|null. Assert first header exactly uf_codigo after BOM strip.

    3. Create scripts/catalog/columnMap.json covering all RESEARCH loadable columns for both packs with id, label, variableType, domain, unit, aliases where semantics match (mock.amputacoes → sih.amputacao_mmii.internacoes; mock.internacoes/mock.obitos → embolia series). Do NOT alias mock.taxa_mortalidade (infantil ≠ hospital) per RESEARCH anti-pattern.

    4. Create scripts/catalog/reference-seed.json: curated reference-only entries (loadable:false, no packId) with every D-05 field filled — prefer ≤15–25 rows from tabnet/egestor/ibge/ans/atlas/ipea/covid catalogs + GUIA highlights that already have official URLs. Omit incomplete rows (D-04 quality &gt; dump). Include e-Gestor APS coverage reference to support eventual mock.cobertura_aps retirement.

    5. Create scripts/catalog/README.md: document npm run catalog:build / catalog:validate; package-first path; regenerate via python3 trabalhos datasus/scripts/coleta_*.py after path port; note 2023 population gaps must stay null.

    6. Port BASE_DIR in both coleta_*.py to Path(__file__).resolve().parents[1] (the trabalhos datasus folder inside the repo). Keep scrape logic unchanged. Document that regenerate is optional when packaged CSVs validate (D-03).

    Do not add npm dependencies. Do not invent numeric cells.
  </action>
  <verify>
    <automated>node -e "import('./scripts/catalog/paths.mjs').then(m=>{const fs=require('fs');const p=m.corpusPath('outputs/coleta_embolia_trombose_uf/metadata.json');if(!fs.existsSync(p))process.exit(1);console.log('ok',p)})" && python3 -c "from pathlib import Path; import ast; src=Path('trabalhos datasus/scripts/coleta_embolia_trombose_municipio_uf.py').read_text(); assert '/Users/pedroalmeida/Desktop/trabalhos datasus' not in src; print('paths ported')"</automated>
  </verify>
  <done>Script scaffolding, columnMap, reference-seed, and repo-relative coleta paths exist</done>
</task>

<task type="auto">
  <name>Task 2: Implement catalog:build and commit generated assets</name>
  <files>scripts/catalog/build.mjs, public/data/catalog/manifest.json, public/data/catalog/variables.json, public/data/catalog/packs/sih.embolia_trombose_uf.json, public/data/catalog/packs/sih.amputacao_mmii_uf.json</files>
  <read_first>
    - `05-RESEARCH.md` Pattern 1 (atomic write), pack JSON example, merge order
    - `scripts/catalog/columnMap.json` and `reference-seed.json` from Task 1
    - CSV bases + metadata.json notes for methodologyNotes
  </read_first>
  <action>
    1. Implement scripts/catalog/build.mjs (Node ESM, zero new deps):
       - Read both base CSVs via parseCsv; build wide PackFile JSON (351 rows each).
       - Derive loadable CatalogEntry rows from columnMap + metadata sources/notes (period 2013–2025, officialUrl from metadata.sources, methodologyNotes non-empty including 2023 gap note where relevant).
       - Deduplicate shared CNES/pop entries (single catalog id; primary packId embolia).
       - Concat reference-seed.json; ensure unique ids.
       - Compute yearsAvailable / nullYears per column when useful (rates empty in 2023).
       - Write atomically to temp dir then rename into public/data/catalog/ (manifest.json, variables.json, packs/*.json). Commit the generated assets (D-03 package-first; CI offline).

    2. manifest.packs must list both packs with grain uf_ano, rowCount 351, years 2013–2025, keys uf_codigo/uf/ano.

    3. Do not call TABNET/IBGE from the build script on the happy path — only read local corpus files.

    Commit hint: feat(05-01): package embolia/amputação catalog packs from trabalhos datasus
  </action>
  <verify>
    <automated>node scripts/catalog/build.mjs && node -e "const m=require('./public/data/catalog/manifest.json');const v=require('./public/data/catalog/variables.json');const e=require('./public/data/catalog/packs/sih.embolia_trombose_uf.json');const a=require('./public/data/catalog/packs/sih.amputacao_mmii_uf.json');if(m.packs.length&lt;2||e.rows.length!==351||a.rows.length!==351)process.exit(1);if(!Array.isArray(v)||v.length&lt;10)process.exit(1);console.log('entries',v.length,'packs ok')"</automated>
  </verify>
  <done>Committed public/data/catalog assets generated from real coletas with no mock numerics</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Offline corpus → build script | Files under trabalhos datasus/outputs become public catalog assets |
| Build script path resolution | Must not read arbitrary filesystem paths outside allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | scripts/catalog/paths.mjs | mitigate | Resolve only under repo CORPUS_DIR allowlisted outputs; reject path traversal |
| T-05-02 | Spoofing | variables.json provenance | mitigate | methodologyNotes + officialUrl copied from coleta metadata; orphans blocked in 05-02 validate |
| T-05-03 | Information disclosure | Runtime scrape | accept | N/A this plan — build is offline only; enforced in later loadCatalog |
| T-05-SC | Tampering | npm/pip installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy: none) |
</threat_model>

<verification>
- node scripts/catalog/build.mjs succeeds
- Both packs rowCount 351; empty rate cells are null not 0 for 2023 densidades
- Absolute Desktop BASE_DIR removed from coleta_*.py
</verification>

<success_criteria>
- CAT-05 pipeline inputs/outputs exist and are reproducible from local corpus
- D-01 packs (embolia + amputação) packaged; lacir_projetos not forced into a pack
- D-08 directory layout under public/data/catalog/ committed
</success_criteria>

<output>
Create `.planning/phases/05-variaveis-no-site-scrape-referencias/05-01-SUMMARY.md` when done
</output>
