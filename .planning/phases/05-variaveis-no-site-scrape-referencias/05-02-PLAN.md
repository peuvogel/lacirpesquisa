---
phase: 05-variaveis-no-site-scrape-referencias
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/features/catalog/types.ts
  - scripts/catalog/validate.mjs
  - package.json
  - src/features/catalog/validateCatalogEntry.test.ts
autonomous: true
requirements: [CAT-02, CAT-05]
must_haves:
  truths:
    - "catalog:validate fails closed on missing D-05 fields, non-http(s) officialUrl, loadable without pack/column, missing pack column, or duplicate ids"
    - "npm scripts catalog:build and catalog:validate exist; validate runs before tests via pretest"
    - "Shared CatalogEntry TypeScript types mirror validate rules"
  artifacts:
    - path: "scripts/catalog/validate.mjs"
      provides: "Fail-closed D-05/D-06 gate"
    - path: "src/features/catalog/types.ts"
      provides: "CatalogEntry, Manifest, PackFile, VariableType"
      exports: ["CatalogEntry", "Manifest", "PackFile", "VariableType"]
    - path: "package.json"
      provides: "catalog:build, catalog:validate, pretest wire"
  key_links:
    - from: "validate.mjs"
      to: "public/data/catalog/variables.json"
      via: "schema + pack column existence"
      pattern: "officialUrl|loadable|packId"
    - from: "package.json pretest"
      to: "catalog:validate"
      via: "npm script chain"
      pattern: "catalog:validate"
---

<objective>
Wave 2 schema + validation gate: shared CatalogEntry types, fail-closed `catalog:validate`, and npm/pretest wiring (D-05, D-06, D-10).

Purpose: Orphan/incomplete provenance must never ship; CI and local test runs enforce the gate.
Output: types.ts, validate.mjs, package.json scripts, unit tests for validation helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-CONTEXT.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-RESEARCH.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-01-SUMMARY.md
@public/data/catalog/variables.json
@package.json
</context>

<interfaces>
D-05 mandatory CatalogEntry fields:
id, label, variableType (categorica|numerica|ordinal|taxa|contagem|texto), domain, sourceSystem, sourceName, tableOrIndicator, period, officialUrl (absolute http/https), methodologyNotes (non-empty), loadable; when loadable===true require packId + columnKey present in pack.metricKeys/rows.
Optional recommended: unit, grain, aliases, yearsAvailable, nullYears.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define CatalogEntry types and validate.mjs fail-closed gate</name>
  <files>src/features/catalog/types.ts, scripts/catalog/validate.mjs, src/features/catalog/validateCatalogEntry.test.ts</files>
  <read_first>
    - `05-CONTEXT.md` D-05, D-06, D-10
    - `05-RESEARCH.md` CatalogEntry interface + Pattern 1
    - Generated `public/data/catalog/*` from 05-01
  </read_first>
  <behavior>
    - Missing officialUrl or empty methodologyNotes → validation error
    - officialUrl not matching /^https?:\/\//i → error
    - loadable:true without packId/columnKey → error
    - loadable columnKey absent from pack → error
    - Duplicate catalog ids → error
    - Valid committed catalog → exit 0
    - Optional: shared CNES/pop values equal across packs when both present (RESEARCH A2) — warn or fail if diverge
  </behavior>
  <action>
    1. Create src/features/catalog/types.ts exporting VariableType, CatalogEntry, Manifest, PackFile, PackRow (index signature for metrics as number|null).

    2. Create scripts/catalog/validate.mjs: load manifest/variables/packs from public/data/catalog; enforce D-05/D-06; exit non-zero with PT-BR or clear English stderr listing first failures. No Zod — hand checks (zero new deps).

    3. Extract pure checkEntry(entry) / checkCatalog(bundle) logic testable from Node or mirror the rules in validateCatalogEntry.test.ts (vitest) against fixture objects — RED then GREEN. Do not weaken rules to pass.

    Commit hint: feat(05-02): fail-closed catalog provenance validation gate
  </action>
  <verify>
    <automated>node scripts/catalog/validate.mjs && npm run test:run -- src/features/catalog/validateCatalogEntry.test.ts</automated>
  </verify>
  <done>Committed catalog passes validate; intentional orphan fixtures fail in unit tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire npm catalog scripts and pretest gate</name>
  <files>package.json</files>
  <read_first>
    - `package.json` scripts block
    - `05-CONTEXT.md` D-10
  </read_first>
  <action>
    1. Add scripts: "catalog:build": "node scripts/catalog/build.mjs", "catalog:validate": "node scripts/catalog/validate.mjs".

    2. Add "pretest": "npm run catalog:validate" so vitest (npm test / test:run via pretest if npm runs lifecycle — ensure `npm run test:run` triggers validate: either pretest on test:run via "test:run": "npm run catalog:validate && vitest run" OR document that npm test uses pretest. Prefer explicit: "test:run": "npm run catalog:validate && vitest run" and keep "test": "vitest" with "pretest": "npm run catalog:validate" so interactive vitest also gates.

    3. Do not add new dependencies. Confirm catalog:validate finishes in under ~2s on committed assets.

    Commit hint: chore(05-02): wire catalog:validate into pretest
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); for (const k of ['catalog:build','catalog:validate']) { if(!p.scripts[k]) { console.error('missing',k); process.exit(1);} } console.log('scripts ok')" && npm run catalog:validate</automated>
  </verify>
  <done>npm catalog:* scripts present; validate gates test runs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/local pretest → catalog JSON | Unvalidated assets must not reach app consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | Orphan catalog entries | mitigate | validate.mjs fails closed on incomplete D-05 (D-06) |
| T-05-05 | Tampering | officialUrl field | mitigate | Require absolute http(s) URL regex |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- npm run catalog:validate exits 0 on committed assets
- Unit tests prove orphan entries are rejected
- package.json contains catalog:build / catalog:validate
</verification>

<success_criteria>
- CAT-02 enforceable at build/CI via validation (not only UI)
- CAT-05 validate path wired per D-10
</success_criteria>

<output>
Create `.planning/phases/05-variaveis-no-site-scrape-referencias/05-02-SUMMARY.md` when done
</output>
