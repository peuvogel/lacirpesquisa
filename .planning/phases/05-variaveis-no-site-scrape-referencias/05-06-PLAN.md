---
phase: 05-variaveis-no-site-scrape-referencias
plan: 06
type: execute
wave: 5
depends_on: [05-04, 05-05]
files_modified:
  - src/routes/variaveis/VariableDetailPanel.tsx
  - src/routes/variaveis/VariaveisPage.tsx
  - src/routes/variaveis/VariaveisPage.test.tsx
  - src/routes/mapas/MapasPage.tsx
  - src/routes/mapas/mapAnalysisState.ts
  - src/routes/mapas/MapasPage.test.tsx
autonomous: true
requirements: [CAT-04]
must_haves:
  truths:
    - "Carregar na Estatística builds SessionDataset via setDataset and navigates to Estatística with optional activeTestId from hint"
    - "Usar no mapa navigates to /mapas with catalog variable IDs applied to map analysis selection"
    - "Multi-select 1–N loadable vars enforces D-16 compatible grain; incompatible selection shows clear PT-BR message and does not load"
    - "Classroom flow needs no TABNET visit for these pack variables"
  artifacts:
    - path: "src/routes/variaveis/VariableDetailPanel.tsx"
      provides: "Load actions for Estatística and Mapas"
    - path: "src/routes/variaveis/VariaveisPage.test.tsx"
      provides: "Handoff integration coverage"
  key_links:
    - from: "VariableDetailPanel"
      to: "SessionProvider.setDataset"
      via: "buildSessionDataset"
      pattern: "setDataset|buildSessionDataset"
    - from: "VariableDetailPanel"
      to: "/mapas"
      via: "navigate state catalogVariableIds"
      pattern: "catalogVariableIds|/mapas"
    - from: "MapasPage"
      to: "location.state / setMapAnalysis"
      via: "apply catalog IDs to active group variables"
      pattern: "catalogVariableIds"
---

<objective>
Wave 5 load/handoff: finish CAT-04 actions from Variáveis into Estatística and Mapas, including multi-select join rules (D-14, D-15, D-16).

Purpose: Users load curated packs in-app without leaving for TABNET.
Output: Wired buttons + Mapas deep-link apply + integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-CONTEXT.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-PATTERNS.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-04-SUMMARY.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-05-SUMMARY.md
@src/routes/mapas/ReviewAnalysisDialog.tsx
@src/shared/session/SessionProvider.tsx
@src/features/catalog/buildSessionDataset.ts
</context>

<interfaces>
Estatística navigate target: prefer navigate('/estatistica') if that route exists; else '/' (EstatisticaPage). Include location.state: { activeTestId, recognizedColumns? } mirroring ReviewAnalysisDialog.

Mapas navigate state shape (discretion locked here):
{ catalogVariableIds: string[] }

MapasPage on mount/location change: if catalogVariableIds present, ensure mapAnalysis groups include those variableIds, set provenance to catalog (or hybrid if paste already present), prefer UF layer.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire Carregar na Estatística + multi-select gate</name>
  <files>src/routes/variaveis/VariableDetailPanel.tsx, src/routes/variaveis/VariaveisPage.tsx, src/routes/variaveis/VariaveisPage.test.tsx</files>
  <read_first>
    - `05-CONTEXT.md` D-14, D-16
    - `src/routes/mapas/ReviewAnalysisDialog.tsx` setDataset + navigate pattern
    - `src/features/catalog/buildSessionDataset.ts`
  </read_first>
  <action>
    1. Implement "Carregar na Estatística": assertCompatibleSelection → buildSessionDataset(selected, packs) → setDataset({... , confirmedAt: Date.now()}) → navigate to Estatística with state.activeTestId from resolveHint(primary selection). Disable button when selection empty or only reference-only vars.

    2. On D-16 failure, show inline Alert in PT-BR (e.g. variáveis não compartilham a mesma chave UF × ano) — do not navigate.

    3. Tests: multi-select two embolia loadables → setDataset called with headers including uf_codigo/ano and sourceLabel containing Catálogo; incompatible path if you can synthesize; reference-only alone cannot load.

    Commit hint: feat(05-06): load catalog selection into Estatística session
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/variaveis/VariaveisPage.test.tsx</automated>
  </verify>
  <done>Estatística handoff works with provenance-bearing sourceLabel</done>
</task>

<task type="auto">
  <name>Task 2: Wire Usar no mapa + Mapas apply catalogVariableIds</name>
  <files>src/routes/variaveis/VariableDetailPanel.tsx, src/routes/variaveis/VariaveisPage.tsx, src/routes/mapas/MapasPage.tsx, src/routes/mapas/mapAnalysisState.ts, src/routes/mapas/MapasPage.test.tsx, src/routes/variaveis/VariaveisPage.test.tsx</files>
  <read_first>
    - `05-CONTEXT.md` D-15
    - `src/routes/mapas/MapasPage.tsx` session sync / setMapAnalysis
    - `src/routes/mapas/mapAnalysisState.ts` TOGGLE_GROUP_VARIABLE / REPLACE_STATE
  </read_first>
  <action>
    1. "Usar no mapa" navigates to /mapas with state.catalogVariableIds from current multi-select (loadable only). Same D-16 preflight as Estatística.

    2. MapasPage consumes location.state once (then clear replace to avoid re-apply loops): merge variable IDs into active group (create default group if needed), set provenance catalog/hybrid, ensure checkbox list shows selection. Keep Phase 4 group/time/paste UX.

    3. Tests: navigate state applies ids; choropleth path still resolves via catalogAnalysisData for those ids.

    Commit hint: feat(05-06): Variáveis to Mapas catalog variable handoff
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/variaveis/VariaveisPage.test.tsx src/routes/mapas/MapasPage.test.tsx</automated>
  </verify>
  <done>Bidirectional classroom load path Variáveis↔Mapas/Estatística complete for packs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User multi-select → session dataset | Selected catalog ids become analysis rows |
| navigate state → Mapas | client-side state only; not auth-sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | location.state catalogVariableIds | mitigate | Resolve only known catalog ids via getCatalogVariableById; ignore unknown |
| T-05-13 | Denial of Service | Huge multi-select | mitigate | Cap selection count (e.g. ≤12 metrics) with PT message — discretionary bound documented in action |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- Variáveis + Mapas handoff tests green
- No runtime DATASUS calls in load path
</verification>

<success_criteria>
- CAT-04 end-to-end: browse → load Estatística / Usar no mapa with real packs
- D-14/D-15/D-16 implemented with clear PT errors
</success_criteria>

<output>
Create `.planning/phases/05-variaveis-no-site-scrape-referencias/05-06-SUMMARY.md` when done
</output>
