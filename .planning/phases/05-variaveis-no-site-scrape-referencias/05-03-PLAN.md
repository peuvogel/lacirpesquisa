---
phase: 05-variaveis-no-site-scrape-referencias
plan: 03
type: tdd
wave: 3
depends_on: [05-02]
files_modified:
  - src/features/catalog/loadCatalog.ts
  - src/features/catalog/loadCatalog.test.ts
  - src/features/catalog/suggestTestForVariable.ts
  - src/features/catalog/suggestTestForVariable.test.ts
  - src/features/catalog/buildSessionDataset.ts
  - src/features/catalog/buildSessionDataset.test.ts
  - src/features/catalog/filterCatalog.ts
  - src/features/catalog/filterCatalog.test.ts
autonomous: true
requirements: [CAT-01, CAT-03, CAT-04, CAT-05]
must_haves:
  truths:
    - "loadCatalog loads manifest/variables/packs from static /data/catalog assets with zero DATASUS/IBGE network calls"
    - "suggestTestForVariable maps variableType to available TEST_REGISTRY ids with PT-BR rationale"
    - "buildSessionDataset builds tidy UF×ano SessionDataset with provenance sourceLabel; blocks incompatible cross-pack joins"
    - "filterCatalog supports search + sourceSystem/variableType/domain/loadable filters"
  artifacts:
    - path: "src/features/catalog/loadCatalog.ts"
      provides: "Cached static catalog loader"
      exports: ["loadCatalog", "getCatalogPack"]
    - path: "src/features/catalog/suggestTestForVariable.ts"
      provides: "CAT-03 hint pure function"
      exports: ["suggestTestForVariable", "resolveHint"]
    - path: "src/features/catalog/buildSessionDataset.ts"
      provides: "CAT-04 SessionDataset builder"
      exports: ["buildSessionDataset", "assertCompatibleSelection"]
    - path: "src/features/catalog/filterCatalog.ts"
      provides: "CAT-01 search/filter pure function"
      exports: ["filterCatalog"]
  key_links:
    - from: "suggestTestForVariable.ts"
      to: "src/features/tests/registry.ts"
      via: "getTestById / isTestAvailable"
      pattern: "getTestById|isTestAvailable"
    - from: "buildSessionDataset.ts"
      to: "SessionDataset"
      via: "headers/rows/sourceLabel/confirmedAt"
      pattern: "sourceLabel|Catálogo LACIR"
    - from: "loadCatalog.ts"
      to: "/data/catalog/*.json"
      via: "fetch to same-origin static OR dynamic import"
      pattern: "data/catalog"
---

<objective>
Wave 3 catalog feature modules: pure-function load/filter/suggest/buildSession against committed assets (TDD) — foundation for Variáveis UI and Mapas/Estatística handoff.

Purpose: CAT-01 filters, CAT-03 hints, CAT-04 dataset assembly, CAT-05 offline load contract before UI wiring.
Output: src/features/catalog/* modules with green unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-CONTEXT.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-RESEARCH.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-PATTERNS.md
@.planning/phases/05-variaveis-no-site-scrape-referencias/05-02-SUMMARY.md
@src/features/catalog/types.ts
@src/features/tests/registry.ts
@src/shared/session/SessionProvider.tsx
@src/geo/loadGeoAsset.ts
@src/geo/loadGeoAsset.test.ts
</context>

<interfaces>
From SessionProvider.tsx:
SessionDataset { headers: string[]; rows: string[][]; sourceLabel: string; confirmedAt: number }

From registry.ts:
getTestById(id), isTestAvailable(id), TEST_REGISTRY

From 05-RESEARCH suggest heuristics:
contagem→poisson, taxa→prais-winsten, numerica→t-student, ordinal→kruskal-dunn, categorica→qui-quadrado, texto→demo; resolveHint falls back to demo if unavailable.

D-16: multi-select same pack OR cross-pack only when keys uf_codigo+ano match (both v1 packs do); otherwise throw/return PT error usable by UI.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: loadCatalog offline loader + filterCatalog</name>
  <files>src/features/catalog/loadCatalog.ts, src/features/catalog/loadCatalog.test.ts, src/features/catalog/filterCatalog.ts, src/features/catalog/filterCatalog.test.ts</files>
  <read_first>
    - `src/geo/loadGeoAsset.ts` + `.test.ts` (offline / no-fetch spy pattern)
    - `05-CONTEXT.md` D-09, D-11 filters
    - `public/data/catalog/manifest.json`
  </read_first>
  <behavior>
    - loadCatalog resolves variables + packs; subsequent calls reuse cache
    - fetch spy: no calls to tabnet.datasus.gov.br / sidra.ibge.gov.br / servicodados.ibge.gov.br (same-origin /data/catalog fetch OK if using public/; prefer documenting chosen strategy)
    - filterCatalog: case-insensitive search on label/id/sourceSystem; filters sourceSystem, variableType, domain, loadable
    - empty query + no filters returns all entries
  </behavior>
  <action>
    Implement loadCatalog and filterCatalog per behaviors. Prefer fetch('/data/catalog/...') in browser with vitest stub of fetch returning committed JSON, OR Vite-friendly import — either acceptable if D-09 holds. Export getPack(packId). No DATASUS hostnames in src/features/catalog.

    Commit hint: feat(05-03): offline loadCatalog + filterCatalog
  </action>
  <verify>
    <automated>npm run test:run -- src/features/catalog/loadCatalog.test.ts src/features/catalog/filterCatalog.test.ts</automated>
  </verify>
  <done>Offline catalog load + search/filter pure functions green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: suggestTestForVariable (CAT-03)</name>
  <files>src/features/catalog/suggestTestForVariable.ts, src/features/catalog/suggestTestForVariable.test.ts</files>
  <read_first>
    - `05-RESEARCH.md` suggestTestForVariable code example
    - `src/routes/mapas/suggestResearchForSelection.ts` + test (registry gating)
    - `src/features/tests/registry.ts`
  </read_first>
  <behavior>
    - Each variableType maps to expected testId per RESEARCH table
    - Every returned testId resolves via getTestById; non-demo ids pass isTestAvailable or resolveHint remaps to demo
    - Rationale is non-empty PT-BR string mentioning the variable label
  </behavior>
  <action>
    Implement suggestTestForVariable + resolveHint exactly per D-13 / RESEARCH heuristics. Pair mode deferred — single-variable only. Do not invent parallel test title maps; use registry.

    Commit hint: feat(05-03): suggestTestForVariable type heuristics
  </action>
  <verify>
    <automated>npm run test:run -- src/features/catalog/suggestTestForVariable.test.ts</automated>
  </verify>
  <done>CAT-03 pure hint function covered for all VariableType values</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: buildSessionDataset multi-select handoff (CAT-04)</name>
  <files>src/features/catalog/buildSessionDataset.ts, src/features/catalog/buildSessionDataset.test.ts</files>
  <read_first>
    - `05-CONTEXT.md` D-14, D-16, D-17
    - `05-RESEARCH.md` buildSessionDataset example
    - `src/routes/mapas/assembleHandoffTable.ts` (row guard patterns)
  </read_first>
  <behavior>
    - Reject non-loadable selection with clear Error
    - Same-pack multi-select: headers uf_codigo, uf, ano, then selected labels; null→"n/d" string cells
    - Cross-pack allowed for embolia+amputação (shared keys); inventing incompatible grain returns PT error
    - sourceLabel includes Catálogo LACIR + sourceSystem/tableOrIndicator cite + period
    - confirmedAt is number (caller may pass Date.now() or function sets it)
  </behavior>
  <action>
    Implement buildSessionDataset(selected, packsById) joining on uf_codigo+ano when multiple packs. Prefer left-join on first pack rows; fill missing metrics as n/d. Never invent numeric values. Export assertCompatibleSelection for UI preflight.

    Commit hint: feat(05-03): buildSessionDataset tidy UF×ano load
  </action>
  <verify>
    <automated>npm run test:run -- src/features/catalog/buildSessionDataset.test.ts</automated>
  </verify>
  <done>SessionDataset builder enforces D-14/D-16 with real pack fixtures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → static catalog JSON | Client trusts committed assets; must not call external DATASUS |
| UI selection → buildSessionDataset | User-chosen variable ids → session rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information disclosure | loadCatalog | mitigate | Unit spy asserts no external DATASUS/IBGE hosts; only static catalog |
| T-05-07 | Tampering | buildSessionDataset join | mitigate | Key-match gate (D-16); reject non-loadable |
| T-05-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- npm run test:run -- src/features/catalog/ green
- grep -R "tabnet.datasus.gov.br" src/features/catalog/ returns empty
</verification>

<success_criteria>
- Pure modules ready for Variáveis UI (05-04) and Mapas swap (05-05)
- TDD RED→GREEN commits for each module
</success_criteria>

<output>
Create `.planning/phases/05-variaveis-no-site-scrape-referencias/05-03-SUMMARY.md` when done
</output>
