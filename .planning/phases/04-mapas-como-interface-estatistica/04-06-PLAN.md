---
phase: 04-mapas-como-interface-estatistica
plan: 06
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - src/routes/mapas/GroupConfigPanel.tsx
  - src/routes/mapas/TemporalidadeControl.tsx
  - src/routes/mapas/VariableCheckboxList.tsx
  - src/routes/mapas/mockAnalysisData.ts
  - src/routes/mapas/MapPrimaryActionBar.tsx
  - src/routes/mapas/MapasPage.tsx
  - src/routes/mapas/VariablePanel.tsx
  - src/routes/mapas/mapAnalysisState.test.ts
  - src/routes/mapas/GroupConfigPanel.test.tsx
  - src/components/ui/sheet.tsx
autonomous: true
requirements: [MAP-06]
must_haves:
  truths:
    - "Opening/creating a group opens panel scoped to that group with time then variables (D-09/D-11)"
    - "User chooses ano único, intervalo de anos, or comparar dois períodos (D-10)"
    - "Variables show provenance badge Exemplo didático or Dados colados por você (D-15)"
    - "Revisar e analisar enables when all groups complete (canReview)"
  artifacts:
    - path: "src/routes/mapas/GroupConfigPanel.tsx"
      provides: "Per-group time + variable config (R4 Mode B)"
    - path: "src/routes/mapas/TemporalidadeControl.tsx"
      provides: "Point/range/compare time UI"
    - path: "src/routes/mapas/MapPrimaryActionBar.tsx"
      provides: "R5 Revisar e analisar CTA"
  key_links:
    - from: "GroupConfigPanel.tsx"
      to: "computeVariableIntersection.ts"
      via: "group territory siglas"
      pattern: "computeVariableIntersection"
    - from: "TemporalidadeControl.tsx"
      to: "mapAnalysisState.ts"
      via: "SET_GROUP_TIME"
      pattern: "SET_GROUP_TIME"
    - from: "MapPrimaryActionBar.tsx"
      to: "derived.canReview"
      via: "disabled prop"
      pattern: "canReview"
---

<objective>
Deliver MAP-06: per-group temporality (point/range/compare) and multi-variable selection with hybrid mock data — completes analysis shape before handoff (RESEARCH Wave 5, D-09–D-15).

Purpose: Groups from Wave 3 need time + variables for MAP-09 handoff and summary strip completion.
Output: GroupConfigPanel, TemporalidadeControl, VariableCheckboxList, MapPrimaryActionBar, tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mapas-como-interface-estatistica/04-UI-SPEC.md
@.planning/phases/04-mapas-como-interface-estatistica/04-PATTERNS.md
@src/routes/mapas/VariablePanel.tsx
@src/routes/mapas/computeVariableIntersection.ts
@src/routes/mapas/mockAnalysisData.ts
</context>

<interfaces>
From `computeVariableIntersection.ts`:
```typescript
export function computeVariableIntersection(
  selectedUFs: string[],
  variablesByUF: Record<string, string[]>,
): VariableAvailability;
```

From UI-SPEC Temporalidade:
- **Ano único** | **Intervalo de anos** | **Comparar dois períodos** (Período A / Período B)

From mapAnalysisState:
```typescript
SET_GROUP_TIME { groupId, time: GroupTimeConfig }
TOGGLE_GROUP_VARIABLE { groupId, variableId }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TemporalidadeControl + time reducer tests</name>
  <files>src/routes/mapas/TemporalidadeControl.tsx, src/routes/mapas/mapAnalysisState.ts, src/routes/mapas/mapAnalysisState.test.ts</files>
  <behavior>
    - Point mode: stores point year string
    - Range mode: start/end validated (end &gt;= start)
    - Compare mode: periodA/periodB fields
    - Invalid range shows inline warning per UI-SPEC
  </behavior>
  <read_first>
    - `04-CONTEXT.md` D-10 compare mode
    - `04-UI-SPEC.md` Temporalidade copy + states
    - `QualTesteModal.tsx` segmented choices (partial analog)
  </read_first>
  <action>
    1. Create TemporalidadeControl: shadcn Tabs or segmented control with three modes per UI-SPEC labels.

    2. Year inputs: Select or number input with min/max plausible (2000–2025) for capacitação; validate range ordering.

    3. Wire SET_GROUP_TIME on change; update SelectionSummaryStrip tempo segment via derived helper from Wave 3.

    4. Expand mapAnalysisState.test.ts for point/range/compare serialization (MAP-06 validation map).

    5. Empty state copy **Quando analisar?** when group has no time yet.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/mapAnalysisState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All three time modes persist in reducer state
    - [ ] Invalid range surfaces warning state without blocking other modes
    - [ ] Copy matches UI-SPEC PT labels
  </acceptance_criteria>
  <done>Temporalidade control + state tests (MAP-06 core)</done>
</task>

<task type="auto">
  <name>Task 2: GroupConfigPanel + VariableCheckboxList with provenance</name>
  <files>src/routes/mapas/GroupConfigPanel.tsx, src/routes/mapas/VariableCheckboxList.tsx, src/routes/mapas/mockAnalysisData.ts, src/routes/mapas/GroupConfigPanel.test.tsx</files>
  <read_first>
    - `VariablePanel.tsx` (intersection + partial rows — reuse pattern)
    - `04-UI-SPEC.md` Key Screens 4–5, provenance badges
    - `04-CONTEXT.md` D-11, D-12, D-13, D-15
  </read_first>
  <action>
    1. Create VariableCheckboxList: extract checkbox list from VariablePanel; add Badge **Exemplo didático** per mock variable (D-15); paste-sourced variables show **Dados colados por você** when provenance hybrid.

    2. Scope computeVariableIntersection to active group's territory siglas (UF-level for now; sub-UF when territories include municipio).

    3. Create GroupConfigPanel: TemporalidadeControl on top; VariableCheckboxList below; empty **O que comparar?** when time set but no variables.

    4. Expand mockAnalysisData with time-series keyed columns for compare mode (mock years 2018–2022 per group) — stable IDs for Phase 5 (D-14).

    5. TOGGLE_GROUP_VARIABLE on checkbox change; partial variables show destructive **Não existe em {UF}** note.

    6. GroupConfigPanel.test.tsx: render with mock group; toggle variable updates state; provenance badge visible.

    7. Opening/creating group sets activeGroupId and switches MapasPage contextPanelMode to 'group' (D-09).
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/GroupConfigPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Variable list scoped to group territories only
    - [ ] Provenance badge on every variable row
    - [ ] Partial variable destructive note renders
    - [ ] Capacitação copy — no untranslated stats jargon
  </acceptance_criteria>
  <done>Per-group variable picker with hybrid mock data</done>
</task>

<task type="auto">
  <name>Task 3: MapPrimaryActionBar + MapasPage R4/R5 wiring</name>
  <files>src/routes/mapas/MapPrimaryActionBar.tsx, src/routes/mapas/MapasPage.tsx, src/routes/mapas/VariablePanel.tsx</files>
  <read_first>
    - `04-UI-SPEC.md` R4/R5, Primary CTA states
    - `MapasPage.tsx` from Wave 3
  </read_first>
  <action>
    1. Create MapPrimaryActionBar: primary **Revisar e analisar** (disabled when !canReview with tooltip/Alert **Complete período e variáveis...**); secondary **Colar territórios**, **Limpar mapa** with confirm dialog copy from UI-SPEC.

    2. MapasPage R4: show GroupConfigPanel when activeGroupId; Mode A explore when none; Mode C paste from Wave 2. Deprecate direct VariablePanel usage — thin re-export or remove from page.

    3. Tablet Sheet fallback: when viewport &lt;1024px, render GroupConfigPanel in Sheet side="right" triggered by active GroupChip (UI-SPEC responsive) — use existing sheet.tsx.

    4. Choropleth variable: default to first selected variable in active group for map coloring.

    5. Wire Revisar button to open ReviewAnalysisDialog stub (open state only) — full dialog in Wave 6; or no-op with test id for Wave 6 hook.

    6. Update canReview logic if needed: all groups must have time + ≥1 variable.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/ && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Revisar e analisar disabled until group config complete
    - [ ] Group panel opens on group create/select
    - [ ] Summary strip shows tempo and agravos segments when set
    - [ ] Limpar mapa confirm uses UI-SPEC copy
  </acceptance_criteria>
  <done>MAP-06 temporality + variables in single-screen flow; R5 CTA gated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock time-series data | Static fixtures; paste hybrid optional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | Year input values | mitigate | Clamp years to 1990–2030; reject non-numeric |
| T-04-06-02 | Spoofing | Provenance badges | accept | UI-only labels; Phase 5 owns catalog truth |
</threat_model>

<verification>
- npm run test:run -- src/routes/mapas/mapAnalysisState.test.ts src/routes/mapas/GroupConfigPanel.test.tsx green
</verification>

<success_criteria>
- MAP-06 time point/range/compare selectable per group
- Multi-variable selection with provenance
- canReview gates Revisar e analisar
</success_criteria>

<output>
Create `.planning/phases/04-mapas-como-interface-estatistica/04-06-SUMMARY.md` when done
</output>
