---
phase: 04-mapas-como-interface-estatistica
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - src/routes/mapas/mockAnalysisData.ts
  - src/routes/mapas/ChoroplethLegend.tsx
  - src/routes/mapas/MapGeoPath.tsx
  - src/routes/mapas/BrazilMapCanvas.tsx
  - src/routes/mapas/MapasPage.tsx
  - src/routes/mapas/ChoroplethLegend.test.tsx
  - src/routes/mapas/BrazilMockMap.test.tsx
  - src/index.css
autonomous: true
requirements: [MAP-01]
must_haves:
  truths:
    - "User sees UF choropleth colored by mock metric with sequential teal scale"
    - "Legend shows labeled breaks (Baixo … Alto) for the active variable"
    - "UF selection glow and click still work on choropleth-filled paths"
  artifacts:
    - path: "src/routes/mapas/ChoroplethLegend.tsx"
      provides: "Sequential legend with PT labels"
    - path: "src/routes/mapas/BrazilMapCanvas.tsx"
      provides: "UF map with choropleth layer"
    - path: "src/routes/mapas/mockAnalysisData.ts"
      provides: "Didactic UF metrics keyed by ibgeCode/sigla"
  key_links:
    - from: "BrazilMapCanvas.tsx"
      to: "src/geo/choroplethScale.ts"
      via: "createChoroplethScale(mockValues)"
      pattern: "createChoroplethScale"
    - from: "MapGeoPath.tsx"
      to: "BRAZIL_UF_PATHS"
      via: "fill from scale(value)"
      pattern: "fill.*choropleth"
---

<objective>
Deliver MAP-01: Brazil UF choropleth with legend driven by bundled mock metrics — first visible vertical slice on existing inline SVG map.

Purpose: Prove d3-scale integration and mock data join keys before drill-down or groups (RESEARCH Wave 1, D-13 mock path).
Output: BrazilMapCanvas, ChoroplethLegend, mockAnalysisData, MapGeoPath extraction, tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mapas-como-interface-estatistica/04-UI-SPEC.md
@.planning/phases/04-mapas-como-interface-estatistica/04-PATTERNS.md
@src/routes/mapas/BrazilMockMap.tsx
@src/routes/mapas/brazilUfPaths.ts
@src/routes/mapas/MapasPage.tsx
@src/geo/choroplethScale.ts
@src/geo/types.ts
</context>

<interfaces>
From `BrazilMockMap.tsx` (preserve a11y contract):
```typescript
export interface BrazilMockMapProps {
  hoveredUF: string | null;
  selectedUFs: string[];
  onHoverUF: (uf: string | null) => void;
  onToggleUF: (uf: string) => void;
}
```

From `choroplethScale.ts` (Wave 0):
```typescript
export function createChoroplethScale(values: number[]): (value: number) => string;
export function legendBreaks(scale, count?: number): { label: string; color: string }[];
```

New `mockAnalysisData.ts`:
```typescript
export interface MockVariable { id: string; label: string; provenance: 'mock'; unit?: string; }
export function getMockMetricByUf(variableId: string): Record<string, number>; // key: sigla
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mock metrics + choropleth scale tests</name>
  <files>src/routes/mapas/mockAnalysisData.ts, src/routes/mapas/ChoroplethLegend.test.tsx, src/geo/choroplethScale.test.ts</files>
  <behavior>
    - getMockMetricByUf('internacoes') returns 27 UF values with SP &gt; 0
    - legendBreaks returns 5 steps with PT labels Baixo/Alto
    - ChoroplethLegend renders title "Intensidade no mapa" and tick labels
  </behavior>
  <read_first>
    - `mockVariablesByUF.ts` (extend variable IDs — ship 4–6 mocks: internações, óbitos, taxa, amputações per RESEARCH discretion)
    - `04-UI-SPEC.md` Copywriting + Color choropleth scale
  </read_first>
  <action>
    1. Create mockAnalysisData.ts: 4–6 didactic variables with provenance 'mock' / label "Exemplo didático" (D-15). Values keyed by UF sigla AND ibgeCode crosswalk via ufCodes.ts. Design IDs for Phase 5 swap (D-14): stable string IDs like mock.internacoes.

    2. Write ChoroplethLegend.test.tsx: renders title, ≥3 legend ticks, uses teal scale colors from UI-SPEC.

    3. Implement ChoroplethLegend.tsx: card on elevated surface; title per UI-SPEC; map legendBreaks to labeled rows; variable name subtitle when activeVariableId passed.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/ChoroplethLegend.test.tsx src/geo/choroplethScale.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] mockAnalysisData exports ≥4 variables with all 27 UFs covered
    - [ ] ChoroplethLegend tests pass with PT copy from UI-SPEC
    - [ ] No purple/rainbow colors in legend output
  </acceptance_criteria>
  <done>Mock data + legend component tested</done>
</task>

<task type="auto">
  <name>Task 2: Extract MapGeoPath and BrazilMapCanvas with choropleth fill</name>
  <files>src/routes/mapas/MapGeoPath.tsx, src/routes/mapas/BrazilMapCanvas.tsx, src/routes/mapas/BrazilMockMap.test.tsx, src/index.css</files>
  <read_first>
    - `BrazilMockMap.tsx` (full file — extract path loop)
    - `04-UI-SPEC.md` § MapGeoPath states, `.lacir-map-glow` motion
    - `04-PATTERNS.md` § BrazilMapCanvas
  </read_first>
  <action>
    1. Create MapGeoPath.tsx: props { d, sigla, name, isHovered, isSelected, fill, glowClass?, groupBadge?, onHover, onToggle }. Preserve role="button", tabIndex={0}, aria-pressed, vectorEffect non-scaling-stroke, focus-visible teal outline.

    2. Create BrazilMapCanvas.tsx: wraps UF view using BRAZIL_UF_PATHS; accepts choroplethValues: Record&lt;string, number&gt;, activeVariableId, selection props. Join mock values by sigla. Apply createChoroplethScale for fills; selected paths keep glow + 2px stroke (D-04).

    3. Add `.lacir-map-glow` to index.css per UI-SPEC (teal drop-shadow; reduced-motion static).

    4. Extend BrazilMockMap.test.tsx → test BrazilMapCanvas: 27 paths, choropleth fill differs when values differ, aria labels preserved, Enter toggles selection.

    5. Do NOT delete BrazilMockMap.tsx yet — re-export from BrazilMapCanvas or deprecate with comment for Wave 3 refactor.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/BrazilMockMap.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Two UFs with different mock values render different fill colors
    - [ ] Selected UF retains teal stroke + glow class
    - [ ] SVG sanitization: no dangerouslySetInnerHTML
  </acceptance_criteria>
  <done>Choropleth-rendered UF map with a11y parity to Phase 1 mock</done>
</task>

<task type="auto">
  <name>Task 3: Wire choropleth into MapasPage with default mock variable</name>
  <files>src/routes/mapas/MapasPage.tsx</files>
  <read_first>
    - `MapasPage.tsx` (current flat selection state)
    - `04-UI-SPEC.md` R3 MapCanvas region
  </read_first>
  <action>
    1. Replace BrazilMockMap import with BrazilMapCanvas in MapasPage.

    2. Add state activeChoroplethVariableId defaulting to first mock variable (e.g. mock.internacoes). Until group panel exists (Wave 5), show choropleth for all UFs using mockAnalysisData.

    3. Render ChoroplethLegend below map when activeChoroplethVariableId set; hide with hint copy when null per UI-SPEC empty state.

    4. Preserve existing hover/select/clear behavior and session sync (mapSelection until Wave 3 upgrades to mapAnalysis).

    5. Map area keeps ~60% width layout shell from MapasPage.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/ && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] MapasPage renders BrazilMapCanvas + ChoroplethLegend
    - [ ] All mapas tests pass
    - [ ] User-visible: UF map shows color variation + legend (manual spot-check optional)
  </acceptance_criteria>
  <done>MAP-01 UF choropleth + legend live on Mapas route</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock data → SVG fill | Static mock values only; no user input in Wave 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | SVG paths | mitigate | Committed brazilUfPaths only; no innerHTML |
| T-04-02-02 | Spoofing | Mock provenance | accept | Static "Exemplo didático" label per D-15 |
</threat_model>

<verification>
- npm run test:run -- src/routes/mapas/ChoroplethLegend.test.tsx src/routes/mapas/BrazilMockMap.test.tsx green
- grep -v '^#' src/routes/mapas/BrazilMapCanvas.tsx | grep -c createChoroplethScale | grep -qv '^0$'
</verification>

<success_criteria>
- MAP-01: choropleth by UF with legend on Mapas page
- Existing map a11y tests still pass
</success_criteria>

<output>
Create `.planning/phases/04-mapas-como-interface-estatistica/04-02-SUMMARY.md` when done
</output>
