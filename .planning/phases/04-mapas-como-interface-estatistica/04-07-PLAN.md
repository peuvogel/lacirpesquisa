---
phase: 04-mapas-como-interface-estatistica
plan: 07
type: execute
wave: 6
depends_on: [04-04, 04-06]
files_modified:
  - src/routes/mapas/ReviewAnalysisDialog.tsx
  - src/routes/mapas/assembleHandoffTable.ts
  - src/routes/mapas/suggestResearchForSelection.ts
  - src/routes/mapas/SuggestedTestCard.tsx
  - src/routes/mapas/TestPickerSelect.tsx
  - src/routes/mapas/IniciarPesquisaModal.tsx
  - src/routes/mapas/MapasPage.tsx
  - src/routes/mapas/MapPrimaryActionBar.tsx
  - src/routes/mapas/assembleHandoffTable.test.ts
  - src/routes/mapas/ReviewAnalysisDialog.test.tsx
  - src/routes/mapas/suggestResearchForSelection.test.ts
autonomous: true
requirements: [MAP-09]
must_haves:
  truths:
    - "Revisar e analisar opens review dialog with selection recap in plain PT (D-20)"
    - "System suggests statistical test from selection shape; user can change before handoff (D-20)"
    - "Confirm navigates to Estatística with assembled tabular dataset + activeTestId + recognizedColumns (D-21)"
    - "IniciarPesquisaModal stub replaced as primary path; collection links secondary (D-22)"
  artifacts:
    - path: "src/routes/mapas/ReviewAnalysisDialog.tsx"
      provides: "Review + handoff dialog (R6)"
    - path: "src/routes/mapas/assembleHandoffTable.ts"
      provides: "Groups → wide tabular rows"
    - path: "src/routes/mapas/suggestResearchForSelection.ts"
      provides: "Extended heuristics for groups/time"
  key_links:
    - from: "ReviewAnalysisDialog.tsx"
      to: "SessionProvider.setDataset"
      via: "assembleHandoffTable → setDataset"
      pattern: "setDataset"
    - from: "ReviewAnalysisDialog.tsx"
      to: "EstatisticaPage"
      via: "navigate('/', { state: { activeTestId, recognizedColumns } })"
      pattern: "recognizedColumns"
    - from: "assembleHandoffTable.ts"
      to: "deriveRecognizedColumnsFromTabular"
      via: "MAPAS_TABULAR_OPTIONS"
      pattern: "deriveRecognizedColumns"
---

<objective>
Deliver MAP-09: review step with suggested test (editable), assembled handoff table, navigation to Estatística — replaces Phase 1 IniciarPesquisaModal stub (D-20–D-22, RESEARCH Wave 6).

Purpose: Completes Mapas → Estatística integration with real dataset rows, not UF list stubs.
Output: ReviewAnalysisDialog, assembleHandoffTable, extended suggestResearchForSelection, migration from IniciarPesquisaModal, integration tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mapas-como-interface-estatistica/04-UI-SPEC.md
@.planning/phases/04-mapas-como-interface-estatistica/04-PATTERNS.md
@src/routes/mapas/IniciarPesquisaModal.tsx
@src/routes/mapas/suggestResearchForSelection.ts
@src/routes/estatistica/EstatisticaPage.tsx
@src/shared/data-input/deriveRecognizedColumnsFromTabular.ts
</context>

<interfaces>
From `IniciarPesquisaModal.tsx` (reuse, do not duplicate):
```typescript
export function resolveHandoffTestId(suggestions: ResearchSuggestion[]): string;
export const MAPAS_TABULAR_OPTIONS = { ... };
```

From `EstatisticaPage.tsx`:
```typescript
// Consumes location.state: { activeTestId?, recognizedColumns? }
```

New `assembleHandoffTable.ts`:
```typescript
export function assembleHandoffTable(
  groups: MapAnalysisGroup[],
  data: MockAnalysisData,
): { headers: string[]; rows: string[][]; sourceLabel: string };
```

Extended `suggestResearchForSelection`:
```typescript
export function suggestResearchForSelection(input: {
  groups: MapAnalysisGroup[];
  provenance: 'mock' | 'paste' | 'hybrid';
}): ResearchSuggestion[];
// Heuristics: group count, time.compare → prais-winsten, multi-var → correlacao/anova, etc.
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assembleHandoffTable + extended suggestion heuristics</name>
  <files>src/routes/mapas/assembleHandoffTable.ts, src/routes/mapas/assembleHandoffTable.test.ts, src/routes/mapas/suggestResearchForSelection.ts, src/routes/mapas/suggestResearchForSelection.test.ts</files>
  <behavior>
    - assembleHandoffTable with 2 groups → headers include Território, Grupo, Período, ≥1 variable column
    - Rows count equals territories × variables (or documented wide shape)
    - suggestResearchForSelection: 2 groups + compare time → includes prais-winsten suggestion
    - 3+ groups + 1 variable → includes anova-tukey
    - Remove stale "Único teste disponível hoje" demo rationale
    - All suggested testIds pass isTestAvailable filter
  </behavior>
  <read_first>
    - `IniciarPesquisaModal.tsx` MAPAS_TABULAR_OPTIONS + handoff pattern
    - `suggestResearchForSelection.ts` current UF-count heuristics
    - `04-RESEARCH.md` Pitfall 5, Pitfall 6
    - `features/tests/registry` isTestAvailable
  </read_first>
  <action>
    1. Create assembleHandoffTable: wide format Território;Grupo;Período;{variable columns} per RESEARCH recommendation; pull values from mockAnalysisData time-series keyed by group time config.

    2. sourceLabel: `Mapas: ${groups.length} grupos · ${timeSummary}` pattern.

    3. Extend suggestResearchForSelection to accept MapAnalysisGroup[] + provenance; map group count, time.mode === 'compare', variable count to registry tests (t-student, anova-tukey, kruskal-dunn, correlacao, prais-winsten, poisson, etc.).

    4. Remove/update stale demo copy line ~97 in suggestResearchForSelection.ts.

    5. Tests: assembleHandoffTable.test.ts table-driven; suggestResearchForSelection.test.ts extend with group/time cases.

    6. Optional paste merge: if user pasted tabular data, prefer paste rows when provenance hybrid — document in function JSDoc.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/assembleHandoffTable.test.ts src/routes/mapas/suggestResearchForSelection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] assembleHandoffTable produces ≥2 columns and ≥1 row for single complete group fixture
    - [ ] Suggestions only include isTestAvailable testIds
    - [ ] Compare time mode suggests prais-winsten in tests
    - [ ] No stale Phase 1 demo rationale string in output
  </acceptance_criteria>
  <done>Handoff table assembly + suggestion engine extended for groups/time</done>
</task>

<task type="auto">
  <name>Task 2: ReviewAnalysisDialog, SuggestedTestCard, TestPickerSelect</name>
  <files>src/routes/mapas/ReviewAnalysisDialog.tsx, src/routes/mapas/SuggestedTestCard.tsx, src/routes/mapas/TestPickerSelect.tsx, src/routes/mapas/ReviewAnalysisDialog.test.tsx</files>
  <read_first>
    - `IniciarPesquisaModal.tsx` (migrate SuggestionRow, resolveHandoffTestId, dialog shell)
    - `04-UI-SPEC.md` Review dialog copy, R6, max-width 640px
    - `04-CONTEXT.md` D-20, D-22
  </read_first>
  <action>
    1. Create ReviewAnalysisDialog (shadcn Dialog max-w-lg ~640px): title **Revisar antes de analisar**; subtitle per UI-SPEC.

    2. **Sua seleção** recap: reuse SelectionSummaryStrip content or compact list from mapAnalysis state.

    3. **Teste sugerido** section: SuggestedTestCard with accent left border; shows primary suggestion rationale.

    4. **Usar outro teste**: TestPickerSelect — shadcn Select filtered by isTestAvailable from TEST_REGISTRY.

    5. Primary confirm **Ir para Estatística**: calls assembleHandoffTable → setDataset → deriveRecognizedColumnsFromTabular(headers, rows, MAPAS_TABULAR_OPTIONS) → navigate('/', { state: { activeTestId: selectedTestId, recognizedColumns } }).

    6. Secondary collapsed **Ver fontes oficiais** — reuse getCollectionLinks / MOCK_COLLECTION_LINKS (D-22 not primary path).

    7. Loading state on confirm button for a11y (~instant).

    8. ReviewAnalysisDialog.test.tsx: migrate patterns from IniciarPesquisaModal.test.tsx — assert setDataset headers/rows, navigate state with activeTestId + recognizedColumns.

    9. Paste-only fallback: if map incomplete but tabular paste loaded, allow handoff from paste (hybrid D-13) — optional secondary path inside dialog.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/ReviewAnalysisDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Dialog copy matches 04-UI-SPEC PT strings
    - [ ] Handoff sets dataset with assembled rows (not empty)
    - [ ] User override testId reflected in navigate state
    - [ ] Focus trap + restore focus to Revisar button on close (Radix default)
  </acceptance_criteria>
  <done>ReviewAnalysisDialog replaces stub modal as primary handoff UI</done>
</task>

<task type="auto">
  <name>Task 3: Wire MapasPage to ReviewAnalysisDialog; deprecate IniciarPesquisaModal primary path</name>
  <files>src/routes/mapas/MapasPage.tsx, src/routes/mapas/MapPrimaryActionBar.tsx, src/routes/mapas/IniciarPesquisaModal.tsx</files>
  <read_first>
    - `MapasPage.tsx` — remove IniciarPesquisaModal as primary if still wired
    - `IniciarPesquisaModal.test.tsx` — keep tests passing via re-export or migrate
  </read_first>
  <action>
    1. MapPrimaryActionBar **Revisar e analisar** opens ReviewAnalysisDialog when canReview.

    2. Remove **Iniciar pesquisa** CTA label everywhere — UI-SPEC forbids both labels (D-20).

    3. Deprecate IniciarPesquisaModal: add @deprecated JSDoc; re-export ReviewAnalysisDialog from same file OR keep for collection-links helper extraction only.

    4. Update IniciarPesquisaModal.test.tsx imports to ReviewAnalysisDialog OR skip with migration note — prefer migrating tests to ReviewAnalysisDialog.test.tsx (Task 2).

    5. Verify EstatisticaPage handoff effect receives recognizedColumns — add integration assertion in ReviewAnalysisDialog.test.tsx with MemoryRouter + SessionProvider + EstatisticaPage smoke if feasible.

    6. mapSelection legacy: stop writing flat mapSelection on handoff; clear or sync from mapAnalysis only.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/ReviewAnalysisDialog.test.tsx src/routes/mapas/suggestResearchForSelection.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] MapasPage uses ReviewAnalysisDialog not IniciarPesquisaModal for primary flow
    - [ ] No "Iniciar pesquisa" visible in MapasPage render tests
    - [ ] Handoff integration test passes with t-student or anova activeTestId
  </acceptance_criteria>
  <done>MAP-09 end-to-end handoff Mapas → Estatística complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assembled table → session | Client-only dataset in memory |
| Test picker | Must whitelist registry testIds only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Tampering | navigate state activeTestId | mitigate | resolveHandoffTestId + isTestAvailable guard; reject unknown testIds |
| T-04-07-02 | Spoofing | Collection links | mitigate | Static MOCK_COLLECTION_LINKS only; target=_blank rel=noopener |
| T-04-07-03 | Denial of Service | Large assembled table | mitigate | Cap rows at 10_000 in assembleHandoffTable |
</threat_model>

<verification>
- npm run test:run -- src/routes/mapas/ReviewAnalysisDialog.test.ts src/routes/mapas/assembleHandoffTable.test.ts green
- grep -v '^#' src/routes/mapas/MapasPage.tsx | grep -c ReviewAnalysisDialog | grep -qv '^0$'
</verification>

<success_criteria>
- MAP-09: multi-variable groups → review → Estatística with real table
- D-20–D-22 locked decisions satisfied
- Stale suggestion copy removed
</success_criteria>

<output>
Create `.planning/phases/04-mapas-como-interface-estatistica/04-07-SUMMARY.md` when done
</output>
