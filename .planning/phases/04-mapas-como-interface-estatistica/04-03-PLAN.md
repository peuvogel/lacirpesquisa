---
phase: 04-mapas-como-interface-estatistica
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/routes/mapas/TerritoryPastePanel.tsx
  - src/routes/mapas/MapasPage.tsx
  - src/geo/matchTerritoryLabels.ts
  - src/geo/matchTerritoryLabels.test.ts
autonomous: true
requirements: [MAP-02]
must_haves:
  truths:
    - "User pastes UF names or siglas line-by-line and sees them recognized on the map"
    - "Unrecognized lines appear in an unmatched report with capacitação-friendly guidance"
    - "Matched territories highlight/select on the Brazil UF map"
  artifacts:
    - path: "src/routes/mapas/TerritoryPastePanel.tsx"
      provides: "Paste textarea + match report (ContextPanel Mode C)"
    - path: "src/geo/matchTerritoryLabels.ts"
      provides: "UF label matching engine"
  key_links:
    - from: "TerritoryPastePanel.tsx"
      to: "matchTerritoryLabels.ts"
      via: "matchUfLabels(pasteText)"
      pattern: "matchUf"
    - from: "MapasPage.tsx"
      to: "TerritoryPastePanel"
      via: "onMatched → setSelectedUFs"
      pattern: "onMatched"
---

<objective>
Deliver MAP-02: paste territory labels as UF name or sigla with offline recognition and map sync — independent of group UX (RESEARCH Wave 2).

Purpose: Reuses normalizeHeaderToken + ufCodes; enables hybrid data path (D-13 paste) before municipality matching in Wave 4.
Output: TerritoryPastePanel, extended matchTerritoryLabels, MapasPage Mode C wiring, tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mapas-como-interface-estatistica/04-UI-SPEC.md
@.planning/phases/04-mapas-como-interface-estatistica/04-PATTERNS.md
@src/shared/data-input/parseTabular.ts
@src/routes/mapas/ufCodes.ts
@src/routes/estatistica/TabularInputPanel.tsx
</context>

<interfaces>
From Wave 0 `matchTerritoryLabels.ts`:
```typescript
export function matchUfLabel(line: string): { status: 'matched' | 'unmatched'; entry?: UfEntry; input: string };
export function matchUfPaste(text: string): { matched: UfEntry[]; unmatched: string[] };
```

From UI-SPEC copy:
- Heading: **Colar territórios**
- Matched section: **Reconhecidos ({n})**
- Unmatched: **Não reconhecidos ({n})**
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend UF paste matching tests and engine</name>
  <files>src/geo/matchTerritoryLabels.ts, src/geo/matchTerritoryLabels.test.ts</files>
  <behavior>
    - "BA" → matched Bahia
    - "bahia" / "Bahia" → matched (normalizeHeaderToken)
    - "São Paulo" / "sao paulo" → matched SP
    - "XYZ" → unmatched
    - Multi-line paste: matched + unmatched arrays correct length
  </behavior>
  <read_first>
    - `parseTabular.ts` normalizeHeaderToken
    - `ufCodes.ts` UF_LIST
    - `04-CONTEXT.md` D-18 (UF paste scope)
  </read_first>
  <action>
    1. Implement matchUfPaste(text): split lines, trim, skip empty; dedupe matched siglas; cap at 100 lines (DoS guard per RESEARCH security).

    2. Alias table: common variants (e.g. "distrito federal" → DF) using normalizeHeaderToken on UF_LIST names.

    3. Expand matchTerritoryLabels.test.ts with table-driven cases above.

    4. Do NOT implement municipality matching here — Wave 4 scope (MAP-04).
  </action>
  <verify>
    <automated>npm run test:run -- src/geo/matchTerritoryLabels.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All 27 UFs matchable by sigla and normalized full name
    - [ ] Unmatched lines preserved in report order
    - [ ] No runtime network calls
  </acceptance_criteria>
  <done>UF paste matching engine complete with tests</done>
</task>

<task type="auto">
  <name>Task 2: Create TerritoryPastePanel component</name>
  <files>src/routes/mapas/TerritoryPastePanel.tsx</files>
  <read_first>
    - `TabularInputPanel.tsx` (textarea + Alert error pattern)
    - `04-UI-SPEC.md` § Territory paste, Copywriting, Mode C
    - `VariablePanel.tsx` EmptyState pattern
  </read_first>
  <action>
    1. Create TerritoryPastePanel: textarea (mono font for preview lists per UI-SPEC), parse on blur or debounced input (300ms), show inline spinner while parsing.

    2. Match report: matched list with sigla chips (teal border); unmatched list in Alert with destructive-adjacent styling; aria-live="polite".

    3. Props: onMatched(siglas: string[]), onClear?, initialText?. Copy per UI-SPEC including helper "Cole nomes de estados, municípios ou siglas — um por linha." (municipality hint for Wave 4).

    4. Error expandable **Ver detalhes** for parse failures per UI-SPEC.

    5. Cap textarea rows display; use IBM Plex Mono for matched/unmatched tables.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/TerritoryPastePanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] MISSING — Wave 0 must create TerritoryPastePanel.test.tsx first if absent
    - [ ] Component renders Colar territórios heading
    - [ ] Pasting "BA\nPE" calls onMatched with ['BA','PE']
    - [ ] Unmatched lines listed under Não reconhecidos
  </acceptance_criteria>
  <done>TerritoryPastePanel with RTL tests</done>
</task>

<task type="auto">
  <name>Task 3: Wire paste panel into MapasPage ContextPanel Mode C</name>
  <files>src/routes/mapas/MapasPage.tsx, src/routes/mapas/TerritoryPastePanel.test.tsx</files>
  <read_first>
    - `MapasPage.tsx`
    - `04-UI-SPEC.md` R4 ContextPanel modes, R5 secondary **Colar dados**
  </read_first>
  <action>
    1. Add contextPanelMode: 'explore' | 'paste' | 'group' state to MapasPage.

    2. Secondary button **Colar territórios** toggles Mode C (TerritoryPastePanel). **Colar dados** reserved for tabular paste in Wave 6 — use territory-specific label now.

    3. onMatched: merge siglas into selectedUFs (or replace per UX choice — default merge + dedupe); update map selection glow; sync mapSelection session.

    4. Toggle back to explore mode after successful match optional — keep panel open showing report (D-03 progressive disclosure).

    5. Create TerritoryPastePanel.test.tsx if Task 2 verify flagged MISSING: render + paste interaction tests.

    6. Set mapAnalysis provenance to 'paste' | 'hybrid' when paste used (if mapAnalysis wired from Wave 0 — else defer flag to Wave 3).
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/TerritoryPastePanel.test.tsx src/routes/mapas/</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Paste "Bahia" selects BA on map state
    - [ ] Unmatched lines visible without blocking map use
    - [ ] Copy matches 04-UI-SPEC PT strings
  </acceptance_criteria>
  <done>MAP-02 paste UF recognition integrated on Mapas page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User paste → match engine | Untrusted text into offline lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Spoofing/Tampering | Pasted territory labels | mitigate | React text escaping in report lists; no innerHTML |
| T-04-03-02 | Denial of Service | Large paste | mitigate | 100-line cap; debounced parse |
</threat_model>

<verification>
- npm run test:run -- src/geo/matchTerritoryLabels.test.ts src/routes/mapas/TerritoryPastePanel.test.tsx green
</verification>

<success_criteria>
- MAP-02: UF name/sigla paste recognized with matched/unmatched report
- Map selection syncs with paste results
</success_criteria>

<output>
Create `.planning/phases/04-mapas-como-interface-estatistica/04-03-SUMMARY.md` when done
</output>
