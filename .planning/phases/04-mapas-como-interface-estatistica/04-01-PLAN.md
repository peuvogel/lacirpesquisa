---
phase: 04-mapas-como-interface-estatistica
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/fetch-geo-assets.mjs
  - src/geo/types.ts
  - src/geo/territoryCatalog.ts
  - src/geo/choroplethScale.ts
  - src/geo/projectGeoToSvg.ts
  - src/geo/loadGeoAsset.ts
  - src/geo/matchTerritoryLabels.ts
  - src/geo/nameTables/muni-BA.json
  - src/geo/topo/muni-29.json
  - src/geo/topo/br-meso.sample.json
  - src/routes/mapas/mapAnalysisState.ts
  - src/shared/session/SessionProvider.tsx
  - src/geo/matchTerritoryLabels.test.ts
  - src/geo/projectGeoToSvg.test.ts
  - src/geo/loadGeoAsset.test.ts
  - src/geo/territoryCatalog.test.ts
  - src/geo/choroplethScale.test.ts
  - src/routes/mapas/mapAnalysisState.test.ts
  - src/shared/session/SessionProvider.test.tsx
autonomous: false
requirements: [MAP-05, MAP-08]
user_setup:
  - service: npm
    why: "Install d3-geo, d3-scale, topojson-client, @dnd-kit/core (+ sortable/utilities) for Phase 4 geo stack and group DnD (04-RESEARCH.md Standard Stack)"
    env_vars: []
    dashboard_config:
      - task: "Human-verify d3-geo, d3-scale, topojson-client on npmjs.com"
        location: "https://www.npmjs.com/package/d3-geo — confirm github.com/d3/d3, 8+ yrs"
      - task: "Human-verify @dnd-kit/core on npmjs.com"
        location: "https://www.npmjs.com/package/@dnd-kit/core — confirm github.com/clauderic/dnd-kit"
must_haves:
  truths:
    - "Geo types, territory catalog, and matchTerritoryLabels resolve UF sigla/name and municipality names offline"
    - "MapAnalysisState reducer + SessionProvider.mapAnalysis persist groups×time×variables shape"
    - "Sample TopoJSON fixtures load via dynamic import with zero runtime fetch"
    - "Wave 0 unit tests pass for geo utilities and session extension"
  artifacts:
    - path: "scripts/fetch-geo-assets.mjs"
      provides: "Build-time IBGE + MS geo fetch (MAP-05)"
    - path: "src/geo/types.ts"
      provides: "TerritoryRef, GeoLevel, join keys"
      exports: ["TerritoryRef", "GeoLevel", "MapViewState"]
    - path: "src/routes/mapas/mapAnalysisState.ts"
      provides: "Groups×time×variables reducer"
      exports: ["useMapAnalysis", "MapAnalysisState", "MapAnalysisGroup"]
    - path: "src/geo/topo/muni-29.json"
      provides: "CI fixture TopoJSON for BA drill tests"
  key_links:
    - from: "loadGeoAsset.ts"
      to: "src/geo/topo/*.json"
      via: "dynamic import()"
      pattern: "import\\(.*topo"
    - from: "SessionProvider.tsx"
      to: "mapAnalysisState.ts"
      via: "setMapAnalysis"
      pattern: "mapAnalysis"
---

<objective>
Wave 0 infrastructure: geo fetch script, typed territory/session model, choropleth scale + projection utilities, territory matching engine, and CI fixtures. Blocks all Mapas UI waves.

Purpose: MAP-05 offline assets and shared types must exist before choropleth, paste, drill-down, or handoff UI (D-17, D-19, RESEARCH Wave 0).
Output: scripts/fetch-geo-assets.mjs, src/geo/* utilities, mapAnalysisState, SessionProvider extension, Wave 0 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-mapas-como-interface-estatistica/04-CONTEXT.md
@.planning/phases/04-mapas-como-interface-estatistica/04-RESEARCH.md
@.planning/phases/04-mapas-como-interface-estatistica/04-PATTERNS.md
@.planning/phases/04-mapas-como-interface-estatistica/04-VALIDATION.md
@src/routes/mapas/ufCodes.ts
@src/shared/data-input/parseTabular.ts
@src/shared/session/SessionProvider.tsx
</context>

<interfaces>
From `src/routes/mapas/ufCodes.ts`:
```typescript
export interface UfEntry { sigla: string; name: string; ibgeCode: string; }
export const UF_LIST: readonly UfEntry[];
export function getUfName(sigla: string): string;
```

From `src/shared/data-input/parseTabular.ts`:
```typescript
export function normalizeHeaderToken(value: unknown): string;
```

From `SessionProvider.tsx` (extend, do not break):
```typescript
export interface SessionState {
  dataset: SessionDataset | null;
  mapSelection: { ufs: string[]; variables: string[] } | null;
  setMapSelection(selection: ...): void;
}
```

New contracts (04-RESEARCH.md Pattern 2):
```typescript
export interface TerritoryRef {
  level: 'uf' | 'municipio' | 'meso' | 'health-macro';
  ibgeCode: string;
  sigla?: string;
  name: string;
}
export interface MapAnalysisGroup {
  id: string; name: string; territoryIds: TerritoryRef[];
  time: { mode: 'point' | 'range' | 'compare'; start?: string; end?: string; point?: string; periodA?: string; periodB?: string };
  variableIds: string[];
}
export interface MapAnalysisState {
  groups: MapAnalysisGroup[]; activeGroupId: string | null;
  mapView: { level: GeoLevel; parentCode?: string };
  provenance: 'mock' | 'paste' | 'hybrid';
}
```
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify geo/DnD package legitimacy and install</name>
  <files>package.json, package-lock.json</files>
  <action>Blocking human gate before npm install (04-RESEARCH.md Package Legitimacy Audit — all tagged [ASSUMED]). Packages: d3-geo@^3.1.1, d3-scale@^4.0.2, topojson-client@^3.1.0, @dnd-kit/core@^6.3.1, @dnd-kit/sortable@^10.0.0, @dnd-kit/utilities (peer). Dev types: @types/d3-geo, @types/d3-scale, @types/topojson-client. jstat already installed — do NOT reinstall. Wait for approval before npm install.</action>
  <what-built>Package legitimacy review for d3-geo, d3-scale, topojson-client, @dnd-kit/* before install</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/d3-geo — confirm github.com/d3/d3, established downloads
    2. Open https://www.npmjs.com/package/topojson-client — confirm github.com/topojson/topojson-client
    3. Open https://www.npmjs.com/package/@dnd-kit/core — confirm github.com/clauderic/dnd-kit
    4. Confirm no [SLOP] verdict in 04-RESEARCH.md
    5. Type "approved" to let Claude run npm install
  </how-to-verify>
  <verify>
    <automated>node -e "const p=require('./package.json'); ['d3-geo','d3-scale','topojson-client','@dnd-kit/core'].forEach(d=>{if(!p.dependencies[d]){console.error('missing',d);process.exit(1)}})"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Human approved all four package families on npm registry
    - [ ] d3-geo, d3-scale, topojson-client, @dnd-kit/core in package.json dependencies
    - [ ] @types/d3-geo, @types/d3-scale, @types/topojson-client in devDependencies
  </acceptance_criteria>
  <done>Geo and DnD packages installed after human approval</done>
  <resume-signal>Type "approved" after verifying npm pages, or describe concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create geo types, fetch script, utilities, and CI fixtures</name>
  <files>scripts/fetch-geo-assets.mjs, src/geo/types.ts, src/geo/territoryCatalog.ts, src/geo/choroplethScale.ts, src/geo/projectGeoToSvg.ts, src/geo/loadGeoAsset.ts, src/geo/matchTerritoryLabels.ts, src/geo/nameTables/muni-BA.json, src/geo/topo/muni-29.json, src/geo/topo/br-meso.sample.json</files>
  <read_first>
    - `04-RESEARCH.md` § Standard Stack, Pattern 1 (lazy TopoJSON), Pattern 3 (matching), Geo Asset Sources
    - `src/routes/mapas/ufCodes.ts` (UF crosswalk with ibgeCode)
    - `src/shared/data-input/parseTabular.ts` (normalizeHeaderToken — reuse, do not duplicate)
    - `src/routes/mapas/brazilUfPaths.ts` (provenance comment style for fetch script)
  </read_first>
  <action>
    1. Create scripts/fetch-geo-assets.mjs (Node ESM): fetch IBGE Malhas v3 per-UF muni TopoJSON (qualidade=minima), br-meso.json, IBGE Localidades name tables per UF, MS health-macro shapefile (manual download step documented if portal unreachable). Simplify health-macro via mapshaper to ≤400 KB target. Write to src/geo/topo/ and src/geo/nameTables/. Document all source URLs in file header. Strip scripts/handlers from any SVG (T-01-SVG rule).

    2. Create src/geo/types.ts: TerritoryRef, GeoLevel, MapViewState, MuniEntry, MatchReport types. Join keys use ibgeCode/codarea — never sigla-only against GeoJSON.

    3. Create src/geo/territoryCatalog.ts per D-07/D-08: grande região presets (N/NE/CO/SE/S) → UF sigla lists; health-macro metadata + municipality crosswalk refs (MAP-08 infra). Export resolvePresetTerritories(presetId): TerritoryRef[].

    4. Create src/geo/choroplethScale.ts: createChoroplethScale(values) using d3-scale scaleSequential with UI-SPEC teal steps (#18181b → #5eead4). Export legendBreaks helper.

    5. Create src/geo/projectGeoToSvg.ts: geoPath + geoMercator wrapper; projectFeaturesToPaths(features, bounds?) → { id, d, properties }[]. Use topojson-client feature() decode.

    6. Create src/geo/loadGeoAsset.ts: dynamic import loaders Record for muni-{ibge}.json, br-meso.json, health-macro.json. No runtime fetch — local JSON only (MAP-05).

    7. Create src/geo/matchTerritoryLabels.ts: matchUfLabel(line) via sigla or normalizeHeaderToken(name); matchMunicipality(name, ufSigla, catalog) scoped by UF; return { matched, unmatched, fuzzy? } report shape for paste UI.

    8. Commit CI fixtures (run fetch script for at least BA/SP sample if network available; otherwise commit minimal valid TopoJSON for muni-29 + nameTables/muni-BA.json + br-meso.sample.json truncated for tests). Full 27-UF set generated by script but may be gitignored except samples — document in script README comment.

    9. Create unit tests: matchTerritoryLabels.test.ts (UF sigla/name, muni exact + unmatched), projectGeoToSvg.test.ts (path count &gt; 0 from fixture), loadGeoAsset.test.ts (dynamic import resolves, no fetch), territoryCatalog.test.ts (Norte preset = 7 UFs, health macro resolves IDs), choroplethScale.test.ts (domain min/max → teal steps).
  </action>
  <verify>
    <automated>npm run test:run -- src/geo/ && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] fetch-geo-assets.mjs documented with IBGE v3 + MS provenance
    - [ ] matchTerritoryLabels resolves "BA", "bahia", rejects unknown UF
    - [ ] loadGeoAsset dynamic import returns fixture topo without fetch mock
    - [ ] territoryCatalog Norte preset returns AC, AP, AM, PA, RO, RR, TO
    - [ ] choroplethScale uses UI-SPEC teal steps, not purple/rainbow
  </acceptance_criteria>
  <done>Geo stack utilities and CI fixtures committed with tests green</done>
</task>

<task type="auto">
  <name>Task 3: Create mapAnalysisState reducer and extend SessionProvider</name>
  <files>src/routes/mapas/mapAnalysisState.ts, src/shared/session/SessionProvider.tsx, src/routes/mapas/mapAnalysisState.test.ts, src/shared/session/SessionProvider.test.tsx</files>
  <read_first>
    - `04-CONTEXT.md` D-01, D-06, D-09 (groups, renamable, panel scope)
    - `04-PATTERNS.md` § mapAnalysisState (useDatasusWizard reducer pattern)
    - `src/shared/data-input/useDatasusWizard.ts` (reducer + commit pattern)
    - `SessionProvider.tsx` + `SessionProvider.test.tsx`
  </read_first>
  <action>
    1. Create mapAnalysisState.ts with typed reducer actions: CREATE_GROUP, RENAME_GROUP, DELETE_GROUP, MERGE_TERRITORIES_TO_GROUP, SET_ACTIVE_GROUP, SET_GROUP_TIME, TOGGLE_GROUP_VARIABLE, SET_MAP_VIEW, REPLACE_STATE. Export useMapAnalysis() returning { state, dispatch, derived: { summaryChips, canReview, allTerritoryIds } }.

    2. Implement canReview: true when ≥1 group has ≥1 territory + valid time + ≥1 variableId (per D-03 minimum shape).

    3. Extend SessionProvider: add mapAnalysis: MapAnalysisState | null and setMapAnalysis. Keep mapSelection for backward compat — derive flat { ufs, variables } from active groups OR deprecate with migration comment. clearSession resets mapAnalysis.

    4. mapAnalysisState.test.ts: CREATE_GROUP + RENAME_GROUP; SET_GROUP_TIME point/range/compare serialization (MAP-06 types); preset merge; canReview false until complete.

    5. Extend SessionProvider.test.tsx: setMapAnalysis persists; clearSession nulls mapAnalysis; hasData unchanged when only mapAnalysis set (D-21 — dataset only on handoff).
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/mapAnalysisState.test.ts src/shared/session/SessionProvider.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] MapAnalysisGroup time modes point | range | compare round-trip in tests
    - [ ] canReview false with empty groups; true with complete group
    - [ ] clearSession clears mapAnalysis without affecting unrelated state incorrectly
    - [ ] SessionProvider exports setMapAnalysis without breaking existing tests
  </acceptance_criteria>
  <done>Typed session model ready for MapasPage orchestrator (Wave 1–6)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | d3-geo, d3-scale, topojson-client, @dnd-kit/* from npm registry |
| Build-time geo fetch | IBGE/MS URLs → committed JSON; not user-controlled at runtime |
| Paste → matchTerritoryLabels | User text crosses into lookup (Wave 2 UI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Geo JSON assets | mitigate | Build script only; strip script/handlers from SVG (T-01-SVG); static committed files |
| T-04-02 | Denial of Service | matchTerritoryLabels paste | mitigate | Cap paste lines at existing tabular row limits; reject empty bulk paste |
| T-04-03 | Tampering | Prototype pollution via geo JSON | accept | Static build-time JSON only; no user-supplied geo files |
| T-04-SC | Tampering | npm geo/DnD packages | mitigate | Blocking human-verify checkpoint (Task 1); [ASSUMED] in RESEARCH.md |
</threat_model>

<verification>
- npm run test:run -- src/geo/ src/routes/mapas/mapAnalysisState.test.ts src/shared/session/SessionProvider.test.tsx green
- npm run typecheck green
- test -f scripts/fetch-geo-assets.mjs && test -f src/geo/topo/muni-29.json
- Geo packages in package.json after human approval
</verification>

<success_criteria>
- Wave 0 Nyquist scaffolds exist per 04-VALIDATION.md
- Session + geo utilities proven before any Mapas UI wave
- Sample TopoJSON committed for offline CI
</success_criteria>

<output>
Create `.planning/phases/04-mapas-como-interface-estatistica/04-01-SUMMARY.md` when done
</output>
