---
phase: 04-mapas-como-interface-estatistica
plan: 05
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - src/routes/mapas/BrazilMapCanvas.tsx
  - src/routes/mapas/MapBreadcrumb.tsx
  - src/routes/mapas/MapGeoPath.tsx
  - src/geo/loadGeoAsset.ts
  - src/geo/matchTerritoryLabels.ts
  - src/geo/matchTerritoryLabels.test.ts
  - src/routes/mapas/TerritoryPastePanel.tsx
  - src/routes/mapas/MapasPage.tsx
  - src/routes/mapas/mapAnalysisState.ts
  - src/geo/projectGeoToSvg.test.ts
  - src/geo/loadGeoAsset.test.ts
autonomous: true
requirements: [MAP-03, MAP-04, MAP-05]
must_haves:
  truths:
    - "User drills from Brazil UF view into a UF and sees município, mesorregião, or macrorregião de saúde choropleths"
    - "Sub-UF geo loads lazily via dynamic import — not in initial bundle (D-19)"
    - "Municipality paste scoped by active UF shows matched/unmatched report (MAP-04)"
    - "All map geometry served from bundled static assets — zero runtime IBGE fetch (MAP-05)"
  artifacts:
    - path: "src/routes/mapas/MapBreadcrumb.tsx"
      provides: "Brasil → UF → level navigation"
    - path: "src/geo/loadGeoAsset.ts"
      provides: "Lazy topo loaders for muni/meso/health-macro"
    - path: "src/routes/mapas/BrazilMapCanvas.tsx"
      provides: "Drill-down choropleth canvas"
  key_links:
    - from: "BrazilMapCanvas.tsx"
      to: "loadGeoAsset.ts"
      via: "loadMuniTopo(ufIbge) on drill"
      pattern: "loadMuniTopo"
    - from: "TerritoryPastePanel.tsx"
      to: "matchMunicipality"
      via: "activeUfScope"
      pattern: "matchMunicipality"
    - from: "mapAnalysisState.ts"
      to: "mapView.level"
      via: "SET_MAP_VIEW"
      pattern: "SET_MAP_VIEW"
---

<objective>
Deliver MAP-03, MAP-04, MAP-05: full drill-down ladder with lazy TopoJSON, municipality matching scoped by UF, offline bundled geo only (D-16/D-17/D-18/D-19).

Purpose: Heaviest geo work isolated after UF choropleth and paste foundations (RESEARCH Wave 4).
Output: MapBreadcrumb, drill-down BrazilMapCanvas, extended muni matching, lazy load wiring, tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mapas-como-interface-estatistica/04-RESEARCH.md
@.planning/phases/04-mapas-como-interface-estatistica/04-UI-SPEC.md
@src/geo/projectGeoToSvg.ts
@src/geo/loadGeoAsset.ts
@src/routes/mapas/BrazilMapCanvas.tsx
</context>

<interfaces>
From `mapAnalysisState.ts`:
```typescript
mapView: { level: 'uf' | 'municipio' | 'meso' | 'health-macro'; parentCode?: string; ufIbge?: string };
// SET_MAP_VIEW action
```

From `loadGeoAsset.ts`:
```typescript
export async function loadMuniTopo(ufIbge: string): Promise<Topology>;
export async function loadMesoTopo(): Promise<Topology>;
export async function loadHealthMacroTopo(): Promise<Topology>;
```

Drill UX (UI-SPEC):
- Breadcrumb: **Brasil** → **{Nome UF} ({sigla})** → level tabs
- Loading: "Carregando mapa…" skeleton
- Error: geo asset load failure Alert
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend lazy geo loaders and projection tests</name>
  <files>src/geo/loadGeoAsset.ts, src/geo/projectGeoToSvg.ts, src/geo/projectGeoToSvg.test.ts, src/geo/loadGeoAsset.test.ts</files>
  <behavior>
    - loadMuniTopo('29') resolves fixture without fetch (vi.mock or real dynamic import)
    - projectFeaturesToPaths returns length &gt; 100 for BA municipalities fixture
    - loadMesoTopo and loadHealthMacroTopo resolve local JSON
    - No window.fetch called in tests
  </behavior>
  <read_first>
    - `04-RESEARCH.md` Pattern 1, Pitfall 1 (bundle bloat), Pitfall 3 (health vs meso)
    - `scripts/fetch-geo-assets.mjs` output paths
  </read_first>
  <action>
    1. Complete loadGeoAsset.ts loaders for all 27 UF ibge codes (generated loader map). Use dynamic import() — Vite code-splits each JSON.

    2. Ensure health-macro.json separate from br-meso.json (Pitfall 3 — MAP-08 geography).

    3. projectGeoToSvg: add fitExtent helper for drill viewBox per UF bounds.

    4. Expand tests: assert path count, codarea join key present on features, fetch not called.

    5. Run fetch-geo-assets.mjs to populate full topo set if not committed; CI keeps muni-29 + samples minimum.

    6. Document in loadGeoAsset.ts header: MAP-05 offline-only contract.
  </action>
  <verify>
    <automated>npm run test:run -- src/geo/loadGeoAsset.test.ts src/geo/projectGeoToSvg.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Dynamic import loads muni-29 fixture in tests
    - [ ] Health macro loader distinct from meso loader
    - [ ] Zero runtime fetch in unit tests
  </acceptance_criteria>
  <done>Lazy geo asset pipeline proven (MAP-05)</done>
</task>

<task type="auto">
  <name>Task 2: MapBreadcrumb + BrazilMapCanvas drill-down ladder</name>
  <files>src/routes/mapas/MapBreadcrumb.tsx, src/routes/mapas/BrazilMapCanvas.tsx, src/routes/mapas/MapGeoPath.tsx, src/routes/mapas/mapAnalysisState.ts, src/routes/mapas/MapasPage.tsx</files>
  <read_first>
    - `04-UI-SPEC.md` Key Screen 7, MapBreadcrumb states, drill loading/error
    - `04-CONTEXT.md` D-16 full MAP-03 ladder
    - `BrazilMapCanvas.tsx` from Wave 1
  </read_first>
  <action>
    1. Create MapBreadcrumb: nav aria-label="Navegação do mapa"; links for Brasil (SET_MAP_VIEW uf), UF name when drilled; segmented control or tabs for município | mesorregião | macrorregião de saúde levels.

    2. BrazilMapCanvas drill flow: double-click UF or explicit "Explorar {UF}" control → SET_MAP_VIEW { level: 'municipio', ufIbge, parentCode: sigla }. Show loading skeleton during import.

    3. Render sub-UF paths via projectGeoToSvg + MapGeoPath; apply choropleth from mockAnalysisData scoped to municipality codes when available (fallback neutral fill + legend hidden hint).

    4. Back to Brasil preserves groups/selection (D-03 — no data loss).

    5. Selection/group rules extend to sub-UF TerritoryRef level in mapAnalysisState MERGE action.

    6. Geo load error: Alert per UI-SPEC with back link.

    7. MapasPage wires breadcrumb above map canvas; passes mapView from state.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/ src/geo/</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Drill into BA loads muni paths (&gt;0) in integration test or canvas test
    - [ ] Breadcrumb shows Brasil → Bahia (BA)
    - [ ] Switching meso vs health-macro changes rendered path set
    - [ ] Returning to Brasil restores UF choropleth
  </acceptance_criteria>
  <done>MAP-03 drill-down choropleths with breadcrumb navigation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Municipality matching scoped by UF in TerritoryPastePanel</name>
  <files>src/geo/matchTerritoryLabels.ts, src/geo/matchTerritoryLabels.test.ts, src/routes/mapas/TerritoryPastePanel.tsx</files>
  <behavior>
    - With UF scope BA: "Salvador" → matched id 2927408
    - With UF scope BA: "XYZ" → unmatched
    - Without UF scope: municipality lines flagged "Selecione um estado no mapa primeiro"
    - Report lists Reconhecidos / Não reconhecidos counts
  </behavior>
  <read_first>
    - `04-CONTEXT.md` D-18 MAP-04
    - `src/geo/nameTables/muni-BA.json`
    - `04-UI-SPEC.md` paste match report copy
  </read_first>
  <action>
    1. Implement matchMunicipalityPaste(text, ufSigla, nameTable) using matchMunicipality from Wave 0; load name table via dynamic import nameTables/muni-{UF}.json.

    2. TerritoryPastePanel: detect municipality lines vs UF lines (heuristic: 2-letter sigla vs longer names); require activeUfScope from mapView.parentCode or selected single UF.

    3. Extend match report UI for municipality matches with nome + id; fuzzy suggestions optional (flag fuzzy in report, not auto-select).

    4. Matched municipalities → TerritoryRef { level: 'municipio', ibgeCode, name } into selection/group merge.

    5. Expand matchTerritoryLabels.test.ts with BA Salvador + unmatched cases.

    6. Cap municipality paste lines at 200.
  </action>
  <verify>
    <automated>npm run test:run -- src/geo/matchTerritoryLabels.test.ts src/routes/mapas/TerritoryPastePanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] MAP-04 matched/unmatched report with UF scope enforced
    - [ ] Municipality match uses offline nameTables only
    - [ ] Copy matches UI-SPEC Reconhecidos / Não reconhecidos
  </acceptance_criteria>
  <done>MAP-04 municipality matching integrated with drill context</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled topo JSON | Trusted build-time assets only |
| Municipality paste | User text → offline lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | Fetched geo at runtime | mitigate | loadGeoAsset throws if fetch attempted; static import only |
| T-04-05-02 | Denial of Service | Drill spam | mitigate | Debounce drill clicks; single in-flight load per UF |
| T-04-05-03 | Spoofing | Paste municipality names | mitigate | React escaped report; scoped lookup prevents cross-UF false match |
</threat_model>

<verification>
- npm run test:run -- src/geo/ src/routes/mapas/TerritoryPastePanel.test.tsx green
- npm run build 2>&1 | head -20 — initial chunk must not include all muni topo (manual spot-check in SUMMARY)
</verification>

<success_criteria>
- Full MAP-03 ladder navigable offline
- MAP-04 municipality report with UF scope
- MAP-05 lazy load only — no runtime IBGE API
</success_criteria>

<output>
Create `.planning/phases/04-mapas-como-interface-estatistica/04-05-SUMMARY.md` when done
</output>
