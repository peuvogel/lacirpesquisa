---
phase: 04-mapas-como-interface-estatistica
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - src/routes/mapas/GroupBar.tsx
  - src/routes/mapas/GroupChip.tsx
  - src/routes/mapas/PresetRegionPills.tsx
  - src/routes/mapas/SelectionSummaryStrip.tsx
  - src/routes/mapas/MapasPage.tsx
  - src/routes/mapas/mapAnalysisState.ts
  - src/routes/mapas/GroupBar.test.tsx
  - src/routes/mapas/SelectionSummaryStrip.test.tsx
  - src/index.css
autonomous: true
requirements: [MAP-07, MAP-08, MAP-10]
must_haves:
  truths:
    - "User creates groups by drag-and-drop OR explicit Criar grupo com seleção button (D-05)"
    - "Regional presets (N/NE/CO/SE/S) and health-macro pills create/fill groups (D-07/D-08)"
    - "Selection summary strip always shows plain-PT territory × grupos × tempo × agravos chips (D-02)"
    - "Map paths glow for selected territories and sync with group membership (D-04/D-08)"
  artifacts:
    - path: "src/routes/mapas/GroupBar.tsx"
      provides: "DnD drop zones + preset pills (R1)"
    - path: "src/routes/mapas/SelectionSummaryStrip.tsx"
      provides: "Live summary strip (R2)"
    - path: "src/routes/mapas/MapasPage.tsx"
      provides: "Single-screen R1–R2 layout (D-01)"
  key_links:
    - from: "GroupBar.tsx"
      to: "mapAnalysisState.ts"
      via: "CREATE_GROUP / MERGE_TERRITORIES_TO_GROUP"
      pattern: "CREATE_GROUP"
    - from: "PresetRegionPills.tsx"
      to: "territoryCatalog.ts"
      via: "resolvePresetTerritories"
      pattern: "resolvePreset"
    - from: "MapasPage.tsx"
      to: "SessionProvider"
      via: "setMapAnalysis"
      pattern: "setMapAnalysis"
---

<objective>
Deliver MAP-07, MAP-08, MAP-10 core didactic UX: GroupBar with DnD + presets, SelectionSummaryStrip, map↔group sync on single-screen Mapas workspace (RESEARCH Wave 3, D-01–D-08).

Purpose: Groups are prerequisite for group config (Wave 5) and handoff (Wave 6). Single-screen fluid flow without stepper.
Output: R1 GroupBar, R2 SummaryStrip, MapasPage orchestrator upgrade, @dnd-kit integration, tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mapas-como-interface-estatistica/04-UI-SPEC.md
@.planning/phases/04-mapas-como-interface-estatistica/04-PATTERNS.md
@.planning/phases/04-mapas-como-interface-estatistica/04-CONTEXT.md
@src/routes/mapas/MapasPage.tsx
@src/routes/mapas/mapAnalysisState.ts
@src/geo/territoryCatalog.ts
</context>

<interfaces>
From `mapAnalysisState.ts`:
```typescript
export function useMapAnalysis(): {
  state: MapAnalysisState;
  dispatch: Dispatch<MapAnalysisAction>;
  derived: { summaryChips: SummaryChip[]; canReview: boolean; allTerritoryIds: TerritoryRef[] };
};
```

From `territoryCatalog.ts`:
```typescript
export function resolvePresetTerritories(presetId: string): TerritoryRef[];
export const REGION_PRESETS: { id: string; label: string }[];
export const HEALTH_MACRO_PRESETS: { id: string; label: string; tooltip: string }[];
```

DnD pattern (04-RESEARCH Pattern 4):
- DndContext + KeyboardSensor + PointerSensor
- useDraggable on selected territory chip overlay from map
- useDroppable per group slot + "Criar grupo" zone
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SelectionSummaryStrip + mapAnalysis derived summary</name>
  <files>src/routes/mapas/SelectionSummaryStrip.tsx, src/routes/mapas/mapAnalysisState.ts, src/routes/mapas/SelectionSummaryStrip.test.tsx</files>
  <behavior>
    - Empty state: "Nada selecionado ainda" + hint copy
    - Partial: "{N} estado(s) selecionado(s)" when territories but no groups
    - Complete template: "BA, PE · 2 grupos · 2018–2022 · Internações" when all segments present
    - aria-live="polite" on container
  </behavior>
  <read_first>
    - `04-UI-SPEC.md` Copywriting R2 + Selection summary states
    - `VariablePanel.tsx` chip styling
  </read_first>
  <action>
    1. Extend mapAnalysisState derived.summaryChips: build plain-PT segments from groups (territory siglas/names, group count, time summary, variable labels from mockAnalysisData labels).

    2. Create SelectionSummaryStrip: empty | partial | complete rendering per UI-SPEC; Badge/chip row + sentence line.

    3. SelectionSummaryStrip.test.tsx: three state fixtures assert copy strings.

    4. Export formatTimeSummary(group.time) helper for reuse in handoff (Wave 6).
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/SelectionSummaryStrip.test.tsx src/routes/mapas/mapAnalysisState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Empty and complete copy match 04-UI-SPEC verbatim
    - [ ] aria-live="polite" present
    - [ ] Summary updates when groups change in reducer tests
  </acceptance_criteria>
  <done>R2 summary strip tested (MAP-10 partial)</done>
</task>

<task type="auto">
  <name>Task 2: GroupBar, GroupChip, PresetRegionPills with @dnd-kit</name>
  <files>src/routes/mapas/GroupBar.tsx, src/routes/mapas/GroupChip.tsx, src/routes/mapas/PresetRegionPills.tsx, src/routes/mapas/GroupBar.test.tsx, src/index.css</files>
  <read_first>
    - `04-UI-SPEC.md` R1, Interaction States (drop zone, GroupChip), D-04/D-05/D-07
    - `04-PATTERNS.md` § GroupBar
    - `@dnd-kit/core` accessibility guide (KeyboardSensor)
  </read_first>
  <action>
    1. Create GroupChip: renamable (double-click or pencil → inline input, Enter confirm, Escape cancel per UI-SPEC); active state drives R4 in Wave 5; delete affordance with confirm dialog copy.

    2. Create PresetRegionPills: Norte/NE/CO/SE/S buttons + health-macro pills (MAP-08) with Tooltip full names. onClick → dispatch CREATE_GROUP filled with resolvePresetTerritories(presetId). Highlight map members (D-08).

    3. Create GroupBar: DndContext wrapping drop zones per group + empty "Grupo n" slot; `.lacir-drop-target--active` CSS on drag-over; min-height 48px drop targets.

    4. Draggable: only selected ungrouped territories (from map selection). DragOverlay ghost chip with sigla. Drop merges into group (MERGE_TERRITORIES_TO_GROUP).

    5. Explicit **Criar grupo com seleção** Button always visible (D-05) — duplicates drop behavior for keyboard users.

    6. Optional context menu **Criar grupo com seleção** on map (secondary, D-05) — wire if low cost.

    7. GroupBar.test.tsx: preset Norte creates group with 7 UFs; button creates group from selection; rename dispatch.

    8. Add `.lacir-drop-target--active` to index.css per UI-SPEC motion (150ms ease-out).
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/GroupBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Criar grupo com seleção works without drag in test
    - [ ] Norte preset resolves 7 territories via territoryCatalog
    - [ ] Health macro preset creates group with health-macro level TerritoryRefs
    - [ ] Drop zone aria-label includes "solte estados selecionados aqui"
  </acceptance_criteria>
  <done>R1 GroupBar with DnD, presets, and MAP-08 health macros</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade MapasPage to single-screen R1–R5 shell with map↔group sync</name>
  <files>src/routes/mapas/MapasPage.tsx, src/routes/mapas/BrazilMapCanvas.tsx</files>
  <read_first>
    - `04-UI-SPEC.md` Layout Regions R1–R5
    - `MapasPage.tsx` current layout
    - `04-CONTEXT.md` D-01, D-03, D-08
  </read_first>
  <action>
    1. Replace flat useState with useMapAnalysis() + sync setMapAnalysis in useEffect (per 04-PATTERNS).

    2. Layout: Display title Mapas → R1 GroupBar → R2 SelectionSummaryStrip → flex row R3 map (58%) + R4 panel (42%) → R5 PrimaryActionBar stub (Revisar disabled until Wave 5/6).

    3. BrazilMapCanvas props extend: groupMembership map (territoryId → groupId), selectedUFs, onToggleUF, glow for selected-eligible (D-04). Clicking preset/group chip highlights member paths (D-08).

    4. Escape: clear ungrouped selection; Shift+Escape → clear-all confirm when groups exist (UI-SPEC a11y).

    5. ContextPanel Mode A explore hint when no active group (retain VariablePanel empty copy upgraded to UI-SPEC Mode A text).

    6. Remove rigid stepper if any Phase 1 stub exists — single screen only (D-01).

    7. Tablet: defer Sheet fallback to Wave 5 polish if time — document TODO only if not implemented; desktop-first acceptable per CONTEXT discretion.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/mapas/ && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] MapasPage renders GroupBar + SelectionSummaryStrip above map
    - [ ] Creating group updates summary strip
    - [ ] mapAnalysis synced to SessionProvider
    - [ ] No multi-step stepper UI
  </acceptance_criteria>
  <done>MAP-07/08/10 single-screen group UX on Mapas (config panel deferred Wave 5)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client DnD | Pure UI; no server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Denial of Service | Unlimited groups | mitigate | Cap groups at 10; cap territories per group at 27 UFs equivalent |
| T-04-04-02 | Tampering | Group rename input | mitigate | Trim length max 40 chars; React controlled input |
</threat_model>

<verification>
- npm run test:run -- src/routes/mapas/GroupBar.test.tsx src/routes/mapas/SelectionSummaryStrip.test.tsx green
- grep -v '^#' src/routes/mapas/MapasPage.tsx | grep -c GroupBar | grep -qv '^0$'
</verification>

<success_criteria>
- Groups creatable via drag AND button
- Presets including health macros work
- Summary strip always visible with plain PT
</success_criteria>

<output>
Create `.planning/phases/04-mapas-como-interface-estatistica/04-04-SUMMARY.md` when done
</output>
