---
phase: 01-redesign-base-react-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - index.html
  - legacy/index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/test/setup.ts
  - src/test/smoke.test.tsx
  - src/lib/utils.ts
  - components.json
  - src/components/ui/
  - public/logo-lacir.png
  - tailwind.config.js
  - postcss.config.js
autonomous: false
requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "Running `npm run dev` serves a React app (not the legacy vanilla page) at the repo root"
    - "Running `npm run test:run` executes vitest against src/ and passes"
    - "Running `npm run build` produces a Vite bundle with no Tailwind v3 config and no CDN importmap"
    - "The purple `#8B5CF6` scaffold no longer exists anywhere in the built app"
  artifacts:
    - path: "vite.config.ts"
      provides: "React + Tailwind v4 plugins, @ alias, vitest jsdom test block"
      contains: "@tailwindcss/vite"
    - path: "index.html"
      provides: "React SPA entry with Sora + IBM Plex Mono, no importmap"
      contains: "/src/main.tsx"
    - path: "src/test/setup.ts"
      provides: "jest-dom matchers + canvas stubs for Chart.js under jsdom"
    - path: "src/lib/utils.ts"
      provides: "cn() = clsx + tailwind-merge"
      exports: ["cn"]
    - path: "public/logo-lacir.png"
      provides: "LACIR brand mark served as a static asset (byte-identical to `logo lacir.png`)"
    - path: "legacy/index.html"
      provides: "Preserved v1.0 entry document as port reference"
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "module script tag"
      pattern: "src=\"/src/main.tsx\""
    - from: "vite.config.ts"
      to: "src/test/setup.ts"
      via: "vitest setupFiles"
      pattern: "setupFiles"
---

<objective>
Replace the legacy vanilla-JS entry point with a working React 19 + Vite 8 + TypeScript + Tailwind v4 toolchain, install the full Phase 1 dependency set (including the vitest/RTL harness that every later plan's `<automated>` verify depends on), generate the shadcn primitive set, and delete the abandoned purple Tailwind v3 scaffold.

Purpose: Every other Phase 1 plan mounts on this toolchain. Nothing else in the phase can be verified automatically until the test harness exists here (01-VALIDATION.md "Wave 0 Requirements").
Output: A booting React shell placeholder, a green vitest smoke test, a passing production build, and zero legacy purple/CDN config surviving.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-redesign-base-react-shell/01-CONTEXT.md
@.planning/phases/01-redesign-base-react-shell/01-RESEARCH.md
@.planning/phases/01-redesign-base-react-shell/01-UI-SPEC.md
@.planning/phases/01-redesign-base-react-shell/01-VALIDATION.md
@package.json
@index.html
@tailwind.config.js
</context>

<interfaces>
<!-- Contracts this plan establishes for every downstream plan. Use these exact names. -->

Path alias: `@/` resolves to `./src/` (tsconfig `paths` + vite `resolve.alias`).

Test commands (do not change in later plans):
- targeted: `npx vitest run <path>`
- full suite: `npm run test:run`
- types: `npm run typecheck`

`src/lib/utils.ts` exports:
- `cn(...inputs: ClassValue[]): string` — clsx + tailwind-merge, the standard shadcn helper.

shadcn primitive export names later plans import from `@/components/ui/*`:
- `button` → `Button`, `buttonVariants`
- `dialog` → `Dialog`, `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter`
- `badge` → `Badge`, `badgeVariants`
- `alert` → `Alert`, `AlertTitle`, `AlertDescription`
- `sheet` → `Sheet`, `SheetTrigger`, `SheetContent`
- `checkbox` → `Checkbox`
- `tooltip` → `Tooltip`, `TooltipTrigger`, `TooltipContent`, `TooltipProvider`
- `tabs` → `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`
- `navigation-menu` → `NavigationMenu`, `NavigationMenuList`, `NavigationMenuItem`, `NavigationMenuLink`
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate before first install</name>
  <files>package.json (read-only at this point — no writes in this task)</files>
  <read_first>
    - `.planning/phases/01-redesign-base-react-shell/01-RESEARCH.md` § Package Legitimacy Audit — the `[ASSUMED]` tags and why `slopcheck` could not run
    - `.planning/phases/01-redesign-base-react-shell/01-UI-SPEC.md` § Registry Safety — the `@cult-ui` deferral for this phase
    - `package.json` — which packages already exist (`chart.js`) versus which are new installs
  </read_first>
  <action>Present the exact package list below to the developer and stop. Do not run `npm install`, `npx shadcn`, or any other package-fetching command until an explicit approval is given. If the developer rejects or replaces a package, update Task 2's install commands accordingly and record the substitution in the SUMMARY.</action>
  <what-built>Nothing yet — this gate runs BEFORE any `npm install`. 01-RESEARCH.md § Package Legitimacy Audit could not run `slopcheck` (sandbox denied the install), so every package below is tagged `[ASSUMED]` and requires a non-zero human verification step per the GSD package legitimacy protocol. This checkpoint is mandatory even though `workflow.human_verify_mode` is `end-of-phase`, because legitimacy gates are never auto-approvable.</what-built>
  <how-to-verify>
    1. Confirm each package name below is the real, expected package (watch for typosquats — a single transposed character is the attack):
       Runtime: `react`, `react-dom`, `react-router-dom`, `tailwindcss`, `@tailwindcss/vite`, `clsx`, `tailwind-merge`, `class-variance-authority`, `lucide-react`, `chart.js` (already in package.json).
       Dev: `vite`, `@vitejs/plugin-react`, `typescript`, `@types/react`, `@types/react-dom`, `@types/node`, `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`.
    2. Spot-check the three least-familiar names on npmjs.com and confirm repo + weekly downloads look like an established package: https://npmjs.com/package/tailwind-merge, https://npmjs.com/package/class-variance-authority, https://npmjs.com/package/@tailwindcss/vite
    3. Confirm no third-party shadcn registry (notably `@cult-ui`) is in scope — 01-UI-SPEC.md § Registry Safety defers it for this phase (registry host returned a Vercel Security Checkpoint / HTTP 500 during vetting).
  </how-to-verify>
  <acceptance_criteria>
    - [ ] Every package name above confirmed as the intended, real package
    - [ ] No `@cult-ui` / non-official registry approved for this phase
    - [ ] Human typed an explicit approval before Task 2 runs any install
  </acceptance_criteria>
  <verify>
    <automated>test ! -d node_modules/react</automated>
    <human-check>Developer typed an explicit approval (or named replacements) for the package list above.</human-check>
  </verify>
  <done>The developer has approved the exact dependency list, or named substitutions, and Task 2 may run installs.</done>
  <resume-signal>Type "approved" to proceed with installs, or name any package to remove/replace.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold the Vite + React + TS + Tailwind v4 toolchain and vitest harness</name>
  <files>package.json, package-lock.json, vite.config.ts, tsconfig.json, tsconfig.node.json, index.html, legacy/index.html, src/main.tsx, src/App.tsx, src/index.css, src/test/setup.ts, src/test/smoke.test.tsx, public/logo-lacir.png</files>
  <read_first>
    - `package.json` (current: vite ^5 devDep, chart.js ^4.5.1 dep, dev/build/preview scripts)
    - `index.html` (lines 1–60: the `<script type="importmap">` block and Google Fonts Inter link that must NOT survive)
    - `.planning/phases/01-redesign-base-react-shell/01-RESEARCH.md` § Standard Stack (verified versions), § Code Examples (vite.config.ts shape), § Validation Architecture (test framework decision), Pitfall 5 and Pitfall 6
    - `.planning/phases/01-redesign-base-react-shell/01-UI-SPEC.md` § Design System (font decision: Sora + IBM Plex Mono)
  </read_first>
  <action>
    Do NOT run `npm create vite@latest` — the repo root is non-empty and the CLI would prompt interactively or offer to delete existing files. Hand-author the scaffold instead, in this order:

    1. Preserve the legacy entry as a port reference: `mkdir -p legacy && git mv index.html legacy/index.html`. Do not move `assets/`, `tests/`, or `tests-manifest.json` — later phases port logic directly from those paths, and 01-PATTERNS.md cites them by path and line number.

    2. Install dependencies (two commands, after the Task 1 gate passed). Runtime: `react@^19.2 react-dom@^19.2 react-router-dom@^7.18 tailwindcss@^4.3 @tailwindcss/vite@^4.3 clsx tailwind-merge class-variance-authority lucide-react`. Dev: `vite@^8.1 @vitejs/plugin-react@^4.5 typescript@~5.9 @types/react@^19 @types/react-dom@^19 @types/node vitest jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event`. `chart.js@^4.5.1` is already a dependency — leave the entry as-is. Record the resolved `vitest` version in the SUMMARY (01-RESEARCH.md left it unpinned).

    3. `vite.config.ts`: default-export `defineConfig` with `plugins: [react(), tailwindcss()]` (the `@tailwindcss/vite` plugin — no PostCSS), `resolve.alias` mapping `'@'` to `path.resolve(__dirname, './src')`, and a `test` block with `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./src/test/setup.ts']`, `css: false`, and `include: ['src/**/*.{test,spec}.{ts,tsx}']`. The `include` restriction is load-bearing: it keeps vitest from scanning the legacy `tests/` directory (which holds v1.0 test *modules*, not test files).

    4. `tsconfig.json`: `strict: true`, `jsx: 'react-jsx'`, `module`/`moduleResolution` set to `ESNext`/`bundler`, `target: 'ES2022'`, `lib` including `DOM` and `DOM.Iterable`, `noEmit: true`, `paths` mapping `"@/*"` to `["./src/*"]`, `types: ['vitest/globals', '@testing-library/jest-dom']`, `include: ['src']`, and a project reference to `tsconfig.node.json`. `tsconfig.node.json` covers `vite.config.ts` with `module: 'ESNext'`, `moduleResolution: 'bundler'`, `types: ['node']`.

    5. New root `index.html`: `lang="pt-BR"`, title `Bioestatística LACIR`, favicon `/logo-lacir.png`, description meta carried from the legacy head, `<link>` preconnect + stylesheet for Google Fonts families `Sora` (weights 400 and 700 only — 01-UI-SPEC.md § Typography allows exactly two weights) and `IBM+Plex+Mono` (weight 400), a `<div id="root">`, and `<script type="module" src="/src/main.tsx">`. Do NOT copy the `<script type="importmap">` block (01-RESEARCH.md Pitfall 6) and do NOT link Inter or `assets/css/styles.css`.

    6. `src/main.tsx`: import `./index.css`, then `createRoot(document.getElementById('root')!).render(<StrictMode><App /></StrictMode>)`.
       `src/App.tsx`: a deliberate placeholder — a `<main>` with an `<h1>` reading `Bioestatística LACIR`. Plan 01-05 replaces this file's body with the router; keep it trivial so the smoke test is stable.
       `src/index.css`: `@import "tailwindcss";` plus a temporary `:root { color-scheme: dark; }` and body background `#0a0f0d` / color `#e6efe9` so the placeholder is not white-on-white. Plan 01-02 adds the `@import "./app/theme.css";` line and the real token set — do not author tokens here.

    7. `src/test/setup.ts`: import `@testing-library/jest-dom/vitest`; call `afterEach(cleanup)` from `@testing-library/react`; stub `HTMLCanvasElement.prototype.getContext` to return a Proxy/object of no-op functions and `HTMLCanvasElement.prototype.toDataURL` to return the fixed string `data:image/png;base64,stub` (jsdom has no canvas renderer — Chart.js and the PNG export hook both need this, per 01-VALIDATION.md Wave 0 Requirements). Guard both stubs so they only install when the real implementation is absent or throws.

    8. `src/test/smoke.test.tsx`: render `<App />` with RTL and assert the heading `Bioestatística LACIR` is in the document, plus assert `toDataURL()` on a fresh canvas returns the stub string (proves the setup file is actually loaded).

    9. `package.json` scripts: `dev: vite`, `build: tsc -b && vite build`, `preview: vite preview`, `test: vitest`, `test:run: vitest run`, `typecheck: tsc --noEmit`. `test` may stay in watch mode for humans, but no plan's `<automated>` verify may ever call it — verifies use `test:run` or `npx vitest run <path>`.

    10. Copy the brand mark without modifying it (D-16): `cp "logo lacir.png" public/logo-lacir.png`. Keep the original file at the repo root untouched.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/test/smoke.test.tsx && npm run build && cmp "logo lacir.png" public/logo-lacir.png</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `npx vitest run src/test/smoke.test.tsx` passes (harness + setup file both live)
    - [ ] `npm run build` exits 0 and emits `dist/index.html`
    - [ ] `grep -c 'importmap' index.html` returns 0 and `grep -c 'Inter' index.html` returns 0
    - [ ] `legacy/index.html` exists and root `index.html` references `/src/main.tsx`
    - [ ] `cmp "logo lacir.png" public/logo-lacir.png` reports no difference
    - [ ] `vite.config.ts` test `include` is scoped to `src/**` (legacy `tests/` not scanned)
  </acceptance_criteria>
  <done>A React placeholder boots via Vite, the vitest+RTL+jsdom harness runs green with canvas stubs in place, the production build succeeds, and the legacy entry document is preserved under `legacy/`.</done>
</task>

<task type="auto">
  <name>Task 3: Generate shadcn primitives and delete the abandoned purple scaffold</name>
  <files>components.json, src/lib/utils.ts, src/components/ui/, tailwind.config.js, postcss.config.js, package.json</files>
  <read_first>
    - `tailwind.config.js` (the dead `brand: '#8B5CF6'` purple token to be deleted, and the `./tests/**/*.js` content globs)
    - `postcss.config.js`
    - `.planning/phases/01-redesign-base-react-shell/01-UI-SPEC.md` § Design System (preset: vite, base color neutral, CSS variables on, radius 0.75rem) and § Registry Safety (no third-party registry this phase)
    - `.planning/phases/01-redesign-base-react-shell/01-RESEARCH.md` Pitfall 5, § Standard Stack installation block
  </read_first>
  <action>
    1. Initialize shadcn non-interactively: `npx shadcn@latest init -y -b neutral`. Expect it to write `components.json`, add CSS variables to `src/index.css`, and create `src/lib/utils.ts`. If it prompts, errors, or cannot reach the registry, fall back to hand-authoring: `components.json` with `$schema` `https://ui.shadcn.com/schema.json`, `style: "new-york"`, `tailwind.css: "src/index.css"`, `tailwind.baseColor: "neutral"`, `tailwind.cssVariables: true`, `rsc: false`, `tsx: true`, and `aliases` for `components: "@/components"`, `ui: "@/components/ui"`, `utils: "@/lib/utils"`, `lib: "@/lib"`, `hooks: "@/hooks"`. Set `--radius: 0.75rem` in `src/index.css` either way.

    2. Ensure `src/lib/utils.ts` exports `cn(...inputs: ClassValue[])` built from `clsx` and `twMerge` (author it if the CLI did not).

    3. Add the primitive set 01-UI-SPEC.md § Design System authorizes: `npx shadcn@latest add -y button dialog navigation-menu sheet badge tabs checkbox tooltip alert`. If the registry is unreachable, fall back to installing the Radix packages directly (`@radix-ui/react-dialog @radix-ui/react-navigation-menu @radix-ui/react-tabs @radix-ui/react-checkbox @radix-ui/react-tooltip`) and hand-author minimal equivalents under `src/components/ui/` that export exactly the names listed in this plan's `<interfaces>` block, styled with `cn()` and CSS variables. Downstream plans import by those names only, so the fallback must not rename anything. Record which path was taken (CLI vs fallback) in the SUMMARY; a fallback install of Radix packages is covered by the Task 1 legitimacy approval.

    4. Do NOT add a `registries` key for `@cult-ui` or any other third-party registry (01-UI-SPEC.md § Registry Safety). D-15's "balanced" accents ship as first-party CSS in plan 01-02.

    5. Delete the dead v3 scaffold: `rm tailwind.config.js postcss.config.js`. Tailwind v4 with `@tailwindcss/vite` needs neither, and the purple `brand` token in the config is the exact drift risk 01-RESEARCH.md Pitfall 5 warns about. Also confirm `autoprefixer` is not a dependency; if `postcss.config.js` referenced it and it sits in `package.json`, remove it.

    6. Add `src/components/ui/primitives.test.tsx`: render `Button`, `Badge`, and an open `Dialog` (with `DialogContent` + `DialogTitle`) and assert each renders its text child, so a broken/renamed primitive fails fast for every downstream plan rather than at the point of use.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/primitives.test.tsx && npm run typecheck && npm run build && test ! -f tailwind.config.js && test ! -f postcss.config.js && test 0 -eq "$(grep -rniI --include='*.ts' --include='*.tsx' --include='*.css' --include='*.json' --include='*.html' -e '8b5cf6' -e 'cult-ui' src components.json index.html | grep -v '^\s*//' | wc -l | tr -d ' ')"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `components.json` exists with base color `neutral`, CSS variables on, and no `registries` entry
    - [ ] `src/lib/utils.ts` exports `cn`
    - [ ] All nine primitive modules exist under `src/components/ui/` with the export names in `<interfaces>`
    - [ ] `tailwind.config.js` and `postcss.config.js` are deleted
    - [ ] No `8B5CF6` (case-insensitive) or `cult-ui` occurrence in `src/`, `components.json`, or `index.html`
    - [ ] `npm run build` still exits 0 after the config deletions
  </acceptance_criteria>
  <done>shadcn primitives are available under `@/components/ui` with stable export names, `cn()` exists, and the purple Tailwind v3 scaffold is gone from the repo and the build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | Third-party package code enters the build graph for the first time in this phase |
| shadcn registry → repo | `shadcn add` copies un-pinned source files directly into `src/components/ui/` |
| CDN → browser runtime | Legacy `index.html` importmap pulled Chart.js from jsdelivr at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install` batch (Task 2) | mitigate | Task 1 blocking-human legitimacy gate over the exact package list before any install; `package-lock.json` committed so later installs are hash-pinned; no third-party shadcn registry configured |
| T-01-SC2 | Tampering | `shadcn add` copied component source | mitigate | Only the official shadcn registry is used; each generated file under `src/components/ui/` is reviewed at add-time in Task 3 (UI-only code: no `fetch`, no `process.env`, no `eval`) before commit |
| T-01-CDN | Tampering | Runtime CDN import of Chart.js via importmap | mitigate | New `index.html` omits the importmap entirely; Chart.js resolves from the npm dependency and is bundled by Vite (01-RESEARCH.md Pitfall 6) — verified by the `grep -c 'importmap' index.html` acceptance criterion |
| T-01-BUILD | Denial of Service | Stale Tailwind v3 config participating in the build | accept | Config files are deleted, not migrated; a stale config could only break the developer build, and no user-facing surface depends on it |
</threat_model>

<verification>
- `npm run test:run` — full suite green (smoke + primitives)
- `npm run typecheck` — no type errors
- `npm run build` — production bundle emitted
- `npm run dev` serves the React placeholder, not the legacy vanilla page
</verification>

<success_criteria>
- React 19 + Vite 8 + TS + Tailwind v4 toolchain boots, builds, and typechecks
- vitest + RTL + jsdom harness runs with canvas stubs installed (unblocks every later plan's automated verify)
- shadcn primitives available under `@/components/ui` with the export names fixed in `<interfaces>`
- Legacy purple Tailwind v3 config, PostCSS config, CDN importmap, and Inter font link are all gone
- `logo lacir.png` preserved unmodified and served as `/logo-lacir.png`
</success_criteria>

<output>
Create `.planning/phases/01-redesign-base-react-shell/01-01-SUMMARY.md` when done (record the resolved `vitest` version and whether shadcn CLI or the hand-authored fallback was used).
</output>
