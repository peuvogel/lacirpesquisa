---
phase: 07-baseline-verde
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/tests/registry.ts
  - src/routes/estatistica/SidebarTestLink.tsx
  - src/routes/estatistica/SidebarTestLink.test.tsx
  - src/routes/estatistica/EstatisticaPage.tsx
  - src/routes/mapas/ReviewAnalysisDialog.test.tsx
autonomous: true
requirements: [QA-01, QA-04]

must_haves:
  truths:
    - "`npm run typecheck` termina com exit 0 — os 2 erros do ground truth (SidebarTestLink.tsx:53 e ReviewAnalysisDialog.test.tsx:67) deixam de existir (QA-01)"
    - "Cadastrar uma entrada nova no TEST_REGISTRY sem ícone correspondente vira erro de compilação, não bug silencioso (D-14)"
    - "Renderizar uma TestRegistryEntry cujo id não está em TEST_ICONS não derruba o sidebar — o fallback FlaskConical assume (QA-04, D-14)"
    - "Um activeTestId inexistente vira erro de typecheck em vez de heading silenciosamente trocado — o fallback `?? 'Estatística'` deixa de existir (D-11)"
    - "O fixture de MapAnalysisState herda de createInitialMapAnalysisState() e não pode divergir do estado inicial de produção (D-13)"
    - "Nenhum comportamento de produção muda: t-Student continua sendo o teste padrão (D-10) e nenhum valor do TEST_REGISTRY é alterado"
    - "Nenhuma dependência npm nova é adicionada (regra herdada da Fase 5)"
  artifacts:
    - path: "src/features/tests/registry.ts"
      provides: "TEST_REGISTRY como fonte do tipo TestId + isTestAvailable como type guard"
      contains: "export type TestId"
    - path: "src/routes/estatistica/SidebarTestLink.tsx"
      provides: "TEST_ICONS exaustivo por TestId + iconFor() com fallback de runtime"
      contains: "Record<TestId, LucideIcon>"
    - path: "src/routes/estatistica/SidebarTestLink.test.tsx"
      provides: "Prova executável de QA-04 (entry sem ícone não derruba o sidebar)"
      min_lines: 40
    - path: "src/routes/estatistica/EstatisticaPage.tsx"
      provides: "activeTestId tipado por TestId, sem fallback de título"
      contains: "useState<TestId>"
    - path: "src/routes/mapas/ReviewAnalysisDialog.test.tsx"
      provides: "Fixture de MapAnalysisState espalhando a fábrica de produção"
      contains: "createInitialMapAnalysisState()"
  key_links:
    - from: "src/routes/estatistica/SidebarTestLink.tsx"
      to: "src/features/tests/registry.ts"
      via: "import type { TestId }"
      pattern: "import type \\{ TestId \\}"
    - from: "src/routes/estatistica/EstatisticaPage.tsx"
      to: "src/features/tests/registry.ts"
      via: "isTestAvailable como type guard estreitando string para TestId"
      pattern: "isTestAvailable"
    - from: "src/routes/mapas/ReviewAnalysisDialog.test.tsx"
      to: "src/routes/mapas/mapAnalysisState.ts"
      via: "spread de createInitialMapAnalysisState()"
      pattern: "\\.\\.\\.createInitialMapAnalysisState\\(\\)"
---

<objective>
Zerar os 2 erros de `npm run typecheck` do ground truth, e fazer isso da forma que **elimina a classe de bug** em vez de remendar o sintoma: o id de teste passa a ser um tipo derivado do `TEST_REGISTRY` (D-11), o mapa de ícones passa a ser exaustivo com fallback de runtime testado (D-14), e o fixture de mapa passa a herdar da fábrica de produção (D-13).

Purpose: destravar `npm run build` (hoje `tsc -b` para nos mesmos 2 erros e o `vite build` nunca roda) e fechar QA-04, que o ROADMAP chamou de "mina para o décimo teste".
Output: typecheck limpo, `TestId` exportado, `SidebarTestLink.test.tsx` novo com a prova de QA-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-baseline-verde/07-CONTEXT.md
@.planning/phases/07-baseline-verde/07-RESEARCH.md
@.planning/phases/07-baseline-verde/07-PATTERNS.md

Decisões travadas que este plano implementa: **D-09** (causa raiz do heading), **D-10** (t-Student permanece o padrão — nenhum código de produção muda), **D-11** (fallback eliminado por tipagem), **D-13** (fixture herda da fábrica), **D-14** (ícone exigido pelo tipo + fallback + teste).

`07-RESEARCH.md` §Pattern 2 e §Common Pitfalls 4 e 5 contêm os três trechos exatos (`as const satisfies`, `id is TestId`, `iconFor` com cast) já verificados com `tsc --noEmit --strict` contra as flags deste projeto. Não re-derivar.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derivar TestId do TEST_REGISTRY e transformar isTestAvailable em type guard</name>
  <files>src/features/tests/registry.ts</files>
  <read_first>
    - `src/features/tests/registry.ts` (arquivo inteiro — 116 linhas; ver a anotação explícita `: readonly TestRegistryEntry[]` na declaração e as funções `getTestById`/`isTestAvailable`/`getTestBadgeLabel` no fim)
    - `.planning/phases/07-baseline-verde/07-RESEARCH.md` §Architecture Patterns → Pattern 2 (padrão verificado por `tsc`)
    - `.planning/phases/07-baseline-verde/07-RESEARCH.md` §Common Pitfalls → Pitfall 5 (`setActiveTestId` para de compilar sem o type guard)
    - `.planning/phases/07-baseline-verde/07-PATTERNS.md` §`src/features/tests/registry.ts`
  </read_first>
  <action>
    Remover a anotação de tipo `: readonly TestRegistryEntry[]` da declaração de `TEST_REGISTRY` (a anotação alarga os literais e impede a derivação) e fechar o array com `as const satisfies readonly TestRegistryEntry[]` — `satisfies` continua validando a forma de cada entrada contra `TestRegistryEntry`, e `as const` preserva os literais dos 9 ids.

    Exportar `export type TestId = (typeof TEST_REGISTRY)[number]['id'];` logo abaixo da declaração do array.

    Trocar a assinatura de `isTestAvailable(id: string): boolean` para `isTestAvailable(id: string): id is TestId`, com corpo equivalente ao atual em runtime: `TEST_REGISTRY.some((entry) => entry.id === id && entry.status === 'available')`. É mudança só de tipo — nenhum chamador muda de comportamento.

    Adicionar sobrecarga a `getTestById` para que um `TestId` devolva `TestRegistryEntry` (nunca `undefined`) e um `string` genérico continue devolvendo `TestRegistryEntry | undefined`: declarar as duas assinaturas de sobrecarga acima da implementação atual e manter a implementação (`TEST_REGISTRY.find(...)`) intocada. Isso é o que permite a Task 3 apagar o `?? 'Estatística'` sem `!`. Se o `tsc` rejeitar a sobrecarga, registre o erro exato na SUMMARY e caia para `getTestById(activeTestId)!.title` na Task 3 — o que **não** é aceitável é manter o fallback.

    Não alterar nenhum valor de `id`, `title`, `subtitle`, `group`, `status` ou `phase`. Não mexer em `getTestBadgeLabel`. `TestStatus` e `TestRegistryEntry` (com `id: string` genérico) permanecem exportados como estão — `TestRegistryEntry.id` **precisa** continuar `string` para que a Task 2 possa construir uma entry fora do registry.
  </action>
  <acceptance_criteria>
    - `grep -c "as const satisfies readonly TestRegistryEntry\[\]" src/features/tests/registry.ts` retorna 1
    - `grep -c "export type TestId" src/features/tests/registry.ts` retorna 1
    - `grep -c "id is TestId" src/features/tests/registry.ts` retorna 1
    - `grep -c ": readonly TestRegistryEntry\[\] = \[" src/features/tests/registry.ts` retorna 0 (a anotação antiga saiu)
    - `grep -c "  id: string;" src/features/tests/registry.ts` retorna 1 (`TestRegistryEntry.id` continua `string`)
    - `git diff src/features/tests/registry.ts | grep "^-" | grep -c "id: '"` retorna 0 (nenhum id do registry foi removido ou reescrito)
    - `npx vitest run src/routes/estatistica/Sidebar.test.tsx` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/routes/estatistica/Sidebar.test.tsx</automated>
  </verify>
  <done>`TestId` é exportado e derivado do array; `isTestAvailable` é type guard; os 9 valores do registry estão byte a byte iguais; `Sidebar.test.tsx` continua verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TEST_ICONS exaustivo + iconFor com fallback + teste de QA-04</name>
  <files>src/routes/estatistica/SidebarTestLink.tsx, src/routes/estatistica/SidebarTestLink.test.tsx</files>
  <read_first>
    - `src/routes/estatistica/SidebarTestLink.tsx` (arquivo inteiro — o `TEST_ICONS: Record<string, LucideIcon>` nas linhas 16-26, o `const Icon = TEST_ICONS[entry.id] ?? FlaskConical` na linha 53, e os dois ramos de render: `dense` nas linhas 56-127 e não-dense nas linhas 129-179)
    - `src/routes/estatistica/ClearDataButton.test.tsx` (forma de render isolado + `getByRole`)
    - `src/routes/estatistica/Sidebar.test.tsx` (padrão `renderX(overrides)` com mocks retornados)
    - `.planning/phases/07-baseline-verde/07-RESEARCH.md` §Common Pitfalls → Pitfall 4 (`TS7053` ao indexar `Record<TestId,...>` com `string`)
    - `.planning/phases/07-baseline-verde/07-RESEARCH.md` §Validation Architecture → "Como provar QA-04 concretamente"
  </read_first>
  <behavior>
    - Uma `TestRegistryEntry` com `id: 'novo-teste-sem-icone'` (id que não existe em `TEST_ICONS`) renderiza sem lançar, e o botão fica acessível pelo título.
    - Uma entry real do registry (`t-student`) renderiza com seu ícone dedicado e o botão fica acessível pelo título 't de Student'.
    - Clicar no botão chama `onSelect` com o `id` da entry.
  </behavior>
  <action>
    Em `SidebarTestLink.tsx`: adicionar `FlaskConical` à lista de ícones já importada de `lucide-react` (o erro de typecheck do ground truth é exatamente esse import faltando) e adicionar `import type { TestId } from '@/features/tests/registry';` junto ao import de tipo já existente.

    Retipar `TEST_ICONS` para `Record<TestId, LucideIcon>` mantendo exatamente os 9 pares atuais (`'t-student'`→`GitCompareArrows`, `correlacao`→`ChartScatter`, `'prais-winsten'`→`TrendingUp`, `'qui-quadrado'`→`Grid3x3`, `'anova-tukey'`→`BarChart3`, `'kruskal-dunn'`→`BarChart3`, `poisson`→`Sigma`, `'binomial-negativa'`→`Sigma`, `logistica`→`Binary`). A partir daqui, faltar uma chave é `TS2741` no momento do cadastro — é isso que D-14 pede.

    Adicionar, em escopo de módulo, `function iconFor(id: string): LucideIcon` que devolve `(TEST_ICONS as Partial<Record<string, LucideIcon>>)[id] ?? FlaskConical`. O cast é o ponto exato onde o fallback de runtime continua vivo (Pitfall 4: indexar direto com `entry.id: string` vira `TS7053`).

    Substituir a linha 53 por `const Icon = iconFor(entry.id);`. Não mudar `SidebarTestLinkProps` — `entry: TestRegistryEntry` com `id: string` é o que permite o teste construir uma entry fora do registry sem violar tipo.

    Criar `src/routes/estatistica/SidebarTestLink.test.tsx` com os 3 casos do bloco `<behavior>`. `SidebarTestLink` não usa `useSession` nem `react-router` — renderizar direto, sem provider nenhum. Usar `dense` nos renders: em modo `dense` o badge só aparece para `status: 'em-breve'`, então o nome acessível do botão é exatamente `entry.title` (em modo não-dense o badge "Disponível" entra no nome acessível e a asserção por nome exato falharia). A entry de QA-04 é `{ id: 'novo-teste-sem-icone', title: 'Novo teste sem ícone', subtitle: 'Ainda sem ícone dedicado', group: 'Grupo experimental', status: 'available', phase: 99 }`, construída localmente e **não** adicionada ao `TEST_REGISTRY`. Asserções: `expect(() => render(...)).not.toThrow()`, `screen.getByRole('button', { name: 'Novo teste sem ícone' })` presente, e `container.querySelector('svg')` não nulo (o fallback renderizou um ícone). Marcar o caso com `(QA-04)` no título do `it`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/routes/estatistica/SidebarTestLink.test.tsx` sai com código 0 e reporta 3 testes passando
    - `npm run typecheck 2>&1 | grep -c "SidebarTestLink"` retorna 0
    - `grep -c "Record<TestId, LucideIcon>" src/routes/estatistica/SidebarTestLink.tsx` retorna 1
    - `grep -c "FlaskConical" src/routes/estatistica/SidebarTestLink.tsx` retorna 2 ou mais (import + fallback em `iconFor`)
    - `grep -c "TEST_ICONS\[entry.id\]" src/routes/estatistica/SidebarTestLink.tsx` retorna 0
    - `grep -c "QA-04" src/routes/estatistica/SidebarTestLink.test.tsx` retorna 1 ou mais
    - `grep -c "novo-teste-sem-icone" src/features/tests/registry.ts` retorna 0 (a entry de teste não vazou para o registry real)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/routes/estatistica/SidebarTestLink.test.tsx src/routes/estatistica/Sidebar.test.tsx</automated>
  </verify>
  <done>O ícone é exigido pelo tipo no cadastro, tolerado em runtime na leitura, e a tolerância tem teste próprio verde.</done>
</task>

<task type="auto">
  <name>Task 3: Eliminar o fallback de título por tipagem e consertar o fixture de MapAnalysisState</name>
  <files>src/routes/estatistica/EstatisticaPage.tsx, src/routes/mapas/ReviewAnalysisDialog.test.tsx</files>
  <read_first>
    - `src/routes/estatistica/EstatisticaPage.tsx` (arquivo inteiro — `useState<string>('t-student')` na linha 89, o handoff de `location.state` nas linhas 99-106, `handleSelectTest`/`handleCrossTestHandoff` nas linhas 108-119, o `pageTitle` na linha 122, o `<h1>` na 138 e o `data-active-test-id` na 141)
    - `src/routes/mapas/ReviewAnalysisDialog.test.tsx` (imports nas linhas 1-11 e o `deriveSelectionSummary({...})` na linha 67)
    - `src/routes/mapas/mapAnalysisState.ts` (a fábrica `createInitialMapAnalysisState()` na linha 199 — os 6 campos que ela devolve)
    - `.planning/phases/07-baseline-verde/07-RESEARCH.md` §Common Pitfalls → Pitfall 5
    - `.planning/phases/07-baseline-verde/07-PATTERNS.md` §`src/routes/estatistica/EstatisticaPage.tsx` e §`src/routes/mapas/ReviewAnalysisDialog.test.tsx`
  </read_first>
  <action>
    Em `EstatisticaPage.tsx`: importar `type TestId` de `@/features/tests/registry` e trocar a linha 89 para `useState<TestId>('t-student')` — o padrão `'t-student'` **permanece** (D-10, nenhum comportamento de produção muda). Com `isTestAvailable` já sendo type guard (Task 1), as três chamadas de `setActiveTestId(...)` guardadas por ele (handoff de `location.state`, `handleSelectTest`, `handleCrossTestHandoff` com early-return) voltam a compilar sem cast — não adicionar `as TestId` em lugar nenhum; se aparecer `TS2345`, a causa é o guard, não o call site.

    Remover o `?? 'Estatística'` da linha 122, deixando `const pageTitle = getTestById(activeTestId).title;` (a sobrecarga da Task 1 garante que um `TestId` sempre resolve). Não tocar no `<h1>` nem no `data-active-test-id` — `router.test.tsx` (plano 07-06) depende dos dois como estão.

    Manter `activeTestId?: string` / `activeTestId: string` nas props internas de `TestModuleSwitch` (linhas 20, 25, 31): passar um `TestId` para um parâmetro `string` é válido e evita alargar o raio da mudança.

    Em `ReviewAnalysisDialog.test.tsx`: adicionar `createInitialMapAnalysisState` ao import já existente de `./mapAnalysisState` (que hoje traz `deriveSelectionSummary`) e espalhá-la como primeiro elemento do objeto passado a `deriveSelectionSummary` na linha 67, mantendo os overrides `groups`, `activeGroupId: 'g1'`, `mapView: { level: 'uf' }` e `provenance: 'catalog'` **depois** do spread. Não escrever `sharedTime` nem `periodScope` à mão — herdá-los da fábrica é o ponto inteiro de D-13.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` sai com código 0 e imprime zero erros (QA-01 fechado)
    - `grep -c "'Estatística'" src/routes/estatistica/EstatisticaPage.tsx` retorna 0
    - `grep -c "useState<TestId>('t-student')" src/routes/estatistica/EstatisticaPage.tsx` retorna 1
    - `grep -c "as TestId" src/routes/estatistica/EstatisticaPage.tsx` retorna 0
    - `grep -c "data-active-test-id" src/routes/estatistica/EstatisticaPage.tsx` retorna 1 (o hook de 07-06 sobreviveu)
    - `grep -c "\.\.\.createInitialMapAnalysisState()" src/routes/mapas/ReviewAnalysisDialog.test.tsx` retorna 1
    - `grep -c "sharedTime" src/routes/mapas/ReviewAnalysisDialog.test.tsx` retorna 0
    - `npx vitest run src/routes/mapas/ReviewAnalysisDialog.test.tsx` sai com código 0
    - `git diff --stat package-lock.json` não retorna nada (zero dependência npm nova)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run src/routes/mapas/ReviewAnalysisDialog.test.tsx</automated>
  </verify>
  <done>`npm run typecheck` está verde, o fallback de título não existe mais, e o fixture de mapa herda o estado inicial real de produção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `location.state` → `EstatisticaPage` | Id de teste vindo do handoff de Variáveis/Mapas — dado externo ao componente, não validado pelo compilador |
| `props.entry` → `SidebarTestLink` | O componente é renderizável isoladamente com qualquer `TestRegistryEntry`, inclusive uma cujo `id` não exista em `TEST_ICONS` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Denial of Service | `SidebarTestLink` (`iconFor`) | mitigate | Fallback de runtime `?? FlaskConical` mantido e coberto por teste dedicado (Task 2) — um id desconhecido não pode lançar e derrubar o sidebar inteiro |
| T-07-01-02 | Tampering | `EstatisticaPage` ← `location.state.activeTestId` | mitigate | A fronteira de runtime continua guardada por `isTestAvailable` (agora type guard); apertar o tipo **não** remove a checagem, só a torna verificável pelo compilador |
| T-07-01-03 | Tampering | `src/features/tests/registry.ts` | mitigate | `as const satisfies readonly TestRegistryEntry[]` valida a forma de cada entrada em tempo de compilação; critério de aceite prova por `git diff` que nenhum id foi alterado |
| T-07-01-SC | Tampering | instalação de pacotes npm | mitigate | Zero pacote novo nesta fase (regra herdada da Fase 5); provado por `git diff --stat package-lock.json` vazio |

Nenhum item de severidade alta (`security_block_on: high`) — o plano não toca autenticação, sessão, rede ou criptografia. `07-RESEARCH.md` §Security Domain marca V2/V3/V4/V6 do ASVS como não aplicáveis a esta fase.
</threat_model>

<verification>
- `npm run typecheck` → exit 0, zero erros
- `npx vitest run src/routes/estatistica/SidebarTestLink.test.tsx src/routes/estatistica/Sidebar.test.tsx src/routes/mapas/ReviewAnalysisDialog.test.tsx` → exit 0
- `git diff --stat package-lock.json` → vazio
</verification>

<success_criteria>
- QA-01 fechado: `npm run typecheck` termina sem erros
- QA-04 fechado: existe teste executável provando que uma entrada sem ícone não derruba o sidebar
- D-11, D-13 e D-14 implementadas exatamente como travadas; D-10 respeitada (nenhuma mudança de comportamento de produção)
</success_criteria>

<output>
Criar `.planning/phases/07-baseline-verde/07-01-SUMMARY.md` ao terminar.
</output>
