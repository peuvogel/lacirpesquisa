---
phase: 07-baseline-verde
plan: 06
type: execute
wave: 4
depends_on: [07-01, 07-04, 07-05]
files_modified:
  - src/app/router.test.tsx
autonomous: true
requirements: [QA-02, QA-03]

must_haves:
  truths:
    - "`router.test.tsx` assevera duas coisas distinguíveis: que `/` renderiza a Estatística (D-04 da Fase 1) e que o teste ativo padrão é t-student (D-12)"
    - "Quando uma das duas quebrar, dá para saber qual — a rota e o teste padrão não compartilham asserção"
    - "Nenhum código de produção muda: t-Student continua o padrão e o heading continua sendo o título do teste ativo (D-10)"
    - "`npm run test:run` passa integralmente — 0 falhas, 0 skipped, 0 `.todo` (QA-02, QA-03)"
    - "A suíte é verde **e silenciosa**: 0 avisos `Not implemented: HTMLCanvasElement` e 0 avisos `was not wrapped in act` (D-08)"
    - "Nenhum teste é marcado como falha conhecida em lugar nenhum do repositório (QA-03)"
  artifacts:
    - path: "src/app/router.test.tsx"
      provides: "Landing route e teste padrão asseverados separadamente"
      contains: "data-active-test-id"
  key_links:
    - from: "src/app/router.test.tsx"
      to: "src/routes/estatistica/EstatisticaPage.tsx"
      via: "#lacir-test-module-mount[data-active-test-id]"
      pattern: "lacir-test-module-mount"
---

<objective>
Fechar o 24º caso quebrado — o único com causa distinta — e provar que a suíte inteira está verde **e** silenciosa.

Purpose: o teste `router.test.tsx:40` achava que asseverava a D-04 da Fase 1 ("Landing route = Estatística") e na verdade asseverava um fallback: `activeTestId` iniciava em `'demo'`, que não está no `TEST_REGISTRY`, então `getTestById('demo')` devolvia `undefined` e o `?? 'Estatística'` disparava. Trocado o padrão para `'t-student'` (entrada real), o fallback parou de disparar e o heading virou 't de Student' (D-09). Separar as duas asserções é o que torna a próxima quebra diagnosticável.
Output: `router.test.tsx` verde com duas asserções independentes, e a suíte inteira em 0 falhas / 0 avisos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-baseline-verde/07-CONTEXT.md
@.planning/phases/07-baseline-verde/07-RESEARCH.md
@.planning/phases/07-baseline-verde/07-PATTERNS.md
@.planning/phases/01-redesign-base-react-shell/01-CONTEXT.md

Decisões travadas que este plano implementa: **D-09** (causa raiz: o heading "Estatística" nunca foi intencional), **D-10** (t-Student permanece o padrão; nenhum código de produção muda), **D-12** (duas asserções distinguíveis), **D-08** (parte `act(...)`: a suíte precisa terminar silenciosa).

A D-04 da Fase 1 diz "Landing route = **Estatística**" — é sobre a **rota**, não sobre o texto do heading. `/` continua sendo Estatística nos dois caminhos; nenhuma decisão travada de fase anterior está em jogo.

Hipótese A2 da pesquisa: a maioria dos 21 avisos de `act(...)` deve desaparecer sozinha com a reescrita das ondas 3 (trocar `waitFor` que falha e re-tenta até timeout por `findBy*` que resolve rápido). O que sobrar é atualização de estado genuinamente fora de um `user.click`/`findBy*` e precisa de tratamento pontual — não de supressão.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separar landing route e teste padrão em router.test.tsx</name>
  <files>src/app/router.test.tsx</files>
  <read_first>
    - `src/app/router.test.tsx` (arquivo inteiro, 39 linhas + os 5 casos — o helper `renderAt(initialPath)` que devolve o resultado de `render`, e o caso quebrado 'renders the Estatística heading at / (landing route, D-04)' na linha 40)
    - `src/routes/estatistica/EstatisticaPage.tsx` (linha 138 `<h1>{pageTitle}</h1>` e linha 141 `<div id="lacir-test-module-mount" data-active-test-id={activeTestId}>` — os dois hooks reais que este teste vai usar)
    - `src/routes/estatistica/Sidebar.test.tsx` (linha 114 — `screen.getByRole('button', { name: 'Qual teste usar?' })`, afordância renderizada só pela Estatística)
    - `src/features/tests/registry.ts` (o `title` exato da entrada `t-student`: `'t de Student'`, com `t` minúsculo)
    - `.planning/phases/07-baseline-verde/07-PATTERNS.md` §`src/app/router.test.tsx` (os landmarks candidatos, com linha exata)
  </read_first>
  <action>
    Substituir o caso único da linha 40 por **dois** casos independentes, mantendo os outros 4 casos do arquivo (meta-análise, variáveis, mapas, rota inexistente) intocados:

    Caso (a) — a rota: título mencionando "landing route (D-04 da Fase 1)". Renderiza `/` e prova que a página da Estatística montou, sem depender de qual teste está ativo. Usar dois landmarks já existentes: o nó `#lacir-test-module-mount` (via `container.querySelector`, desestruturando o retorno de `renderAt('/')`) não pode ser nulo, e `screen.getByRole('button', { name: 'Qual teste usar?' })` está no documento. Nenhuma asserção sobre heading neste caso.

    Caso (b) — o teste padrão: título mencionando "teste ativo padrão é t-student (D-10/D-12)". Renderiza `/` e prova o comportamento atual por duas vias que quebram juntas se o padrão mudar: `expect(container.querySelector('#lacir-test-module-mount')).toHaveAttribute('data-active-test-id', 't-student')` e `expect(screen.getByRole('heading', { name: 't de Student' })).toBeInTheDocument()`.

    Não usar `screen.getByRole('link', { name: 'Estatística' })` como prova da rota — esse link vem do `Header` e aparece em **toda** rota, então não distingue nada.

    Nenhuma mudança em `EstatisticaPage.tsx` nem em qualquer arquivo de produção (D-10). Não reintroduzir o fallback `?? 'Estatística'` removido em 07-01.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/app/router.test.tsx` sai com código 0, com 6 testes passando e 0 skipped
    - `grep -c "it(" src/app/router.test.tsx` retorna 6 (os 5 originais viraram 6 ao desdobrar o caso da landing)
    - `grep -c "getByRole('heading', { name: 'Estatística' })" src/app/router.test.tsx` retorna 0
    - `grep -c "lacir-test-module-mount" src/app/router.test.tsx` retorna 2 ou mais
    - `grep -c "data-active-test-id', 't-student'" src/app/router.test.tsx` retorna 1
    - `git diff --stat src/routes/estatistica/EstatisticaPage.tsx` não retorna nada (nenhum código de produção mudou nesta task)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/router.test.tsx</automated>
  </verify>
  <done>A rota e o teste padrão têm asserções separadas; quando uma quebrar, o nome do caso diz qual das duas.</done>
</task>

<task type="auto">
  <name>Task 2: Provar suíte verde E silenciosa e eliminar avisos residuais de act()</name>
  <files>src/app/router.test.tsx</files>
  <read_first>
    - `.planning/phases/07-baseline-verde/07-VALIDATION.md` (§"Sampling Rate" e a linha de D-08 na tabela por task)
    - `.planning/phases/07-baseline-verde/07-RESEARCH.md` §Validation Architecture → "Como provar 'suíte verde E silenciosa' (D-08)" (os comandos exatos, com expectativa invertida)
    - `.planning/phases/07-baseline-verde/07-RESEARCH.md` §Assumptions Log linha A2 (por que se espera que os avisos de `act` sumam sozinhos)
    - `src/test/setup.ts` (o stub já corrigido em 07-02 — nenhuma alteração adicional deve ser necessária aqui)
    - Para cada aviso residual: o arquivo de teste que o emitiu, localizado com o comando de diagnóstico abaixo
  </read_first>
  <action>
    Rodar a suíte inteira capturando a saída (`npx vitest run 2>&1 | tee /tmp/vitest-out.txt`) e conferir três contagens: falhas, `Not implemented: HTMLCanvasElement` e `was not wrapped in act`. Usar o **reporter padrão** — `--reporter=basic` foi removido no Vitest 4 e falha no startup.

    Para cada aviso `was not wrapped in act` que sobrar, localizar o arquivo e o caso responsáveis com `grep -B 20 "was not wrapped in act" /tmp/vitest-out.txt` e corrigir na origem: a causa é uma atualização de estado disparada fora de um `user.*`/`findBy*` (timer que resolve depois da asserção, promessa de import dinâmico, callback de `ResizeObserver`). A correção aceitável é esperar o efeito com `findBy*`/`waitFor` sobre uma afordância real, ou envolver a ação disparadora em `act()` quando ela não é uma interação de usuário. **Não** é aceitável: silenciar `console.error`, filtrar o aviso, adicionar `vi.spyOn(console, 'error')` para engolir saída, nem marcar o teste como conhecido.

    Confirmar também que a regra de QA-03 vale para o repositório inteiro: nenhum `it.skip`, `it.todo`, `xit(`, `describe.skip`, `test.skip` ou `.failing(` em nenhum arquivo de teste, e nenhum comentário do tipo "falha conhecida"/"known failure" acompanhando teste desativado.

    Registrar na SUMMARY as três contagens finais (falhas, canvas, act) e, se algum aviso de act exigiu correção, qual arquivo e qual foi a causa real — é esse registro que torna a hipótese A2 da pesquisa verificada em vez de assumida.
  </action>
  <acceptance_criteria>
    - `npm run test:run` sai com código 0
    - `npx vitest run 2>&1 | grep -c "was not wrapped in act"` retorna 0 (contra 21 do ground truth)
    - `npx vitest run 2>&1 | grep -c "Not implemented: HTMLCanvasElement"` retorna 0 (contra 190 do ground truth)
    - `npx vitest run 2>&1 | grep -E "Tests +[0-9]+ passed"` mostra 0 failed e 0 skipped
    - `grep -rn "it.skip\|it.todo\|xit(\|describe.skip\|test.skip\|\.failing(" src` não retorna nenhuma linha
    - `npm run typecheck` sai com código 0
    - `git diff --stat package-lock.json` não retorna nada
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <done>Suíte inteira verde, zero avisos de canvas, zero avisos de act, zero testes pulados ou anotados como falha conhecida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suíte → afirmação "baseline verde" | Se o verde vier de teste pulado, asserção afrouxada ou aviso suprimido, o milestone inteiro passa a raciocinar sobre um sinal falso |
| `router.test.tsx` → decisão travada D-04 da Fase 1 | O teste é o único registro executável de "Landing route = Estatística"; asseverar a coisa errada já aconteceu uma vez |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Repudiation | `src/app/router.test.tsx` | mitigate | Duas asserções independentes (landmark da página × `data-active-test-id`) — uma quebra não pode mascarar a outra, que é exatamente o defeito de D-09 |
| T-07-06-02 | Tampering | contagem de avisos | mitigate | Critérios de aceite proíbem explicitamente supressão de `console.error` e filtro de saída; a prova é `grep -c` na saída bruta do runner |
| T-07-06-03 | Repudiation | repositório inteiro | mitigate | Varredura por `it.skip`/`it.todo`/`xit`/`.failing(` em todo `src/` — nenhum verde pode vir de teste desligado (QA-03) |
| T-07-06-SC | Tampering | instalação de pacotes npm | mitigate | Nenhum pacote instalado; `git diff --stat package-lock.json` vazio |

Nenhum item de severidade alta: plano restrito a arquivos de teste, sem superfície de produção.
</threat_model>

<verification>
- `npm run test:run` → exit 0, 0 failed, 0 skipped
- `npx vitest run 2>&1 | grep -c "was not wrapped in act"` → 0
- `npx vitest run 2>&1 | grep -c "Not implemented: HTMLCanvasElement"` → 0
- `grep -rn "it.skip\|it.todo\|xit(\|describe.skip\|test.skip\|\.failing(" src` → sem resultado
- `npm run typecheck` → exit 0
</verification>

<success_criteria>
- QA-02 fechado no nível da suíte: `npm run test:run` passa integralmente
- QA-03 fechado: nenhum teste vermelho "conhecido", nenhum pulado, nenhum afrouxado
- D-08 fechada por completo: verde **e** silenciosa
- D-09/D-10/D-12 implementadas: causa raiz registrada em teste, sem mudança de produção
</success_criteria>

<output>
Criar `.planning/phases/07-baseline-verde/07-06-SUMMARY.md` ao terminar.
</output>
