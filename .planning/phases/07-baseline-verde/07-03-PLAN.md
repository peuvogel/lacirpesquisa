---
phase: 07-baseline-verde
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/shared/flow/FlowSteps.tsx
  - src/shared/flow/FlowSteps.test.tsx
  - src/features/tests/t-student/TStudentTest.tsx
  - src/features/tests/correlacao/CorrelacaoTest.tsx
  - src/features/tests/prais-winsten/PraisWinstenTest.tsx
  - src/features/tests/qui-quadrado/QuiQuadradoTest.tsx
  - src/features/tests/anova-tukey/AnovaTukeyTest.tsx
  - src/features/tests/kruskal-dunn/KruskalDunnTest.tsx
  - src/features/tests/poisson/PoissonTest.tsx
  - src/features/tests/binomial-negativa/BinomialNegativaTest.tsx
  - src/features/tests/logistica/LogisticaTest.tsx
  - src/routes/estatistica/demo/TesteDemo.tsx
autonomous: true
requirements: [QA-02]

must_haves:
  truths:
    - "O modo `layout=\"stepper\"` não existe mais no repositório — nenhum teste pode dirigir uma segunda realidade que a aplicação não tem (D-01, D-05)"
    - "A limpeza é da API inteira, não só do ramo: saem `layout`, `onStepChange`, `FLOW_STEP_LABELS`, o import de `Check` e `effectiveCanAdvance` (D-02)"
    - "`FlowStepsProps` fica com exatamente 5 campos: `active`, `canAdvance`, `dados`, `configurar`, `resultados` (D-02)"
    - "Os 10 call sites deixam de repassar `onStepChange`, mas cada módulo mantém seu `setActiveStep` interno intacto (D-02)"
    - "A cobertura de `FlowSteps.test.tsx` sobre o layout scroll cresce: 5 testes vivos onde havia 2 (D-03)"
    - "Nenhum comportamento de produção muda — o ramo removido era inalcançável, e nenhum caminho novo de volta ao painel de colagem é adicionado (D-04)"
    - "`npm run typecheck` continua em exit 0 depois da remoção"
  artifacts:
    - path: "src/shared/flow/FlowSteps.tsx"
      provides: "Layout scroll como única forma, sem branch"
      contains: "aria-label=\"Resultados\""
    - path: "src/shared/flow/FlowSteps.test.tsx"
      provides: "5 casos vivos cobrindo o layout scroll"
      min_lines: 60
  key_links:
    - from: "src/shared/flow/FlowSteps.test.tsx"
      to: "src/shared/flow/FlowSteps.tsx"
      via: "getByRole('region', { name: 'Resultados' }) + id lacir-flow-results"
      pattern: "lacir-flow-results"
    - from: "10 call sites"
      to: "src/shared/flow/FlowSteps.tsx"
      via: "<FlowSteps active={activeStep} canAdvance={canAdvance} ...>"
      pattern: "<FlowSteps"
---

<objective>
Remover o modo `layout="stepper"` como código morto, com limpeza completa da API (D-01/D-02), e fazer a cobertura de scroll crescer para compensar os 5 testes de stepper que saem (D-03).

Purpose: enquanto o prop `layout` existir, existe uma segunda realidade que os testes das Fases 8–12 podem dirigir — e foi exatamente isso que produziu as 23 falhas herdadas. A guarda contra a recorrência é a própria remoção, não uma convenção escrita (D-05).
Output: `FlowSteps` com um só caminho de render, 10 call sites simplificados, e `FlowSteps.test.tsx` cobrindo mais do que cobria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-baseline-verde/07-CONTEXT.md
@.planning/phases/07-baseline-verde/07-RESEARCH.md
@.planning/phases/07-baseline-verde/07-PATTERNS.md

Decisões travadas que este plano implementa: **D-01** (stepper removido como código morto), **D-02** (limpeza completa da API, não só do ramo), **D-03** (cobertura de scroll cresce), **D-04** (a ausência de caminho não-destrutivo de volta ao painel é aceita como está — nada de UX nova nesta fase), **D-05** (a guarda é a remoção, sem teste de fumaça extra nem regra no CLAUDE.md).

Depende de 07-01 apenas para o gate: só depois que os 2 erros herdados de typecheck saem é que `npm run typecheck` vira sinal utilizável para provar esta remoção.

Verificado por grep antes do planejamento: **nenhum call site de produção passa `layout`** — as 7 ocorrências de `layout=` no repositório estão todas dentro de `FlowSteps.test.tsx`. Os 10 call sites só precisam perder `onStepChange`.

**Estado esperado da suíte ao fim desta onda (leia antes de interpretar um vermelho):** ao terminar a Wave 2, os 23 testes de módulo continuam vermelhos. Isso é **dívida herdada carregada uma onda adiante, não uma regressão nova causada por este plano**. O motivo é que a produção nunca passou `layout="stepper"` — o botão de nav `Configurar` que esses 23 testes clicam já não existia em runtime antes desta edição, então remover o ramo morto não muda nada sobre por que eles falham. Eles ficam verdes na Wave 3, quando `07-04` e `07-05` os reescrevem para o idioma de `runToResultados`. O sinal utilizável desta onda são os 2 módulos que já estavam verdes (`t-student`, `correlacao`), o `FlowSteps.test.tsx` reescrito e o `npm run typecheck` — não a contagem total da suíte.

Escopo da Task 1 revisado e aceito deliberadamente: 12 arquivos numa task só, porque a edição nos 10 call sites é mecanicamente idêntica (apagar uma linha `onStepChange={setActiveStep}` em cada) e a contagem é provada por `grep -rc "<FlowSteps" src`; dividir a task só multiplicaria a leitura do mesmo contexto.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover o ramo stepper e a API morta de FlowSteps, e limpar os 10 call sites</name>
  <files>src/shared/flow/FlowSteps.tsx, src/features/tests/t-student/TStudentTest.tsx, src/features/tests/correlacao/CorrelacaoTest.tsx, src/features/tests/prais-winsten/PraisWinstenTest.tsx, src/features/tests/qui-quadrado/QuiQuadradoTest.tsx, src/features/tests/anova-tukey/AnovaTukeyTest.tsx, src/features/tests/kruskal-dunn/KruskalDunnTest.tsx, src/features/tests/poisson/PoissonTest.tsx, src/features/tests/binomial-negativa/BinomialNegativaTest.tsx, src/features/tests/logistica/LogisticaTest.tsx, src/routes/estatistica/demo/TesteDemo.tsx</files>
  <read_first>
    - `src/shared/flow/FlowSteps.tsx` (arquivo inteiro, 117 linhas — o import de `Check` na linha 2, `FLOW_STEP_LABELS` nas 9-13, `onStepChange` na 17, o bloco de doc + `layout?: 'scroll' | 'stepper'` nas 22-26, `effectiveCanAdvance` na 44, o `useEffect` de `scrollIntoView` nas 47-54, o ramo stepper nas 56-94 e o corpo scroll nas 96-116)
    - `src/features/tests/poisson/PoissonTest.tsx` (linhas 225-233 — a forma exata da chamada `<FlowSteps active={activeStep} onStepChange={setActiveStep} canAdvance={canAdvance} ...>`, idêntica nos 10)
    - `.planning/phases/07-baseline-verde/07-PATTERNS.md` §`src/shared/flow/FlowSteps.tsx` (o "o que sai" e o "o que fica", com âncoras de linha)
    - Localizar os 10 call sites com `grep -rn "onStepChange={setActiveStep}" src` antes de editar
  </read_first>
  <action>
    Em `FlowSteps.tsx`, remover: o import de `Check` de `lucide-react`; a constante exportada `FLOW_STEP_LABELS` inteira; o campo `onStepChange` de `FlowStepsProps` e seu parâmetro desestruturado; o campo `layout?: 'scroll' | 'stepper'` (com o bloco de doc que o descreve), seu valor default e seu uso; a constante `effectiveCanAdvance`; e o bloco `if (layout === 'stepper') { ... }` por completo (nav + `<section>{content}</section>`).

    O corpo que hoje é o "else" implícito vira o único corpo da função, sem `if`: `showConfig = canAdvance.configurar`, `showResults = canAdvance.resultados || active === 'resultados'`, e o `return` com `<div className="space-y-10">` contendo `<section aria-label="Dados e configuração" className="space-y-4">` e, condicionalmente, `<section ref={resultsRef} aria-label="Resultados" id="lacir-flow-results" className="scroll-mt-6 space-y-4 border-t border-border pt-8">`. Trocar todas as leituras de `effectiveCanAdvance` por `canAdvance` direto — a entrada `dados: true` existia só para a nav e nunca era lida aqui.

    No `useEffect` de `scrollIntoView`, remover a guarda `if (layout !== 'scroll') return;` e tirar `layout` do array de dependências. Manter `resultsRef`, o id `lacir-flow-results`, o `aria-label` das duas seções e as classes exatamente como estão — `FlowSteps.test.tsx` e o helper `runToResultados` (plano 07-02) dependem dos dois rótulos e do id.

    `FlowStepsProps` sai com exatamente 5 campos: `active`, `canAdvance`, `dados`, `configurar`, `resultados`. `FlowStep` continua exportado.

    Nos 10 call sites, apagar **só** a linha `onStepChange={setActiveStep}` de dentro de `<FlowSteps ...>`. Manter `active={activeStep}`, `canAdvance={canAdvance}` e os três slots. Não tocar em `useState<FlowStep>(...)` nem em nenhuma chamada interna de `setActiveStep(...)` (`setActiveStep('resultados')`, `setActiveStep('dados')`, o soft-reset `if (activeStep === 'resultados') setActiveStep('configurar')`) — elas continuam governando qual conteúdo a seção 'Dados e configuração' mostra (D-02: o `setActiveStep` interno permanece, só deixa de ser repassado).

    Não adicionar nenhum caminho novo de volta ao painel de colagem: `ClearDataButton` continua sendo o único, com seu diálogo de confirmação (D-04). Não adicionar teste de fumaça nem regra em CLAUDE.md contra a reintrodução do stepper — a remoção é a guarda (D-05).
  </action>
  <acceptance_criteria>
    - `grep -rc "stepper" src` não retorna nenhuma linha com contagem maior que 0
    - `grep -rc "FLOW_STEP_LABELS" src` não retorna nenhuma linha com contagem maior que 0
    - `grep -rc "onStepChange" src/features src/routes src/shared/flow/FlowSteps.tsx` não retorna nenhuma linha com contagem maior que 0
    - `grep -c "effectiveCanAdvance" src/shared/flow/FlowSteps.tsx` retorna 0
    - `grep -c "Check" src/shared/flow/FlowSteps.tsx` retorna 0
    - `grep -c "aria-label=\"Resultados\"" src/shared/flow/FlowSteps.tsx` retorna 1 e `grep -c "lacir-flow-results" src/shared/flow/FlowSteps.tsx` retorna 1
    - `grep -c "layout" src/shared/flow/FlowSteps.tsx` retorna 0
    - `grep -rc "<FlowSteps" src` lista exatamente 10 arquivos com contagem 1
    - `npx vitest run src/features/tests/t-student/TStudentTest.test.tsx src/features/tests/correlacao/CorrelacaoTest.test.tsx` sai com código 0 (os dois módulos que já estavam verdes seguem verdes)
    - `npm run typecheck 2>&1 | grep -c "error TS"` retorna 0 depois que `FlowSteps.test.tsx` for ajustado na Task 2; ao fim **desta** task o único arquivo com erro permitido é `src/shared/flow/FlowSteps.test.tsx` (ainda passa `layout` e `onStepChange`), verificável com `npm run typecheck 2>&1 | grep "error TS" | grep -vc "FlowSteps.test.tsx"` retornando 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/features/tests/t-student/TStudentTest.test.tsx src/features/tests/correlacao/CorrelacaoTest.test.tsx</automated>
  </verify>
  <done>O prop `layout` e o prop `onStepChange` não existem em lugar nenhum do `src/`; o único erro de typecheck restante está em `FlowSteps.test.tsx`, que a Task 2 fecha.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever FlowSteps.test.tsx — remover os 5 casos de stepper e fazer a cobertura de scroll crescer</name>
  <files>src/shared/flow/FlowSteps.test.tsx</files>
  <read_first>
    - `src/shared/flow/FlowSteps.test.tsx` (arquivo inteiro — `describe('FlowSteps stepper layout')` nas linhas 8-103 é o que sai; `describe('FlowSteps scroll layout')` nas 105-140 é o molde dos casos novos)
    - `src/shared/flow/FlowSteps.tsx` (já editado pela Task 1 — as duas `<section>` e o `useEffect` de `scrollIntoView`)
    - `src/routes/mapas/ReviewAnalysisDialog.test.tsx` (linha 118 — `Element.prototype.scrollIntoView = vi.fn();`, o padrão de mock já usado no repositório)
    - `.planning/phases/07-baseline-verde/07-PATTERNS.md` §`src/shared/flow/FlowSteps.test.tsx` (os 3 casos exigidos por D-03, com a técnica de asserção de cada um)
  </read_first>
  <action>
    Apagar o `describe('FlowSteps stepper layout', ...)` inteiro (os 5 casos que clicam na nav e leem `aria-current`). Nos 2 casos que ficam, remover os props `layout="scroll"` e `onStepChange={() => {}}` das chamadas de `<FlowSteps>` — os dois props não existem mais.

    Adicionar ao `describe` de scroll os 3 casos exigidos por D-03, na mesma forma dos existentes (`render` direto, sem `userEvent`, `canAdvance` inline):

    1. `active="resultados"` com `canAdvance` tendo `resultados: false` renderiza o conteúdo de Resultados — cobre isoladamente o lado `active === 'resultados'` da disjunção `showResults`.
    2. a seção de resultados é alcançável por `screen.getByRole('region', { name: 'Resultados' })` e esse mesmo elemento satisfaz `toHaveAttribute('id', 'lacir-flow-results')` — trava o rótulo acessível e o id de que o helper `runToResultados` e o scroll dependem.
    3. `scrollIntoView` é chamado ao chegar em resultados: mockar `Element.prototype.scrollIntoView = vi.fn()` (mesmo padrão de `ReviewAnalysisDialog.test.tsx:118`), renderizar com resultados visíveis e assertar `toHaveBeenCalled()`.

    Nenhum caso pode ser marcado com `.skip`, `.todo` ou comentado (QA-03). O arquivo termina com pelo menos 5 casos vivos, todos sobre o layout scroll — contra os 2 que existiam, o que satisfaz a regra de D-03 de não sair da fase com cobertura líquida menor.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/shared/flow/FlowSteps.test.tsx` sai com código 0 e reporta 5 ou mais testes passando, 0 skipped
    - `grep -c "it(" src/shared/flow/FlowSteps.test.tsx` retorna 5 ou mais
    - `grep -c "stepper\|aria-current\|layout=\|onStepChange" src/shared/flow/FlowSteps.test.tsx` retorna 0
    - `grep -c "it.skip\|it.todo\|describe.skip" src/shared/flow/FlowSteps.test.tsx` retorna 0
    - `grep -c "lacir-flow-results" src/shared/flow/FlowSteps.test.tsx` retorna 1 ou mais
    - `grep -c "scrollIntoView" src/shared/flow/FlowSteps.test.tsx` retorna 1 ou mais
    - `npm run typecheck` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/shared/flow/FlowSteps.test.tsx && npm run typecheck</automated>
  </verify>
  <done>Cinco ou mais casos vivos cobrindo só o layout que a aplicação de fato usa, typecheck verde, e zero vestígio do idioma de stepper no arquivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `FlowSteps` ← 10 módulos de teste estatístico | Componente compartilhado por toda a superfície de Estatística; uma remoção errada quebra os 10 de uma vez |
| API pública de `FlowSteps` ← testes | Enquanto `layout` existir, um teste pode exercitar um caminho que nenhum usuário alcança — a origem exata das 23 falhas herdadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | `src/shared/flow/FlowSteps.tsx` | mitigate | A remoção preserva verbatim `aria-label` das duas seções, o id `lacir-flow-results` e o `resultsRef`; critérios de aceite provam cada um por grep, e os 2 módulos que já estavam verdes servem de canário |
| T-07-03-02 | Repudiation | superfície de teste do `FlowSteps` | mitigate | D-05: com o prop `layout` fora, deixa de existir um segundo caminho que um teste possa dirigir; a guarda é estrutural, não uma convenção que alguém possa esquecer |
| T-07-03-03 | Denial of Service | 10 módulos de Estatística | accept | Um erro na edição mecânica dos call sites derruba o typecheck imediatamente (falha alta e barata), não em runtime na aula |
| T-07-03-SC | Tampering | instalação de pacotes npm | mitigate | Nenhum pacote instalado; provado por `git diff --stat package-lock.json` vazio |

Nenhum item de severidade alta: o plano só remove código morto de uma camada Browser/Client, sem tocar entrada de usuário, rede, sessão ou criptografia.
</threat_model>

<verification>
- `npm run typecheck` → exit 0
- `npx vitest run src/shared/flow/FlowSteps.test.tsx` → exit 0, ≥ 5 testes, 0 skipped
- `grep -rc "stepper\|FLOW_STEP_LABELS\|onStepChange" src` → nenhuma contagem maior que 0
- `git diff --stat package-lock.json` → vazio
</verification>

<success_criteria>
- D-01/D-02 cumpridas: o modo stepper e toda a sua API saíram do repositório
- D-03 cumprida: 5 ou mais casos vivos sobre o layout scroll, contra os 2 que existiam
- D-04/D-05 respeitadas: nenhuma UX nova, nenhuma guarda por convenção
- Os 10 call sites compilam e os 2 módulos que já estavam verdes continuam verdes
</success_criteria>

<output>
Criar `.planning/phases/07-baseline-verde/07-03-SUMMARY.md` ao terminar.
</output>
