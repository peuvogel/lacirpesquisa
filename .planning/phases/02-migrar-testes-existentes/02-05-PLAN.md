---
phase: 02-migrar-testes-existentes
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/features/tests/prais-winsten/praisConfig.ts
  - src/features/tests/prais-winsten/praisEngine.ts
  - src/features/tests/prais-winsten/praisEngine.test.ts
  - src/features/tests/prais-winsten/praisInterpretation.ts
  - src/features/tests/prais-winsten/praisInterpretation.test.ts
  - src/features/tests/prais-winsten/praisCharts.ts
  - src/features/tests/prais-winsten/SeriesPreviewTable.tsx
  - src/features/tests/prais-winsten/PraisWinstenConfigPanel.tsx
  - src/features/tests/prais-winsten/PraisWinstenTest.tsx
  - src/features/tests/prais-winsten/PraisWinstenTest.test.tsx
autonomous: true
requirements: [TEST-03]
user_setup: []

must_haves:
  truths:
    - "User runs Prais-Winsten with model alpha/beta/rho matching v1.0 at display precision"
    - "Configurar shows series preview, full legacy knobs within shared shell, α and research question"
    - "Resultados shows trend + residual charts with customization and PNG export"
    - "Temporal series requires ≥3 valid points (UI-SPEC error copy)"
  artifacts:
    - path: "src/features/tests/prais-winsten/praisEngine.ts"
      provides: "parseTemporalValue + statsEngine.praisWinsten"
    - path: "src/features/tests/prais-winsten/PraisWinstenTest.tsx"
      provides: "FlowSteps orchestrator"
    - path: "src/features/tests/prais-winsten/SeriesPreviewTable.tsx"
      provides: "Mono preview table max 8 rows"
  key_links:
    - from: "praisEngine.ts"
      to: "src/shared/stats/statsEngine.ts"
      via: "praisWinsten(time, values)"
      pattern: "statsEngine\\.praisWinsten"
    - from: "praisEngine.test.ts"
      to: "src/test/fixtures/tests/prais-exemplo.txt"
      via: "model field parity"
      pattern: "prais-exemplo"
---

<objective>
Ship the Prais-Winsten module (TEST-03) on the shared shell with series preview, full Configurar knobs (Claude's discretion on layout), dual charts (trend + residuals), and parity via statsEngine.praisWinsten + fixtures.

Purpose: Prais-Winsten validates temporal series parsing and AR(1) corrected regression. Runs parallel with Correlação (02-04) — no file overlap.
Output: Complete prais-winsten feature module with engine tests and RTL smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-migrar-testes-existentes/02-CONTEXT.md
@.planning/phases/02-migrar-testes-existentes/02-PATTERNS.md
@.planning/phases/02-migrar-testes-existentes/02-UI-SPEC.md
@tests/prais-winsten/module.js
@tests/prais-winsten/config.json
@src/test/fixtures/tests/prais-exemplo.txt
</context>

<interfaces>
Legacy call site (tests/prais-winsten/module.js ~1076):
- stats.praisWinsten(dataset.time, dataset.values)
- fitted = time.map(t => Math.pow(10, model.alpha + model.beta * t))

PraisEngine output:
- model: { alpha, beta, rho, se, t, p, ... } per legacy shape
- fitted[], residuals[]
- metrics[], interpretation: string[]

SeriesPreviewTable: max 8 rows, IBM Plex Mono, "… e mais {n} linhas" truncation (UI-SPEC)

derivePraisSeries from datasusNormalizer for TABNET path
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port Prais-Winsten engine and parity tests</name>
  <files>src/features/tests/prais-winsten/praisConfig.ts, src/features/tests/prais-winsten/praisEngine.ts, src/features/tests/prais-winsten/praisEngine.test.ts</files>
  <read_first>
    - `tests/prais-winsten/module.js` runAnalysis ~1076, parseTemporalValue, dataset builder
    - `tests/prais-winsten/config.json` (exampleRows, didacticCards, defaultQuestion)
    - `src/shared/stats/statsEngine.ts` praisWinsten
    - `src/test/fixtures/tests/prais-exemplo.txt` and tabnet/prais-derive.txt
    - `02-RESEARCH.md` legacy fitted line formula
  </read_first>
  <action>
    1. praisConfig.ts: TABULAR_OPTIONS for time + value columns; export config constants; chart preset metadata.

    2. praisEngine.ts:
       - parseTemporalSeries(confirmedDataset): map rows to { time: number[], values: number[] } using parseTemporalValue logic ported from legacy
       - runPraisWinsten(time, values): call statsEngine.praisWinsten; compute fitted on log scale as legacy: Math.pow(10, alpha + beta * t)
       - computeResiduals(observed, fitted)
       - buildMetrics(model): beta, rho, p with display formatters
       - validateSeries: ≥3 valid points or return UI-SPEC error "A série temporal precisa de pelo menos 3 pontos válidos."
       - DataSUS: derivePraisSeries when applicable

    3. praisEngine.test.ts:
       - Assert model alpha, beta, rho, p match legacy Stats output on prais-exemplo at display precision
       - Assert fitted values match legacy formula for each time point
       - TABNET derive fixture covered
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/prais-winsten/praisEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Model coefficients match legacy on exemplo fixture
    - [ ] Fitted series values match legacy pow(10, alpha+beta*t)
    - [ ] Series &lt;3 points returns validation error string
    - [ ] TABNET derive path tested
  </acceptance_criteria>
  <done>Prais-Winsten engine verified at display precision</done>
</task>

<task type="auto">
  <name>Task 2: Port interpretation, charts, and series preview</name>
  <files>src/features/tests/prais-winsten/praisInterpretation.ts, src/features/tests/prais-winsten/praisInterpretation.test.ts, src/features/tests/prais-winsten/praisCharts.ts, src/features/tests/prais-winsten/SeriesPreviewTable.tsx</files>
  <read_first>
    - `tests/prais-winsten/module.js` interpretation builders
    - `02-UI-SPEC.md` Prais charts (trend + residual tabs, log axis toggle, zero-line)
    - `src/routes/estatistica/ColumnPreviewTable.tsx` (preview pattern)
    - `src/shared/charts/chartFactories/timeseriesChart.ts`, residualChart.ts
  </read_first>
  <action>
    1. praisInterpretation.ts: string[] paragraphs; trend direction conclusion; key stats formatted; no HTML.

    2. praisCharts.ts: two chart slots — trend (observed+fitted) and residual bar; presets for customizer; toggles: linha ajustada, rótulos, linha zero resíduos, escala log Y on trend only.

    3. SeriesPreviewTable.tsx: read-only mono table; time + value columns; max 8 rows + truncation message per UI-SPEC; used in Configurar before confirm.

    4. Create `praisInterpretation.test.ts`:
       - Assert **trend/significance conclusion parity** with legacy interpretation for prais-exemplo at α=0.05 (D-07, D-10: same direction/significance call, not byte-identical prose)
       - Assert formatted beta, rho, p values appear at display precision (D-08)
       - Assert no HTML tags in output paragraphs
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/prais-winsten/praisInterpretation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] SeriesPreviewTable truncates at 8 rows with correct PT message
    - [ ] Trend and residual chart presets exported
    - [ ] Interpretation plain string array with conclusion parity to legacy (trend direction + significance)
    - [ ] Key stats (beta, rho, p) present at display precision per D-08
  </acceptance_criteria>
  <done>Prais interpretation, dual charts, and series preview ready</done>
</task>

<task type="auto">
  <name>Task 3: Wire PraisWinstenTest UI and RTL smoke test</name>
  <files>src/features/tests/prais-winsten/PraisWinstenConfigPanel.tsx, src/features/tests/prais-winsten/PraisWinstenTest.tsx, src/features/tests/prais-winsten/PraisWinstenTest.test.tsx</files>
  <read_first>
    - `src/features/tests/t-student/TStudentTest.tsx` orchestrator pattern
    - `02-UI-SPEC.md` § Prais-Winsten (series preview, column role confirm)
    - `02-CONTEXT.md` Claude's discretion on series UX layout
  </read_first>
  <action>
    1. PraisWinstenConfigPanel: AlphaSelector; ResearchQuestionField; DidacticCards; SeriesPreviewTable; ColumnPreviewTable for role confirm; full legacy knobs affecting analysis ported into stacked sections (period, log transform if legacy exposes, etc. — read module.js config surface).

    2. PraisWinstenTest.tsx: FlowSteps; Usar exemplo from praisConfig; Resultados with ResultsPanelWithCustomizer — tab or toggle between trend/residual charts sharing customizer state per UI-SPEC; exportFilename prais-winsten-lacirstat.png.

    3. PraisWinstenTest.test.tsx: smoke Usar exemplo → Analisar → interpretation + PNG; series preview visible on Configurar.

    Awaiting EstatisticaPage wiring in 02-06.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/prais-winsten/PraisWinstenTest.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] RTL smoke test passes
    - [ ] Series preview renders in Configurar
    - [ ] Both chart views reachable in Resultados
  </acceptance_criteria>
  <done>TEST-03 module complete and smoke-tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Temporal paste → time/value arrays | User data drives regression iteration |
| Log axis toggle | Transforms displayed values only when data supports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | praisInterpretation | mitigate | string[] only |
| T-02-02 | Denial of Service | Long time series | mitigate | parseTabular row limits; cap series length before praisWinsten iteration (match legacy if it caps; else reasonable bound e.g. 10k points with guard) |
| T-02-06 | Tampering | parseTemporalValue | mitigate | Reject non-numeric time/value; return validation error not NaN propagation |
</threat_model>

<verification>
- `npm run test:run -- src/features/tests/prais-winsten/` all green
- No file overlap with 02-04 correlacao (parallel wave 3)
</verification>

<success_criteria>
- TEST-03: Prais-Winsten with parity, series preview, dual customizable charts, interpretation, PNG
</success_criteria>

<output>
Create `.planning/phases/02-migrar-testes-existentes/02-05-SUMMARY.md` when done
</output>
