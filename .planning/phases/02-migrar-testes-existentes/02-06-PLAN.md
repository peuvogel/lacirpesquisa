---
phase: 02-migrar-testes-existentes
plan: 06
type: execute
wave: 4
depends_on: ["02-03", "02-04", "02-05"]
files_modified:
  - src/features/tests/registry.ts
  - src/features/tests/registry.test.ts
  - src/routes/estatistica/EstatisticaPage.tsx
  - src/routes/estatistica/SidebarTestLink.tsx
  - src/routes/estatistica/Sidebar.tsx
  - src/routes/estatistica/QualTesteModal.tsx
  - src/routes/mapas/IniciarPesquisaModal.tsx
  - src/routes/estatistica/EstatisticaPage.test.tsx
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
user_setup: []

must_haves:
  truths:
    - "t-student, correlacao, prais-winsten show Disponível in sidebar and Qual teste modal (D-17)"
    - "Teste demo keeps Demonstração badge and remains default landing (D-15, D-16)"
    - "EstatisticaPage renders all four available test modules via static switch"
    - "Mapas Iniciar pesquisa handoff lands on suggested test Configurar when available, else t-Student"
    - "Full test suite green including extended registry tests"
  artifacts:
    - path: "src/features/tests/registry.ts"
      provides: "status available for three migrated tests"
      contains: "status: 'available'"
    - path: "src/routes/estatistica/EstatisticaPage.tsx"
      provides: "renderActiveTest switch for demo + 3 migrated modules"
  key_links:
    - from: "EstatisticaPage.tsx"
      to: "TStudentTest|CorrelacaoTest|PraisWinstenTest"
      via: "static import switch on activeTestId"
      pattern: "case 't-student'|case 'correlacao'|case 'prais-winsten'"
    - from: "Sidebar.tsx"
      to: "TEST_REGISTRY"
      via: "isTestAvailable gate"
      pattern: "TEST_REGISTRY"
---

<objective>
Flip registry status, wire all migrated modules into EstatisticaPage, add demo Demonstração badge (D-17), preserve demo as default landing (D-16), and integrate Mapas handoff routing — completing Phase 2 integration.

Purpose: Individual modules from 02-03/04/05 are useless until registry unlocks sidebar/modal and EstatisticaPage mounts them. Single registry edit prevents Pitfall 4 drift.
Output: Production-ready Phase 2 routing with 4 available tests (demo + 3 real).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-migrar-testes-existentes/02-CONTEXT.md
@.planning/phases/02-migrar-testes-existentes/02-UI-SPEC.md
@.planning/phases/02-migrar-testes-existentes/02-RESEARCH.md
@src/features/tests/registry.ts
@src/routes/estatistica/EstatisticaPage.tsx
@src/features/tests/registry.test.ts
</context>

<interfaces>
TEST_REGISTRY entries to flip (lines 39-60):
- t-student, correlacao, prais-winsten: status 'em-breve' → 'available'
- demo: stays 'available'

EstatisticaPage renderActiveTest switch (RESEARCH Pattern 5):
- case 'demo': TesteDemo
- case 't-student': TStudentTest
- case 'correlacao': CorrelacaoTest
- case 'prais-winsten': PraisWinstenTest

SidebarTestLink badge logic:
- demo id → Badge "Demonstração" (muted outline)
- other available → Badge "Disponível" (teal)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Flip TEST_REGISTRY and extend registry tests</name>
  <files>src/features/tests/registry.ts, src/features/tests/registry.test.ts</files>
  <read_first>
    - `src/features/tests/registry.ts` lines 29-60
    - `src/features/tests/registry.test.ts` (current expects only demo available)
    - `02-CONTEXT.md` D-15, D-17
    - `02-PATTERNS.md` § registry flip
  </read_first>
  <action>
    1. Update TEST_REGISTRY: set status 'available' for t-student, correlacao, prais-winsten. Do NOT change ids, titles, groups, or phase fields.

    2. Extend registry.test.ts:
       - Expect exactly 4 available entries: demo, t-student, correlacao, prais-winsten
       - Assert isTestAvailable('t-student') === true
       - Assert phase 3 tests still em-breve
       - Assert demo remains in group 'Demonstração'

    3. Add optional badge helper export getTestBadgeLabel(entry) if needed by Sidebar — demo → 'Demonstração', available non-demo → 'Disponível', em-breve → 'Em breve' (existing pattern).
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Three migrated tests status available
    - [ ] registry.test.ts updated and passing
    - [ ] No duplicate test lists introduced elsewhere
  </acceptance_criteria>
  <done>Registry reflects Phase 2 shipped tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire EstatisticaPage and sidebar/modal badges</name>
  <files>src/routes/estatistica/EstatisticaPage.tsx, src/routes/estatistica/SidebarTestLink.tsx, src/routes/estatistica/Sidebar.tsx, src/routes/estatistica/QualTesteModal.tsx</files>
  <read_first>
    - `src/routes/estatistica/EstatisticaPage.tsx` lines 14-39
    - `02-UI-SPEC.md` sidebar demo Demonstração badge vs Disponível for migrated
    - `02-CONTEXT.md` D-16 default landing demo
    - Static imports for TStudentTest, CorrelacaoTest, PraisWinstenTest from feature paths
  </read_first>
  <action>
    1. EstatisticaPage.tsx:
       - Static import TStudentTest, CorrelacaoTest, PraisWinstenTest
       - Replace demo-only ternary with renderActiveTest(activeTestId) switch per RESEARCH Pattern 5
       - Keep useState default activeTestId = 'demo' (D-16)
       - handleSelectTest unchanged — isTestAvailable gate now passes for 3 new tests

    2. SidebarTestLink.tsx: when entry.id === 'demo', render Badge variant outline "Demonstração"; when status available and not demo, "Disponível" teal badge per UI-SPEC.

    3. QualTesteModal.tsx: same badge distinction for demo row vs migrated available rows; em-breve entries remain non-clickable.

    4. EstatisticaPage.test.tsx: assert switching sidebar to t-student mounts module (data-active-test-id or heading text from t-Student flow); demo still default on fresh render.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/estatistica/EstatisticaPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All four available tests mount from sidebar selection
    - [ ] Default active test remains demo
    - [ ] Demo shows Demonstração badge in sidebar and modal
    - [ ] Migrated tests show Disponível badge
  </acceptance_criteria>
  <done>Users can select and run all migrated tests from Estatística shell</done>
</task>

<task type="auto">
  <name>Task 3: Mapas handoff routing and phase integration gate</name>
  <files>src/routes/mapas/IniciarPesquisaModal.tsx, .planning/phases/02-migrar-testes-existentes/02-VALIDATION.md</files>
  <read_first>
    - `src/routes/mapas/IniciarPesquisaModal.tsx` navigation on Iniciar pesquisa
    - `02-RESEARCH.md` open question: handoff lands on suggested test if available else t-Student; cold start stays demo (D-16)
    - `02-VALIDATION.md` manual PNG spot-check instructions
  </read_first>
  <action>
    1. IniciarPesquisaModal: when navigating to Estatística with session data, set target test id to suggested test from suggestion engine IF isTestAvailable(suggestedId), else 't-student' if available, else 'demo'. Pass via router state or session field consumed by EstatisticaPage on mount to set activeTestId and advance to Configurar step if data present.

    2. EstatisticaPage: on mount, read optional handoff test id from location.state or session; if valid and available, setActiveTestId — but only when NOT cold start (has session dataset). Cold navigation to /estatistica still defaults demo.

    3. Update 02-VALIDATION.md frontmatter: set nyquist_compliant true; fill Task IDs 02-t-engine, 02-c-engine, 02-p-engine, 02-t-flow, 02-registry with plan references; mark Wave 0 items complete placeholders for executor.

    4. Run full suite gate: npm run test:run && npm run typecheck && npm run build — all must pass.
  </action>
  <verify>
    <automated>npm run test:run && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Full test suite green
    - [ ] Production build succeeds
    - [ ] Mapas handoff routes to t-student (or suggested) when data present
    - [ ] 02-VALIDATION.md task map updated with plan task IDs
  </acceptance_criteria>
  <done>Phase 2 integration complete — all success criteria from ROADMAP achievable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router state handoff | Test id from Mapas navigation — must validate against registry |
| Sidebar selection | Only isTestAvailable ids mount modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | IniciarPesquisa handoff test id | mitigate | Whitelist against TEST_REGISTRY ids + isTestAvailable; ignore unknown ids → fallback demo |
| T-02-01 | Tampering | All migrated modules | mitigate | Verified in 02-03/04/05 — plain interpretation strings |
| T-02-02 | Denial of Service | Session dataset on handoff | mitigate | Existing parse limits; no new unbounded storage |
</threat_model>

<verification>
- Full suite: npm run test:run
- Build: npm run build
- Manual (document in SUMMARY): PNG after customizer on each migrated test; Qual teste modal demo labeling
</verification>

<success_criteria>
- All four Phase 2 ROADMAP success criteria satisfied:
  1. t de Student in new shell with parity
  2. Correlação with parity
  3. Prais-Winsten with parity
  4. Each shows interpretation + PNG export
- D-15, D-16, D-17 demo/registry decisions honored
</success_criteria>

<output>
Create `.planning/phases/02-migrar-testes-existentes/02-06-SUMMARY.md` when done
</output>
