---
phase: 02-migrar-testes-existentes
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/features/tests/correlacao/correlacaoConfig.ts
  - src/features/tests/correlacao/correlacaoEngine.ts
  - src/features/tests/correlacao/correlacaoEngine.test.ts
  - src/features/tests/correlacao/correlacaoInterpretation.ts
  - src/features/tests/correlacao/correlacaoInterpretation.test.ts
  - src/features/tests/correlacao/correlacaoCharts.ts
  - src/features/tests/correlacao/CorrelacaoConfigPanel.tsx
  - src/features/tests/correlacao/CorrelacaoDatasusKnobs.tsx
  - src/features/tests/correlacao/CorrelacaoTest.tsx
  - src/features/tests/correlacao/CorrelacaoTest.test.tsx
autonomous: true
requirements: [TEST-02]
user_setup: []

must_haves:
  truths:
    - "User runs Pearson and Spearman correlation with display-rounded parity to v1.0 Stats outputs"
    - "Single registry entry with method toggle in Configurar (Pearson default)"
    - "Method switch soft-resets like t-Student (D-13 discretion for Correlação)"
    - "Scatter charts with deep customization; values statistically correct (D-05)"
  artifacts:
    - path: "src/features/tests/correlacao/correlacaoEngine.ts"
      provides: "Dataset build + statsEngine.pearson/spearman"
    - path: "src/features/tests/correlacao/CorrelacaoTest.tsx"
      provides: "FlowSteps orchestrator"
    - path: "src/features/tests/correlacao/correlacaoEngine.test.ts"
      provides: "Stats-level + fixture parity (Pitfall 2 oracle gap)"
  key_links:
    - from: "correlacaoEngine.ts"
      to: "src/shared/stats/statsEngine.ts"
      via: "pearson/spearman calls"
      pattern: "statsEngine\\.pearson|statsEngine\\.spearman"
    - from: "correlacaoEngine.test.ts"
      to: "src/test/fixtures/tests/correlacao-exemplo.txt"
      via: "expected output snapshot at display precision"
      pattern: "correlacao-exemplo"
---

<objective>
Ship the Correlação Pearson/Spearman module (TEST-02) on the shared shell with full Configurar knobs, deep chart customization, and numeric parity via statsEngine + fixture oracles (legacy module.js exports only renderTestModule — Pitfall 2).

Purpose: Correlação validates Pearson/Spearman on real classroom data shapes. Can run parallel with Prais-Winsten (02-05) after shared infra from 02-01/02-02.
Output: Complete correlacao feature module with engine tests and RTL smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-migrar-testes-existentes/02-CONTEXT.md
@.planning/phases/02-migrar-testes-existentes/02-PATTERNS.md
@.planning/phases/02-migrar-testes-existentes/02-UI-SPEC.md
@tests/correlacao/module.js
@tests/correlacao/config.json
@src/test/fixtures/tests/correlacao-exemplo.txt
</context>

<interfaces>
Legacy call site (tests/correlacao/module.js ~1771):
- stats.pearson(dataset.x, dataset.y)
- stats.spearman(dataset.x, dataset.y)

CorrelacaoEngine output:
- method: 'pearson' | 'spearman'
- r, p, n, ci (as applicable)
- metrics[], interpretation: string[], chartPresets

Method soft-reset: same as t-Student D-13 when Pearson ↔ Spearman changes

TABULAR_OPTIONS from correlacao config.json column expectations
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port correlacao engine and parity tests</name>
  <files>src/features/tests/correlacao/correlacaoConfig.ts, src/features/tests/correlacao/correlacaoEngine.ts, src/features/tests/correlacao/correlacaoEngine.test.ts</files>
  <read_first>
    - `tests/correlacao/module.js` runAnalysis section (~1771) and dataset builder logic
    - `tests/correlacao/config.json` (examples, didacticCards, defaultQuestion, column aliases)
    - `src/shared/stats/statsEngine.ts` pearson/spearman
    - `src/test/fixtures/tests/correlacao-exemplo.txt` and tabnet/correlacao-derive.txt
    - `02-RESEARCH.md` Pitfall 2 (no module exports — Stats-level parity)
  </read_first>
  <action>
    1. correlacaoConfig.ts: TABULAR_OPTIONS, defaultQuestion, didacticCards, exampleText from config.json; chart preset labels per UI-SPEC (Dispersão, Dispersão com ranks, Dispersão + linha de ajuste).

    2. correlacaoEngine.ts:
       - buildDatasetFromConfirmed(confirmedDataset): extract x[], y[] numeric pairs (drop invalid rows)
       - runCorrelation(x, y, method): call statsEngine.pearson or spearman; compute p-value using ported tcdf/fisherCI as legacy does
       - buildMetrics(result, method): r, p, n with fmtP/fmtNumber
       - validatePairs: minimum n for correlation (match legacy guards)
       - DataSUS: deriveCorrelationPairs path when source is DATASUS

    3. correlacaoEngine.test.ts:
       - Run engine on correlacao-exemplo fixture; compare r and p at display precision against legacy Stats executed on same x,y vectors (extract vectors by running legacy runAnalysis logic inline in test OR pre-computed expected snapshot from legacy session)
       - TABNET derive fixture: deriveCorrelationPairs → engine output matches legacy expected values
       - Pearson and Spearman both covered on exemplo fixture

    Optional: add non-behavior-changing named export to tests/correlacao/module.js for runAnalysis pure helper ONLY if Stats+fixture cannot reach **interpretation conclusion parity** — prefer Stats+fixture approach per RESEARCH Open Question 1 resolution.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/correlacao/correlacaoEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Pearson r and p match legacy at display precision on exemplo fixture
    - [ ] Spearman rho and p match legacy on same fixture
    - [ ] TABNET derive path covered
    - [ ] Minimum sample validation returns PT error string
    - [ ] Engine output includes sufficient fields for interpretation conclusion parity (r, p, n, α threshold) per D-07
  </acceptance_criteria>
  <done>Correlacao engine verified at display-rounded parity</done>
</task>

<task type="auto">
  <name>Task 2: Port interpretation and chart presets</name>
  <files>src/features/tests/correlacao/correlacaoInterpretation.ts, src/features/tests/correlacao/correlacaoInterpretation.test.ts, src/features/tests/correlacao/correlacaoCharts.ts</files>
  <read_first>
    - `tests/correlacao/module.js` interpretation builders
    - `02-UI-SPEC.md` Correlação chart types and toggles (Linha de regressão, outliers, equação)
    - `src/shared/charts/chartFactories/scatterChart.ts`
  </read_first>
  <action>
    1. correlacaoInterpretation.ts: port legacy interpretation to string[]; significance vs α; method-specific wording (Pearson linear vs Spearman monotonic); fmtP/fmtNumber for r and p; no HTML (T-02-01).

    2. correlacaoCharts.ts: presets mapping to buildScatterChartData, buildRankScatterChartData, buildScatterWithFitChartData; annotation toggles for regression line, outlier highlight, equation label; default = Pearson scatter / Spearman rank scatter based on method.

    3. Create `correlacaoInterpretation.test.ts`:
       - Assert **significance conclusion parity** with legacy interpretation for correlacao-exemplo at α=0.05 for both Pearson and Spearman (D-07, D-10: same significant/not-significant call, not byte-identical prose)
       - Assert formatted r and p values appear at display precision (D-08)
       - Assert no HTML tags in output paragraphs
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/correlacao/correlacaoInterpretation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Interpretation string[] with correct significance conclusion matching legacy (Pearson + Spearman)
    - [ ] Key stats (r/ρ, p, n) present at display precision per D-08
    - [ ] Three chart presets per UI-SPEC
    - [ ] Regression overlay toggle produces annotation config when enabled
  </acceptance_criteria>
  <done>Correlacao interpretation and chart presets complete</done>
</task>

<task type="auto">
  <name>Task 3: Wire CorrelacaoTest UI and RTL smoke test</name>
  <files>src/features/tests/correlacao/CorrelacaoConfigPanel.tsx, src/features/tests/correlacao/CorrelacaoDatasusKnobs.tsx, src/features/tests/correlacao/CorrelacaoTest.tsx, src/features/tests/correlacao/CorrelacaoTest.test.tsx</files>
  <read_first>
    - `src/features/tests/t-student/TStudentTest.tsx` (reference orchestrator from 02-03 if available, else TesteDemo)
    - `02-UI-SPEC.md` § Correlação (Método de correlação, Pearson/Spearman cards)
    - `02-PATTERNS.md` method soft-reset
  </read_first>
  <action>
    1. CorrelacaoConfigPanel: ModeChoiceCard as method selector (Pearson default); AlphaSelector; ResearchQuestionField; DidacticCards; ColumnPreviewTable; CorrelacaoDatasusKnobs for pair selection on DATASUS source; SoftResetAlert on method change (D-13 discretion).

    2. CorrelacaoTest.tsx: FlowSteps orchestrator mirroring TStudentTest; Usar exemplo; ResultsPanelWithCustomizer; exportFilename correlacao-lacirstat.png; ClearDataButton.

    3. CorrelacaoTest.test.tsx: Chart.js mock; Usar exemplo → Analisar dados → interpretation + PNG; method switch shows soft reset alert.

    Awaiting EstatisticaPage wiring in 02-06.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/correlacao/CorrelacaoTest.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] RTL smoke test passes full Dados→Resultados path
    - [ ] Pearson default selected in Configurar
    - [ ] Method soft-reset preserves paste text
  </acceptance_criteria>
  <done>TEST-02 module complete and smoke-tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User paste → x/y vectors | Numeric pairs fed to correlation engine |
| Chart outlier highlight | Derived from data statistics — not user HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | correlacaoInterpretation | mitigate | string[] plain text only |
| T-02-02 | Denial of Service | Large pair arrays | mitigate | parseTabular row limits; validate n before O(n log n) rank for Spearman |
| T-02-05 | Information Disclosure | Outlier highlight toggle | accept | Highlights computed points only; no external data leak |
</threat_model>

<verification>
- `npm run test:run -- src/features/tests/correlacao/` all green
- Parallel-safe: no files overlap with 02-05 prais-winsten paths
</verification>

<success_criteria>
- TEST-02: Correlação Pearson/Spearman with parity, customization, interpretation, PNG
</success_criteria>

<output>
Create `.planning/phases/02-migrar-testes-existentes/02-04-SUMMARY.md` when done
</output>
