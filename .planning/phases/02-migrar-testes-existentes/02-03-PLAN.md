---
phase: 02-migrar-testes-existentes
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/features/tests/t-student/tStudentConfig.ts
  - src/features/tests/t-student/tStudentEngine.ts
  - src/features/tests/t-student/tStudentEngine.test.ts
  - src/features/tests/t-student/tStudentInterpretation.ts
  - src/features/tests/t-student/tStudentCharts.ts
  - src/features/tests/t-student/TStudentConfigPanel.tsx
  - src/features/tests/t-student/TStudentDatasusKnobs.tsx
  - src/features/tests/t-student/TStudentTest.tsx
  - src/features/tests/t-student/TStudentTest.test.tsx
autonomous: true
requirements: [TEST-01]
user_setup: []

must_haves:
  truths:
    - "User runs t de Student independent (Welch) and paired modes with display-rounded parity to v1.0"
    - "Configurar shows mode choice cards (default independent), full α/research question/didactic cards, and DataSUS knobs when applicable"
    - "Mode switch keeps paste text but clears mode-specific config and results (D-13)"
    - "Resultados shows metrics, customizable chart, PT interpretation, and PNG export"
    - "Usar exemplo pre-fills from config.json exampleText"
  artifacts:
    - path: "src/features/tests/t-student/TStudentTest.tsx"
      provides: "FlowSteps orchestrator per D-01"
    - path: "src/features/tests/t-student/tStudentEngine.ts"
      provides: "Welch + paired computation wrapping statsEngine + safeWelch guards"
    - path: "src/features/tests/t-student/tStudentEngine.test.ts"
      provides: "Differential parity vs tests/t-student/module.js exports"
  key_links:
    - from: "tStudentEngine.test.ts"
      to: "tests/t-student/module.js"
      via: "safeWelch/safePaired parity on fixtures"
      pattern: "tests/t-student/module.js"
    - from: "TStudentTest.tsx"
      to: "ResultsPanelWithCustomizer"
      via: "tStudentCharts presets + engine output"
      pattern: "ResultsPanelWithCustomizer"
---

<objective>
Ship the t de Student module (TEST-01) on the shared FlowSteps shell: Welch independent (default) and paired modes, full Configurar knobs (D-02), deep chart customization (D-04), and differential parity against legacy module.js exports (D-07, D-08, D-09).

Purpose: t-Student is the richest legacy oracle (exported safeWelch, safePaired, interpretation builders). It validates the Wave 0 Stats port and chart infrastructure before Correlação/Prais.
Output: Complete t-Student feature module with engine tests and RTL smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-migrar-testes-existentes/02-CONTEXT.md
@.planning/phases/02-migrar-testes-existentes/02-PATTERNS.md
@.planning/phases/02-migrar-testes-existentes/02-UI-SPEC.md
@tests/t-student/module.js
@tests/t-student/module-guided.js
@tests/t-student/config.json
@src/routes/estatistica/demo/TesteDemo.tsx
@src/test/fixtures/tests/t-student-exemplo.txt
</context>

<interfaces>
From tests/t-student/module.js exports (parity oracle):
- safeWelch(g1, g2, stats), safePaired(pairs, stats)
- buildManualInterpretation(result, alpha, labels, question, utils)
- parseDataset (oracle only — production uses useTabularInput)

From tests/t-student/module-guided.js:
- TABULAR aliases: unidade, grupo_a, grupo_b (lines 24-37)
- Mode choice: independent default (D-12), Welch label (D-14)

TStudentEngine output shape:
- metrics: { label, value }[] for ResultsPanel
- interpretation: string[] (NOT HTML — Pitfall 7)
- chartPresets for useChartCustomizer
- result object for factories

Mode state: 'independent' | 'paired' — soft reset on change per D-13
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port t-Student engine and differential tests</name>
  <files>src/features/tests/t-student/tStudentConfig.ts, src/features/tests/t-student/tStudentEngine.ts, src/features/tests/t-student/tStudentEngine.test.ts</files>
  <read_first>
    - `tests/t-student/module.js` lines 607–628 (safeWelch), paired equivalent, buildManualInterpretation ~833–858
    - `tests/t-student/module-guided.js` lines 24–47 (aliases, wide format)
    - `tests/t-student/config.json` (defaultQuestion, didacticCards, exampleText)
    - `src/shared/stats/statsEngine.ts` (from 02-01)
    - `src/test/fixtures/tests/t-student-exemplo.txt`
    - `02-CONTEXT.md` D-11–D-14 (single registry entry, Welch only, soft reset)
  </read_first>
  <action>
    1. Create `tStudentConfig.ts`:
       - Export TABULAR_OPTIONS with aliases from module-guided.js
       - Export defaultQuestion, didacticCards, exampleText from config.json as typed constants
       - Export chart preset metadata for customizer (types per UI-SPEC: Distribuição por grupo, Diferença de médias IC95%, Barras de médias)

    2. Create `tStudentEngine.ts`:
       - runIndependentWelch(g1, g2): mirror safeWelch zero-denominator guards, call statsEngine
       - runPairedT(pairs): mirror safePaired
       - buildDatasetFromConfirmed(confirmedDataset, mode): map useTabularInput output to g1/g2 or pairs — wide format primary per RESEARCH recommendation
       - buildMetrics(result, mode): metric cards with fmtP, fmtNumber, fmtSigned per D-08
       - validateSampleSize(mode, dataset): return PT error strings from UI-SPEC (n&lt;2 per group, paired length mismatch)
       - DataSUS path: wrap deriveIndependentTTest / derivePairedTTest when source is DATASUS (only after 02-01 derive parity)

    3. Create `tStudentEngine.test.ts`:
       - Import legacy module.js exports
       - Assert runIndependentWelch matches safeWelch on t-student-exemplo fixture (display-rounded p, t, diff, ci per D-08)
       - Assert runPairedT matches safePaired on paired subset of exemplo or dedicated paired fixture
       - Assert TABNET derive path produces same Welch output as legacy guided flow when given t-student-derive fixture
       - Use displayParity helper with fmtP/fmtSigned

    Do NOT use legacy parseDataset in production path — useTabularInput + tStudentConfig aliases only.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/t-student/tStudentEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] tStudentEngine.test.ts passes differential parity for Welch and paired on exemplo fixture
    - [ ] TABNET derive fixture case covered in engine tests
    - [ ] Engine returns validation errors as PT strings matching UI-SPEC
    - [ ] No equal-variance Student toggle (D-14 deferred)
  </acceptance_criteria>
  <done>Welch and paired engines verified against legacy exports at display precision</done>
</task>

<task type="auto">
  <name>Task 2: Port interpretation and chart presets</name>
  <files>src/features/tests/t-student/tStudentInterpretation.ts, src/features/tests/t-student/tStudentCharts.ts</files>
  <read_first>
    - `tests/t-student/module.js` buildManualInterpretation and guided interpretation variants
    - `02-PATTERNS.md` § tStudentInterpretation (string[] not HTML)
    - `02-UI-SPEC.md` t-Student chart presets and annotation toggles
    - `src/shared/charts/chartFactories/tStudentCharts.ts` (from 02-02)
  </read_first>
  <action>
    1. Create `tStudentInterpretation.ts`:
       - Port buildManualInterpretation logic to return string[] paragraphs
       - Use fmtP, fmtNumber, fmtSigned at build time for key stats in prose
       - Same significance conclusion as legacy at chosen α (D-10: light cleanup OK, conclusion unchanged)
       - Include research question context when provided
       - NEVER return HTML tags — plain PT strings only (T-02-01 mitigation)

    2. Create `tStudentCharts.ts`:
       - Define chartPresets array for useChartCustomizer: default = diff chart (v1.0 default)
       - Map presets to buildTStudentDiffChartData, buildTStudentDistChartData, buildTStudentMeansBarChartData
       - Annotation toggle keys: showConfidenceIntervals, showMeanValues — wire to chartjs-plugin-annotation config in merge step
       - Export getDefaultTStudentChartPreset(mode, result, labels)

    3. Add unit tests in tStudentEngine.test.ts or new tStudentInterpretation.test.ts: assert interpretation paragraphs contain formatted p-value and significance phrase matching legacy for exemplo fixture.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/t-student/tStudentInterpretation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Interpretation returns string[] with no HTML entities or tags
    - [ ] Significance call matches legacy for exemplo at α=0.05
    - [ ] tStudentCharts exports at least 3 presets matching UI-SPEC labels
  </acceptance_criteria>
  <done>PT interpretation and chart presets ready for ResultsPanelWithCustomizer</done>
</task>

<task type="auto">
  <name>Task 3: Wire TStudentTest FlowSteps UI and RTL smoke test</name>
  <files>src/features/tests/t-student/TStudentConfigPanel.tsx, src/features/tests/t-student/TStudentDatasusKnobs.tsx, src/features/tests/t-student/TStudentTest.tsx, src/features/tests/t-student/TStudentTest.test.tsx</files>
  <read_first>
    - `src/routes/estatistica/demo/TesteDemo.tsx` (full orchestrator pattern)
    - `02-UI-SPEC.md` § t de Student components + Copywriting (Tipo de comparação, t independente Welch, t pareado)
    - `02-PATTERNS.md` § TStudentTest deltas + soft reset (D-13)
    - `src/routes/estatistica/demo/TesteDemo.test.tsx` (Chart.js mock, FlowSteps smoke)
    - `tests/t-student/module-guided.js` DataSUS knob sections (~2395+)
  </read_first>
  <action>
    1. TStudentConfigPanel (per D-02):
       - ModeChoiceCard radiogroup: "t independente (Welch)" default (D-12), "t pareado" (UI-SPEC copy)
       - AlphaSelector, ResearchQuestionField (placeholder from config defaultQuestion)
       - DidacticCards from tStudentConfig
       - ColumnPreviewTable on confirm path (showPreview=false on Dados like TesteDemo)
       - TStudentDatasusKnobs when loadedInput source indicates DATASUS: period/category/group selectors ported from module-guided.js semantics
       - SoftResetAlert when mode changes after prior confirm (D-13): keep tabular rawText, clear modeSpecificConfig + confirmedDataset, navigate to configurar if on resultados

    2. TStudentTest.tsx:
       - Clone TesteDemo state shape: activeStep, loadedInput, confirmedDataset, mode, alpha, researchQuestion
       - Dados: TabularInputPanel + UseExampleButton loading exampleText
       - Configurar: TStudentConfigPanel + "Analisar dados" CTA
       - Resultados: ResultsPanelWithCustomizer with engine metrics, customizer, interpretation, exportFilename t-student-lacirstat.png, ClearDataButton
       - Session publish on confirm (setDataset)
       - Run engine on Resultados mount when confirmedDataset present; show validation errors per UI-SPEC

    3. TStudentTest.test.tsx:
       - Chart.js mock (hoisted pattern from TesteDemo.test.tsx)
       - Wrap SessionProvider
       - Smoke: Usar exemplo → Configurar → Analisar dados → "O que isso significa?" + Baixar gráfico PNG present
       - Assert no raw HTML tags in interpretation text content
       - Mode switch test: change to pareado, assert SoftResetAlert copy visible

    Module is built but NOT yet wired in EstatisticaPage — registry flip happens in 02-06.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/t-student/TStudentTest.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Full FlowSteps smoke test passes
    - [ ] Mode default is independent (Welch label visible)
    - [ ] Soft reset preserves paste, clears results on mode change
    - [ ] Interpretation renders as plain text (no dangerouslySetInnerHTML)
  </acceptance_criteria>
  <done>TEST-01 module complete and smoke-tested; awaiting registry flip in 02-06</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User paste → engine | Tabular data crosses into statistical computation |
| Interpretation output → DOM | Engine prose rendered in InterpretationText |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | tStudentInterpretation | mitigate | string[] only; format numbers at build time; InterpretationText renders plain &lt;p&gt; — no HTML from paste |
| T-02-02 | Denial of Service | Large paste in TStudentTest | mitigate | Reuse useTabularInput / parseTabular limits from Phase 1; validateSampleSize before engine run |
| T-02-04 | Tampering | Research question field | mitigate | Trim length (e.g. 500 chars); embed as plain text in interpretation only |
</threat_model>

<verification>
- `npm run test:run -- src/features/tests/t-student/` all green
- Engine parity on exemplo + TABNET fixtures
- RTL smoke covers Usar exemplo → Resultados path
</verification>

<success_criteria>
- TEST-01: t de Student runs with Welch/paired modes, display-rounded parity, customizable charts, PT interpretation, PNG export
- D-11–D-14 locked decisions implemented
</success_criteria>

<output>
Create `.planning/phases/02-migrar-testes-existentes/02-03-SUMMARY.md` when done
</output>
