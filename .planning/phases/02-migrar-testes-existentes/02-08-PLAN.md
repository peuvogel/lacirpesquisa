---
phase: 02-migrar-testes-existentes
plan: 08
type: execute
wave: 6
depends_on: ["02-07"]
files_modified:
  - src/shared/data-input/recognizedColumnsFromTabular.ts
  - src/shared/data-input/recognizedColumnsFromTabular.test.ts
  - src/routes/estatistica/ColumnPreviewTable.tsx
  - src/routes/estatistica/ColumnPreviewTable.test.tsx
  - src/features/tests/t-student/TStudentConfigPanel.tsx
  - src/features/tests/t-student/TStudentTest.tsx
  - src/features/tests/correlacao/CorrelacaoConfigPanel.tsx
  - src/features/tests/correlacao/CorrelacaoTest.tsx
  - src/features/tests/prais-winsten/PraisWinstenConfigPanel.tsx
  - src/features/tests/prais-winsten/PraisWinstenTest.tsx
  - src/routes/estatistica/EstatisticaPage.test.tsx
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
user_setup: []

must_haves:
  truths:
    - "ColumnPreviewTable role overrides affect analysis (WR-01 closed)"
    - "Mapas handoff dataset → Configurar confirm → Resultados shows metrics (Truth #6 verified)"
  artifacts:
    - path: "src/routes/estatistica/ColumnPreviewTable.tsx"
      provides: "onConfirm emits recognizedColumns alongside headers/rows"
    - path: "src/routes/estatistica/EstatisticaPage.test.tsx"
      provides: "Handoff integration test through Resultados"
      contains: "Analisar dados"
  key_links:
    - from: "ColumnPreviewTable.handleConfirm"
      to: "deriveRecognizedColumnsFromRoles"
      via: "tabularOptions prop"
      pattern: "recognizedColumns"
    - from: "TStudentTest handleConfigureConfirm"
      to: "buildDatasetFromConfirmed"
      via: "confirmedDataset.recognizedColumns from confirm payload"
      pattern: "recognizedColumns:.*confirmed"
---

<objective>
Close WR-01 and Verification Truth #6: wire ColumnPreview confirm to emit role→domain column mapping, and prove Mapas handoff completes analysis through Resultados (not just Configurar opens).

Purpose: User column role adjustments in Configurar must reach engines; integration test locks the Mapas → Estatística end-to-end path.
Output: ColumnPreview confirm contract extended, three migrated tests consume confirmed mapping, EstatisticaPage RTL test reaches Resultados with metrics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/02-migrar-testes-existentes/02-07-SUMMARY.md
</execution_context>

<context>
@.planning/phases/02-migrar-testes-existentes/02-VERIFICATION.md
@.planning/phases/02-migrar-testes-existentes/02-REVIEW.md
@src/routes/estatistica/ColumnPreviewTable.tsx
@src/routes/estatistica/EstatisticaPage.test.tsx
@src/features/tests/correlacao/correlacaoConfig.ts

<interfaces>
From ColumnPreviewTable (current — extend):
```typescript
export interface ColumnPreviewTableProps {
  headers: string[];
  bodyRows: string[][];
  recognizedColumns: Record<string, number>;
  onConfirm: (confirmed: { headers: string[]; rows: string[][] }) => void;
}
```

From 02-07 helper (extend in this plan):
```typescript
export function deriveRecognizedColumnsFromTabular(
  headers: string[], rows: string[][], options: TabularInputOptions
): Record<string, number>;
// Add: deriveRecognizedColumnsFromRoles(roles, headers, options)
```

ConfirmedDataset pattern to extend in each Test.tsx:
```typescript
interface ConfirmedDataset {
  headers: string[];
  rows: string[][];
  sourceLabel: string;
  recognizedColumns: Record<string, number>; // NEW — from confirm payload
}
```

Integration test fixture (correlacao handoff — matches exampleText shape):
```typescript
const dataset: SessionDataset = {
  headers: ['id', 'variavel_x', 'variavel_y', 'observacao_opcional'],
  rows: [
    ['UF1', '12,3', '45,2', ''],
    ['UF2', '14,1', '43,8', ''],
    ['UF3', '10,9', '48,0', ''],
    ['UF4', '15,2', '42,7', ''],
  ],
  sourceLabel: 'Mapas — SP',
  confirmedAt: Date.now(),
};
// location.state.activeTestId: 'correlacao'
// Expected after confirm: Resultados step + metric "r de Pearson" visible
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Role-based column mapping + ColumnPreview confirm contract</name>
  <files>src/shared/data-input/recognizedColumnsFromTabular.ts, src/shared/data-input/recognizedColumnsFromTabular.test.ts, src/routes/estatistica/ColumnPreviewTable.tsx, src/routes/estatistica/ColumnPreviewTable.test.tsx</files>
  <read_first>src/routes/estatistica/ColumnPreviewTable.tsx, src/routes/estatistica/ColumnPreviewTable.test.tsx, src/shared/data-input/types.ts, src/features/tests/t-student/tStudentConfig.ts</read_first>
  <behavior>
    - Test 1: deriveRecognizedColumnsFromRoles assigns numeric columns to grupo_a/grupo_b via positionFallback for t-student
    - Test 2: tempo role maps to tempo key; first two numeric map to variavel_x/variavel_y for correlacao
    - Test 3: ColumnPreviewTable with tabularOptions emits recognizedColumns on confirm (RTL)
    - Test 4: User role override (change column to ignorar) removes that index from mapping
  </behavior>
  <action>
    Per WR-01 / 02-REVIEW.md, close the confirm-path gap:

    1. Add `deriveRecognizedColumnsFromRoles(roles: ColumnRole[], headers: string[], options: TabularInputOptions): Record<string, number>` to `recognizedColumnsFromTabular.ts`.

    Mapping rules (use each test's `TABULAR_OPTIONS.positionFallback.keysByIndex` + `requiredKeys`):
    - Start from header-derived map via `deriveRecognizedColumnsFromTabular` when headers match aliases.
    - For columns user marked `ignorar`, exclude from mapping.
    - Assign `tempo` role columns to `tempo` domain key when present in options.
    - Assign `numerica` columns in left-to-right order to remaining numeric required keys (grupo_a→grupo_b, variavel_x→variavel_y, or variavel_y for prais when tempo already assigned).
    - Assign first `categorica` to id/unidade key when alias exists and not yet mapped.
    - Fall back to positionFallback.keysByIndex for unassigned required keys when column count allows.

    2. Extend `ColumnPreviewTable`:
    - Add optional prop `tabularOptions?: TabularInputOptions`.
    - Change `onConfirm` payload to `{ headers, rows, recognizedColumns: Record<string, number> }`.
    - In `handleConfirm`: when `tabularOptions` provided, call `deriveRecognizedColumnsFromRoles(roles, headers, tabularOptions)`; else fall back to existing `recognizedColumns` prop unchanged.
    - Import `ColumnRole` type stays exported from ColumnPreviewTable.

    3. Update `ColumnPreviewTable.test.tsx` for new confirm shape. Existing consumers without tabularOptions must still work (TesteDemo can pass through without recognizedColumns change until later — backward compatible: if no tabularOptions, emit recognizedColumns from prop passthrough).

    Do NOT touch useChartCustomizer / PNG debounce (WR-02 out of scope).
  </action>
  <acceptance_criteria>
    - onConfirm includes recognizedColumns when tabularOptions prop is set
    - Role override changes which indices map to domain keys
    - ColumnPreviewTable.test.tsx updated and green
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/shared/data-input/recognizedColumnsFromTabular.test.ts src/routes/estatistica/ColumnPreviewTable.test.tsx</automated>
  </verify>
  <done>ColumnPreview confirm emits role-aware recognizedColumns; helper covered by unit tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire confirm mapping through migrated tests</name>
  <files>src/features/tests/t-student/TStudentConfigPanel.tsx, src/features/tests/t-student/TStudentTest.tsx, src/features/tests/correlacao/CorrelacaoConfigPanel.tsx, src/features/tests/correlacao/CorrelacaoTest.tsx, src/features/tests/prais-winsten/PraisWinstenConfigPanel.tsx, src/features/tests/prais-winsten/PraisWinstenTest.tsx</files>
  <read_first>src/features/tests/t-student/TStudentTest.tsx, src/features/tests/correlacao/CorrelacaoTest.tsx, src/features/tests/prais-winsten/PraisWinstenTest.tsx, src/features/tests/t-student/TStudentConfigPanel.tsx</read_first>
  <action>
    Wire confirmed column mapping into analysis for all three migrated tests:

    1. Extend `ConfirmedDataset` (or equivalent) in each Test.tsx with `recognizedColumns: Record<string, number>`.

    2. Update `handleConfigureConfirm` signature to accept `{ headers, rows, recognizedColumns }` from ColumnPreviewTable.

    3. Store `recognizedColumns` on confirmedDataset at confirm time.

    4. In resultsContent useMemo, pass `confirmedDataset.recognizedColumns` (not `loadedInput.recognizedColumns`) to `buildDatasetFromConfirmed` / engine input. Keep datasus derive path unchanged.

    5. Pass `tabularOptions={TABULAR_OPTIONS}` to ColumnPreviewTable via each ConfigPanel; update ConfigPanel `onConfirm` prop types accordingly.

    Preserve paste/example path: confirm still re-derives from roles (should match parser output when roles unchanged).

    Do NOT expand scope to TesteDemo pickAnalysisColumns refactor — demo uses different analysis path per D-06.
  </action>
  <acceptance_criteria>
    - buildDatasetFromConfirmed receives confirmedDataset.recognizedColumns at results time in all three modules
    - ConfigPanels pass tabularOptions to ColumnPreviewTable
    - Existing TStudentTest/CorrelacaoTest/PraisWinstenTest exemplo RTL flows still reach Resultados
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/features/tests/t-student/TStudentTest.test.tsx src/features/tests/correlacao/CorrelacaoTest.test.tsx src/features/tests/prais-winsten/PraisWinstenTest.test.tsx</automated>
  </verify>
  <done>Migrated tests analyze using confirm-time recognizedColumns; WR-01 closed</done>
</task>

<task type="auto">
  <name>Task 3: Mapas handoff integration test through Resultados</name>
  <files>src/routes/estatistica/EstatisticaPage.test.tsx</files>
  <read_first>src/routes/estatistica/EstatisticaPage.test.tsx, src/features/tests/correlacao/correlacaoConfig.ts, src/routes/mapas/IniciarPesquisaModal.test.tsx</read_first>
  <action>
    Extend `EstatisticaPage.test.tsx` to close Verification Truth #6 integration gap.

    Add test: **"session handoff completes correlacao analysis through Resultados"**

    Setup:
    - Seed SessionProvider with correlacao-compatible SessionDataset (headers/rows from correlacao exampleText — id, variavel_x, variavel_y with ≥4 numeric pairs).
    - MemoryRouter initial entry: `{ pathname: '/', state: { activeTestId: 'correlacao' } }`.
    - Re-use existing SeedSession helper pattern.

    Flow (userEvent):
    1. Wait for mount data-active-test-id='correlacao' and Configurar step active (session bootstrap from 02-07).
    2. Click "Analisar dados" (ColumnPreview confirm).
    3. Wait for Resultados step: `aria-current="step"` on Resultados button.
    4. Assert analysis succeeded (NOT validation alert):
       - `screen.getByText('O que isso significa?')` present
       - Metric visible: `screen.getByText('r de Pearson')` or coefficient value pattern
       - Absence of "Cada grupo precisa" / empty-vector validation messages

    Mock chart.js same as other test modules (copy ChartMock hoist from TStudentTest.test.tsx if needed to avoid canvas errors).

    Keep existing handoff test that only asserts Configurar opens — do not remove it.
  </action>
  <acceptance_criteria>
    - New RTL test proves handoff data produces Resultados metrics, not just Configurar landing
    - Test fails on pre-02-07 code (empty recognizedColumns) and passes after gap closure
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/routes/estatistica/EstatisticaPage.test.tsx</automated>
  </verify>
  <done>Verification Truth #6 integration path covered by automated test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Column role UI → recognizedColumns map | User-selected roles cross into engine column index selection |
| Session handoff strings → interpretation | Research question + headers remain plain text through React children |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Tampering | deriveRecognizedColumnsFromRoles | mitigate | Map only to known domain keys from TabularInputOptions.requiredKeys; ignore unknown roles; indices bounds-checked against headers.length |
| T-02-08-02 | Information Disclosure | Session paste in RTL fixture | accept | Test fixture uses public example data only |
| T-02-08-03 | XSS | Headers/cells in ColumnPreviewTable | mitigate | Keep existing plain-text JSX rendering — no dangerouslySetInnerHTML; role labels from static ROLE_OPTIONS |
| T-02-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After all tasks:
- `npm run test:run -- src/routes/estatistica/EstatisticaPage.test.tsx src/routes/estatistica/ColumnPreviewTable.test.tsx src/features/tests/t-student/TStudentTest.test.tsx src/features/tests/correlacao/CorrelacaoTest.test.tsx src/features/tests/prais-winsten/PraisWinstenTest.test.tsx` passes
- Full suite `npm run test:run` remains green
- WR-02 (PNG debounce) explicitly deferred — not in this plan
</verification>

<success_criteria>
- WR-01 closed: Configurar role overrides flow to engines via confirm payload
- Truth #6 closed: Mapas handoff → Configurar confirm → Resultados metrics verified in RTL
- All three migrated tests use confirmed recognizedColumns at analysis time
</success_criteria>

<output>
Create `.planning/phases/02-migrar-testes-existentes/02-08-SUMMARY.md` when done
</output>
