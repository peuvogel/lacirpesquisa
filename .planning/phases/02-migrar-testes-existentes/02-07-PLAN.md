---
phase: 02-migrar-testes-existentes
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - src/shared/data-input/recognizedColumnsFromTabular.ts
  - src/shared/data-input/recognizedColumnsFromTabular.test.ts
  - src/features/tests/t-student/TStudentTest.tsx
  - src/features/tests/correlacao/CorrelacaoTest.tsx
  - src/features/tests/prais-winsten/PraisWinstenTest.tsx
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-02, TEST-03]
user_setup: []

must_haves:
  truths:
    - "Session-injected Mapas handoff data resolves domain column keys (grupo_a/grupo_b, variavel_x/variavel_y, tempo/variavel_y) on bootstrap"
    - "buildDatasetFromConfirmed receives non-empty recognizedColumns for session path in all three migrated tests"
  artifacts:
    - path: "src/shared/data-input/recognizedColumnsFromTabular.ts"
      provides: "Shared deriveRecognizedColumnsFromTabular(headers, rows, options) helper"
      exports: ["deriveRecognizedColumnsFromTabular"]
    - path: "src/features/tests/t-student/TStudentTest.tsx"
      provides: "Session bootstrap with derived recognizedColumns"
      contains: "deriveRecognizedColumnsFromTabular"
  key_links:
    - from: "TStudentTest.tsx initialLoadedFromSession"
      to: "deriveRecognizedColumnsFromTabular"
      via: "TABULAR_OPTIONS from tStudentConfig"
      pattern: "deriveRecognizedColumnsFromTabular.*TABULAR_OPTIONS"
    - from: "buildDatasetFromConfirmed"
      to: "loadedInput.recognizedColumns"
      via: "session bootstrap populates domain keys before Configurar confirm"
      pattern: "recognizedColumns:.*deriveRecognizedColumnsFromTabular"
---

<objective>
Close CR-01 blocker: migrated tests bootstrap session handoff with empty `recognizedColumns: {}`, breaking Mapas → Estatística analysis.

Purpose: Re-use the same tabular parser path as paste/example (`readTabularPasteState` + `TABULAR_OPTIONS`) so session-injected headers/rows resolve domain keys before engines run.
Output: Shared helper + session/datasus bootstrap wired in all three migrated test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-migrar-testes-existentes/02-VERIFICATION.md
@.planning/phases/02-migrar-testes-existentes/02-REVIEW.md
@.planning/phases/02-migrar-testes-existentes/02-CONTEXT.md
@.planning/phases/02-migrar-testes-existentes/02-PATTERNS.md

<interfaces>
From src/shared/data-input/parseTabular.ts:
```typescript
export function readTabularPasteState(
  text: string,
  stats: LegacyStatsAdapter,
  options: TabularInputOptions = {},
): TabularLoadState;
// loaded state includes recognizedColumns: Record<string, RecognizedColumn>
```

From src/shared/data-input/legacyAdapters.ts:
```typescript
export const legacyStats: LegacyStatsAdapter = statsEngine;
```

From src/shared/data-input/useTabularInput.ts (index-only reshape pattern):
```typescript
function toIndexMap(recognizedColumns: Record<string, RecognizedColumn>): Record<string, number> {
  return Object.fromEntries(Object.entries(recognizedColumns).map(([key, column]) => [key, column.index]));
}
```

From src/features/tests/t-student/TStudentTest.tsx (broken session bootstrap — replace):
```typescript
function initialLoadedFromSession(...): TStudentLoadedInput | null {
  if (!sessionDataset) return null;
  return {
    headers: sessionDataset.headers,
    rows: sessionDataset.rows,
    recognizedColumns: {}, // CR-01: must derive
    sourceLabel: sessionDataset.sourceLabel,
  };
}
```

CR-01 fix sketch (02-REVIEW.md): serialize headers/rows to delimited text, call readTabularPasteState with test TABULAR_OPTIONS + legacyStats, map RecognizedColumn → index-only Record.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared deriveRecognizedColumnsFromTabular helper</name>
  <files>src/shared/data-input/recognizedColumnsFromTabular.ts, src/shared/data-input/recognizedColumnsFromTabular.test.ts</files>
  <read_first>src/shared/data-input/parseTabular.ts, src/shared/data-input/legacyAdapters.ts, src/features/tests/t-student/tStudentConfig.ts, src/features/tests/correlacao/correlacaoConfig.ts, src/features/tests/prais-winsten/praisConfig.ts, src/features/tests/t-student/tStudentEngine.test.ts</read_first>
  <behavior>
    - Test 1: t-student example headers/rows → `{ grupo_a, grupo_b }` index map (and optional unidade)
    - Test 2: correlacao example headers/rows → `{ variavel_x, variavel_y }` index map
    - Test 3: prais-winsten example headers/rows → `{ tempo, variavel_y }` index map
    - Test 4: position-fallback 3-column wide table with generic headers still resolves required keys
    - Test 5: unrecognizable table returns `{}` (no throw)
  </behavior>
  <action>
    Create `deriveRecognizedColumnsFromTabular(headers: string[], rows: string[][], options: TabularInputOptions): Record<string, number>` per CR-01 / 02-REVIEW.md fix sketch.

    Implementation rules:
    - Join headers with `;` and rows with `;` then newline — same shape `readTabularPasteState` expects for paste text.
    - Call `readTabularPasteState(text, legacyStats, options)`.
    - If status !== 'loaded', return `{}`.
    - Map `parsed.recognizedColumns` to index-only via the same `toIndexMap` pattern as `useTabularInput` (do not duplicate RecognizedColumn objects downstream).
    - Export only the helper; no React imports.

    Do NOT hand-roll alias matching — delegate entirely to existing parser (D-09 parity discipline).
  </action>
  <acceptance_criteria>
    - Helper exported and covered by unit tests for all three test TABULAR_OPTIONS fixtures
    - Returns `{}` gracefully when parser cannot load (no exceptions)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/shared/data-input/recognizedColumnsFromTabular.test.ts</automated>
  </verify>
  <done>deriveRecognizedColumnsFromTabular resolves domain keys for t-student, correlacao, and prais example fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Wire session and DATASUS bootstrap in migrated tests</name>
  <files>src/features/tests/t-student/TStudentTest.tsx, src/features/tests/correlacao/CorrelacaoTest.tsx, src/features/tests/prais-winsten/PraisWinstenTest.tsx</files>
  <read_first>src/features/tests/t-student/TStudentTest.tsx, src/features/tests/correlacao/CorrelacaoTest.tsx, src/features/tests/prais-winsten/PraisWinstenTest.tsx, src/shared/data-input/recognizedColumnsFromTabular.ts</read_first>
  <action>
    Per CR-01, replace hardcoded `recognizedColumns: {}` in all session/datasus bootstrap paths across the three migrated test modules:

    1. **initialLoadedFromSession** — call `deriveRecognizedColumnsFromTabular(sessionDataset.headers, sessionDataset.rows, TABULAR_OPTIONS)` from each test's config module.

    2. **handleDatasusSessionChange** — after `datasusNormalizedToTabular`, derive recognizedColumns from converted headers/rows with same TABULAR_OPTIONS instead of `{}`.

    Import helper from `@/shared/data-input/recognizedColumnsFromTabular` and each test's own `TABULAR_OPTIONS` constant.

    Do NOT change results-time logic yet (plan 02-08 wires confirm overrides). Do NOT change paste-path useEffect (already populated via useTabularInput).

    Preserve existing FlowSteps behavior: session present → land on Configurar step.
  </action>
  <acceptance_criteria>
    - No `recognizedColumns: {}` remains in initialLoadedFromSession or datasus setLoadedInput paths in the three Test.tsx files
    - Session bootstrap produces the same domain keys as paste path for identical headers/rows
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/features/tests/t-student/tStudentEngine.test.ts src/features/tests/correlacao/correlacaoEngine.test.ts src/features/tests/prais-winsten/praisEngine.test.ts src/shared/data-input/recognizedColumnsFromTabular.test.ts</automated>
  </verify>
  <done>All three migrated tests populate recognizedColumns on session/DATASUS bootstrap; engines can resolve domain keys after Configurar confirm on handoff data</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session dataset → tabular parser | Untrusted pasted headers/cell strings from Mapas handoff cross into parser |
| Parser output → engine vectors | Column indices select numeric slices fed to statsEngine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | deriveRecognizedColumnsFromTabular | mitigate | Re-use readTabularPasteState — no new string concatenation beyond delimited re-serialization; parser already handles mojibake/normalization |
| T-02-07-02 | Spoofing | Session headers as column aliases | accept | Header text is display-only until mapped to indices; engines parse numeric cells via legacy Stats.parseNumber |
| T-02-07-03 | Denial of Service | Malformed wide tables | mitigate | Parser returns error state → helper returns `{}`; existing validation alerts surface user-friendly PT errors (no throw/crash) |
| T-02-07-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After both tasks:
- `grep -v '^#' src/features/tests/t-student/TStudentTest.tsx | grep -c 'recognizedColumns: {}'` returns 0 for session/datasus bootstrap contexts (paste reset `{}` in tabular.reset path may remain)
- Unit tests for helper pass with all three TABULAR_OPTIONS
- Existing engine differential suites remain green
</verification>

<success_criteria>
- Session handoff path resolves non-empty recognizedColumns for valid t-student/correlacao/prais-shaped datasets
- CR-01 root cause (empty bootstrap map) eliminated in all three migrated modules
</success_criteria>

<output>
Create `.planning/phases/02-migrar-testes-existentes/02-07-SUMMARY.md` when done
</output>
