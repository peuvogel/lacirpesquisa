---
phase: 02-migrar-testes-existentes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/shared/stats/statsEngine.ts
  - src/shared/stats/statsEngine.test.ts
  - src/shared/data-input/legacyAdapters.ts
  - src/shared/data-input/datasusNormalizer.derive.test.ts
  - src/shared/charts/chartAnnotationSetup.ts
  - src/test/fixtures/tests/t-student-exemplo.txt
  - src/test/fixtures/tests/correlacao-exemplo.txt
  - src/test/fixtures/tests/prais-exemplo.txt
  - src/test/fixtures/tabnet/t-student-derive.txt
  - src/test/fixtures/tabnet/correlacao-derive.txt
  - src/test/fixtures/tabnet/prais-derive.txt
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03]
user_setup:
  - service: npm
    why: "Install chartjs-plugin-annotation for deep chart customization (D-04)"
    env_vars: []
    dashboard_config:
      - task: "Human-verify package legitimacy before install"
        location: "https://www.npmjs.com/package/chartjs-plugin-annotation — confirm publisher chartjs, ~1M+ weekly downloads, repo github.com/chartjs/chartjs-plugin-annotation"

must_haves:
  truths:
    - "Ported Stats engine returns identical numeric outputs to assets/js/app.js Stats for pearson, spearman, welchT, praisWinsten, tcdf, and related helpers"
    - "deriveIndependentTTest, derivePairedTTest, deriveCorrelationPairs, derivePraisSeries match assets/js/datasus-normalizer.js on one TABNET fixture each"
    - "chartjs-plugin-annotation registers once without breaking ChartCanvas lifecycle"
    - "Exemplo + TABNET fixtures exist for all three tests and are exercised by differential suites"
  artifacts:
    - path: "src/shared/stats/statsEngine.ts"
      provides: "Full Stats port from assets/js/app.js:240-533"
      exports: ["statsEngine", "parseNumber", "mean", "variance", "sd", "welchT", "pearson", "spearman", "rank", "praisWinsten", "tcdf", "tInv", "fisherCI"]
    - path: "src/shared/stats/statsEngine.test.ts"
      provides: "Differential parity vs legacy Stats call sites"
    - path: "src/shared/data-input/datasusNormalizer.derive.test.ts"
      provides: "derive* numeric verification per D-09"
    - path: "src/shared/charts/chartAnnotationSetup.ts"
      provides: "One-time Annotation plugin registration"
      contains: "Chart.register"
  key_links:
    - from: "src/shared/stats/statsEngine.test.ts"
      to: "assets/js/app.js"
      via: "differential parity on shared fixtures"
      pattern: "assets/js/app.js"
    - from: "src/shared/data-input/datasusNormalizer.derive.test.ts"
      to: "assets/js/datasus-normalizer.js"
      via: "derive* function parity"
      pattern: "deriveIndependentTTest|derivePairedTTest|deriveCorrelationPairs|derivePraisSeries"
---

<objective>
Port the full legacy `Stats` engine and verify all four `derive*` helpers numerically before any migrated test UI ships. Install and register `chartjs-plugin-annotation` for Phase 2 deep chart customization. Commit per-test exemplo and TABNET fixtures as parity oracles (D-09).

Purpose: Phase 1 only ported `parseNumber`/`mean`; correlacao and prais-winsten call internal Stats methods that do not exist yet (Pitfall 1). derive* functions are typed but unverified (Pitfall 3). This Wave 0 gate blocks all test modules.
Output: `statsEngine.ts`, differential test suites, fixtures, annotation plugin registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-migrar-testes-existentes/02-CONTEXT.md
@.planning/phases/02-migrar-testes-existentes/02-RESEARCH.md
@.planning/phases/02-migrar-testes-existentes/02-PATTERNS.md
@.planning/phases/02-migrar-testes-existentes/02-VALIDATION.md
@assets/js/app.js
@assets/js/datasus-normalizer.js
@src/shared/data-input/parseTabular.test.ts
@src/shared/format.ts
</context>

<interfaces>
From `assets/js/app.js:240-533` — minimum Stats surface to port verbatim:
- parseNumber, mean, variance, sd, gammaln, betacf, ibeta, tcdf, tInv, fisherCI
- welchT(a, b), pearson(x, y), rank(arr), spearman(x, y)
- olsTransformed, estimateRho, praisWinsten(years, values)

From `src/shared/format.ts`:
- fmtP, fmtNumber, fmtSigned — use in display-rounded parity assertions per D-08

From `tests/t-student/module.js:607-628`:
- safeWelch wraps Stats with zero-denominator guards — test engines mirror these guards; oracle is safeWelch not raw welchT

Expand `legacyAdapters.ts` legacyStats to re-export full statsEngine (backward compatible with parseTabular callers).
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify chartjs-plugin-annotation legitimacy and install</name>
  <what-built>Package legitimacy review for chartjs-plugin-annotation@^3.1.0 before npm install (RESEARCH.md Package Legitimacy Audit — slopcheck unavailable, tagged [ASSUMED])</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/chartjs-plugin-annotation
    2. Confirm publisher is chartjs org, repository is github.com/chartjs/chartjs-plugin-annotation, weekly downloads ~1M+
    3. Confirm latest major version 3.x is compatible with chart.js 4.5.1 (peer dependency chart.js ^4.0.0)
    4. Type "approved" to let Claude run `npm install chartjs-plugin-annotation@^3.1.0`
  </how-to-verify>
  <resume-signal>Type "approved" after verifying npm package page, or describe concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Port full Stats engine with differential parity tests</name>
  <files>src/shared/stats/statsEngine.ts, src/shared/stats/statsEngine.test.ts, src/shared/data-input/legacyAdapters.ts, src/shared/charts/chartAnnotationSetup.ts</files>
  <read_first>
    - `assets/js/app.js` lines 240–533 (full Stats object — copy verbatim, convert this. calls to local calls, add types)
    - `src/shared/data-input/parseTabular.test.ts` lines 1–73 (differential import pattern)
    - `src/shared/charts/ChartCanvas.tsx` lines 23–35 (controller registration pattern for chartAnnotationSetup.ts)
    - `02-PATTERNS.md` § statsEngine.ts port discipline
    - `02-RESEARCH.md` § Don't Hand-Roll (do not substitute jstat/stdlib)
  </read_first>
  <action>
    1. Create `src/shared/stats/statsEngine.ts` porting the entire Stats object from `assets/js/app.js:240-533`. Export a typed `statsEngine` object with all methods. Do not replace formulas with npm libraries.

    2. Update `src/shared/data-input/legacyAdapters.ts`: expand `legacyStats` to delegate to statsEngine (parseNumber, mean, and all statistical methods parsers/engines need). Keep existing exports backward compatible.

    3. Create `src/shared/charts/chartAnnotationSetup.ts`: import Annotation from chartjs-plugin-annotation, call Chart.register(Annotation) once. Export `ensureChartAnnotationsRegistered()` idempotent helper. Import this from ChartCanvas or call at module init — same pattern as existing controller registration.

    4. Create `src/shared/stats/statsEngine.test.ts` with differential parity:
       - Import untouched legacy Stats behavior by executing the same inputs through a thin test harness that loads `assets/js/app.js` Stats (same Vitest ESM import pattern as parseTabular.test.ts)
       - Test welchT, pearson, spearman, rank, tcdf, tInv, fisherCI, praisWinsten on hand-crafted numeric arrays
       - Use displayParity helper from 02-RESEARCH.md (fmtP/fmtNumber/fmtSigned) for p-values and coefficients per D-08
       - Assert finite outputs for edge cases: n=2 arrays, constant arrays, tied ranks for spearman

    5. Run `npm run typecheck` — statsEngine must satisfy LegacyStatsAdapter type if referenced in types.ts; extend the interface as needed without breaking parseTabular tests.
  </action>
  <verify>
    <automated>npm run test:run -- src/shared/stats/statsEngine.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] statsEngine.ts exports all methods listed in RESEARCH.md minimum surface
    - [ ] statsEngine.test.ts passes with differential assertions against legacy Stats
    - [ ] legacyAdapters.legacyStats delegates to statsEngine without breaking existing parseTabular parity suite
    - [ ] chartAnnotationSetup.ts registers Annotation plugin idempotently
    - [ ] No npm stats libraries added beyond chartjs-plugin-annotation
  </acceptance_criteria>
  <done>Full Stats port verified; annotation plugin installed and registered; legacyAdapters expanded</done>
</task>

<task type="auto">
  <name>Task 3: Verify derive* helpers and commit parity fixtures</name>
  <files>src/shared/data-input/datasusNormalizer.derive.test.ts, src/test/fixtures/tests/t-student-exemplo.txt, src/test/fixtures/tests/correlacao-exemplo.txt, src/test/fixtures/tests/prais-exemplo.txt, src/test/fixtures/tabnet/t-student-derive.txt, src/test/fixtures/tabnet/correlacao-derive.txt, src/test/fixtures/tabnet/prais-derive.txt</files>
  <read_first>
    - `tests/t-student/config.json` (exampleText, templates path)
    - `tests/correlacao/config.json` (examples[0])
    - `tests/prais-winsten/config.json` (exampleRows)
    - `src/shared/data-input/datasusNormalizer.ts` lines 593+ (deriveIndependentTTest, derivePairedTTest, deriveCorrelationPairs, derivePraisSeries)
    - `assets/js/datasus-normalizer.js` matching derive exports
    - `02-VALIDATION.md` § Wave 0 Requirements
  </read_first>
  <action>
    1. Create `src/test/fixtures/tests/t-student-exemplo.txt` from `tests/t-student/config.json` exampleText or `tests/t-student/templates/` exemplo CSV (wide format unidade;grupo_a;grupo_b per module-guided.js).

    2. Create `src/test/fixtures/tests/correlacao-exemplo.txt` from correlacao config.json first example.

    3. Create `src/test/fixtures/tests/prais-exemplo.txt` from prais-winsten config.json exampleRows rendered as pasteable text.

    4. Create one TABNET-style derive fixture per test under `src/test/fixtures/tabnet/`:
       - `t-student-derive.txt`: TABNET table suitable for deriveIndependentTTest or derivePairedTTest (metadata rows, semicolon, pt-BR decimals)
       - `correlacao-derive.txt`: TABNET suitable for deriveCorrelationPairs
       - `prais-derive.txt`: TABNET suitable for derivePraisSeries (time + value columns)

    5. Create `src/shared/data-input/datasusNormalizer.derive.test.ts`:
       - Differential import of `assets/js/datasus-normalizer.js` derive functions alongside TS exports
       - Run each derive* on its TABNET fixture with identical normalized source input
       - Assert output group vectors / pair arrays / time series match legacy exactly
       - Also smoke-test exemplo fixtures produce non-empty derive output

    Per D-09: fixtures feed all downstream engine tests; do not skip TABNET cases.
  </action>
  <verify>
    <automated>npm run test:run -- src/shared/data-input/datasusNormalizer.derive.test.ts && test 3 -eq "$(ls src/test/fixtures/tests/*.txt 2>/dev/null | wc -l | tr -d ' ')" && test 3 -eq "$(ls src/test/fixtures/tabnet/*-derive.txt 2>/dev/null | wc -l | tr -d ' ')"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Three exemplo fixtures under src/test/fixtures/tests/
    - [ ] Three TABNET derive fixtures under src/test/fixtures/tabnet/
    - [ ] derive.test.ts passes differential parity for all four derive* functions
    - [ ] Fixtures documented in test file comments (source: which config.json field)
  </acceptance_criteria>
  <done>derive* numerically verified; all six fixtures committed and exercised</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legacy JS import in Vitest | Untouched v1.0 modules loaded as parity oracle — read-only, no eval of user input |
| npm install | chartjs-plugin-annotation from npm registry — supply-chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Interpretation builders (downstream) | mitigate | Port legacy HTML builders to string[] paragraphs; InterpretationText renders plain text only — no dangerouslySetInnerHTML on user-derived content |
| T-02-02 | Denial of Service | Tabular paste / derive fixtures | mitigate | Reuse Phase 1 parse limits; derive tests use bounded fixture sizes; engine guards reject n&lt;2 / empty arrays before heavy computation |
| T-02-SC | Tampering | chartjs-plugin-annotation npm install | mitigate | Blocking human-verify checkpoint (Task 1) before install; package tagged [ASSUMED] in RESEARCH.md |
</threat_model>

<verification>
- `npm run test:run -- src/shared/stats/statsEngine.test.ts src/shared/data-input/datasusNormalizer.derive.test.ts` green
- Existing Phase 1 parseTabular parity suite still green: `npm run test:run -- src/shared/data-input/parseTabular.test.ts`
- chartjs-plugin-annotation present in package.json dependencies
</verification>

<success_criteria>
- Stats engine parity proven before any test UI work begins
- derive* functions verified against legacy normalizer on TABNET fixtures
- Annotation plugin installed (human-approved) and registration helper exists
- Six fixtures committed (3 exemplo + 3 TABNET derive)
</success_criteria>

<output>
Create `.planning/phases/02-migrar-testes-existentes/02-01-SUMMARY.md` when done
</output>
