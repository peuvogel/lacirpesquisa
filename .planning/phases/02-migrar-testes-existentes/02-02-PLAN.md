---
phase: 02-migrar-testes-existentes
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/shared/charts/chartFactories/scatterChart.ts
  - src/shared/charts/chartFactories/tStudentCharts.ts
  - src/shared/charts/chartFactories/timeseriesChart.ts
  - src/shared/charts/chartFactories/residualChart.ts
  - src/shared/charts/chartFactories/index.ts
  - src/shared/charts/ChartCustomizer.tsx
  - src/shared/charts/useChartCustomizer.ts
  - src/shared/charts/ResultsPanelWithCustomizer.tsx
  - src/features/tests/shared/DidacticCards.tsx
  - src/features/tests/shared/AlphaSelector.tsx
  - src/features/tests/shared/ResearchQuestionField.tsx
  - src/features/tests/shared/ModeChoiceCard.tsx
  - src/features/tests/shared/SoftResetAlert.tsx
  - src/features/tests/shared/UseExampleButton.tsx
  - src/shared/charts/ChartCanvas.tsx
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
user_setup: []

must_haves:
  truths:
    - "Chart factories return pure ChartData + ChartOptions consumed by ChartCanvas without imperative new Chart()"
    - "ChartCustomizer exposes type presets, axis titles, annotation toggles, and 3 theme variants per UI-SPEC"
    - "ResultsPanelWithCustomizer preserves PNG export via single canvas ref after customizer changes"
    - "Shared Configurar components render with UI-SPEC copy and accessibility (44px tap targets, role=radiogroup)"
  artifacts:
    - path: "src/shared/charts/chartFactories/index.ts"
      provides: "Re-exports all chart factory builders"
    - path: "src/shared/charts/ChartCustomizer.tsx"
      provides: "Deep customize panel for migrated tests (D-04)"
    - path: "src/shared/charts/ResultsPanelWithCustomizer.tsx"
      provides: "ResultsPanel + customizer grid layout"
    - path: "src/features/tests/shared/ModeChoiceCard.tsx"
      provides: "Selectable mode/method cards with role=radiogroup"
  key_links:
    - from: "ResultsPanelWithCustomizer.tsx"
      to: "ChartCanvas.tsx"
      via: "onCanvasReady + debounced options merge"
      pattern: "onCanvasReady|useChartExport"
    - from: "chartFactories/*.ts"
      to: "assets/js/chart-manager.js"
      via: "ported render* logic as pure data/options"
      pattern: "mergeChartOptions"
---

<objective>
Build the shared chart infrastructure (ported factories + ChartCustomizer + ResultsPanelWithCustomizer) and reusable Configurar components that all three migrated tests mount. Demo stays on plain ResultsPanel (D-06).

Purpose: D-04 requires deep chart customization on migrated tests only. Factories port v1.0 chart-manager render functions to pure data/options for ChartCanvas. Shared components prevent duplicating α selector, didactic cards, and mode choice UX across three modules.
Output: Chart factories, customizer hook/panel, ResultsPanelWithCustomizer, six shared test components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-migrar-testes-existentes/02-UI-SPEC.md
@.planning/phases/02-migrar-testes-existentes/02-PATTERNS.md
@.planning/phases/02-migrar-testes-existentes/02-RESEARCH.md
@assets/js/chart-manager.js
@src/shared/charts/chartTheme.ts
@src/shared/charts/ChartCanvas.tsx
@src/routes/estatistica/ResultsPanel.tsx
@src/routes/estatistica/demo/TesteDemo.tsx
</context>

<interfaces>
From `src/routes/estatistica/ResultsPanel.tsx`:
- Props: title, metrics, chart ({ type, data, options, ariaLabel }), interpretation (string[]), exportFilename, actions?, onCanvasReady?
- ChartCanvas receives merged options via mergeChartOptions(BASE_OPTS, options)

From `src/shared/charts/chartTheme.ts`:
- COLORS, BASE_OPTS, mergeChartOptions exports

From `assets/js/chart-manager.js` (port targets):
- renderTStudentDiffChart, renderTStudentDistChart (~525-589)
- scatter/rank scatter factories for correlacao
- timeseries + residual bar for prais-winsten

ChartCustomizer state shape (define in useChartCustomizer.ts):
- chartTypePreset: string
- axisLabels: { x: string; y: string }
- annotations: Record<string, boolean>
- themeVariant: 'lacir' | 'neutral' | 'publication'
- Returns merged ChartOptions + debounced update (~150ms per Pitfall 5)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port chart factories from chart-manager.js</name>
  <files>src/shared/charts/chartFactories/scatterChart.ts, src/shared/charts/chartFactories/tStudentCharts.ts, src/shared/charts/chartFactories/timeseriesChart.ts, src/shared/charts/chartFactories/residualChart.ts, src/shared/charts/chartFactories/index.ts</files>
  <read_first>
    - `assets/js/chart-manager.js` — locate renderTStudentDiffChart, renderTStudentDistChart, scatter factories, timeseries/residual render functions
    - `src/shared/charts/chartTheme.ts` (COLORS, BASE_OPTS, mergeChartOptions)
    - `02-PATTERNS.md` § chartFactories port pattern (return { data, options } not new Chart)
    - `src/shared/format.ts` (fmtSigned, fmtNumber for tooltip callbacks)
  </read_first>
  <action>
    1. Create `src/shared/charts/chartFactories/tStudentCharts.ts`:
       - buildTStudentDiffChartData(result, labels) — port renderTStudentDiffChart datasets/tooltip callbacks
       - buildTStudentDistChartData(g1, g2, labels) — port distribution chart
       - buildTStudentMeansBarChartData(result, labels) — bar of means preset for customizer
       All return { data: ChartData; options: ChartOptions } using mergeChartOptions(BASE_OPTS, overrides).

    2. Create `scatterChart.ts`:
       - buildScatterChartData(x, y, labels?) — Pearson scatter
       - buildRankScatterChartData(x, y) — Spearman rank scatter
       - buildScatterWithFitChartData(x, y, regressionLine?) — optional regression overlay data

    3. Create `timeseriesChart.ts`:
       - buildTimeseriesChartData(time, observed, fitted?) — observed + fitted Prais line

    4. Create `residualChart.ts`:
       - buildResidualBarChartData(time, residuals) — zero-line annotation config placeholder for annotation plugin

    5. Export all from `index.ts`. Plotted values must match v1.0 tooltip numeric formatting (D-05: values correct, layout may differ). Use fmtSigned/fmtNumber in tooltip callbacks.

    6. Add minimal unit test `src/shared/charts/chartFactories/chartFactories.test.ts`: assert each factory returns non-empty datasets and merged options for a small fixture result object.
  </action>
  <verify>
    <automated>npm run test:run -- src/shared/charts/chartFactories/chartFactories.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Four factory modules export build* functions returning ChartData + ChartOptions
    - [ ] No imperative Chart instantiation in factories
    - [ ] Tooltip callbacks use pt-BR formatters from format.ts
    - [ ] chartFactories.test.ts passes for all factory entry points
  </acceptance_criteria>
  <done>Chart factories ported as pure data/options builders</done>
</task>

<task type="auto">
  <name>Task 2: Build ChartCustomizer and ResultsPanelWithCustomizer</name>
  <files>src/shared/charts/ChartCustomizer.tsx, src/shared/charts/useChartCustomizer.ts, src/shared/charts/ResultsPanelWithCustomizer.tsx, src/shared/charts/ChartCanvas.tsx</files>
  <read_first>
    - `02-UI-SPEC.md` § Component Inventory (ChartCustomizer groups: Tipo de gráfico, Eixos, Anotações, Tema visual)
    - `02-UI-SPEC.md` § Copywriting Contract (Personalizar gráfico, Restaurar padrão do teste)
    - `src/routes/estatistica/ResultsPanel.tsx` lines 48–78 (layout to preserve)
    - `02-RESEARCH.md` Pitfall 5 (PNG export after customizer — debounce, single canvas ref)
    - `src/shared/charts/chartAnnotationSetup.ts` (from 02-01 — ensure imported in ChartCanvas)
  </read_first>
  <action>
    1. Create `useChartCustomizer.ts`:
       - Accept presets: { id, label, buildChart: (engineOutput) => ChartProps }[] and defaultPresetId
       - State: chartTypePreset, axisLabels {x,y}, annotationToggles Record, themeVariant
       - mergeCustomizerIntoOptions(baseOptions) applies axis titles, theme variant color tweaks (3 presets per UI-SPEC: LACIR padrão, Neutro alto contraste, Publicação clara), annotation plugin config from toggles
       - Debounce option updates ~150ms before emitting to ChartCanvas
       - resetToDefault() restores test preset defaults

    2. Create `ChartCustomizer.tsx`:
       - shadcn Select for chart type presets, Switch for annotation toggles, inputs for axis titles
       - Theme Select with 3 options per UI-SPEC
       - "Restaurar padrão do teste" button (44px min height)
       - Panel title "Personalizar gráfico"
       - Collapsible on viewport below lg per UI-SPEC

    3. Create `ResultsPanelWithCustomizer.tsx`:
       - Wrap ResultsPanel metrics + interpretation + PNG button
       - CSS grid: chart 62% / customizer 38% at lg+
       - Pass debounced chart props to ChartCanvas; wire onCanvasReady to useChartExport
       - Do NOT modify TesteDemo — it keeps plain ResultsPanel (D-06)

    4. Update ChartCanvas.tsx to import ensureChartAnnotationsRegistered from chartAnnotationSetup at module load (if not already wired in 02-01).
  </action>
  <verify>
    <automated>npm run test:run -- src/shared/charts/useChartCustomizer.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] useChartCustomizer.test.ts covers preset switch, reset, debounced merge
    - [ ] ResultsPanelWithCustomizer renders metrics, chart, customizer, interpretation, PNG CTA
    - [ ] ChartCustomizer copy matches UI-SPEC Copywriting Contract
    - [ ] TesteDemo.tsx unchanged (still uses ResultsPanel)
  </acceptance_criteria>
  <done>Deep customize panel wired with debounced Chart.js options merge and PNG-safe canvas ref</done>
</task>

<task type="auto">
  <name>Task 3: Create shared Configurar components</name>
  <files>src/features/tests/shared/DidacticCards.tsx, src/features/tests/shared/AlphaSelector.tsx, src/features/tests/shared/ResearchQuestionField.tsx, src/features/tests/shared/ModeChoiceCard.tsx, src/features/tests/shared/SoftResetAlert.tsx, src/features/tests/shared/UseExampleButton.tsx</files>
  <read_first>
    - `02-UI-SPEC.md` § Shared components + Copywriting Contract
    - `tests/t-student/module-guided.js` lines 1474–1497 (choice cards, α select)
    - `src/components/ui/alert.tsx`, `src/components/ui/collapsible` (add shadcn collapsible/select/label/switch if missing via npx shadcn add)
    - `02-PATTERNS.md` § ModeChoiceCard (role=radiogroup, aria-checked, 44px tap height)
  </read_first>
  <action>
    1. DidacticCards: accept didacticCards[] from config; shadcn Collapsible; heading "Entenda este teste"; collapsed by default; titles from config.json.

    2. AlphaSelector: shadcn Select; label "Nível de significância (α)"; options 1%/5%/10%; default 5%; controlled value + onChange.

    3. ResearchQuestionField: Label + 2-row textarea; label "Pergunta de pesquisa"; placeholder prop per test.

    4. ModeChoiceCard: radiogroup container; each card is button role=radio aria-checked; selected = 2px teal border + accent-soft fill; min 44px height; title Label 14px/700, description Body muted.

    5. SoftResetAlert: Alert with teal left border, role=status aria-live=polite; title "Modo alterado."; body per UI-SPEC D-13 copy.

    6. UseExampleButton: Button variant outline; label "Usar exemplo" (capital U per UI-SPEC); onClick loads example text into tabular input.

    7. Add RTL smoke test `src/features/tests/shared/sharedComponents.test.tsx`: render ModeChoiceCard group, AlphaSelector, SoftResetAlert; assert ARIA roles and UI-SPEC copy strings present.

    Install missing shadcn primitives if needed: collapsible, select, label, switch (UI-SPEC Component library list).
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/shared/sharedComponents.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All six shared components export and render with UI-SPEC copy
    - [ ] ModeChoiceCard uses role=radiogroup and aria-checked
    - [ ] sharedComponents.test.tsx passes
    - [ ] Required shadcn primitives present in src/components/ui/
  </acceptance_criteria>
  <done>Reusable Configurar building blocks ready for t-Student, Correlação, Prais modules</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChartCustomizer user input | Axis title strings from text inputs cross into Chart.js options |
| Didactic card content | Static from config.json — not user paste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | ChartCustomizer axis title inputs | mitigate | Sanitize/truncate axis labels to reasonable length (e.g. 120 chars); pass as Chart.js text options only — never innerHTML |
| T-02-02 | Denial of Service | Customizer rapid toggles | mitigate | Debounce ~150ms; ChartCanvas destroy-before-recreate prevents runaway instance creation |
| T-02-03 | Tampering | DidacticCards content | accept | Static config.json strings only; no user HTML |
</threat_model>

<verification>
- Chart factory + customizer + shared component tests green
- TesteDemo still uses plain ResultsPanel (grep confirms no ResultsPanelWithCustomizer import in TesteDemo.tsx)
- Full suite: `npm run test:run`
</verification>

<success_criteria>
- Chart factories return pure ChartData/Options for all three test chart types
- ChartCustomizer implements D-04 deep customize with 3 theme variants
- Shared Configurar components match UI-SPEC copy and accessibility
</success_criteria>

<output>
Create `.planning/phases/02-migrar-testes-existentes/02-02-SUMMARY.md` when done
</output>
