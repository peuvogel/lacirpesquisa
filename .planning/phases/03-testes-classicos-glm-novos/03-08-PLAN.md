---
phase: 03-testes-classicos-glm-novos
plan: 08
type: execute
wave: 4
depends_on: ["03-06"]
files_modified:
  - src/features/tests/logistica/logisticaConfig.ts
  - src/features/tests/logistica/logisticaEngine.ts
  - src/features/tests/logistica/logisticaEngine.test.ts
  - src/features/tests/logistica/logisticaInterpretation.ts
  - src/features/tests/logistica/logisticaInterpretation.test.ts
  - src/features/tests/logistica/logisticaCharts.ts
  - src/features/tests/logistica/LogisticaConfigPanel.tsx
  - src/features/tests/logistica/LogisticaTest.tsx
  - src/features/tests/logistica/LogisticaTest.test.tsx
autonomous: true
requirements: [TEST-09, UX-02]

must_haves:
  truths:
    - "User runs Logistic regression and sees OR with IC95% on metrics (TEST-09, D-06)"
    - "Warning nudge for rare events / separation when outcome extremely imbalanced"
    - "Binary outcome validated (0/1 or two-level factor)"
    - "Engine matches JASP golden at display precision"
  artifacts:
    - path: "src/features/tests/logistica/logisticaEngine.ts"
      provides: "glmEngine.fitLogistic + OR/CI"
    - path: "src/features/tests/logistica/LogisticaTest.tsx"
      provides: "FlowSteps GLM shell"
  key_links:
    - from: "logisticaEngine.ts"
      to: "glmEngine.fitLogistic"
      via: "binary outcome + predictors"
      pattern: "fitLogistic"
    - from: "logisticaEngine.ts"
      to: "exp(beta ± z*SE)"
      via: "OR and CI95% on metrics"
      pattern: "oddsRatio|ciLower|ciUpper"
---

<objective>
Ship Regressão Logística module (TEST-09): binary outcome + predictors, OR with IC95%, separation/imbalance nudge. Parallel with 03-07; registry flip in 03-09.

Purpose: Wave B2b — completes GLM trio; reuses glmCoefForestChart with OR scale.
Output: Complete logistica feature folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testes-classicos-glm-novos/03-CONTEXT.md
@.planning/phases/03-testes-classicos-glm-novos/03-PATTERNS.md
@src/features/tests/poisson/PoissonTest.tsx
@src/shared/stats/glmEngine.ts
@src/test/fixtures/tests/logistica-exemplo.txt
@src/test/fixtures/jasp/logistica-exemplo.golden.json
</context>

<interfaces>
TABULAR_OPTIONS:
- requiredKeys: desfecho_binario + ≥1 predictor
- Outcome: 0/1 numeric or two-level categorical coerced to 0/1

Metrics: OR = exp(β), CI95% = exp(β ± z×SE) per D-06

Nudge: min class proportion &lt;5% or |β|&gt;10 separation heuristic
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port Logistic engine and golden tests</name>
  <files>src/features/tests/logistica/logisticaConfig.ts, src/features/tests/logistica/logisticaEngine.ts, src/features/tests/logistica/logisticaEngine.test.ts</files>
  <behavior>
    - Golden: OR, CI, p match logistica-exemplo.golden.json at display precision
    - Nudge: imbalanced fixture → warning separation/rare events message
    - Validation: non-binary outcome → PT error
  </behavior>
  <read_first>
    - `src/shared/stats/glmEngine.ts` fitLogistic
    - `src/features/tests/poisson/poissonEngine.ts` (design matrix builder)
    - `03-CONTEXT.md` D-11, D-06 Logística nudge
  </read_first>
  <action>
    1. Create logisticaConfig.ts: desfecho_binario + predictor aliases; exampleText; didacticCards.

    2. Create logisticaEngine.ts:
       - buildDatasetFromConfirmed: coerce binary outcome to 0/1; build design matrix
       - validateDataset: exactly 2 outcome levels after coercion; min events per class; PT errors
       - runAnalysis: glmEngine.fitLogistic
       - buildMetrics: OR + CI95% per term (not raw β on primary cards); deviance
       - computeAssumptionNudges: min class &lt;5% or |β|&gt;10 → warning (D-06/D-07)
       - toEngineOutput for OR forest chart

    3. logisticaEngine.test.ts: golden OR/CI parity; nudge cases; binary validation.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/logistica/logisticaEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] OR and CI95% match golden at display precision
    - [ ] Imbalance nudge fires on sparse outcome fixture
    - [ ] Non-binary outcome rejected with friendly PT message
  </acceptance_criteria>
  <done>Logistic engine verified against JASP golden</done>
</task>

<task type="auto">
  <name>Task 2: Logistic UI with OR forest chart and FlowSteps shell</name>
  <files>src/features/tests/logistica/logisticaInterpretation.ts, src/features/tests/logistica/logisticaInterpretation.test.ts, src/features/tests/logistica/logisticaCharts.ts, src/features/tests/logistica/LogisticaConfigPanel.tsx, src/features/tests/logistica/LogisticaTest.tsx, src/features/tests/logistica/LogisticaTest.test.tsx</files>
  <read_first>
    - `src/features/tests/poisson/PoissonTest.tsx`
    - `src/shared/charts/chartFactories/glmCoefForestChart.ts` (OR scale option)
    - `03-PATTERNS.md` § LogisticaTest
  </read_first>
  <action>
    1. logisticaInterpretation.ts: alpha-aware; report OR and CI in prose; plain PT string[].

    2. logisticaCharts.ts: glmCoefForestChart with OR scale and CI whiskers; default preset.

    3. LogisticaConfigPanel.tsx + LogisticaTest.tsx:
       - FlowSteps clone from Poisson (no NB CTA)
       - AssumptionNudgeStrip for imbalance/separation
       - Soft-reset on role change
       - exportFilename logistica-lacirstat.png

    4. LogisticaTest.test.tsx: smoke flow; assert OR labels on metrics; nudge on imbalanced exemplo variant if needed.

    Do NOT flip registry — 03-09.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/logistica/logisticaInterpretation.test.ts src/features/tests/logistica/LogisticaTest.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] OR + IC95% visible in results smoke
    - [ ] AssumptionNudgeStrip renders on imbalance case
    - [ ] FlowSteps smoke green
  </acceptance_criteria>
  <done>Complete Logistic module ready for Wave B gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Binary outcome coercion | User categorical/numeric → 0/1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Tampering | Outcome coercion | mitigate | Explicit two-level check; reject &gt;2 levels with PT error |
| T-03-08-02 | Tampering | logisticaInterpretation | mitigate | string[] only; fmtNumber for OR display |
| T-03-08-03 | Denial of Service | Logistic IRLS | mitigate | Reuse glmEngine caps; warn on separation instead of infinite loop |
</threat_model>

<verification>
- npm run test:run -- src/features/tests/logistica/ green
- npm run typecheck
</verification>

<success_criteria>
- TEST-09 complete with OR+CI95% and UX-02 nudges
- Parallel-safe with 03-07
</success_criteria>

<output>
Create `.planning/phases/03-testes-classicos-glm-novos/03-08-SUMMARY.md` when done
</output>
