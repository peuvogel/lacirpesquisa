---
phase: 03-testes-classicos-glm-novos
plan: 03
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/features/tests/anova-tukey/anovaConfig.ts
  - src/features/tests/anova-tukey/anovaEngine.ts
  - src/features/tests/anova-tukey/anovaEngine.test.ts
  - src/features/tests/anova-tukey/anovaInterpretation.ts
  - src/features/tests/anova-tukey/anovaInterpretation.test.ts
  - src/features/tests/anova-tukey/anovaCharts.ts
  - src/features/tests/anova-tukey/AnovaConfigPanel.tsx
  - src/features/tests/anova-tukey/AnovaTukeyTest.tsx
  - src/features/tests/anova-tukey/AnovaTukeyTest.test.tsx
  - src/shared/charts/chartFactories/anovaChart.ts
  - src/shared/charts/chartFactories/postHocHeatmapChart.ts
autonomous: true
requirements: [TEST-05, UX-02]

must_haves:
  truths:
    - "User runs one-way ANOVA with F, p, η² on metric cards and Tukey pairwise table below (TEST-05, D-14, D-16)"
    - "Pairwise table sorted by p ajustado; heatmap preset when k≤6 groups (D-15)"
    - "Nudge suggests Kruskal-Wallis when group sizes unequal or SD ratio high (D-06, D-03)"
    - "k=2 groups shows info tip that t-Student may be simpler (D-10)"
  artifacts:
    - path: "src/features/tests/anova-tukey/anovaEngine.ts"
      provides: "oneWayAnova + tukeyHsd wrapper"
    - path: "src/features/tests/anova-tukey/AnovaTukeyTest.tsx"
      provides: "Pairwise table section + AssumptionNudgeStrip"
    - path: "src/shared/charts/chartFactories/postHocHeatmapChart.ts"
      provides: "Adjusted-p heatmap for k≤6"
  key_links:
    - from: "anovaEngine.ts"
      to: "statsEngine.oneWayAnova|tukeyHsd"
      via: "grouped numeric outcome"
      pattern: "oneWayAnova|tukeyHsd"
    - from: "AnovaTukeyTest.tsx"
      to: "kruskal-dunn"
      via: "nudge CTA onNavigateTest"
      pattern: "kruskal-dunn"
---

<objective>
Ship ANOVA de uma via + Tukey post-hoc module (TEST-05): numeric outcome + categorical factor, omnibus metrics, compact pairwise table, optional heatmap, cross-nudge to Kruskal. Registry flip deferred to 03-05.

Purpose: Wave A2a — parallel with Kruskal (03-04). Validates post-hoc table pattern (D-14) and ANOVA→Kruskal cross-nudge (D-03).
Output: Complete anova-tukey feature folder with engine golden parity and RTL smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testes-classicos-glm-novos/03-CONTEXT.md
@.planning/phases/03-testes-classicos-glm-novos/03-PATTERNS.md
@src/features/tests/t-student/TStudentTest.tsx
@src/test/fixtures/tests/anova-tukey-exemplo.txt
@src/test/fixtures/jasp/anova-tukey-exemplo.golden.json
</context>

<interfaces>
TABULAR_OPTIONS:
- requiredKeys: desfecho (numeric), grupo (categorical)
- numericKeys: desfecho only

PairwiseRow: { contrast, statistic, pAdj, ciLower?, ciUpper? } — sort pAdj ascending

Engine returns pairwise[] on AnalysisResult for table rendering in AnovaTukeyTest.tsx
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port ANOVA+Tukey engine and golden tests</name>
  <files>src/features/tests/anova-tukey/anovaConfig.ts, src/features/tests/anova-tukey/anovaEngine.ts, src/features/tests/anova-tukey/anovaEngine.test.ts</files>
  <behavior>
    - Golden: F, df, p, eta2 match anova-tukey-exemplo.golden.json at display precision
    - Tukey: at least one pairwise row with pAdj matching golden
    - Nudge: unequal n ratio &gt;3 → warning with kruskal-dunn CTA
    - k=2: info nudge about t-Student
  </behavior>
  <read_first>
    - `src/shared/stats/statsEngine.ts` oneWayAnova, tukeyHsd (03-01)
    - `03-CONTEXT.md` D-10, D-14, D-16
    - `03-RESEARCH.md` Tukey default (JASP ANOVA)
  </read_first>
  <action>
    1. Create anovaConfig.ts: TABULAR_OPTIONS desfecho+grupo; exampleText; didacticCards; defaultQuestion.

    2. Create anovaEngine.ts:
       - buildDatasetFromConfirmed: group numeric desfecho by categorical grupo levels
       - validateDataset: ≥2 groups, ≥2 obs per group for Tukey, desfecho numeric
       - runAnalysis: statsEngine.oneWayAnova + tukeyHsd; return pairwise sorted by pAdj
       - buildMetrics: F, df, p, η², group ns and means summary
       - computeAssumptionNudges per D-06: max(n)/min(n)&gt;3, SD ratio&gt;2 → Kruskal CTA; k=2 → t-Student info; lightweight normality hint (no Shapiro package)
       - toEngineOutput: groups, pairwise, omnibus for charts

    3. Create anovaEngine.test.ts: golden parity omnibus + Tukey; nudge cases; validation errors.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/anova-tukey/anovaEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Omnibus and Tukey pAdj match golden at display precision
    - [ ] Pairwise default sort by pAdj ascending
    - [ ] Cross-nudge CTA targets kruskal-dunn testId
  </acceptance_criteria>
  <done>ANOVA+Tukey engine verified against JASP golden</done>
</task>

<task type="auto">
  <name>Task 2: UI with pairwise table, charts, and FlowSteps shell</name>
  <files>src/features/tests/anova-tukey/anovaInterpretation.ts, src/features/tests/anova-tukey/anovaInterpretation.test.ts, src/features/tests/anova-tukey/anovaCharts.ts, src/shared/charts/chartFactories/anovaChart.ts, src/shared/charts/chartFactories/postHocHeatmapChart.ts, src/features/tests/anova-tukey/AnovaConfigPanel.tsx, src/features/tests/anova-tukey/AnovaTukeyTest.tsx, src/features/tests/anova-tukey/AnovaTukeyTest.test.tsx</files>
  <read_first>
    - `03-PATTERNS.md` § Pairwise results table (ColumnPreviewTable styling)
    - `src/features/tests/t-student/TStudentTest.tsx`
    - `03-CONTEXT.md` D-15 (heatmap k≤6 only)
  </read_first>
  <action>
    1. Create anovaInterpretation.ts: alpha-aware; mention post-hoc Tukey when significant; plain PT string[].

    2. Create anovaChart.ts: group means with CI or box summary preset.

    3. Create postHocHeatmapChart.ts: adjusted-p matrix heatmap; register as chart preset only when group count ≤6.

    4. Create anovaCharts.ts: means preset default; conditional heatmap preset when k≤6.

    5. Create AnovaConfigPanel.tsx + AnovaTukeyTest.tsx:
       - FlowSteps shell; soft-reset on grupo column change
       - Resultados layout: AssumptionNudgeStrip → section "Comparações par a par" with bordered table (contraste | estatística | p ajustado | IC) → ResultsPanelWithCustomizer
       - handleCrossTestHandoff for Kruskal CTA via EstatisticaPage pattern (stub navigate callback in test)

    6. AnovaTukeyTest.test.tsx: smoke flow; assert pairwise table headers; heatmap preset absent when k&gt;6 in default exemplo.

    Do NOT flip registry — 03-05.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/anova-tukey/anovaInterpretation.test.ts src/features/tests/anova-tukey/AnovaTukeyTest.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Pairwise table renders between nudge strip and chart panel (D-16)
    - [ ] Heatmap preset available only for k≤6
    - [ ] FlowSteps smoke green
  </acceptance_criteria>
  <done>Complete ANOVA+Tukey module ready for Wave A gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User paste → group vectors | Numeric outcome validation before ANOVA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | anovaInterpretation | mitigate | string[] only; fmtP at build time |
| T-03-03-02 | Denial of Service | Group iteration | mitigate | Cap max groups (e.g. 20) and total N before Tukey all-pairs |
| T-03-03-03 | Tampering | Cross-test CTA | mitigate | CTA testId whitelist: kruskal-dunn only from computeAssumptionNudges |
</threat_model>

<verification>
- npm run test:run -- src/features/tests/anova-tukey/ green
- npm run typecheck
</verification>

<success_criteria>
- TEST-05 complete with Tukey table, optional heatmap, UX-02 nudges
- Parallel-safe with 03-04 (no file overlap)
</success_criteria>

<output>
Create `.planning/phases/03-testes-classicos-glm-novos/03-03-SUMMARY.md` when done
</output>
