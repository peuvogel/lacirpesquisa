---
phase: 03-testes-classicos-glm-novos
plan: 05
type: execute
wave: 2
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - src/features/tests/registry.ts
  - src/features/tests/registry.test.ts
  - src/routes/estatistica/EstatisticaPage.tsx
  - src/routes/estatistica/EstatisticaPage.test.tsx
  - src/routes/estatistica/Sidebar.tsx
  - src/routes/estatistica/Sidebar.test.tsx
  - src/routes/estatistica/QualTesteModal.tsx
  - src/routes/estatistica/SidebarTestLink.tsx
autonomous: true
requirements: [TEST-04, TEST-05, TEST-06, UX-02]

must_haves:
  truths:
    - "qui-quadrado, anova-tukey, kruskal-dunn show Disponível in sidebar and Qual teste modal (D-02)"
    - "EstatisticaPage renders all three classical modules via static switch"
    - "AssumptionNudgeStrip wired on all three with empirical nudges (UX-02)"
    - "Cross-nudge ANOVA→Kruskal handoff preserves recognizedColumns"
    - "registry.test.ts expects 7 available entries (demo + 3 Phase 2 + 3 Wave A)"
  artifacts:
    - path: "src/features/tests/registry.ts"
      provides: "status available for three classical tests"
    - path: "src/routes/estatistica/EstatisticaPage.tsx"
      provides: "renderActiveTest switch for Wave A modules"
  key_links:
    - from: "EstatisticaPage.tsx"
      to: "QuiQuadradoTest|AnovaTukeyTest|KruskalDunnTest"
      via: "static import switch"
      pattern: "case 'qui-quadrado'|case 'anova-tukey'|case 'kruskal-dunn'"
    - from: "AnovaTukeyTest.tsx"
      to: "EstatisticaHandoffState"
      via: "onNavigateTest → activeTestId kruskal-dunn + recognizedColumns"
      pattern: "recognizedColumns"
---

<objective>
Wave A integration gate: flip registry for three classical tests, wire EstatisticaPage routes, verify UX-02 nudges end-to-end, and enable ANOVA→Kruskal cross-handoff. GLM tests remain em-breve until 03-09.

Purpose: D-02 requires complete modules before registry flip; D-01 Wave A must land before Wave B GLM work proceeds in earnest.
Output: Three classical tests available in sidebar/modal with working navigation and handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testes-classicos-glm-novos/03-CONTEXT.md
@.planning/phases/02-migrar-testes-existentes/02-06-PLAN.md
@src/features/tests/registry.ts
@src/routes/estatistica/EstatisticaPage.tsx
@src/features/tests/registry.test.ts
</context>

<interfaces>
Registry entries to flip (lines 63-86):
- qui-quadrado, anova-tukey, kruskal-dunn: em-breve → available

EstatisticaHandoffState extend:
- activeTestId?: string
- recognizedColumns?: Record<string, number>

Keep poisson, binomial-negativa, logistica em-breve.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Flip Wave A registry and extend registry tests</name>
  <files>src/features/tests/registry.ts, src/features/tests/registry.test.ts</files>
  <read_first>
    - `src/features/tests/registry.ts` lines 29-110
    - `src/features/tests/registry.test.ts`
    - `02-06-PLAN.md` Task 1 (Phase 2 flip pattern)
  </read_first>
  <action>
    1. Set status 'available' for qui-quadrado, anova-tukey, kruskal-dunn only. Do not change ids, titles, groups, phase fields.

    2. Extend registry.test.ts:
       - Expect exactly 7 available: demo, t-student, correlacao, prais-winsten, qui-quadrado, anova-tukey, kruskal-dunn
       - Assert isTestAvailable true for each Wave A id
       - Assert poisson, binomial-negativa, logistica still em-breve

    3. Verify SidebarTestLink badge: available → "Disponível" teal; em-breve unchanged.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Three classical tests status available
    - [ ] GLM three still em-breve
    - [ ] registry.test.ts passing with count 7
  </acceptance_criteria>
  <done>Registry reflects Wave A shipped tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire EstatisticaPage, handoff, and UX-02 integration tests</name>
  <files>src/routes/estatistica/EstatisticaPage.tsx, src/routes/estatistica/EstatisticaPage.test.tsx, src/routes/estatistica/Sidebar.tsx, src/routes/estatistica/Sidebar.test.tsx, src/routes/estatistica/QualTesteModal.tsx, src/routes/estatistica/SidebarTestLink.tsx</files>
  <read_first>
    - `src/routes/estatistica/EstatisticaPage.tsx` renderActiveTest switch
    - `03-PATTERNS.md` § EstatisticaPage handoff
    - `03-CONTEXT.md` D-03 cross-nudge ANOVA→Kruskal
  </read_first>
  <action>
    1. Extend EstatisticaHandoffState with recognizedColumns if not present.

    2. Add static imports and switch cases for QuiQuadradoTest, AnovaTukeyTest, KruskalDunnTest.

    3. Implement handleCrossTestHandoff(testId, roleSnapshot) passed to modules:
       - setActiveTestId(testId)
       - preserve recognizedColumns in location.state or session for Configurar pre-fill

    4. Wire AnovaTukeyTest onNavigateTest to handoff kruskal-dunn with same column mapping.

    5. Extend EstatisticaPage.test.tsx:
       - Each Wave A test id renders without null
       - ANOVA nudge CTA navigates to kruskal-dunn with recognizedColumns preserved (mock navigate)

    6. Extend Sidebar.test.tsx / QualTesteModal: Wave A entries clickable (not disabled em-breve).

    7. Smoke-run all three module RTL tests still green after integration.
  </action>
  <verify>
    <automated>npm run test:run -- src/routes/estatistica/EstatisticaPage.test.tsx src/routes/estatistica/Sidebar.test.tsx src/features/tests/qui-quadrado/QuiQuadradoTest.test.tsx src/features/tests/anova-tukey/AnovaTukeyTest.test.tsx src/features/tests/kruskal-dunn/KruskalDunnTest.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All three modules mount from sidebar selection
    - [ ] AssumptionNudgeStrip visible in each module smoke test
    - [ ] ANOVA→Kruskal handoff test passes
    - [ ] Qual teste modal shows Disponível for Wave A tests
  </acceptance_criteria>
  <done>Wave A classical tests integrated and available in app shell</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Handoff state | recognizedColumns crosses route navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | EstatisticaHandoffState | mitigate | Validate testId against isTestAvailable before switch; ignore unknown ids |
| T-03-05-02 | Elevation of Privilege | Registry flip | mitigate | Flip only after 03-02/03/04 engine+RTL tests pass in CI |
</threat_model>

<verification>
- npm run test:run -- src/features/tests/registry.test.ts src/routes/estatistica/EstatisticaPage.test.tsx green
- npm run test:run -- src/features/tests/qui-quadrado/ src/features/tests/anova-tukey/ src/features/tests/kruskal-dunn/ green
</verification>

<success_criteria>
- Wave A complete: three classical tests available with UX-02 nudges
- Cross-nudge ANOVA→Kruskal handoff working
- GLM tests still em-breve; Wave B unblocked
</success_criteria>

<output>
Create `.planning/phases/03-testes-classicos-glm-novos/03-05-SUMMARY.md` when done
</output>
