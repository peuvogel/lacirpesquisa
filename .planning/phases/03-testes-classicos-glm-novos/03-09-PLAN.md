---
phase: 03-testes-classicos-glm-novos
plan: 09
type: execute
wave: 5
depends_on: ["03-06", "03-07", "03-08"]
files_modified:
  - src/features/tests/registry.ts
  - src/features/tests/registry.test.ts
  - src/routes/estatistica/EstatisticaPage.tsx
  - src/routes/estatistica/EstatisticaPage.test.tsx
  - src/routes/estatistica/QualTesteModal.tsx
  - src/routes/estatistica/Sidebar.test.tsx
autonomous: true
requirements: [TEST-04, TEST-05, TEST-06, TEST-07, TEST-08, TEST-09, UX-02]

must_haves:
  truths:
    - "All six Phase 3 tests show Disponível in sidebar and Qual teste modal (D-02)"
    - "registry.test.ts expects 10 available entries (demo + 3 Phase 2 + 6 Phase 3)"
    - "Poisson→NB handoff integration test passes with preserved column roles (D-20)"
    - "Full test suite green; each test family has engine golden + RTL smoke"
    - "Phase 3 success criteria from ROADMAP.md satisfied"
  artifacts:
    - path: "src/features/tests/registry.ts"
      provides: "all six Phase 3 tests available"
    - path: "src/routes/estatistica/EstatisticaPage.tsx"
      provides: "full renderActiveTest switch for all Phase 3 modules"
  key_links:
    - from: "registry.ts"
      to: "TEST_REGISTRY"
      via: "six entries status available"
      pattern: "poisson|binomial-negativa|logistica.*available"
    - from: "EstatisticaPage.test.tsx"
      to: "PoissonTest → BinomialNegativaTest"
      via: "handoff integration test"
      pattern: "binomial-negativa"
---

<objective>
Wave B / Phase 3 final gate: flip registry for three GLM tests, wire EstatisticaPage routes, run full verification across all six new modules, confirm Poisson→NB handoff and UX-02 on every test.

Purpose: D-01 Wave B completion; D-02 requires no half-wired entries; ROADMAP Phase 3 success criteria must all be TRUE.
Output: Phase 3 complete — six tests available, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-testes-classicos-glm-novos/03-CONTEXT.md
@.planning/phases/03-testes-classicos-glm-novos/03-RESEARCH.md
@.planning/phases/03-testes-classicos-glm-novos/03-05-SUMMARY.md
@src/features/tests/registry.ts
@src/routes/estatistica/EstatisticaPage.tsx
</context>

<interfaces>
Registry flip (remaining em-breve → available):
- poisson, binomial-negativa, logistica

Final available count: 10 (demo + t-student + correlacao + prais-winsten + 6 Phase 3)

EstatisticaPage switch add:
- case 'poisson': PoissonTest
- case 'binomial-negativa': BinomialNegativaTest
- case 'logistica': LogisticaTest
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Flip Wave B registry and wire GLM routes</name>
  <files>src/features/tests/registry.ts, src/features/tests/registry.test.ts, src/routes/estatistica/EstatisticaPage.tsx, src/routes/estatistica/QualTesteModal.tsx, src/routes/estatistica/Sidebar.test.tsx</files>
  <read_first>
    - `03-05-PLAN.md` Task 1 (Wave A flip pattern)
    - `src/features/tests/registry.ts` lines 87-110
    - `src/routes/estatistica/EstatisticaPage.tsx`
  </read_first>
  <action>
    1. Set status 'available' for poisson, binomial-negativa, logistica.

    2. Extend registry.test.ts:
       - Expect exactly 10 available entries
       - All six Phase 3 ids isTestAvailable === true
       - No em-breve entries remain for phase 3 tests

    3. Add EstatisticaPage switch cases with static imports for PoissonTest, BinomialNegativaTest, LogisticaTest.

    4. QualTesteModal + Sidebar: GLM entries clickable with Disponível badge.

    5. Confirm Wave A three cases still present (no regression from 03-05).
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/registry.test.ts src/routes/estatistica/Sidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All six Phase 3 tests available in registry
    - [ ] registry.test.ts count === 10
    - [ ] EstatisticaPage mounts all three GLM modules
  </acceptance_criteria>
  <done>Registry and routing complete for entire Phase 3</done>
</task>

<task type="auto">
  <name>Task 2: Full phase verification — handoffs, nudges, suite green</name>
  <files>src/routes/estatistica/EstatisticaPage.test.tsx</files>
  <read_first>
    - `03-RESEARCH.md` § Validation Architecture Phase Requirements → Test Map
    - `03-CONTEXT.md` success criteria (D-04 through D-22)
    - All six *Engine.test.ts and *Test.test.tsx from plans 03-02 through 03-08
  </read_first>
  <action>
    1. Extend EstatisticaPage.test.tsx:
       - Poisson overdispersion CTA → navigates to binomial-negativa with recognizedColumns (D-20 integration test)
       - Smoke mount each GLM test id

    2. Run full Phase 3 test matrix:
       - npm run test:run -- src/shared/stats/statsEngine.test.ts src/shared/stats/glmEngine.test.ts
       - npm run test:run -- src/features/tests/qui-quadrado/ src/features/tests/anova-tukey/ src/features/tests/kruskal-dunn/
       - npm run test:run -- src/features/tests/poisson/ src/features/tests/binomial-negativa/ src/features/tests/logistica/
       - npm run test:run -- src/features/tests/shared/sharedComponents.test.tsx

    3. Run npm run typecheck and npm run test:run (full suite).

    4. Verify ROADMAP Phase 3 success criteria checklist in SUMMARY:
       - χ² effect size + expected-cell warning
       - ANOVA Tukey + Kruskal Dunn post-hoc
       - Poisson overdispersion + NB separate entry
       - Logistic OR + CI95%
       - UX-02 nudges on all six tests

    5. Document any golden fixture source confidence (R vs textbook) in 03-09-SUMMARY.md.
  </action>
  <verify>
    <automated>npm run test:run && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Full test suite green
    - [ ] Poisson→NB handoff integration test passes
    - [ ] All six engine golden tests pass
    - [ ] All six RTL smoke tests pass
    - [ ] No Phase 3 registry entries remain em-breve
  </acceptance_criteria>
  <done>Phase 3 verification complete — ready for Phase 4</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full app routing | All test modules mounted from registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Tampering | Registry completeness | mitigate | registry.test.ts asserts exact available set; CI fails on drift |
| T-03-09-02 | Repudiation | Golden fixture staleness | accept | Document fixture source in SUMMARY; R regen script for maintenance |
| T-03-09-03 | Denial of Service | Full suite runtime | accept | Capacitação-sized fixtures only; suite completes in CI budget |
</threat_model>

<verification>
- npm run test:run green (full suite)
- npm run typecheck green
- registry.test.ts: 10 available, 0 Phase 3 em-breve
- Manual spot-check optional: one PNG export per test family (document in SUMMARY if skipped)
</verification>

<success_criteria>
- All TEST-04 through TEST-09 and UX-02 requirements shipped
- Six tests available in sidebar and Qual teste modal
- Phase 3 ROADMAP success criteria satisfied
- Phase 4 unblocked
</success_criteria>

<output>
Create `.planning/phases/03-testes-classicos-glm-novos/03-09-SUMMARY.md` when done
</output>
