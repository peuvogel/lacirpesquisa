---
phase: 03-testes-classicos-glm-novos
plan: 04
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/features/tests/kruskal-dunn/kruskalConfig.ts
  - src/features/tests/kruskal-dunn/kruskalEngine.ts
  - src/features/tests/kruskal-dunn/kruskalEngine.test.ts
  - src/features/tests/kruskal-dunn/kruskalInterpretation.ts
  - src/features/tests/kruskal-dunn/kruskalInterpretation.test.ts
  - src/features/tests/kruskal-dunn/kruskalCharts.ts
  - src/features/tests/kruskal-dunn/KruskalConfigPanel.tsx
  - src/features/tests/kruskal-dunn/KruskalDunnTest.tsx
  - src/features/tests/kruskal-dunn/KruskalDunnTest.test.tsx
autonomous: true
requirements: [TEST-06, UX-02]

must_haves:
  truths:
    - "User runs Kruskal-Wallis with H, p on metrics and Dunn pairwise table with Holm-adjusted p (TEST-06)"
    - "Info nudge explains rank-based alternative when normality doubtful (D-06)"
    - "Pairwise table sorted by p ajustado; heatmap when k≤6 reuses postHocHeatmapChart (D-14, D-15)"
    - "Engine matches JASP golden at display precision"
  artifacts:
    - path: "src/features/tests/kruskal-dunn/kruskalEngine.ts"
      provides: "kruskalWallis + dunnPostHoc wrapper"
    - path: "src/features/tests/kruskal-dunn/KruskalDunnTest.tsx"
      provides: "FlowSteps + pairwise section"
  key_links:
    - from: "kruskalEngine.ts"
      to: "statsEngine.kruskalWallis|dunnPostHoc|rank"
      via: "grouped numeric outcome"
      pattern: "kruskalWallis|dunnPostHoc"
    - from: "kruskalCharts.ts"
      to: "postHocHeatmapChart.ts"
      via: "shared factory from 03-03"
      pattern: "postHocHeatmap"
---

<objective>
Ship Kruskal-Wallis + Dunn post-hoc module (TEST-06): same role mapping as ANOVA (desfecho + grupo), rank-based omnibus, Holm-adjusted Dunn pairwise, info nudges. Parallel with 03-03; registry flip in 03-05.

Purpose: Wave A2b — nonparametric counterpart; separate registry entry per D-03; reuses postHocHeatmapChart from 03-03 when that plan lands first (if 03-04 runs first, import factory stub or duplicate minimally — prefer waiting for 03-03 postHocHeatmapChart or implement thin re-export).
Output: Complete kruskal-dunn feature folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testes-classicos-glm-novos/03-CONTEXT.md
@.planning/phases/03-testes-classicos-glm-novos/03-PATTERNS.md
@src/features/tests/anova-tukey/anovaEngine.ts
@src/test/fixtures/tests/kruskal-dunn-exemplo.txt
@src/test/fixtures/jasp/kruskal-dunn-exemplo.golden.json
</context>

<interfaces>
Same TABULAR_OPTIONS shape as ANOVA: desfecho numeric, grupo categorical.

Dunn pairwise: Holm-adjusted pAdj as primary sort key (RESEARCH A2).

Reuse anovaChart.ts for rank/median summary visualization (import from shared chartFactories).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port Kruskal+Dunn engine and golden tests</name>
  <files>src/features/tests/kruskal-dunn/kruskalConfig.ts, src/features/tests/kruskal-dunn/kruskalEngine.ts, src/features/tests/kruskal-dunn/kruskalEngine.test.ts</files>
  <behavior>
    - Golden: H, df, p match kruskal-dunn-exemplo.golden.json
    - Dunn: pairwise pAdj (Holm) matches golden at display precision
    - Nudge: always includes info rank-alternative message
    - k=2: optional Mann-Whitney info line
  </behavior>
  <read_first>
    - `src/shared/stats/statsEngine.ts` kruskalWallis, dunnPostHoc, rank
    - `src/features/tests/anova-tukey/anovaEngine.ts` (dataset builder pattern — parallel structure)
    - `03-CONTEXT.md` D-06 Kruskal nudges
  </read_first>
  <action>
    1. Create kruskalConfig.ts: mirror ANOVA role keys; distinct didacticCards emphasizing nonparametric use.

    2. Create kruskalEngine.ts:
       - buildDatasetFromConfirmed: same desfecho+grupo pattern as ANOVA
       - validateDataset: ≥3 groups recommended tip in nudge; allow k≥2
       - runAnalysis: statsEngine.kruskalWallis + dunnPostHoc; pairwise sorted by pAdj
       - buildMetrics: H, df, p, group medians/rank summaries
       - computeAssumptionNudges: info rank-alternative; k=2 Mann-Whitney note; k&lt;3 post-hoc tip
       - toEngineOutput for charts + table

    3. kruskalEngine.test.ts: golden parity; Holm pAdj; nudge cases.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/kruskal-dunn/kruskalEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Kruskal H and Dunn Holm pAdj match golden
    - [ ] Pairwise sorted by pAdj ascending
    - [ ] Info nudge always present per D-06
  </acceptance_criteria>
  <done>Kruskal+Dunn engine verified against JASP golden</done>
</task>

<task type="auto">
  <name>Task 2: UI with pairwise table and FlowSteps shell</name>
  <files>src/features/tests/kruskal-dunn/kruskalInterpretation.ts, src/features/tests/kruskal-dunn/kruskalInterpretation.test.ts, src/features/tests/kruskal-dunn/kruskalCharts.ts, src/features/tests/kruskal-dunn/KruskalConfigPanel.tsx, src/features/tests/kruskal-dunn/KruskalDunnTest.tsx, src/features/tests/kruskal-dunn/KruskalDunnTest.test.tsx</files>
  <read_first>
    - `src/features/tests/anova-tukey/AnovaTukeyTest.tsx` (pairwise table layout — copy structure)
    - `src/shared/charts/chartFactories/anovaChart.ts` (reuse for rank summary)
    - `src/shared/charts/chartFactories/postHocHeatmapChart.ts` (from 03-03 if available)
  </read_first>
  <action>
    1. kruskalInterpretation.ts: alpha-aware; mention Dunn post-hoc; plain PT string[].

    2. kruskalCharts.ts: rank/median summary preset via anovaChart factory; heatmap preset when k≤6 importing postHocHeatmapChart.

    3. KruskalConfigPanel.tsx + KruskalDunnTest.tsx:
       - Identical FlowSteps/pairwise table layout to AnovaTukeyTest
       - AssumptionNudgeStrip above metrics
       - Section "Comparações par a par" with Dunn results
       - Soft-reset on grupo change

    4. KruskalDunnTest.test.tsx: smoke flow; pairwise table present; interpretation visible.

    Do NOT flip registry — 03-05.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/kruskal-dunn/kruskalInterpretation.test.ts src/features/tests/kruskal-dunn/KruskalDunnTest.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Pairwise table between nudge and chart panel
    - [ ] FlowSteps smoke green
    - [ ] No registry flip in this plan
  </acceptance_criteria>
  <done>Complete Kruskal+Dunn module ready for Wave A gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User paste → rank computation | Same validation as ANOVA path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | kruskalInterpretation | mitigate | string[] only |
| T-03-04-02 | Denial of Service | Dunn all-pairs | mitigate | Reuse ANOVA group cap; reject k&gt;20 |
| T-03-04-03 | Information Disclosure | Rank ties | accept | Tie correction in engine; no PII in ranks |
</threat_model>

<verification>
- npm run test:run -- src/features/tests/kruskal-dunn/ green
- npm run typecheck
</verification>

<success_criteria>
- TEST-06 complete with Dunn Holm pairwise and UX-02 nudges
- Parallel-safe with 03-03
</success_criteria>

<output>
Create `.planning/phases/03-testes-classicos-glm-novos/03-04-SUMMARY.md` when done
</output>
