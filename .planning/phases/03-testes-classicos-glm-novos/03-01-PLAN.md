---
phase: 03-testes-classicos-glm-novos
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/shared/stats/statsEngine.ts
  - src/shared/stats/statsEngine.test.ts
  - src/shared/stats/glmEngine.ts
  - src/shared/stats/glmEngine.test.ts
  - src/features/tests/shared/AssumptionNudgeStrip.tsx
  - src/features/tests/shared/assumptionNudges.ts
  - src/features/tests/shared/sharedComponents.test.tsx
  - src/test/fixtures/tests/qui-quadrado-exemplo.txt
  - src/test/fixtures/tests/anova-tukey-exemplo.txt
  - src/test/fixtures/tests/kruskal-dunn-exemplo.txt
  - src/test/fixtures/tests/poisson-exemplo.txt
  - src/test/fixtures/tests/binomial-negativa-exemplo.txt
  - src/test/fixtures/tests/logistica-exemplo.txt
  - src/test/fixtures/jasp/qui-quadrado-exemplo.golden.json
  - src/test/fixtures/jasp/anova-tukey-exemplo.golden.json
  - src/test/fixtures/jasp/kruskal-dunn-exemplo.golden.json
  - src/test/fixtures/jasp/poisson-exemplo.golden.json
  - src/test/fixtures/jasp/binomial-negativa-exemplo.golden.json
  - src/test/fixtures/jasp/logistica-exemplo.golden.json
  - scripts/oracle/generate-phase3-fixtures.R
autonomous: false
requirements: [TEST-04, TEST-05, TEST-06, TEST-07, TEST-08, TEST-09, UX-02]
user_setup:
  - service: npm
    why: "Install jstat and ml-matrix for classical distributions, Tukey HSD, and GLM IRLS (D-19)"
    env_vars: []
    dashboard_config:
      - task: "Human-verify package legitimacy before install"
        location: "https://www.npmjs.com/package/jstat — confirm github.com/jstat/jstat, 10+ yrs, high downloads"
      - task: "Human-verify ml-matrix legitimacy before install"
        location: "https://www.npmjs.com/package/ml-matrix — confirm github.com/mljs/matrix, 8+ yrs, high downloads"

must_haves:
  truths:
    - "statsEngine exposes χ² independence, one-way ANOVA, Kruskal-Wallis, Tukey HSD, and Dunn post-hoc with jstat-backed CDFs"
    - "glmEngine fits Poisson, Negative Binomial, and Logistic models via IRLS and returns Wald p-values, OR/CI for logistic"
    - "Six golden JSON fixtures exist and glmEngine/statsEngine tests pass at display-rounded precision (D-21)"
    - "AssumptionNudgeStrip renders info/warning nudges without blocking results (D-04, UX-02)"
  artifacts:
    - path: "src/shared/stats/statsEngine.ts"
      provides: "Classical test primitives"
      exports: ["runChiSquareIndependence", "oneWayAnova", "kruskalWallis", "tukeyHsd", "dunnPostHoc"]
    - path: "src/shared/stats/glmEngine.ts"
      provides: "IRLS GLM for Poisson/NB/Logistic"
      exports: ["fitPoisson", "fitNegativeBinomial", "fitLogistic"]
    - path: "src/features/tests/shared/AssumptionNudgeStrip.tsx"
      provides: "UX-02 soft nudge strip"
    - path: "src/test/fixtures/jasp/*.golden.json"
      provides: "JASP-oracle numeric acceptance"
  key_links:
    - from: "statsEngine.test.ts"
      to: "src/test/fixtures/jasp/*.golden.json"
      via: "displayParity with fmtP/fmtNumber"
      pattern: "golden\\.json"
    - from: "glmEngine.ts"
      to: "ml-matrix"
      via: "QR/solve in IRLS weighted least squares"
      pattern: "ml-matrix"
---

<objective>
Extend the numeric layer for Phase 3: classical tests in statsEngine, GLM IRLS in glmEngine, JASP golden fixtures, shared AssumptionNudgeStrip, and minimal npm deps (jstat, ml-matrix). Wave 0 blocks all six test modules.

Purpose: Phase 3 has no legacy MVP oracle — acceptance is JASP golden JSON at display precision (D-18, D-21). GLM and post-hoc math are highest risk; engines must be proven before any UI (RESEARCH Pitfall 1).
Output: statsEngine extensions, glmEngine.ts, six golden fixtures, AssumptionNudgeStrip, package deps installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-testes-classicos-glm-novos/03-CONTEXT.md
@.planning/phases/03-testes-classicos-glm-novos/03-RESEARCH.md
@.planning/phases/03-testes-classicos-glm-novos/03-PATTERNS.md
@src/shared/stats/statsEngine.ts
@src/shared/format.ts
@src/features/tests/shared/SoftResetAlert.tsx
</context>

<interfaces>
From `src/shared/stats/statsEngine.ts` — reuse rank, mean, sd, parseNumber; extend with:
- runChiSquareIndependence(table: number[][]): { chi2, df, p, cramersV, expected, cellsBelow5, pctBelow5 }
- oneWayAnova(groups: Record<string, number[]>): { f, dfBetween, dfWithin, p, eta2, groupStats }
- kruskalWallis(groups): { h, df, p }
- tukeyHsd(groups): PairwiseRow[] with pAdj, meanDiff, ci
- dunnPostHoc(groups): PairwiseRow[] with Holm-adjusted pAdj

From new `glmEngine.ts`:
- fitPoisson(y, X): GlmFitResult + pearsonChi2/dfResid overdispersion ratio
- fitNegativeBinomial(y, X): GlmFitResult & { theta: number }
- fitLogistic(y, X): GlmFitResult + oddsRatios with CI95%

From `assumptionNudges.ts`:
- NudgeSeverity = 'info' | 'warning'
- AssumptionNudge { severity, message, cta?: { label, testId } }

Golden fixture shape (03-RESEARCH.md):
- source, inputFixture, displayPrecision, expected { ... }
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify jstat and ml-matrix legitimacy and install</name>
  <files>package.json, package-lock.json</files>
  <action>Blocking human gate before npm install of jstat@^1.9.6 and ml-matrix@^6.14.0 (03-RESEARCH.md Package Legitimacy Audit — tagged [ASSUMED]). Present npm registry evidence; wait for approval. After approval: npm install jstat@^1.9.6 ml-matrix@^6.14.0. Do NOT add @stdlib or @tangent.to/ds unless GLM spike fails in Task 2.</action>
  <what-built>Package legitimacy review for jstat and ml-matrix before npm install</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/jstat — confirm github.com/jstat/jstat, established package, chisquare.cdf and tukeyhsd APIs
    2. Open https://www.npmjs.com/package/ml-matrix — confirm github.com/mljs/matrix, QR/solve support
    3. Confirm no [SLOP] verdict; both tagged [ASSUMED] in RESEARCH.md
    4. Type "approved" to let Claude run npm install jstat@^1.9.6 ml-matrix@^6.14.0
  </how-to-verify>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.jstat||!p.dependencies['ml-matrix']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Human approved both packages on npm registry
    - [ ] jstat@^1.9.6 and ml-matrix@^6.14.0 in package.json dependencies
  </acceptance_criteria>
  <done>jstat and ml-matrix installed after human approval</done>
  <resume-signal>Type "approved" after verifying npm pages, or describe concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extend statsEngine and create glmEngine with golden tests</name>
  <files>src/shared/stats/statsEngine.ts, src/shared/stats/statsEngine.test.ts, src/shared/stats/glmEngine.ts, src/shared/stats/glmEngine.test.ts, scripts/oracle/generate-phase3-fixtures.R, src/test/fixtures/tests/*.txt, src/test/fixtures/jasp/*.golden.json</files>
  <read_first>
    - `src/shared/stats/statsEngine.ts` (existing rank, mean, sd, ibeta — extend, do not replace)
    - `03-RESEARCH.md` § Pattern 2 algorithms and Golden fixture pipeline
    - `03-PATTERNS.md` § statsEngine and glmEngine export shapes
    - `src/shared/format.ts` (fmtP, fmtNumber for displayParity)
    - JASP Data Library CSVs under jasp-desktop-development/Resources/Data Sets/Data Library/ if present
  </read_first>
  <action>
    1. Extend statsEngine.ts per D-17/D-19:
       - runChiSquareIndependence: Pearson χ², Cramér's V, expected counts, cellsBelow5/pctBelow5; p via jstat.chisquare.cdf
       - oneWayAnova: SS_between/within, F, η²; p via jstat.centralf.cdf
       - kruskalWallis: pooled statsEngine.rank, H → χ² approx
       - tukeyHsd: jstat.tukeyhsd or studentized-range; return sorted-ready PairwiseRow[]
       - dunnPostHoc: pairwise z with Holm-adjusted p as primary pAdj (D-14, RESEARCH A2)
       - Cap contingency dimensions at 20×20 (DoS guard)

    2. Create glmEngine.ts per D-11/D-17:
       - Design matrix: intercept + main effects; contr.treatment dummy coding
       - IRLS via ml-matrix QR/solve; max iterations with convergence flag
       - fitPoisson, fitNegativeBinomial (glm.nb-style θ iteration), fitLogistic
       - Outputs: β, SE, Wald z, p, deviance, dfResid, pearsonChi2; NB theta; logistic OR + CI95%

    3. Create six synthetic capacitação exemplo fixtures under src/test/fixtures/tests/ (pasteable TSV/semicolon text per test role keys from 03-PATTERNS.md).

    4. Create scripts/oracle/generate-phase3-fixtures.R documenting R chisq.test, aov+TukeyHSD, kruskal.test+DunnTest, glm, glm.nb calls. If R unavailable, hand-enter golden JSON from published JASP tutorial values; mark source "textbook" in fixture metadata.

    5. Commit six golden JSON files under src/test/fixtures/jasp/ with displayPrecision metadata.

    6. Create statsEngine.test.ts and glmEngine.test.ts: displayParity vs golden at fmtP/fmtNumber precision; assert finite outputs; GLM tests split -t poisson, -t nb, -t logistic.
  </action>
  <verify>
    <automated>npm run test:run -- src/shared/stats/statsEngine.test.ts src/shared/stats/glmEngine.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] statsEngine classical functions pass golden parity for χ², ANOVA, Kruskal, Tukey, Dunn
    - [ ] glmEngine passes golden parity for Poisson, NB, Logistic at display precision
    - [ ] Six exemplo + six golden JSON fixtures committed
    - [ ] generate-phase3-fixtures.R documented for future regen
    - [ ] No R/WASM shipped to browser
  </acceptance_criteria>
  <done>Numeric layer proven against JASP golden fixtures before any test UI</done>
</task>

<task type="auto">
  <name>Task 3: Create AssumptionNudgeStrip and shared nudge types</name>
  <files>src/features/tests/shared/AssumptionNudgeStrip.tsx, src/features/tests/shared/assumptionNudges.ts, src/features/tests/shared/sharedComponents.test.tsx</files>
  <read_first>
    - `src/features/tests/shared/SoftResetAlert.tsx` (Alert shell pattern)
    - `03-CONTEXT.md` D-04, D-05, D-06 (severity levels, never block)
    - `03-PATTERNS.md` § AssumptionNudgeStrip
  </read_first>
  <action>
    1. Create assumptionNudges.ts: export NudgeSeverity, AssumptionNudge, optional sortNudges helper (warnings first).

    2. Create AssumptionNudgeStrip.tsx per D-04/D-05:
       - Map info → teal left border; warning → amber/warning border (mirror SoftResetAlert structure)
       - role="status" aria-live="polite"
       - Render each nudge message; optional cta Button calls onNavigateTest(testId)
       - Never hide or disable results — strip only
       - Return null when nudges array empty

    3. Extend sharedComponents.test.tsx:
       - Render strip with info and warning nudges
       - Assert CTA button fires onNavigateTest with testId
       - Assert no nudges → nothing rendered
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/shared/sharedComponents.test.tsx -t AssumptionNudge</automated>
  </verify>
  <acceptance_criteria>
    - [ ] AssumptionNudgeStrip renders info and warning with distinct styling
    - [ ] CTA navigates via onNavigateTest callback
    - [ ] Component never blocks analysis (no error severity, no conditional hide of sibling content)
  </acceptance_criteria>
  <done>Shared UX-02 nudge strip ready for per-test computeAssumptionNudges wiring</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | jstat and ml-matrix from npm registry — supply-chain risk |
| Tabular → engine | User paste crosses into numeric computation — validate before heavy work |
| Golden fixture R script | Dev-only scripts/oracle/ — not bundled to browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | statsEngine contingency builder | mitigate | Cap table at 20×20; reject empty/degenerate tables before χ² loop |
| T-03-02 | Denial of Service | glmEngine IRLS | mitigate | Max iteration cap; return converged:false with friendly upstream error; bound design matrix columns |
| T-03-03 | Tampering | AssumptionNudgeStrip CTA | mitigate | onNavigateTest only accepts registry testIds validated by caller; no arbitrary URL navigation |
| T-03-SC | Tampering | jstat/ml-matrix npm install | mitigate | Blocking human-verify checkpoint (Task 1); packages tagged [ASSUMED] in RESEARCH.md |
</threat_model>

<verification>
- npm run test:run -- src/shared/stats/statsEngine.test.ts src/shared/stats/glmEngine.test.ts green
- npm run test:run -- src/features/tests/shared/sharedComponents.test.tsx green
- test 6 -eq "$(ls src/test/fixtures/jasp/*.golden.json 2>/dev/null | wc -l | tr -d ' ')"
- jstat and ml-matrix in package.json after human approval
</verification>

<success_criteria>
- Classical and GLM engines pass JASP golden parity at display precision
- AssumptionNudgeStrip component tested and ready for module wiring
- Six exemplo + six golden fixtures committed
- Wave 0 complete — Wave A modules unblocked
</success_criteria>

<output>
Create `.planning/phases/03-testes-classicos-glm-novos/03-01-SUMMARY.md` when done
</output>
