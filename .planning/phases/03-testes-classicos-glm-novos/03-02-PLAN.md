---
phase: 03-testes-classicos-glm-novos
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/features/tests/qui-quadrado/quiQuadradoConfig.ts
  - src/features/tests/qui-quadrado/quiQuadradoEngine.ts
  - src/features/tests/qui-quadrado/quiQuadradoEngine.test.ts
  - src/features/tests/qui-quadrado/quiQuadradoInterpretation.ts
  - src/features/tests/qui-quadrado/quiQuadradoInterpretation.test.ts
  - src/features/tests/qui-quadrado/quiQuadradoCharts.ts
  - src/features/tests/qui-quadrado/QuiQuadradoConfigPanel.tsx
  - src/features/tests/qui-quadrado/QuiQuadradoTest.tsx
  - src/features/tests/qui-quadrado/QuiQuadradoTest.test.tsx
  - src/shared/charts/chartFactories/contingencyChart.ts
autonomous: true
requirements: [TEST-04, UX-02]

must_haves:
  truths:
    - "User runs qui-quadrado with two categorical columns and sees χ², p, Cramér's V on metric cards (TEST-04)"
    - "Warning nudge fires when expected cell counts &lt; 5 with % cells flagged (D-06, UX-02)"
    - "Chart shows observed vs expected grouped bars; PNG export works"
    - "Engine matches JASP golden at display precision (D-21)"
  artifacts:
    - path: "src/features/tests/qui-quadrado/QuiQuadradoTest.tsx"
      provides: "FlowSteps orchestrator"
    - path: "src/features/tests/qui-quadrado/quiQuadradoEngine.ts"
      provides: "Contingency builder + statsEngine.runChiSquareIndependence"
    - path: "src/shared/charts/chartFactories/contingencyChart.ts"
      provides: "Observed vs expected bar chart"
  key_links:
    - from: "quiQuadradoEngine.ts"
      to: "statsEngine.runChiSquareIndependence"
      via: "built contingency table from two categoricals"
      pattern: "runChiSquareIndependence"
    - from: "QuiQuadradoTest.tsx"
      to: "AssumptionNudgeStrip"
      via: "computeAssumptionNudges output"
      pattern: "AssumptionNudgeStrip"
---

<objective>
Ship the Qui-quadrado de independência module (TEST-04): two categorical columns → contingency table, effect size, expected-cell nudges, chart, and JASP golden parity. Do NOT flip registry yet — Wave A gate (03-05) handles integration.

Purpose: First Wave A classical test; validates categorical parsing pattern (no numericKeys on categoricals per RESEARCH Pitfall 4) and AssumptionNudgeStrip wiring.
Output: Complete qui-quadrado feature folder; registry remains em-breve until 03-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testes-classicos-glm-novos/03-CONTEXT.md
@.planning/phases/03-testes-classicos-glm-novos/03-PATTERNS.md
@src/features/tests/t-student/TStudentTest.tsx
@src/features/tests/t-student/tStudentEngine.ts
@src/test/fixtures/tests/qui-quadrado-exemplo.txt
@src/test/fixtures/jasp/qui-quadrado-exemplo.golden.json
</context>

<interfaces>
TABULAR_OPTIONS (03-PATTERNS.md):
- requiredKeys: categoria_a, categoria_b
- numericKeys: none — string coercion only

Engine surface:
- buildDatasetFromConfirmed, validateDataset, runAnalysis, buildMetrics, computeAssumptionNudges, toEngineOutput

Nudges (D-06): warn when any E&lt;5 (% cells); 2×2 sparse → Fisher tip (info only)
Metrics: χ², df, p, Cramér's V, N, cells below 5
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port qui-quadrado engine and golden tests</name>
  <files>src/features/tests/qui-quadrado/quiQuadradoConfig.ts, src/features/tests/qui-quadrado/quiQuadradoEngine.ts, src/features/tests/qui-quadrado/quiQuadradoEngine.test.ts</files>
  <behavior>
    - Golden parity: χ², df, p, cramersV match qui-quadrado-exemplo.golden.json at fmtP/fmtNumber precision
    - Nudge: fixture with expected&lt;5 cells → computeAssumptionNudges returns severity warning
    - Validation: single column or numeric-only columns → PT error strings, no throw
  </behavior>
  <read_first>
    - `src/features/tests/t-student/tStudentEngine.ts` (engine surface pattern)
    - `src/shared/stats/statsEngine.ts` runChiSquareIndependence (from 03-01)
    - `03-CONTEXT.md` D-09, D-12 (two categoricals primary path)
    - `03-PATTERNS.md` qui-quadrado TABULAR_OPTIONS
  </read_first>
  <action>
    1. Create quiQuadradoConfig.ts: TABULAR_OPTIONS without numericKeys; exampleText from fixture; didacticCards; defaultQuestion; role aliases categoria_a/categoria_b.

    2. Create quiQuadradoEngine.ts:
       - buildDatasetFromConfirmed: iterate rows, String trim both columns, build contingency table O[i,j]
       - validateDataset: min 2×2 table, min total N, both columns have ≥2 levels
       - runAnalysis: call statsEngine.runChiSquareIndependence
       - buildMetrics: χ², df, p (fmtP), Cramér's V, N, cells below 5 count
       - computeAssumptionNudges per D-06/D-07: empirical expected counts; 2×2 sparse Fisher tip
       - toEngineOutput: bundle table, expected, observed for charts + nudges

    3. Create quiQuadradoEngine.test.ts: golden parity; nudge cases; validation error strings.

    Skip aggregated matrix paste path (D-09 optional — defer).
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/qui-quadrado/quiQuadradoEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Golden parity passes at display precision
    - [ ] computeAssumptionNudges fires warning on low expected counts
    - [ ] Categorical columns never parsed as numeric
    - [ ] Friendly PT validation errors for wrong types
  </acceptance_criteria>
  <done>Qui-quadrado engine verified against JASP golden fixture</done>
</task>

<task type="auto">
  <name>Task 2: Port interpretation, contingency chart, and FlowSteps UI</name>
  <files>src/features/tests/qui-quadrado/quiQuadradoInterpretation.ts, src/features/tests/qui-quadrado/quiQuadradoInterpretation.test.ts, src/features/tests/qui-quadrado/quiQuadradoCharts.ts, src/shared/charts/chartFactories/contingencyChart.ts, src/features/tests/qui-quadrado/QuiQuadradoConfigPanel.tsx, src/features/tests/qui-quadrado/QuiQuadradoTest.tsx, src/features/tests/qui-quadrado/QuiQuadradoTest.test.tsx</files>
  <read_first>
    - `src/features/tests/t-student/TStudentTest.tsx` (FlowSteps orchestrator)
    - `src/features/tests/t-student/TStudentConfigPanel.tsx` (ConfigPanel layout)
    - `src/features/tests/t-student/tStudentInterpretation.ts` (string[] paragraphs)
    - `03-CONTEXT.md` D-08, D-12, D-13 (soft-reset on role change)
    - `03-PATTERNS.md` § QuiQuadradoTest results composition
  </read_first>
  <action>
    1. Create quiQuadradoInterpretation.ts: alpha-aware significance; mention Cramér's V effect size; plain PT string[] only.

    2. Create contingencyChart.ts: grouped bars observed vs expected; pure ChartData+Options return pattern.

    3. Create quiQuadradoCharts.ts: preset id contingency-default; annotation toggles for counts/p-values.

    4. Create QuiQuadradoConfigPanel.tsx: AlphaSelector, ResearchQuestionField, DidacticCards, ColumnPreviewTable, SoftResetAlert on role change (D-13).

    5. Create QuiQuadradoTest.tsx:
       - FlowSteps Dados → Configurar → Resultados
       - Usar exemplo from config
       - Resultados: AssumptionNudgeStrip + ResultsPanelWithCustomizer
       - exportFilename qui-quadrado-lacirstat.png

    6. Create QuiQuadradoTest.test.tsx: Chart.js mock; Usar exemplo → Configurar → Analisar dados smoke; assert interpretation + PNG + nudge strip when warning fixture used.

    Do NOT flip registry status — 03-05 handles integration.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/qui-quadrado/quiQuadradoInterpretation.test.ts src/features/tests/qui-quadrado/QuiQuadradoTest.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] FlowSteps smoke passes with interpretation and Baixar todos
    - [ ] AssumptionNudgeStrip visible when engine returns warnings
    - [ ] Contingency chart preset renders without imperative Chart()
    - [ ] Registry NOT flipped in this plan
  </acceptance_criteria>
  <done>Complete qui-quadrado module ready for Wave A gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User paste → contingency table | Untrusted categorical strings cross into numeric engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | quiQuadradoInterpretation | mitigate | string[] only; no dangerouslySetInnerHTML |
| T-03-02-02 | Denial of Service | Contingency builder | mitigate | Reuse statsEngine 20×20 cap; validate before runAnalysis |
| T-03-02-03 | Spoofing | Categorical coercion | mitigate | Force String trim; reject numeric-only mis-mapping with PT validation alert |
</threat_model>

<verification>
- npm run test:run -- src/features/tests/qui-quadrado/ green
- npm run typecheck
</verification>

<success_criteria>
- TEST-04 engine + UI complete with golden parity and UX-02 nudges
- Module mountable via EstatisticaPage switch in 03-05
</success_criteria>

<output>
Create `.planning/phases/03-testes-classicos-glm-novos/03-02-SUMMARY.md` when done
</output>
