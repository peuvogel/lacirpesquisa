---
phase: 03-testes-classicos-glm-novos
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-05"]
files_modified:
  - src/features/tests/poisson/poissonConfig.ts
  - src/features/tests/poisson/poissonEngine.ts
  - src/features/tests/poisson/poissonEngine.test.ts
  - src/features/tests/poisson/poissonInterpretation.ts
  - src/features/tests/poisson/poissonInterpretation.test.ts
  - src/features/tests/poisson/poissonCharts.ts
  - src/features/tests/poisson/PoissonConfigPanel.tsx
  - src/features/tests/poisson/PoissonTest.tsx
  - src/features/tests/poisson/PoissonTest.test.tsx
  - src/shared/charts/chartFactories/glmCoefForestChart.ts
autonomous: true
requirements: [TEST-07, UX-02]

must_haves:
  truths:
    - "User runs Poisson regression with coefficient metrics and overdispersion ratio (TEST-07)"
    - "Warning nudge + CTA opens Binomial Negativa with same column mapping when overdispersed (D-20, UX-02)"
    - "Does NOT auto-switch to NB — explicit user navigation only"
    - "Engine matches JASP golden at display precision"
  artifacts:
    - path: "src/features/tests/poisson/poissonEngine.ts"
      provides: "glmEngine.fitPoisson wrapper + overdispersion"
    - path: "src/features/tests/poisson/PoissonTest.tsx"
      provides: "NB handoff CTA in nudge and actions"
    - path: "src/shared/charts/chartFactories/glmCoefForestChart.ts"
      provides: "Coefficient forest chart for GLM family"
  key_links:
    - from: "poissonEngine.ts"
      to: "glmEngine.fitPoisson"
      via: "count outcome + predictors design matrix"
      pattern: "fitPoisson"
    - from: "PoissonTest.tsx"
      to: "binomial-negativa"
      via: "onNavigateTest CTA preserving recognizedColumns"
      pattern: "binomial-negativa"
---

<objective>
Ship Regressão de Poisson module (TEST-07): count outcome + predictors, glmEngine.fitPoisson, overdispersion check (Pearson χ²/df), warning CTA to Binomial Negativa. Registry flip deferred to 03-09. Creates glmCoefForestChart shared by NB and Logistic.

Purpose: Wave B1 — first GLM module; establishes GLM UI pattern and Poisson→NB handoff (D-20).
Output: Complete poisson feature folder + glmCoefForestChart factory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testes-classicos-glm-novos/03-CONTEXT.md
@.planning/phases/03-testes-classicos-glm-novos/03-PATTERNS.md
@src/features/tests/correlacao/CorrelacaoTest.tsx
@src/shared/stats/glmEngine.ts
@src/test/fixtures/tests/poisson-exemplo.txt
@src/test/fixtures/jasp/poisson-exemplo.golden.json
</context>

<interfaces>
TABULAR_OPTIONS (D-11):
- requiredKeys: contagem (non-negative integer outcome) + ≥1 predictor roles
- numericKeys: contagem + numeric predictors; categoricals as string dummies

Overdispersion: pearsonChi2/dfResid; warning threshold &gt;1.25 (RESEARCH A5)

Handoff: { activeTestId: 'binomial-negativa', recognizedColumns: roleSnapshot }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port Poisson engine and golden tests</name>
  <files>src/features/tests/poisson/poissonConfig.ts, src/features/tests/poisson/poissonEngine.ts, src/features/tests/poisson/poissonEngine.test.ts</files>
  <behavior>
    - Golden: coefficients, p-values, deviance match poisson-exemplo.golden.json at display precision
    - Overdispersion ratio computed and exposed on metrics
    - Nudge: ratio &gt;1.25 → warning with binomial-negativa CTA
    - Validation: negative counts, non-integer counts → PT errors
  </behavior>
  <read_first>
    - `src/shared/stats/glmEngine.ts` fitPoisson (03-01)
    - `src/features/tests/correlacao/correlacaoEngine.ts` multi-column builder
    - `03-CONTEXT.md` D-11, D-20
  </read_first>
  <action>
    1. Create poissonConfig.ts: TABULAR_OPTIONS with contagem + predictor aliases; exampleText; didacticCards; main effects only.

    2. Create poissonEngine.ts:
       - buildDatasetFromConfirmed: count outcome + predictor columns → design matrix (intercept + dummies)
       - validateDataset: non-negative integers for contagem; min rows &gt; predictors+1; friendly PT errors
       - runAnalysis: glmEngine.fitPoisson; compute overdispersionRatio = pearsonChi2/dfResid
       - buildMetrics: coef table summary on cards (key terms), deviance, dfResid, overdispersion ratio
       - computeAssumptionNudges: warning + CTA binomial-negativa when ratio &gt;1.25 (D-06/D-20)
       - toEngineOutput for forest chart

    3. poissonEngine.test.ts: golden parity; overdispersion nudge; validation cases.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/poisson/poissonEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Golden parity at display precision
    - [ ] Overdispersion nudge fires with binomial-negativa CTA
    - [ ] No auto-switch logic — CTA only
  </acceptance_criteria>
  <done>Poisson engine verified against JASP golden</done>
</task>

<task type="auto">
  <name>Task 2: GLM forest chart and Poisson FlowSteps UI</name>
  <files>src/features/tests/poisson/poissonInterpretation.ts, src/features/tests/poisson/poissonInterpretation.test.ts, src/features/tests/poisson/poissonCharts.ts, src/shared/charts/chartFactories/glmCoefForestChart.ts, src/features/tests/poisson/PoissonConfigPanel.tsx, src/features/tests/poisson/PoissonTest.tsx, src/features/tests/poisson/PoissonTest.test.tsx</files>
  <read_first>
    - `src/features/tests/correlacao/CorrelacaoTest.tsx` (multi-predictor roles, soft-reset)
    - `03-PATTERNS.md` § Poisson NB handoff
    - `src/routes/estatistica/EstatisticaPage.tsx` handoff from 03-05
  </read_first>
  <action>
    1. Create glmCoefForestChart.ts: horizontal bar/range forest of coefficients with CI; reusable by NB/Logistic.

    2. poissonInterpretation.ts: alpha-aware; mention overdispersion when ratio high; plain PT string[].

    3. poissonCharts.ts: forest preset default; predicted-vs-observed optional second preset.

    4. PoissonConfigPanel.tsx + PoissonTest.tsx:
       - FlowSteps; soft-reset on predictor/outcome role change
       - AssumptionNudgeStrip with onNavigateTest for NB CTA
       - ResultsPanelWithCustomizer actions: optional "Abrir Binomial Negativa" button duplicating CTA (D-20)
       - handleCrossTestHandoff to binomial-negativa with recognizedColumns

    5. PoissonTest.test.tsx: smoke flow; assert overdispersion nudge on overdispersed fixture subset; CTA calls handoff with columns.

    Do NOT flip registry — 03-09.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/tests/poisson/poissonInterpretation.test.ts src/features/tests/poisson/PoissonTest.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Forest chart preset renders
    - [ ] NB CTA in nudge and/or actions; no silent model switch
    - [ ] Handoff preserves recognizedColumns in test
  </acceptance_criteria>
  <done>Complete Poisson module ready for Wave B gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User paste → GLM design matrix | Numeric validation before IRLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Denial of Service | glmEngine IRLS | mitigate | Reuse glmEngine iteration cap; validate row count before fit |
| T-03-06-02 | Tampering | Poisson→NB handoff | mitigate | CTA only navigates to binomial-negativa; validate testId whitelist |
| T-03-06-03 | Tampering | poissonInterpretation | mitigate | string[] only |
</threat_model>

<verification>
- npm run test:run -- src/features/tests/poisson/ green
- npm run typecheck
</verification>

<success_criteria>
- TEST-07 complete with overdispersion check and NB handoff CTA
- glmCoefForestChart available for 03-07 and 03-08
</success_criteria>

<output>
Create `.planning/phases/03-testes-classicos-glm-novos/03-06-SUMMARY.md` when done
</output>
