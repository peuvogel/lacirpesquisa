---
phase: 09-pipeline-confi-vel-coleta-completa
plan: 05
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - pipeline/sih/src/sih_pipeline/oracle_scrape.py
  - pipeline/sih/tests/test_oracle_scrape.py
  - pipeline/sih/tests/fixtures/oracle_tabnet.json
  - pipeline/sih/tests/fixtures/tabnet_prn_sample.html
  - pipeline/sih/reports/oracle-descartados.md
autonomous: true
requirements: [PIPE-01, PIPE-05, PIPE-06]

must_haves:
  truths:
    - "O oráculo dos 85 pares é buscado ao vivo do TabNet hoje e conferido contra o CSV guardado — um par que não reproduz é descartado como oráculo, não depurado (D-04)"
    - "A ferramenta de raspagem faz uma coisa só: buscar N valores pontuais para a fixture; sem upload, sem cache, sem --skip-done, sem segredo (D-18)"
    - "Uma resposta do TabNet sem bloco <PRE> levanta erro, nunca devolve tabela vazia como sucesso (SC-1 aplicado ao oráculo)"
    - "A fixture do oráculo é snapshot versionado com data de raspagem por entrada, e o refresh é comando explícito — nunca parte do npm run gate (Fase 8 D-09)"
    - "Cada par descartado tem a razão do descarte escrita, e o descarte é visível, não silencioso"
  artifacts:
    - path: "pipeline/sih/src/sih_pipeline/oracle_scrape.py"
      provides: "Raspador mínimo do TabNet — porta de post_tabnet/parse_prn_table, sem o resto das 548 linhas"
      exports: ["post_tabnet", "parse_prn_table", "scrape_pairs", "REQUEST_DELAY_SEC"]
    - path: "pipeline/sih/tests/fixtures/oracle_tabnet.json"
      provides: "Os 85 pares (agravo × UF × ano) re-raspados ao vivo, cada um com valor, data de raspagem e confirmação contra o CSV guardado"
      contains: "raspadoEm"
    - path: "pipeline/sih/reports/oracle-descartados.md"
      provides: "Registro escrito dos pares descartados como oráculo e por quê (D-04)"
  key_links:
    - from: "oracle_scrape.py"
      to: "trabalhos datasus/outputs/coleta_sih_multi"
      via: "comparação do valor re-raspado contra o CSV guardado, por par"
      pattern: "coleta_sih_multi"
    - from: "oracle_tabnet.json"
      to: "tests/test_reconcile_gate.py"
      via: "subset UF=AC vira a fixture congelada do gate permanente (09-11)"
      pattern: "oracle"
---

<objective>
Construir o oráculo em que o SC-7 vai se apoiar — e provar que ele merece confiança antes de usá-lo.
Os 85 pares vieram do mesmo pipeline que produziu 424 de 654 CSVs com 0 bytes; depurar faixa CID
contra número que a coleta velha inventou seria pior que não depurar. O D-04 exige re-raspagem ao vivo
e descarte (não depuração) de qualquer par que não reproduza.

Purpose: sem um oráculo auditado, o gate do SC-7 mede a fidelidade a um erro.
Output: raspador mínimo (D-18), `oracle_tabnet.json` com os pares que reproduzem, e o registro escrito
dos que foram descartados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md
@.planning/notes/2026-08-04-pysus-microdado-spike.md
</context>

<interfaces>
`oracle_scrape.py` exporta:

- `REQUEST_DELAY_SEC = 1.5` (constante de módulo, herdada literalmente do scraper aposentado)
- `post_tabnet(url: str, data: list[tuple[str, str]], timeout: int = 180) -> str`
- `parse_prn_table(text: str) -> list[list[str]]` — levanta `RuntimeError` se não houver bloco `<PRE>`
- `scrape_pairs(pairs: list[dict]) -> list[dict]` — `pairs` são `{"tabnetCode", "uf", "ano"}`; devolve
  `{"tabnetCode", "diseaseId", "uf", "ano", "valorTabnet", "raspadoEm"}`

Formato de `tests/fixtures/oracle_tabnet.json` (array JSON, uma entrada por par confirmado):
`{"diseaseId", "tabnetCode", "uf", "ano", "medida": "internacoes", "valorTabnet": <int>,
"valorCsvGuardado": <int>, "raspadoEm": "<ISO-8601>", "fonte": "sih/cnv/nibr.def"}`.
Só entram pares em que `valorTabnet == valorCsvGuardado`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raspador mínimo — porta de post_tabnet/parse_prn_table, e só isso (D-18)</name>
  <read_first>
    - `trabalhos datasus/scripts/coleta_sih_multi_disease.py` linhas 60-95 — `REQUEST_DELAY_SEC`, `post_tabnet` e `parse_prn_table`, que são exatamente o que se porta (e as linhas ao redor, que são exatamente o que **não** se porta)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md` §`oracle_scrape.py` (o excerto verbatim e a lista explícita do que não portar: `disease_done()`, `cleanup_raw()`, orquestração em lote, `OUT_ROOT`)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §D-18 ("sem upload, sem cache, sem `--skip-done`, sem segredo")
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-RESEARCH.md` §"Security Domain" linha V5 (validar a forma da resposta HTML antes de confiar no `<PRE>`)
    - `pipeline/sih/tests/test_oracle_scrape.py` (esqueleto a substituir)
  </read_first>
  <files>pipeline/sih/src/sih_pipeline/oracle_scrape.py, pipeline/sih/tests/test_oracle_scrape.py, pipeline/sih/tests/fixtures/tabnet_prn_sample.html</files>
  <behavior>
    - `parse_prn_table` sobre uma resposta com `<PRE>` devolve a lista de linhas, com `;` como separador e entidades HTML já desescapadas
    - `parse_prn_table` sobre uma resposta **sem** `<PRE>` levanta `RuntimeError` — nunca devolve `[]`
    - `parse_prn_table` sobre uma resposta com `<PRE>` vazio levanta `RuntimeError` — uma tabela vazia é falha, não zero
    - `parse_prn_table` preserva acentuação (a resposta do TabNet é latin-1)
    - `post_tabnet` respeita `REQUEST_DELAY_SEC` antes de cada requisição
    - `REQUEST_DELAY_SEC` é ≥ 1.0
    - O módulo não define nenhuma constante de segredo e não importa `os.environ`
  </behavior>
  <action>
Portar de `coleta_sih_multi_disease.py` **apenas** `REQUEST_DELAY_SEC`, `post_tabnet` e
`parse_prn_table`, preservando o comportamento verificado: corpo `urlencode(..., doseq=True,
encoding="latin-1")` codificado em latin-1, cabeçalhos `Content-Type: application/x-www-form-urlencoded`
e `User-Agent`, resposta decodificada com `latin-1, errors="replace"`, extração do bloco `<PRE>` por
regex com `re.S | re.I`, `html.unescape` e `csv.reader(..., delimiter=";")`.

Endurecer um ponto além do original (ASVS V5): além de levantar `RuntimeError` quando não há bloco
`<PRE>`, levantar também quando o bloco existe mas não produz nenhuma linha de dado — "tabela vazia"
é o modo de falha que esta fase inteira existe para matar, e no oráculo ele seria especialmente
tóxico (um par com valor 0 entraria como verdade).

Não portar: `disease_done()`, `cleanup_raw()`, `scrape_one()`, qualquer `OUT_ROOT`/escrita de CSV em
massa, qualquer `--skip-done`, e qualquer segredo. O arquivo inteiro deve caber confortavelmente em
menos de 150 linhas — se passar disso, algo do que o D-18 proíbe entrou junto.

Salvar em `tests/fixtures/tabnet_prn_sample.html` uma resposta real e pequena do TabNet (capturada
durante a Task 2, ou uma resposta reduzida a mão a partir de uma real, sem inventar conteúdo) para
que os testes de parsing rodem **sem rede**. Escrever `tests/test_oracle_scrape.py` cobrindo os sete
comportamentos declarados, com `post_tabnet` sempre mockado — nenhum teste desta suíte pode tocar a
rede, porque ela roda dentro de `npm run gate`. Remover o `allow_module_level=True`.
  </action>
  <acceptance_criteria>
    - `cd pipeline/sih && uv run pytest tests/test_oracle_scrape.py -x -q` sai 0 com ≥ 7 testes
    - `wc -l < pipeline/sih/src/sih_pipeline/oracle_scrape.py` retorna um número < 150
    - `grep -cE 'skip.done|cleanup_raw|disease_done|OUT_ROOT|INGEST_SECRET' pipeline/sih/src/sih_pipeline/oracle_scrape.py` retorna 0
    - `grep -c 'os.environ' pipeline/sih/src/sih_pipeline/oracle_scrape.py` retorna 0
    - `grep -cE 'urlopen|requests\.' pipeline/sih/tests/test_oracle_scrape.py` retorna 0 (nenhum teste toca rede)
    - `cd pipeline/sih && uv run python -c "from sih_pipeline.oracle_scrape import parse_prn_table; parse_prn_table('<html>sem pre</html>')"` sai não-zero com `RuntimeError`
    - `grep -c 'allow_module_level' pipeline/sih/tests/test_oracle_scrape.py` retorna 0
  </acceptance_criteria>
  <verify>
    <automated>cd pipeline/sih && uv run pytest tests/test_oracle_scrape.py -x -q</automated>
  </verify>
  <done>Existe uma ferramenta pequena, testada e sem rede nos testes, que busca N valores pontuais do TabNet.</done>
</task>

<task type="auto">
  <name>Task 2: Re-raspar os 85 pares ao vivo e descartar o que não reproduz (D-04)</name>
  <read_first>
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §D-04 (inteiro — a regra do descarte) e §D-03 (a amostra AC/2019)
    - `.planning/notes/2026-08-04-pysus-microdado-spike.md` §5 (a tabela dos 85 pares e as hipóteses já descartadas) e §6 (o estado real da coleta: 424 de 654 CSVs com 0 bytes — a razão de existir desta task)
    - `trabalhos datasus/scripts/coleta_sih_multi_disease.py` (os parâmetros de formulário do TabNet: nome do `.def`, campos de linha/coluna/conteúdo, e o formato do período — a fonte da verdade sobre como montar a requisição)
    - `scripts/catalog/diseases.json` (mapa `tabnetCode` → `id` canônico, para que a fixture guarde o id da taxonomia da Fase 8 e não o id pré-migração)
    - `scripts/catalog/extra-diseases.json` (a forma "dado com motivo escrito" que o registro de descarte precisa imitar)
  </read_first>
  <files>pipeline/sih/tests/fixtures/oracle_tabnet.json, pipeline/sih/reports/oracle-descartados.md</files>
  <action>
Identificar, a partir dos CSVs de `trabalhos datasus/outputs/coleta_sih_multi/`, o conjunto de pares
(agravo × UF × ano) que hoje tem valor de `internacoes` não vazio — é o conjunto de ~85 pares que o
spike usou. Para cada par, montar a requisição TabNet equivalente (mesmo `.def`, mesmo recorte de
período, mesma linha/coluna/conteúdo que o scraper aposentado usava) e buscar o valor **hoje**.

Comparar par a par com o valor guardado no CSV:
- valores iguais → a entrada vai para `tests/fixtures/oracle_tabnet.json`, com `raspadoEm` em ISO-8601
  e `valorCsvGuardado` registrado ao lado, para que a conferência fique auditável depois;
- valores diferentes, ou requisição que falha, ou par cujo CSV está vazio → **descartado como
  oráculo**, com uma linha em `pipeline/sih/reports/oracle-descartados.md` contendo agravo, UF, ano,
  valor guardado, valor re-raspado (ou o erro) e a razão do descarte. Nunca "ajustar" o par.

Mapear cada `tabnetCode` para o `id` canônico atual usando `scripts/catalog/diseases.json` — os
diretórios de `coleta_sih_multi/` ainda usam os ids pré-migração da Fase 8 (registrado no handoff da
08-10), e gravar um id legado na fixture reintroduziria a corrupção que a Fase 8 acabou de matar. Um
`tabnetCode` sem correspondência canônica é descarte com razão escrita, não um id inventado.

Respeitar `REQUEST_DELAY_SEC = 1.5` entre requisições — são ~85 requisições, algo em torno de dois a
três minutos de raspagem, não uma coleta.

Registrar no SUMMARY, medidos: quantos pares foram tentados, quantos reproduziram, quantos foram
descartados e por qual motivo agrupado. Se **menos de 40** pares reproduzirem, parar e reportar em vez
de seguir: um oráculo tão pequeno não sustenta o gate do SC-7, e a causa precisa ser entendida antes.
  </action>
  <acceptance_criteria>
    - `pipeline/sih/tests/fixtures/oracle_tabnet.json` é um array JSON não-vazio; toda entrada tem `diseaseId`, `tabnetCode`, `uf`, `ano`, `valorTabnet` (inteiro), `valorCsvGuardado` (inteiro) e `raspadoEm` (ISO-8601)
    - Toda entrada satisfaz `valorTabnet === valorCsvGuardado` — o comando de verificação abaixo lança se alguma não satisfizer
    - Todo `diseaseId` da fixture existe em `scripts/catalog/diseases.json`
    - `pipeline/sih/tests/fixtures/oracle_tabnet.json` contém ao menos uma entrada com `uf` igual a `AC` e `ano` igual a `2019` (é a base da fixture do gate, D-03/D-06)
    - `pipeline/sih/reports/oracle-descartados.md` existe e tem uma linha por par descartado, cada uma com uma razão escrita em texto — ou declara explicitamente "nenhum par descartado"
    - `(tentados) == (reproduzidos) + (descartados)` — a soma fecha, registrada no SUMMARY
    - Reproduzidos ≥ 40
  </acceptance_criteria>
  <verify>
    <automated>node -e "const o=require('./pipeline/sih/tests/fixtures/oracle_tabnet.json');const d=require('./scripts/catalog/diseases.json');const ids=new Set((Array.isArray(d)?d:d.diseases).map(x=>x.id));if(!Array.isArray(o)||o.length<40)throw new Error('oraculo pequeno demais: '+o.length);for(const e of o){for(const k of ['diseaseId','tabnetCode','uf','ano','valorTabnet','valorCsvGuardado','raspadoEm'])if(e[k]===undefined)throw new Error('campo ausente '+k);if(e.valorTabnet!==e.valorCsvGuardado)throw new Error('par nao reproduz e nao foi descartado: '+e.diseaseId+'/'+e.uf+'/'+e.ano);if(!ids.has(e.diseaseId))throw new Error('id fora da taxonomia canonica: '+e.diseaseId);}if(!o.some(e=>e.uf==='AC'&&e.ano===2019))throw new Error('sem par AC/2019 para a fixture do gate');console.log('oraculo OK:',o.length,'pares')"</automated>
  </verify>
  <done>O oráculo do SC-7 é dado re-raspado hoje, conferido contra o guardado, com os descartes escritos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TabNet (HTML de terceiro) → fixture versionada | Texto raspado vira dado de referência que decide correções de faixa CID |
| CSVs do corpus legado → fixture | Números produzidos pelo pipeline que a fase declara não confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | `parse_prn_table` sobre resposta HTML do TabNet | mitigate | `RuntimeError` quando não há bloco `<PRE>` **e** quando o bloco não produz linha de dado; nenhum valor raspado é concatenado em SQL nesta plan (ASVS V5) |
| T-09-04 | Repudiation | Oráculo herdando o erro do corpus que a fase aposenta | mitigate | D-04: todo par é re-raspado ao vivo e só entra se reproduzir o CSV guardado; os descartes ficam escritos em `reports/oracle-descartados.md` com razão |
| T-09-19 | Denial of Service | Pressão sobre o TabNet | mitigate | `REQUEST_DELAY_SEC = 1.5` herdado literalmente do scraper aposentado; ~85 requisições pontuais, nunca uma coleta |
| T-09-21 | Tampering | Fixture chaveada por id legado pré-migração da Fase 8 | mitigate | Todo `diseaseId` é resolvido contra `scripts/catalog/diseases.json` e a asserção falha se um id não canônico entrar |
| T-09-22 | Denial of Service | Teste do gate tocando a rede e ficando lento/instável | mitigate | Todos os testes de `test_oracle_scrape.py` mockam `post_tabnet`; o refresh do oráculo é comando explícito, fora do `gate` (Fase 8 D-09) |
</threat_model>

<verification>
- `cd pipeline/sih && uv run pytest tests/test_oracle_scrape.py -q` sai 0 e não toca rede
- O comando `node -e` de validação da fixture sai 0
- `npm run gate` sai 0
</verification>

<success_criteria>
- Existe um raspador mínimo (< 150 linhas) que faz uma coisa só, sem segredo e sem cache
- Os 85 pares foram re-raspados ao vivo, os que reproduzem estão na fixture e os que não reproduzem estão registrados como descarte com razão escrita
- A fixture cobre AC/2019, base do gate permanente do SC-7
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-05-SUMMARY.md` when done
</output>
