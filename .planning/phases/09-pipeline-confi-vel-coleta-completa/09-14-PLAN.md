---
phase: 09-pipeline-confi-vel-coleta-completa
plan: 14
type: execute
wave: 9
depends_on: ["09-13"]
files_modified:
  - scripts/catalog/uploadSihToSupabase.mjs
  - trabalhos datasus/scripts/coleta_sih_multi_disease.py
  - trabalhos datasus/scripts/scrape_upload_sih.py
  - trabalhos datasus/scripts/launch_overnight.py
  - package.json
  - scripts/catalog/generateSihRetireMigration.mjs
  - supabase/migrations/20260806000000_sih_retire_muni.sql
  - supabase/rollback/20260806000000_sih_retire_muni_down.sql
  - supabase/verify/sih-retire.sql
  - src/features/catalog/sihRetireMigration.test.ts
  - pipeline/sih/tests/test_suite_integrity.py
  - docs/SUPABASE-CATALOG.md
autonomous: false
requirements: [PIPE-01, PIPE-06, DATA-02]

must_haves:
  truths:
    - "`scripts/catalog/uploadSihToSupabase.mjs` não existe mais — o CR-04 do 08-REVIEW morre por remoção, sem exempt-list nova (D-19)"
    - "Os três scrapers Python saem do package.json e do uso; no lugar ficou o raspador mínimo de reconciliação (D-18)"
    - "A Edge Function `sih-ingest` é aposentada e o `INGEST_SECRET` se resolve por remoção, não por rotação (D-17)"
    - "`sih_metric_muni` sai do banco: o grão município passa a ser servido do Storage particionado, e a saída só acontece depois de as 27 partições estarem comprovadamente legíveis (D-20)"
    - "`docs/SUPABASE-CATALOG.md` descreve o contrato real de hoje: dimensão `local`, ledger de cobertura, município no Storage, tabelas de população, e o caminho de upload por COPY"
    - "Nenhum esqueleto de teste com `allow_module_level=True` sobrevive ao fim da fase — todo arquivo criado na Onda 0 tem dono e conteúdo"
    - "Uma varredura de segredo roda sobre o repositório e não encontra `INGEST_SECRET` nem credencial em texto plano no código vivo"
  artifacts:
    - path: "supabase/migrations/20260806000000_sih_retire_muni.sql"
      provides: "DDL de aposentadoria de sih_metric_muni, gerada de schema-v3.json"
      contains: "sih_metric_muni"
    - path: "supabase/verify/sih-retire.sql"
      provides: "Prova pós-aposentadoria com RAISE EXCEPTION"
      contains: "raise exception"
    - path: "pipeline/sih/tests/test_suite_integrity.py"
      provides: "Gate que falha se algum esqueleto de teste da Onda 0 sobreviver"
    - path: "docs/SUPABASE-CATALOG.md"
      provides: "Contrato v3 do backend: local, ledger, Storage, população, COPY"
      contains: "sih_collection_status"
  key_links:
    - from: "package.json"
      to: "trabalhos datasus/scripts/"
      via: "ausência: nenhum script npm invoca os scrapers aposentados"
      pattern: "scrape:"
    - from: "docs/SUPABASE-CATALOG.md"
      to: "pipeline/sih/src/sih_pipeline/upload.py"
      via: "a seção de ingest descreve o COPY pelo Session Pooler, não a Edge Function"
      pattern: "pooler"
---

<objective>
Aposentar o caminho TabNet por remoção. Esta é a plan em que deleções são entregas: o uploader com o
defeito do CR-04, os três scrapers que produziram 424 de 654 CSVs vazios, a Edge Function que carrega
um segredo em texto plano no histórico, e a tabela de município que não cabe no plano gratuito. Nada
disso é limpeza cosmética — cada remoção fecha um risco nominal do ROADMAP.

Purpose: enquanto o caminho antigo existir, ele é executável, e um pipeline que registra sucesso com
0 linhas continua a um comando de distância.
Output: quatro arquivos deletados, dois scripts npm removidos, Edge Function e segredo removidos,
`sih_metric_muni` aposentada com prova, e a documentação do contrato atualizada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md
@docs/SUPABASE-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deletar o uploader, os três scrapers e a Edge Function; remover o segredo por remoção</name>
  <read_first>
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md` §"Arquivos a DELETAR (D-18/D-19) — blast radius medido" (a tabela inteira: quem referencia o quê e a ação por arquivo)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-REVIEW.md` §CR-04 (o defeito do `uploadSihToSupabase.mjs` que morre por remoção)
    - `.planning/ROADMAP.md` §"Riscos conhecidos" (as linhas do `INGEST_SECRET` e da Edge Function não versionada)
    - `package.json` (as entradas `scrape:sih-multi` e `scrape:overnight`)
    - `trabalhos datasus/scripts/scrape_upload_sih.py` linha 33 (o `INGEST_SECRET` em texto plano)
    - `pipeline/sih/src/sih_pipeline/oracle_scrape.py` (o substituto mínimo já existe desde o 09-05 — conferir antes de deletar a fonte da porta)
  </read_first>
  <files>scripts/catalog/uploadSihToSupabase.mjs, trabalhos datasus/scripts/coleta_sih_multi_disease.py, trabalhos datasus/scripts/scrape_upload_sih.py, trabalhos datasus/scripts/launch_overnight.py, package.json</files>
  <action>
Antes de deletar, confirmar que o substituto existe: `pipeline/sih/src/sih_pipeline/oracle_scrape.py`
precisa estar presente e com testes verdes (é a porta de `post_tabnet`/`parse_prn_table` feita no
09-05). Deletar a fonte de uma porta que não aconteceu seria perder a referência.

Deletar com `git rm`: `scripts/catalog/uploadSihToSupabase.mjs`,
`trabalhos datasus/scripts/coleta_sih_multi_disease.py`,
`trabalhos datasus/scripts/scrape_upload_sih.py` e
`trabalhos datasus/scripts/launch_overnight.py`. Remover de `package.json` as entradas
`scrape:sih-multi` e `scrape:overnight`.

O corpus de `trabalhos datasus/outputs/coleta_sih_multi/` **não é deletado** — ele já está no
`.gitignore` e nunca esteve no git; vira arquivo histórico na máquina do operador, como o D-19 diz.
O que muda é que ele deixou de ser entrada de build, o que já aconteceu no 09-13.

Aposentar a Edge Function: `supabase functions delete sih-ingest --project-ref hmfbxqemububjyhdckrj` e
`supabase secrets unset INGEST_SECRET --project-ref hmfbxqemububjyhdckrj`. Confirmar por
`supabase functions list` e `supabase secrets list` que nenhum dos dois aparece mais. Este é o D-17 em
ação: o `INGEST_SECRET` se resolve por **remoção**, não por rotação — não existe mais nada que o leia.

Rodar uma varredura de segredo sobre o código vivo (não sobre o histórico, que é imutável e cujo risco
o ROADMAP já classifica como nulo porque o repositório não tem remote): procurar por
`INGEST_SECRET`, por `lacir-sih-ingest`, e por padrões de chave `service_role`/JWT em todo o
`working tree` rastreado. Qualquer ocorrência em código vivo é bloqueante; ocorrências em documentos
de planejamento que citam o segredo como fato histórico são aceitáveis e ficam registradas no SUMMARY.
  </action>
  <acceptance_criteria>
    - `test ! -e scripts/catalog/uploadSihToSupabase.mjs` sai 0
    - `test ! -e "trabalhos datasus/scripts/coleta_sih_multi_disease.py"` sai 0
    - `test ! -e "trabalhos datasus/scripts/scrape_upload_sih.py"` sai 0
    - `test ! -e "trabalhos datasus/scripts/launch_overnight.py"` sai 0
    - `node -e "const s=require('./package.json').scripts; if(s['scrape:sih-multi']||s['scrape:overnight']) process.exit(1)"` sai 0
    - `supabase functions list --project-ref hmfbxqemububjyhdckrj` não lista `sih-ingest`
    - `supabase secrets list --project-ref hmfbxqemububjyhdckrj` não lista `INGEST_SECRET`
    - `git grep -n 'lacir-sih-ingest' -- ':!.planning' ':!docs'` não retorna nada
    - `git grep -nE 'INGEST_SECRET' -- ':!.planning'` não retorna nada
    - `cd pipeline/sih && uv run pytest tests/test_oracle_scrape.py -q` sai 0 — o substituto continua funcionando depois de a fonte sumir
    - `npm run gate` sai 0
  </acceptance_criteria>
  <verify>
    <automated>test ! -e scripts/catalog/uploadSihToSupabase.mjs &amp;&amp; test ! -e "trabalhos datasus/scripts/coleta_sih_multi_disease.py" &amp;&amp; test ! -e "trabalhos datasus/scripts/scrape_upload_sih.py" &amp;&amp; test ! -e "trabalhos datasus/scripts/launch_overnight.py" &amp;&amp; node -e "const s=require('./package.json').scripts; if(s['scrape:sih-multi']||s['scrape:overnight']) process.exit(1)" &amp;&amp; npm run gate</automated>
  </verify>
  <done>O caminho TabNet deixou de ser executável, e o segredo deixou de ter leitor.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aposentar `sih_metric_muni` do banco, com prova de que o Storage já serve (D-20)</name>
  <read_first>
    - `scripts/catalog/generateSihSchemaMigration.mjs` (o gerador do 09-03 — mesma forma a reproduzir: `MIGRATION_TIMESTAMP` constante, `sqlString()`, bloco de integridade com `RAISE EXCEPTION` e `IS DISTINCT FROM`, guard `isDirectRun`)
    - `src/features/catalog/sihSchemaMigration.test.ts` (a forma do teste que impede edição manual e afirma que rollback/verify ficam fora de `supabase/migrations/`)
    - `docs/SUPABASE-CATALOG.md` §"Real schema" (o DDL real de `sih_metric_muni`, o índice `sih_metric_muni_disease_uf_ano` e a FK `sih_metric_muni_disease_id_fkey` — os nomes exatos, a consultar em `pg_constraint` antes de escrever o SQL)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §D-20 (o que fica no banco e o que sai)
    - `pipeline/sih/reports/particoes-dimensionamento.md` e `pipeline/sih/reports/cobertura-final.md` (a prova de que as 27 partições existem e são legíveis)
  </read_first>
  <files>scripts/catalog/generateSihRetireMigration.mjs, supabase/migrations/20260806000000_sih_retire_muni.sql, supabase/rollback/20260806000000_sih_retire_muni_down.sql, supabase/verify/sih-retire.sql, src/features/catalog/sihRetireMigration.test.ts</files>
  <action>
Esta task é **[BLOCKING]**: a fase não pode passar por verificação sem ela, e o `npm run build`/
`tsc --noEmit` passariam sem o push porque os tipos vêm de configuração e não do banco vivo.

**Pré-condição inegociável, verificada antes de qualquer DDL:** as 27 partições precisam existir no
bucket e responder 200 a um `GET` anônimo, e a contagem total de linhas municipais dentro delas
precisa ser igual à contagem de `sih_metric_muni` no banco. Enquanto essa igualdade não for provada,
não dropar nada. Registrar os dois números no SUMMARY.

Criar `scripts/catalog/generateSihRetireMigration.mjs` no molde de `generateSihSchemaMigration.mjs`,
com `MIGRATION_TIMESTAMP = '20260806000000'` como constante literal, gerando três arquivos: o `up` em
`supabase/migrations/`, o `down` em `supabase/rollback/` e o `verify` em `supabase/verify/`.

O `up` consulta `pg_constraint`/`pg_indexes` para os nomes reais antes de dropar — nunca adivinhar,
como a Fase 8 já aprendeu — e faz: `drop index if exists sih_metric_muni_disease_uf_ano`, depois
`drop table sih_metric_muni`. Um bloco de integridade prévio, na mesma transação, aborta com
`RAISE EXCEPTION` se `sih_disease` ou `sih_metric_uf` tiverem sua contagem alterada — o
`ON DELETE CASCADE` de `sih_disease` torna um erro de ordem capaz de levar métricas junto sem erro
visível, e essa é exatamente a armadilha a bloquear.

O `down` recria a tabela com o DDL exato de `docs/SUPABASE-CATALOG.md` mais o índice, vazia. O `verify`
afirma, com `RAISE EXCEPTION`: `sih_metric_muni` ausente de `information_schema.tables`,
`sih_metric_uf` com a contagem final registrada na 09-12, `sih_disease` com 331 linhas, e as cinco
tabelas do schema v3 presentes com RLS ligada.

Escrever `src/features/catalog/sihRetireMigration.test.ts` no molde de `sihSchemaMigration.test.ts`:
regeneração byte a byte dos três arquivos e a asserção de que `down`/`verify` não estão sob
`supabase/migrations/`.

Aplicar com `supabase db push --linked` (exportando `SUPABASE_ACCESS_TOKEN`; sem TTY, se pedir
confirmação que não pode ser suprimida, parar e reportar em vez de aplicar por outro caminho) e rodar
`supabase db query --linked -f supabase/verify/sih-retire.sql`.
  </action>
  <acceptance_criteria>
    - Antes do drop: as 27 partições respondem 200 a `GET` anônimo, e o total de linhas municipais nelas é igual à contagem de `sih_metric_muni` — os dois números estão no SUMMARY
    - `node scripts/catalog/generateSihRetireMigration.mjs` sai 0 e é idempotente (`git diff --exit-code` após dupla execução)
    - `npx vitest run src/features/catalog/sihRetireMigration.test.ts` sai 0
    - `grep -c '/supabase/migrations/' supabase/rollback/20260806000000_sih_retire_muni_down.sql` retorna 0
    - `supabase db push --linked` sai 0 e a migração aparece como aplicada em `supabase migration list --linked`
    - `supabase db query --linked -f supabase/verify/sih-retire.sql` sai 0
    - `select to_regclass('public.sih_metric_muni')` devolve `null`
    - `select count(*) from sih_disease` devolve 331 e `select count(*) from sih_metric_uf` devolve o número registrado na 09-12
    - `select pg_size_pretty(sum(pg_total_relation_size(relid))) from pg_catalog.pg_statio_user_tables` devolve um valor abaixo de 500 MB, registrado no SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>node scripts/catalog/generateSihRetireMigration.mjs &amp;&amp; git diff --exit-code supabase/migrations/20260806000000_sih_retire_muni.sql &amp;&amp; npx vitest run src/features/catalog/sihRetireMigration.test.ts &amp;&amp; supabase db query --linked -f supabase/verify/sih-retire.sql</automated>
  </verify>
  <done>O grão município saiu do banco depois de provado que o Storage o serve, e o projeto cabe no plano gratuito.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar o contrato documentado e fechar o gate anti-esqueleto</name>
  <read_first>
    - `docs/SUPABASE-CATALOG.md` (arquivo inteiro — a seção "Real schema (v2)" a estender para v3, a seção "App contract", a seção "Overnight ingest" que cita `npm run scrape:overnight` e `node scripts/catalog/uploadSihToSupabase.mjs`, ambos agora inexistentes, e a tabela de diretórios de `supabase/`)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §canonical_refs (a lista do que mudou no contrato: dimensão `local`, ledger, município no Storage, tabelas de população)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md` §"Arquivos a DELETAR" (a linha final sobre `docs/SUPABASE-CATALOG.md` precisar de atualização, não deleção)
    - `pipeline/sih/tests/` (o conjunto de arquivos com `allow_module_level=True` criados na Onda 0 pelo 09-02)
    - `pipeline/sih/README.md` (para conferir se os comandos documentados ainda batem com o `package.json` de hoje)
  </read_first>
  <files>docs/SUPABASE-CATALOG.md, pipeline/sih/tests/test_suite_integrity.py</files>
  <action>
Reescrever `docs/SUPABASE-CATALOG.md` para descrever o contrato **real de hoje**, capturado ao vivo e
não intencionado — a mesma disciplina que a Fase 8 aplicou ao criar a seção "Real schema (v2)".
Precisa cobrir:

- **Schema v3**: `sih_disease` (331), `sih_metric_uf` com a dimensão `local` e a PK
  `(disease_id, uf_codigo, ano, local)`, `sih_collection_status` com a chave do D-13 e as colunas de
  proveniência e divergência, e as quatro tabelas de população. Capturar o DDL com
  `supabase db dump -s public`, não transcrever de memória.
- **Onde mora o grão município**: Storage, bucket `sih-municipio`, 27 objetos `v1/{uf}.json.gz`,
  formato JSON colunar, descompressão no cliente com `DecompressionStream`. Dizer explicitamente que
  `sih_metric_muni` **não existe mais** e por quê (teto de 500 MB, medição do CONTEXT).
- **Caminho de ingest**: `COPY` para staging + swap transacional pelo Session Pooler
  (`pipeline/sih/src/sih_pipeline/upload.py`), substituindo a seção "Overnight ingest" inteira. A
  Edge Function `sih-ingest` e o `INGEST_SECRET` deixam de ser mencionados como caminho vivo; se forem
  citados, é numa nota de histórico dizendo que foram removidos nesta fase.
- **Como interpretar ausência**: a regra do D-14 (status `coletado` + linha ausente = zero verdadeiro;
  `falhou`/`nunca_tentado` = ausente), apontando para a função pura de `audit.py`.
- **Proveniência**: `derived_at`, `cid_map_version` e `divergencia_razao` no ledger, e a frase que a
  Fase 10 renderiza.
- **Tabela de diretórios de `supabase/`**: acrescentar as migrações e verifies novos, mantendo a
  explicação de que só `migrations/` é varrido por `db push`.

Criar `pipeline/sih/tests/test_suite_integrity.py` com o gate anti-esqueleto que o `09-02` prometeu:
varrer `pipeline/sih/tests/*.py` e falhar se qualquer arquivo ainda contiver
`allow_module_level=True`. Acrescentar um segundo caso que falha se algum módulo de teste tiver zero
testes coletáveis — um arquivo esvaziado passa despercebido pelo primeiro caso.

Conferir por fim que `pipeline/sih/README.md` cita apenas comandos que existem em `package.json`.
  </action>
  <acceptance_criteria>
    - `cd pipeline/sih && uv run pytest tests/test_suite_integrity.py -x -q` sai 0
    - `grep -rl 'allow_module_level' pipeline/sih/tests/ | grep -v test_suite_integrity` não retorna nada
    - `grep -c 'sih_collection_status' docs/SUPABASE-CATALOG.md` é ≥ 1
    - `grep -c 'sih-municipio' docs/SUPABASE-CATALOG.md` é ≥ 1
    - `grep -c 'pooler' docs/SUPABASE-CATALOG.md` é ≥ 1
    - `grep -c 'scrape:overnight' docs/SUPABASE-CATALOG.md` retorna 0 e `grep -c 'uploadSihToSupabase' docs/SUPABASE-CATALOG.md` retorna 0
    - `grep -c 'zero verdadeiro' docs/SUPABASE-CATALOG.md` é ≥ 1
    - Todo comando citado em `pipeline/sih/README.md` no formato `npm run X` existe em `package.json` — verificado por script
    - `npm run gate` sai 0
  </acceptance_criteria>
  <verify>
    <automated>cd pipeline/sih &amp;&amp; uv run pytest tests/test_suite_integrity.py -x -q &amp;&amp; cd .. &amp;&amp; grep -q 'sih_collection_status' docs/SUPABASE-CATALOG.md &amp;&amp; grep -q 'sih-municipio' docs/SUPABASE-CATALOG.md &amp;&amp; ! grep -q 'uploadSihToSupabase' docs/SUPABASE-CATALOG.md &amp;&amp; npm run gate</automated>
  </verify>
  <done>O contrato documentado descreve o sistema que existe, e nenhum esqueleto de teste sobreviveu à fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Código executável antigo → operação futura | Enquanto o caminho antigo existir, ele pode ser rodado por engano |
| Segredo em texto plano → superfície de ataque | `INGEST_SECRET` só deixa de ser risco quando nada mais o lê |
| DDL destrutiva → dado de produção | `drop table` sobre 1,1M linhas é irreversível sem o Storage servindo antes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-50 | Elevation of Privilege | `INGEST_SECRET` em texto plano e Edge Function não versionada | mitigate | Remoção da função e `secrets unset`, mais varredura de segredo sobre o working tree; o histórico git permanece, mas o repositório não tem remote e nada foi publicado (risco registrado no ROADMAP) — resolvido por remoção do leitor, não por rotação (D-17/ASVS V14) |
| T-09-51 | Tampering | Uploader com o defeito do CR-04 continuando executável | mitigate | Deleção do arquivo, verificada por `test ! -e`; nenhuma exempt-list nova foi criada (D-19) |
| T-09-52 | Denial of Service | Scrapers aposentados sendo rodados por engano e pressionando o TabNet | mitigate | Arquivos deletados e entradas de `package.json` removidas, verificadas por asserção sobre o JSON |
| T-09-53 | Denial of Service | `drop table sih_metric_muni` antes de o Storage servir de fato | mitigate | Pré-condição verificada: 27 partições respondendo 200 e contagem de linhas igual à do banco, registradas no SUMMARY antes de qualquer DDL |
| T-09-54 | Tampering | `ON DELETE CASCADE` de `sih_disease` levando métricas junto num erro de ordem | mitigate | Bloco de integridade na mesma transação, com `RAISE EXCEPTION` sobre as contagens de `sih_disease` e `sih_metric_uf` |
| T-09-55 | Repudiation | Documentação descrevendo um contrato que não existe mais | mitigate | `docs/SUPABASE-CATALOG.md` regravado a partir de `supabase db dump -s public`; critérios de aceitação verificam a ausência das referências mortas |
| T-09-56 | Repudiation | Esqueleto de teste da Onda 0 sobrevivendo e dando falsa cobertura | mitigate | `test_suite_integrity.py` falha com qualquer `allow_module_level=True` remanescente e com qualquer módulo de zero testes |
</threat_model>

<verification>
- Os quatro arquivos deletados não existem; `package.json` não tem `scrape:*`
- `supabase functions list` e `supabase secrets list` não mostram `sih-ingest` nem `INGEST_SECRET`
- `supabase db query --linked -f supabase/verify/sih-retire.sql` sai 0 e `to_regclass('public.sih_metric_muni')` é `null`
- `cd pipeline/sih && uv run pytest -q` sai 0 sem nenhum módulo pulado
- `npm run gate` sai 0
</verification>

<success_criteria>
- O caminho TabNet deixou de existir: uploader, scrapers, Edge Function e segredo, todos removidos
- `sih_metric_muni` foi aposentada com prova prévia de que o Storage a substitui
- O banco cabe no plano gratuito, medido
- `docs/SUPABASE-CATALOG.md` descreve o contrato v3 real
- Nenhum esqueleto de teste sobreviveu à fase
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-14-SUMMARY.md` when done
</output>
