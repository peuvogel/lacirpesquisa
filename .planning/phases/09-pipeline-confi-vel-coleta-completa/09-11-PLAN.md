---
phase: 09-pipeline-confi-vel-coleta-completa
plan: 11
type: execute
wave: 5
depends_on: ["09-08"]
files_modified:
  - pipeline/sih/reports/confirmacao-uf-grande.md
  - pipeline/sih/tests/fixtures/oracle_ac_2019.json
  - pipeline/sih/tests/test_reconcile_gate.py
  - scripts/catalog/cid-corrections.json
  - scripts/catalog/cid-divergencias.json
autonomous: false
requirements: [PIPE-05, DATA-01, DATA-03]

must_haves:
  truths:
    - "A correção depurada no AC é confirmada contra uma UF grande (SP ou MG) sem redepurar — uma categoria vazia no Acre passaria verde por vacuidade (D-03)"
    - "O humano aprova a tabela completa de correções de uma vez: código, faixa antiga, faixa nova, delta antes, delta depois e razão — um checkpoint, não um por categoria (D-07)"
    - "Nenhum upload acontece antes desta aprovação — o upload é a única etapa irreversível da fase (D-01/D-07)"
    - "A reconciliação vira gate permanente: uma mudança futura no matcher ou na camada de correção que desloque número falha na suíte, não em produção (D-06)"
    - "O gate roda sobre um recorte pequeno de parquet (~140–250 KB de AC/2019), nunca sobre os 10 GB"
    - "A fixture do oráculo é snapshot congelado e versionado; o refresh é comando explícito e documentado, jamais parte do npm run gate (Fase 8 D-09)"
    - "Uma divergência que resiste a duas rodadas tem saída definida e escrita: vira divergência não explicada honesta, com as hipóteses descartadas listadas e a decisão do usuário datada — nunca um laço infinito entre 09-08 e 09-11, e nunca uma razão inventada (D-02/D-08)"
  artifacts:
    - path: "pipeline/sih/reports/confirmacao-uf-grande.md"
      provides: "Resultado da confirmação em SP ou MG: pares conferidos, exatos, explicados, inexplicados"
    - path: "pipeline/sih/tests/fixtures/oracle_ac_2019.json"
      provides: "Subconjunto congelado do oráculo que AC/2019 sozinho reproduz — a entrada do gate permanente"
    - path: "pipeline/sih/tests/test_reconcile_gate.py"
      provides: "Gate permanente do SC-7, invocado por npm run gate via pipeline:reconcile-gate"
  key_links:
    - from: "package.json test:run"
      to: "pipeline/sih/tests/test_reconcile_gate.py"
      via: "npm run pipeline:test, já encadeado na 09-02"
      pattern: "pipeline:test"
    - from: "test_reconcile_gate.py"
      to: "pipeline/sih/tests/fixtures/rdac_2019.parquet"
      via: "agrega a fixture pequena e compara com o oráculo congelado"
      pattern: "rdac_2019"
---

<objective>
Fechar o SC-7 com as duas provas que faltam: que a correção não é um ajuste que só funciona no Acre, e
que um humano com julgamento clínico olhou a tabela inteira antes de qualquer coisa irreversível
acontecer. E depois congelar o resultado num gate que impede a regressão silenciosa.

Purpose: a 08-07 pegou um erro clínico real exatamente assim — `ait` mapeado para I65-I69 em vez de
G45 — num checkpoint em lote, e a 08-CONTEXT registra que o `KNOWN_BY_CODE` também "passou por
revisão": 21 linhas plausíveis, nenhuma conferida. A diferença entre os dois casos é o formato da
apresentação, e é isso que este plano reproduz.
Output: confirmação em UF grande, aprovação humana da tabela completa, e o gate permanente do D-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-VALIDATION.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirmar a correção contra uma UF grande, sem redepurar (D-03)</name>
  <read_first>
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §D-03 (inteiro — a razão de existir da UF grande: "UF grande exercita categorias que um estado de 900 mil habitantes nunca exercita; uma categoria vazia no AC passaria verde por vacuidade")
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-VALIDATION.md` §"Manual-Only Verifications" (a confirmação da UF grande acontece uma vez, manualmente, fora do gate — um mês de SP mede ~16 MB só o `.dbc`)
    - `pipeline/sih/reports/reconciliacao-sc7.md` (o estado final da depuração no AC)
    - `pipeline/sih/tests/fixtures/oracle_tabnet.json` (os pares do oráculo; filtrar os da UF escolhida)
    - `pipeline/sih/src/sih_pipeline/reconcile.py`
  </read_first>
  <files>pipeline/sih/reports/confirmacao-uf-grande.md</files>
  <action>
Escolher SP ou MG — a que tiver mais pares no oráculo re-raspado do 09-05 — e agregar o ano
correspondente a partir do parquet que a corrida do 09-04 já baixou (nenhum download novo é
necessário; se aquele ano/UF ainda não tiver terminado de baixar, aguardar ou baixar só os 12 arquivos
daquela UF/ano).

Rodar `reconcile.compare` com as correções da Task 2 do 09-08 **já aplicadas** e **sem alterar nada**.
A regra do D-03 é explícita: esta etapa confirma, não redepura. Se a confirmação mostrar categorias
inexplicadas que o AC não expôs, elas voltam ao 09-08 como trabalho — mas isso é uma decisão a
registrar, não um ajuste a fazer aqui.

Escrever `pipeline/sih/reports/confirmacao-uf-grande.md` com: a UF e o ano escolhidos e por quê,
quantos pares foram conferidos, quantos exatos, quantos explicados, quantos inexplicados, e — o dado
que justifica a existência desta task — quantas categorias foram exercitadas aqui que tinham **zero
registros** no AC/2019. Esse número é a medida de quanto a depuração no Acre estava passando verde por
vacuidade.

Não commitar nenhum parquet de UF grande: a fixture do gate continua sendo só AC/2019.
  </action>
  <acceptance_criteria>
    - `pipeline/sih/reports/confirmacao-uf-grande.md` existe e registra a UF, o ano, e as quatro contagens (conferidos, exatos, explicados, inexplicados)
    - O relatório registra explicitamente quantas categorias tinham zero registros no AC/2019 e foram exercitadas na UF grande
    - Nenhuma entrada de `scripts/catalog/cid-corrections.json` foi alterada durante esta task — `git diff --stat scripts/catalog/cid-corrections.json` é vazio no fim da task
    - Nenhum arquivo `.parquet` ou `.dbc` de SP/MG foi adicionado ao git (`git status --short | grep -cE '\.(parquet|dbc)$'` retorna 0)
    - Se houver inexplicados, o relatório os lista nominalmente e o SUMMARY declara se voltam ao 09-08
  </acceptance_criteria>
  <verify>
    <automated>test -s pipeline/sih/reports/confirmacao-uf-grande.md &amp;&amp; grep -qE '\b(SP|MG)\b' pipeline/sih/reports/confirmacao-uf-grande.md &amp;&amp; grep -qi 'inexplicad' pipeline/sih/reports/confirmacao-uf-grande.md &amp;&amp; git diff --quiet scripts/catalog/cid-corrections.json</automated>
  </verify>
  <done>A correção foi confirmada num estado que exercita categorias que o Acre nunca exerceu.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint clínico em lote — aprovar a tabela completa antes de qualquer upload (D-07)</name>
  <read_first>
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §D-07 (inteiro — um checkpoint, não um por categoria; o precedente da 08-07 e a advertência sobre o `KNOWN_BY_CODE`) e §D-02 (exato ou razão escrita)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-07-SUMMARY.md` (o formato exato da apresentação que funcionou, e o erro que ele pegou: `ait` de I65-I69 para G45)
    - `scripts/catalog/cid-corrections.json` e `scripts/catalog/cid-divergencias.json` (o que está sendo aprovado)
    - `pipeline/sih/reports/reconciliacao-sc7.md` e `pipeline/sih/reports/confirmacao-uf-grande.md`
  </read_first>
  <files>scripts/catalog/cid-corrections.json, scripts/catalog/cid-divergencias.json</files>
  <action>
Montar a apresentação **antes** de pausar, usando `ReconciliationResult.render_markdown()` do 09-08.
Uma tabela única, ordenada por delta absoluto decrescente, com uma linha por correção e as colunas:
código TabNet, id canônico do agravo, rótulo clínico, faixa CID antiga, faixa CID nova, delta antes,
delta depois, e a razão escrita. Logo abaixo, uma segunda tabela com as divergências residuais de
`cid-divergencias.json` (agravo, delta mediano, razão) — são as que vão virar proveniência visível
para o estudante, então precisam ser lidas com o mesmo cuidado.

Incluir, no cabeçalho da apresentação, os três números que dão contexto: exatos antes da depuração
(31, medidos pelo spike), exatos depois, e quantas categorias a confirmação em UF grande exercitou
que o AC não exercia.

Nenhum upload pode ter acontecido até aqui, e nenhum acontece até esta aprovação — o plano `09-10`
depende deste. Registrar a aprovação (ou as correções pedidas) nos próprios arquivos JSON: uma
correção rejeitada sai do `cid-corrections.json`, e uma razão reescrita pelo usuário substitui a
anterior no texto, sem paráfrase do agente.

**Teto de iteração e a terceira saída — nenhum laço infinito entre `09-08` e `09-11`.** Se o usuário
mandar linhas de volta, elas voltam ao `09-08` e retornam a este checkpoint uma segunda vez. A partir
da **segunda** apresentação da mesma linha, "voltar para depurar" deixa de ser opção, e restam duas
saídas — ambas compatíveis com o D-02, que exige exato **ou** razão escrita e nunca uma banda de
tolerância:

1. **Aprovar** a linha como está.
2. **Registrar como divergência não explicada honesta:** a categoria sai de `inexplicado` e entra em
   `scripts/catalog/cid-divergencias.json` com `"mecanismoIdentificado": false`,
   `"hipotesesDescartadas"` listando nominalmente o que foi testado e descartado por medição no
   `09-08`, e um `razao` que diga exatamente isso em português legível por estudante — por exemplo
   "o agregado do microdado fica X% acima do TabNet nesta categoria; foram testadas e descartadas por
   medição as hipóteses A, B e C; o mecanismo não foi identificado". Isto **não** é banda de
   tolerância: é por categoria, escrito, datado, e o D-08 o torna visível na proveniência que a
   Fase 10 renderiza junto do número — quem citar o dado lê a incerteza junto.

A saída 2 é decisão do usuário, registrada com data no SUMMARY e no próprio JSON — nunca do agente, e
nunca por cansaço de iteração. O que continua proibido em qualquer caminho é inventar mecanismo: uma
razão falsa é pior que uma incerteza declarada, e é exatamente o defeito que o `KNOWN_BY_CODE` da
Fase 8 documentou.
  </action>
  <what-built>
    A tabela completa de correções de faixa CID, depurada categoria a categoria contra AC/2019 e
    confirmada contra uma UF grande, mais a tabela das divergências residuais que virarão proveniência
    visível na Fase 10.
  </what-built>
  <how-to-verify>
    1. Ler a primeira tabela linha a linha. Para cada correção, a pergunta clínica é: **a faixa CID
       nova corresponde ao que aquele rótulo clínico significa?** — não "o delta melhorou". Foi
       exatamente assim que a 08-07 pegou o `ait` mapeado para I65-I69 (doenças cerebrovasculares)
       quando o correto era G45 (isquemia cerebral transitória).
    2. Prestar atenção especial às correções cuja razão fala em "categoria restante de..." absorvendo
       registros: são as que mais mudam número e as mais fáceis de justificar de forma plausível e
       errada.
    3. Ler a segunda tabela (divergências residuais). Para cada uma, a pergunta é: **um estudante que
       citar este número num trabalho consegue defendê-lo com esta frase?** Se a razão não sustenta o
       número, ela precisa ser reescrita ou a categoria volta para depuração.
    4. Conferir que nenhuma linha diz "diferença metodológica" ou equivalente sem dizer qual.
    5. Aprovar em lote; apontar quais linhas precisam voltar; ou, para uma linha que já voltou uma vez
       ao `09-08` e continuou irreconciliável, escolher a terceira saída e registrá-la como
       divergência não explicada honesta, com as hipóteses descartadas listadas. A pergunta aqui é:
       **prefiro que o estudante leia "o mecanismo não foi identificado; testamos A, B e C" ou uma
       explicação plausível que ninguém verificou?** A segunda é o modo de falha que a Fase 8
       documentou.
  </how-to-verify>
  <resume-signal>Digite "aprovado" para liberar o upload; ou liste as linhas que precisam voltar ao 09-08 (por código TabNet) com o que está errado; ou digite "divergencia-honesta: <códigos>" para registrar essas categorias como divergência não explicada, com as hipóteses descartadas listadas, liberando o upload com a incerteza visível na proveniência (D-02/D-08)</resume-signal>
  <acceptance_criteria>
    - A tabela apresentada tem uma linha por entrada de `scripts/catalog/cid-corrections.json`, com as oito colunas listadas
    - A tabela de divergências residuais tem uma linha por entrada de `scripts/catalog/cid-divergencias.json`
    - O cabeçalho traz exatos antes, exatos depois, e categorias novas exercitadas na UF grande
    - Nenhuma linha de `sih_metric_uf` foi escrita até este ponto da fase — `select count(*) from sih_metric_uf where local = 'residencia'` continua 0
    - Após a aprovação, `scripts/catalog/cid-corrections.json` e `scripts/catalog/cid-divergencias.json` refletem exatamente o que foi aprovado, e o SUMMARY registra quantas linhas o usuário mandou voltar
    - `npm run catalog:validate` sai 0 depois de qualquer alteração pedida
    - Nenhuma linha é apresentada uma terceira vez: toda linha que voltou ao `09-08` uma vez e reaparece aqui termina aprovada ou gravada em `cid-divergencias.json` com `mecanismoIdentificado: false`, e o SUMMARY registra qual das duas para cada uma
    - Toda entrada com `mecanismoIdentificado: false` tem `hipotesesDescartadas` não-vazio e um `razao` que nomeia as hipóteses testadas e diz que o mecanismo não foi identificado
  </acceptance_criteria>
  <verify>
    <automated>npm run catalog:validate &amp;&amp; node -e "const c=require('./scripts/catalog/cid-corrections.json'),d=require('./scripts/catalog/cid-divergencias.json');if(!Array.isArray(c)||!Array.isArray(d))throw new Error('formato');for(const e of c){if(!e.reason||e.reason.length<40)throw new Error('correcao sem razao aprovada: '+e.tabnetCode);}for(const e of d){if(!e.razao||e.razao.length<30)throw new Error('divergencia sem razao aprovada: '+e.diseaseId);if(e.mecanismoIdentificado===false&&(!Array.isArray(e.hipotesesDescartadas)||e.hipotesesDescartadas.length===0))throw new Error('divergencia nao explicada sem hipoteses descartadas: '+e.diseaseId);}console.log('aprovado:',c.length,'correcoes,',d.length,'divergencias')"</automated>
  </verify>
  <done>Um humano com julgamento clínico aprovou a tabela inteira de uma vez, e o upload está liberado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Congelar a reconciliação como gate permanente (D-06)</name>
  <read_first>
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §D-06 (inteiro — fixture congelada e versionada; o teste precisa de um recorte pequeno de parquet, nunca dos 10 GB)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-VALIDATION.md` §"Como cada critério de sucesso é PROVADO" → SC-7 (os quatro passos do desenho: fixture de entrada, fixture de oráculo, o teste, a ponte Node→Python)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md` §`reconcile.py` (o analog de forma: `renameMigration.test.ts` comparando saída determinística contra fixture congelada)
    - `src/features/catalog/renameMigration.test.ts` (a disciplina "comparar contra fixture congelada, campo a campo")
    - `pipeline/sih/tests/fixtures/oracle_tabnet.json` (o oráculo completo do 09-05 — a fixture do gate é o subconjunto UF=AC)
    - `pipeline/sih/tests/fixtures/rdac_2019.parquet` (a entrada, produzida no 09-07)
    - `pipeline/sih/tests/test_reconcile_gate.py` (esqueleto a substituir)
  </read_first>
  <files>pipeline/sih/tests/fixtures/oracle_ac_2019.json, pipeline/sih/tests/test_reconcile_gate.py</files>
  <behavior>
    - O gate agrega `rdac_2019.parquet` com o matcher real e a camada de correção aprovada, e compara com `oracle_ac_2019.json`
    - `result.ok` precisa ser `True` — todo par exato ou explicado, nunca inexplicado
    - Alterar um valor de `cid-corrections.json` faz o gate falhar (provado alterando, rodando e revertendo)
    - Alterar um valor de `oracle_ac_2019.json` faz o gate falhar
    - O gate não abre nenhuma conexão de rede
    - O gate roda em menos de 10 segundos (o `09-VALIDATION.md` fixa a latência de feedback em ~60 s para a suíte inteira)
    - `oracle_ac_2019.json` contém apenas pares cuja `uf` é `AC`
  </behavior>
  <action>
Extrair de `pipeline/sih/tests/fixtures/oracle_tabnet.json` o subconjunto cuja `uf` é `AC` e gravá-lo
como `pipeline/sih/tests/fixtures/oracle_ac_2019.json`, preservando `raspadoEm` por entrada. Este é o
snapshot congelado do D-06: os pares de SP/MG **não** entram (um mês de SP mede ~16 MB só o `.dbc`,
grande demais para um gate leve), e o refresh futuro é o comando explícito
`uv run python -m sih_pipeline.cli oracle-scrape --uf AC --ano 2019`, documentado no README — nunca
parte automática do `gate`. É o mesmo padrão "snapshot versionado em vez de `fetch()` ao vivo" que a
Fase 8 D-09 estabeleceu.

Escrever `pipeline/sih/tests/test_reconcile_gate.py` cobrindo os sete comportamentos: carrega a
fixture parquet, constrói o índice com `lista-morb-cid.json` + `cid-corrections.json`, agrega filtrando
`local == 'ocorrencia'`, compara com `oracle_ac_2019.json` passando `cid-divergencias.json` como
divergências conhecidas, e afirma `result.ok`. A mensagem de falha precisa imprimir a tabela dos
inexplicados — quem quebrar o gate no futuro precisa ver **qual** número se deslocou, não só que algo
quebrou.

O gate já está encadeado no `npm run gate` pelo `pipeline:test` que a 09-02 acrescentou; confirmar
que `npm run pipeline:reconcile-gate` também roda isoladamente, porque é o comando que o
`09-VALIDATION.md` nomeia. Remover o `allow_module_level=True`.

Registrar no SUMMARY o tempo de parede medido do gate e o tamanho da fixture parquet.
  </action>
  <acceptance_criteria>
    - `npm run pipeline:reconcile-gate` sai 0
    - `cd pipeline/sih && uv run pytest tests/test_reconcile_gate.py -x -q` sai 0 e leva < 10 s (medido com `time`)
    - `node -e "const o=require('./pipeline/sih/tests/fixtures/oracle_ac_2019.json'); if(!o.length) throw new Error('vazio'); for(const e of o) if(e.uf!=='AC') throw new Error('par fora de AC: '+e.uf)"` sai 0
    - Alterar um `newRange` de `scripts/catalog/cid-corrections.json` faz `npm run pipeline:reconcile-gate` sair não-zero — provado e revertido, com o resultado registrado no SUMMARY
    - `grep -cE 'urlopen|requests\.|ftplib|socket' pipeline/sih/tests/test_reconcile_gate.py` retorna 0
    - `grep -c 'allow_module_level' pipeline/sih/tests/test_reconcile_gate.py` retorna 0
    - `npm run gate` sai 0
  </acceptance_criteria>
  <verify>
    <automated>npm run pipeline:reconcile-gate</automated>
  </verify>
  <done>Uma mudança futura no matcher ou na camada de correção que desloque número falha na suíte, não em produção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Julgamento clínico humano → dado servido ao estudante | A aprovação desta task decide o que 30 alunos vão citar em trabalhos |
| Fixture congelada → gate de CI | Um snapshot errado congela o erro em vez de detectá-lo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-37 | Repudiation | Aprovação de carimbo — 21 linhas plausíveis, nenhuma conferida (o precedente do `KNOWN_BY_CODE`) | mitigate | Apresentação em lote único com colunas de faixa antiga/nova e rótulo clínico lado a lado; as instruções de verificação dirigem a pergunta certa ("a faixa corresponde ao rótulo?") em vez de "o delta melhorou?" (D-07) |
| T-09-38 | Repudiation | Correção validada só onde a categoria é vazia | mitigate | Confirmação obrigatória em UF grande, com a contagem de categorias com zero registros no AC registrada explicitamente (D-03) |
| T-09-39 | Tampering | Regressão futura do matcher deslocando número em produção | mitigate | Gate permanente encadeado em `npm run gate`, com prova de que uma alteração o derruba (D-06) |
| T-09-22 | Denial of Service | Gate lento ou instável por depender de rede | mitigate | Fixture parquet de ~140–250 KB e oráculo congelado; nenhum acesso de rede no teste; tempo medido e registrado |
| T-09-40 | Elevation of Privilege | Upload acontecendo antes da aprovação clínica | mitigate | O plano `09-10` declara `depends_on: ["09-11"]`; o critério de aceitação verifica que `sih_metric_uf` ainda não tem nenhuma linha de `local = 'residencia'` no momento do checkpoint |
</threat_model>

<verification>
- `npm run pipeline:reconcile-gate` sai 0 e leva < 10 s
- `npm run gate` sai 0
- `pipeline/sih/reports/confirmacao-uf-grande.md` existe com as quatro contagens
- A aprovação humana está registrada no SUMMARY, com quantas linhas voltaram
</verification>

<success_criteria>
- SC-7 fechado: cada par ou bate exato, ou tem razão escrita aprovada por humano
- A correção foi confirmada fora do recorte em que foi depurada
- O gate permanente existe, é rápido, não toca rede, e comprovadamente falha quando o número se desloca
- Nada irreversível aconteceu antes desta aprovação
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-11-SUMMARY.md` when done
</output>
