---
phase: 09-pipeline-confi-vel-coleta-completa
plan: 13
type: execute
wave: 8
depends_on: ["09-12"]
files_modified:
  - scripts/catalog/generateSihPacks.mjs
  - scripts/catalog/paths.mjs
  - scripts/catalog/build.mjs
  - scripts/catalog/metricless-diseases.json
  - public/data/catalog/packs/
  - src/features/catalog/sihPacks.test.ts
autonomous: true
requirements: [DATA-01, DATA-04]

must_haves:
  truths:
    - "Os 10 packs que a tela Variáveis lê são regerados a partir do dado novo, nunca mantidos à mão (TAX-06/D-19)"
    - "O corpus legado de 654 CSVs deixa de ser entrada de build e vira arquivo histórico — 424 deles tinham 0 bytes (D-19)"
    - "A proibição de rodar `catalog:build`, herdada do handoff da 08-10, se dissolve por aposentadoria do corpus, não por re-chaveamento dele"
    - "`scripts/catalog/metricless-diseases.json` fica vazio: o código 330 passou a ter dado, e o invariante 'o conjunto de órfãos não cresce' continua válido com zero membros"
    - "Todo pack regerado carrega a data de derivação e a versão do mapa CID que o produziu (DATA-04)"
    - "A leitura da fonte servida para gerar os packs é paginada — 1000 linhas com HTTP 200 não pode reaparecer no caminho de build"
  artifacts:
    - path: "scripts/catalog/generateSihPacks.mjs"
      provides: "Gerador dos 10 packs a partir da fonte servida, no padrão de generateDiseaseSeeds.mjs"
      exports: ["PACK_IDS", "buildPack", "main"]
    - path: "scripts/catalog/metricless-diseases.json"
      provides: "Registro de agravos órfãos de métrica — vazio a partir desta fase"
    - path: "src/features/catalog/sihPacks.test.ts"
      provides: "Invariante que prova a regeneração dos packs e impede edição manual"
  key_links:
    - from: "generateSihPacks.mjs"
      to: "sih_metric_uf"
      via: "leitura paginada via PostgREST com a chave anon"
      pattern: "content-range"
    - from: "public/data/catalog/packs/"
      to: "src/features/catalog/catalogAnalysisData.ts"
      via: "os 10 imports estáticos existentes, cujos caminhos não mudam"
      pattern: "catalogAnalysisData"
---

<objective>
Fechar o ciclo do TAX-06 sobre o dado novo: os 10 packs que a tela Variáveis ainda lê passam a ser
derivados da fonte que esta fase construiu, e o corpus de 654 CSVs — do qual 424 têm 0 bytes — deixa
de ser entrada de build.

Purpose: o handoff da 08-10 deixou uma proibição de rodar `catalog:build` porque os diretórios do
corpus ainda usavam ids pré-migração. O D-19 resolve isso por aposentadoria, não por re-chaveamento —
mas só depois que existe dado novo do qual gerar os packs, que é agora.
Output: gerador dos packs, os 10 packs regerados, `metricless-diseases.json` vazio, e `catalog:build`
rodável de novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md
@.planning/phases/08-taxonomia-can-nica-integridade/08-10-SUMMARY.md
@scripts/catalog/paths.mjs
@scripts/catalog/build.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventariar os consumidores vivos e gerar os packs a partir da fonte servida</name>
  <read_first>
    - `src/features/catalog/catalogAnalysisData.ts` linhas 1-30 (os 10 imports estáticos — **este arquivo não é modificado**: a CONTEXT diz explicitamente "não mexer aqui", porque é o que a Fase 10 substitui; só o conteúdo dos JSON importados muda)
    - `src/features/catalog/catalogAnalysisData.test.ts` (o teste que já lê `public/data/catalog/packs` — precisa continuar verde)
    - `scripts/catalog/generateDiseaseSeeds.mjs` (o analog: build lendo Postgres via PostgREST)
    - `scripts/catalog/build.mjs` linhas 200-290 (como os packs são montados hoje a partir de `PACK_SOURCES`)
    - `scripts/catalog/paths.mjs` (o `PACK_SOURCES` gerado de `diseases.json`, o `multiPackSource()` e o `ALLOWED_PREFIXES`)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §D-19 ("o planner deve confirmar quais packs ainda têm consumidor vivo antes de mexer")
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-RESEARCH.md` §"Common Pitfalls" Pitfall 13 (paginação obrigatória)
  </read_first>
  <files>scripts/catalog/generateSihPacks.mjs</files>
  <action>
Começar pelo inventário que o D-19 pede. O planejamento já confirmou por `grep` que os 10 packs de
`public/data/catalog/packs/` são importados estaticamente por
`src/features/catalog/catalogAnalysisData.ts` (linhas 9-18) e lidos por
`src/features/catalog/catalogAnalysisData.test.ts` — esses são os consumidores vivos. Reconfirmar com
`grep -rn 'catalog/packs' src scripts` antes de mexer e registrar o resultado no SUMMARY; se aparecer
um consumidor novo, ele entra no inventário em vez de ser atropelado.

Criar `scripts/catalog/generateSihPacks.mjs` no molde de `generateDiseaseSeeds.mjs`: lê `sih_metric_uf`
via PostgREST com a chave anon, **paginando** com o cabeçalho `Range` e conferindo `content-range` até
cobrir o total anunciado (uma leitura truncada aqui produziria um pack silenciosamente incompleto — o
mesmo defeito que o milestone existe para matar), e escreve os 10 arquivos com exatamente o mesmo nome
e a mesma forma que os atuais, para que os imports de `catalogAnalysisData.ts` não mudem.

`PACK_IDS` é a lista literal dos 10 ids atuais, derivada do conteúdo do diretório e conferida contra
os imports — não inventada. Cada pack ganha, na sua seção de metadados, `derivedAt` e `cidMapVersion`
com os mesmos valores que `sih_collection_status` carrega (DATA-04): a proveniência precisa ser a
mesma nos dois lugares, senão o estudante vê duas datas para o mesmo número.

Filtrar `local = 'ocorrencia'` na leitura: é o padrão do app (D-10), e os packs atuais não têm a
dimensão `local`; introduzi-la aqui mudaria a forma do arquivo e quebraria o consumidor que a Fase 10
ainda vai substituir.

O script npm `catalog:sih-packs` **já existe** — foi declarado pelo plano `09-02`, dono único de
`package.json` na Onda 1. Não alterar `package.json` aqui; apenas conferir que
`npm run catalog:sih-packs` invoca este gerador e sai 0.
  </action>
  <acceptance_criteria>
    - O SUMMARY registra a saída de `grep -rn 'catalog/packs' src scripts` com a lista dos consumidores
    - `node scripts/catalog/generateSihPacks.mjs --dry-run` sai 0 e imprime as 10 contagens de linha que seriam escritas
    - `grep -cE 'content-range|Range' scripts/catalog/generateSihPacks.mjs` é ≥ 1
    - `grep -c 'derivedAt' scripts/catalog/generateSihPacks.mjs` é ≥ 1 e `grep -c 'cidMapVersion' scripts/catalog/generateSihPacks.mjs` é ≥ 1
    - `grep -c 'ocorrencia' scripts/catalog/generateSihPacks.mjs` é ≥ 1
    - `npm run catalog:sih-packs -- --dry-run` sai 0 e `git diff --exit-code package.json` sai 0 — este plano não toca `package.json`
    - `git diff --quiet src/features/catalog/catalogAnalysisData.ts` sai 0 — o consumidor não foi tocado
  </acceptance_criteria>
  <verify>
    <automated>node scripts/catalog/generateSihPacks.mjs --dry-run &amp;&amp; git diff --quiet src/features/catalog/catalogAnalysisData.ts</automated>
  </verify>
  <done>Existe um gerador dos 10 packs a partir da fonte servida, com paginação e proveniência, sem tocar no consumidor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerar os 10 packs, esvaziar metricless-diseases.json e provar a regeneração</name>
  <read_first>
    - `src/features/catalog/renameMigration.test.ts` (a forma "regenerar em memória e comparar byte a byte com o commitado")
    - `scripts/catalog/metricless-diseases.json` (o conteúdo atual — o código 330 é o único membro)
    - `scripts/catalog/validate.mjs` (o invariante que consome `metricless-diseases.json`; conferir se ele tolera lista vazia e ajustar a mensagem, nunca a regra)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §"Claude's Discretion" (código 330 passa a ter coleta; `metricless-diseases.json` fica vazio e o invariante continua válido com zero membros)
    - `public/data/catalog/packs/` (os 10 arquivos atuais)
  </read_first>
  <files>public/data/catalog/packs/, scripts/catalog/metricless-diseases.json, src/features/catalog/sihPacks.test.ts</files>
  <behavior>
    - Regenerar os 10 packs em memória e comparar byte a byte com os arquivos commitados
    - Cada pack tem `derivedAt` e `cidMapVersion` não vazios
    - Cada pack cobre a janela 2013–2025 e as 27 UFs
    - Nenhum pack contém um `disease_id` que seja tombstone da Fase 8
    - `metricless-diseases.json` é um array vazio e `npm run catalog:validate` continua verde com ele vazio
    - Ausência viaja como `null` nos packs, nunca como `0`
  </behavior>
  <action>
Rodar `node scripts/catalog/generateSihPacks.mjs` de verdade e commitar os 10 arquivos regerados.
Antes de commitar, comparar o conteúdo antigo contra o novo e registrar no SUMMARY o que mudou por
pack: quantas linhas a mais, e a variação percentual de `internacoes` no ano mais recente. Uma
variação muito diferente da ordem de grandeza que a reconciliação explicou é sinal de que algo além da
troca de fonte mudou, e precisa ser investigado antes do commit — não depois.

Esvaziar `scripts/catalog/metricless-diseases.json` para um array vazio. Conferir que
`scripts/catalog/validate.mjs` continua verde: o invariante que ele sustenta é "o conjunto de órfãos
não cresce", e zero membros satisfaz isso. Se a implementação atual assumir lista não-vazia, ajustar a
**mensagem**, nunca a regra.

Escrever `src/features/catalog/sihPacks.test.ts` cobrindo os seis comportamentos, no molde de
`renameMigration.test.ts`. O teste de regeneração byte a byte é o que impede um pack de ser editado à
mão depois — a mesma disciplina que a Fase 8 estabeleceu para o SQL.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/features/catalog/sihPacks.test.ts` sai 0 com ≥ 6 testes
    - `node -e "const m=require('./scripts/catalog/metricless-diseases.json'); if(!Array.isArray(m)||m.length!==0) process.exit(1)"` sai 0
    - `npm run catalog:validate` sai 0
    - `npx vitest run src/features/catalog/catalogAnalysisData.test.ts` sai 0 — o consumidor existente continua funcionando
    - Rodar o gerador duas vezes não muda nenhum byte: `git diff --exit-code public/data/catalog/packs/` sai 0
    - O SUMMARY registra, por pack, a variação percentual de `internacoes` no ano mais recente entre o conteúdo antigo e o novo
    - `npm run gate` sai 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/catalog/generateSihPacks.mjs &amp;&amp; git diff --exit-code public/data/catalog/packs/ &amp;&amp; npx vitest run src/features/catalog/sihPacks.test.ts src/features/catalog/catalogAnalysisData.test.ts</automated>
  </verify>
  <done>Os 10 packs são derivados do dado novo, provados por regeneração, e o registro de órfãos ficou vazio.</done>
</task>

<task type="auto">
  <name>Task 3: O corpus legado deixa de ser entrada de build (D-19)</name>
  <read_first>
    - `scripts/catalog/paths.mjs` (arquivo inteiro — `ALLOWED_PREFIXES`, `corpusPath`, `multiPackSource`, `LEGACY_PACKS` e o laço que monta `PACK_SOURCES` a partir de `diseases.json` com a guarda `assertNotTombstone`)
    - `scripts/catalog/build.mjs` (todos os pontos que consomem `PACK_SOURCES` e `corpusPath`)
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-10-SUMMARY.md` §"Handoff to Fase 9" (a proibição de rodar `catalog:build` e por quê)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md` §"Arquivos a DELETAR" (a linha do corpus: `paths.mjs` provavelmente mantém a leitura até os packs serem regerados — o que acabou de acontecer na Task 2)
    - `src/features/catalog/` (os testes que hoje dependem de `paths.mjs`, para não quebrá-los em silêncio)
  </read_first>
  <files>scripts/catalog/paths.mjs, scripts/catalog/build.mjs</files>
  <action>
Agora que os 10 packs vêm da fonte servida, o corpus de `trabalhos datasus/outputs/coleta_sih_multi/`
não é mais entrada de build. Repontar `build.mjs` para consumir os packs gerados por
`generateSihPacks.mjs` em vez de montar cada pack a partir de CSV, e remover de `paths.mjs` o que
existir só para servir o corpus: as entradas de `ALLOWED_PREFIXES` referentes a
`outputs/coleta_sih_multi`, `outputs/coleta_embolia_trombose_uf` e `outputs/coleta_vascular_amputacao`,
o `multiPackSource()`, o `LEGACY_PACKS` e o laço que monta `PACK_SOURCES` a partir de `diseases.json`.

`corpusPath()` em si **permanece** se ainda houver leitor legítimo de `build/catalogos` — conferir
antes de remover. A guarda `assertNotTombstone`/`CYCLE_CANONICAL_IDS` de `tombstones.mjs` também
permanece: ela é invariante da Fase 8, e o invariante F (`noTombstoneLiterals.test.ts`) depende dela.
Remover a guarda junto com o corpus seria jogar fora a proteção junto com o que ela protegia.

Rodar `npm run catalog:build` — é o comando que o handoff da 08-10 proibiu, e o D-19 diz que a
proibição se dissolve por aposentadoria do corpus. Ele precisa sair 0 e não produzir nenhum diff
inesperado sobre os arquivos gerados.

Registrar no SUMMARY: quais entradas foram removidas de `paths.mjs`, quais permaneceram e por quê, e o
resultado de `npm run catalog:build`.
  </action>
  <acceptance_criteria>
    - `grep -c 'coleta_sih_multi' scripts/catalog/paths.mjs` retorna 0
    - `grep -c 'multiPackSource' scripts/catalog/paths.mjs` retorna 0
    - `grep -c 'assertNotTombstone' scripts/catalog/tombstones.mjs` continua ≥ 1 — a guarda da Fase 8 sobreviveu
    - `npm run catalog:build` sai 0
    - `git diff --exit-code public/data/catalog/` sai 0 depois do `catalog:build` — o build é idempotente sobre o que a Task 2 gerou
    - `npm run catalog:validate` sai 0
    - `npx vitest run src/features/catalog/` sai 0 — incluindo `noTombstoneLiterals.test.ts`
    - `npm run gate` sai 0
  </acceptance_criteria>
  <verify>
    <automated>npm run catalog:build &amp;&amp; git diff --exit-code public/data/catalog/ &amp;&amp; npm run catalog:validate &amp;&amp; npm run gate</automated>
  </verify>
  <done>`catalog:build` volta a ser rodável e o corpus de 654 CSVs deixou de ser entrada de build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST → arquivo de build | Uma leitura truncada vira pack silenciosamente incompleto embutido no bundle |
| Corpus legado → build | 424 de 654 CSVs com 0 bytes ainda alcançáveis pelo caminho de geração |
| Artefato derivado → edição manual | Um pack editado à mão reintroduz a classe de defeito que a Fase 8 matou |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Information Disclosure | `generateSihPacks.mjs` lendo 1000 de N linhas com HTTP 200 | mitigate | Paginação por `content-range` obrigatória, verificada por critério de aceitação (Pitfall 13/MAPA-06) |
| T-09-47 | Tampering | Pack editado à mão depois de gerado | mitigate | `sihPacks.test.ts` regenera em memória e compara byte a byte com o commitado; `git diff --exit-code` após dupla execução é critério de aceitação (TAX-06) |
| T-09-48 | Tampering | Id aposentado da Fase 8 reaparecendo num pack | mitigate | Teste explícito de ausência de tombstone nos packs; `assertNotTombstone`/`tombstones.mjs` preservados ao remover o corpus |
| T-09-49 | Repudiation | Pack sem proveniência, ou com data diferente da do ledger | mitigate | `derivedAt` e `cidMapVersion` iguais aos de `sih_collection_status`, verificados por teste (DATA-04) |
| T-09-30 | Repudiation | Ausência coagida a zero no pack | mitigate | Teste que afirma `null` para ausência; é o defeito null-vs-zero que o milestone inteiro persegue |
</threat_model>

<verification>
- `npx vitest run src/features/catalog/` sai 0
- `npm run catalog:build` sai 0 e é idempotente
- `npm run catalog:validate` sai 0 com `metricless-diseases.json` vazio
- `npm run gate` sai 0
</verification>

<success_criteria>
- Os 10 packs são gerados do dado novo e a regeneração é provada por teste
- `metricless-diseases.json` está vazio e o invariante segue válido
- `catalog:build` voltou a ser rodável, e o corpus legado saiu do caminho de build
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-13-SUMMARY.md` when done
</output>
