---
phase: 09-pipeline-confi-vel-coleta-completa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - scripts/catalog/collection-order.json
autonomous: false
requirements: [PIPE-04, DATA-01, DATA-02]
user_setup:
  - service: supabase-postgres
    why: "D-17 — o COPY + swap transacional exige conexão Postgres privilegiada (senha do banco), não apenas a service_role key. O RESEARCH marca isto como bloqueante sem fallback."
    env_vars:
      - name: SIH_PIPELINE_DB_URL
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string -> Session pooler (porta 5432, host *.pooler.supabase.com)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API keys -> service_role"
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL (https://hmfbxqemububjyhdckrj.supabase.co)"
    dashboard_config:
      - task: "Copiar a Database password do projeto hmfbxqemububjyhdckrj; se estiver perdida, usar 'Reset database password' e anotar o novo valor fora do repositório"
        location: "Supabase Dashboard -> Project Settings -> Database -> Database password"

must_haves:
  truths:
    - "O operador tem, fora do repositório e fora do bundle, uma string de conexão Session Pooler funcional para o COPY do D-17"
    - "A credencial do banco nunca entra no git nem no bundle Vite (nada de prefixo VITE_)"
    - "A ordem de coleta e de upload por relevância cirúrgica existe como dado versionado com razão escrita, e nenhum agravo é excluído por ela (D-23)"
    - "O critério de fase pronta está declarado por escrito antes de qualquer implementação"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusão de .env.pipeline e dos artefatos locais do pipeline Python"
      contains: ".env.pipeline"
    - path: "scripts/catalog/collection-order.json"
      provides: "Ordem de coleta/upload por relevância cirúrgica, com razão escrita por grupo (D-23)"
      contains: "reason"
  key_links:
    - from: ".env.pipeline"
      to: "git"
      via: "git check-ignore"
      pattern: "\\.env\\.pipeline"
    - from: "scripts/catalog/collection-order.json"
      to: "scripts/catalog/diseases.json"
      via: "todo id listado existe na taxonomia canônica"
      pattern: "diseases\\.json"
---

<objective>
Resolver os dois pré-requisitos humanos que travam o resto da fase antes que eles virem surpresa: a
credencial Postgres do D-17 (bloqueante, sem fallback, segundo o `09-RESEARCH.md` §"Environment
Availability") e a ordem de relevância cirúrgica do D-23 (julgamento clínico do usuário, não do agente).

Purpose: o upload do D-16/D-17 é a única etapa irreversível da fase; descobrir na Onda 6 que falta a
senha do banco custaria dias. E a ordem de coleta precisa existir antes da corrida longa começar
(Onda 2), senão o app fica útil tarde.
Output: `.gitignore` cobrindo o arquivo de credencial e os caches do pipeline; `scripts/catalog/collection-order.json`
com a ordem de relevância cirúrgica e razão escrita por grupo.
</objective>

<criterio_de_fase_pronta>
## Declaração obrigatória (item deixado à discrição do planner em `09-CONTEXT.md` §"Claude's Discretion")

**A Fase 9 fecha com a coleta completa carregada e servida — não com o pipeline provado num recorte.**

Razões:

1. `DATA-01`, `DATA-02` e o SC-5 do `ROADMAP.md` exigem literalmente as 4 medidas para os **331**
   agravos nos dois grãos. Fechar num recorte deixaria três requisitos da fase por provar.
2. O **D-16** proíbe convivência entre dado TabNet e dado de microdado: a substituição é total e
   atômica. Não existe estado "meio servido" que possa ser declarado pronto — ou o app serve o
   microdado inteiro, ou continua servindo o corpus que a própria fase declarou não confiável
   (424 de 654 CSVs com 0 bytes).
3. O desacoplamento que o `ROADMAP.md` pede ("entregar o schema de `sih_collection_status` cedo na
   fase, para a Fase 10 poder começar enquanto a coleta roda") **é atendido por outra via**: o plano
   `09-03` aplica o schema em produção ainda na **Onda 1**. A Fase 10 destrava na Onda 1, não no
   fim da fase.

Consequência operacional: a corrida longa de download (~10 GB, dias) começa na Onda 2 (plano `09-04`)
e roda em segundo plano enquanto as Ondas 3–5 constroem e depuram a agregação — exatamente o que o
**D-01** autoriza ("baixar já, subir só depois de fechar").
</criterio_de_fase_pronta>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Credencial Postgres do D-17 (Session Pooler) fora do git e fora do bundle</name>
  <files>.gitignore</files>
  <read_first>
    - `.gitignore` (estado atual: já cobre `.env`, `.env.local`, `.local/`, `supabase/.temp/`)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §"D-17" e §"Integration Points" (`.env.local` hoje só tem `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY`; a senha do banco é pré-requisito **offline**, jamais no bundle)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-RESEARCH.md` §"Environment Availability" (linha "Senha do banco Postgres — Bloqueante se ausente") e §"Common Pitfalls" Pitfall 12 (conexão Direct é IPv6-only no plano gratuito; usar Session Pooler)
  </read_first>
  <action>
    Antes de pedir qualquer coisa ao usuário, preparar o terreno automaticamente:

    Acrescentar ao `.gitignore`, num bloco comentado identificado como "Fase 9 — pipeline SIH
    (D-17/D-12)", as entradas: `.env.pipeline`, `pipeline/sih/.venv/`, `pipeline/sih/cache/`,
    `pipeline/sih/reports/*.log`, `pipeline/sih/.pytest_cache/`, `pipeline/sih/**/__pycache__/`.
    O comentário deve dizer por que cada uma existe: `.env.pipeline` guarda a senha do banco e a
    `service_role` key (nunca `VITE_*`, nunca no bundle); `cache/` guarda os ~10 GB de parquet bruto e
    os agregados intermediários, que ficam na máquina do operador (CONTEXT §"Localização do parquet
    bruto (~10 GB) e dos agregados intermediários: local, na pasta do usuário, fora do controle de
    versão e fora do bundle"); os `.log` de corrida são ruído de execução.

    **Atenção ao que NÃO se ignora:** os relatórios em Markdown de `pipeline/sih/reports/`
    (`reconciliacao-sc7.md`, `oracle-descartados.md`, `populacao-dimensionamento.md`,
    `particoes-dimensionamento.md`, `confirmacao-uf-grande.md`, `cobertura-final.md`) **são
    versionados**. Eles são a razão escrita que o D-02 exige, o registro de descarte do D-04 e as
    medições que o D-21 e o D-24 pedem antes de travar decisão — ignorá-los apagaria justamente a
    evidência que esta fase existe para produzir. Por isso a entrada é `reports/*.log`, e não
    `reports/`.

    **NÃO** criar `.env.pipeline` com valores. Criar apenas `pipeline/sih/.env.pipeline.example`? Não —
    o diretório `pipeline/sih/` só nasce em `09-02`. Em vez disso, documentar as três variáveis no
    próprio comentário do `.gitignore`, com os nomes exatos: `SIH_PIPELINE_DB_URL`,
    `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_URL`.

    Então pausar e pedir ao usuário o que só ele pode fazer.
  </action>
  <what-built>
    `.gitignore` estendido para que o arquivo de credencial do pipeline (`.env.pipeline`) e todos os
    artefatos locais do pipeline Python sejam ignorados pelo git antes de existirem.
  </what-built>
  <how-to-verify>
    1. No Supabase Dashboard do projeto `hmfbxqemububjyhdckrj`, ir em **Project Settings → Database**.
    2. Copiar a **Database password**. Se ela estiver perdida, usar **Reset database password** e
       anotar o novo valor fora do repositório (a Fase 8 já precisou dela para o `pg_dump`).
    3. Ainda em **Project Settings → Database → Connection string**, escolher a aba **Session pooler**
       (não "Direct connection", não "Transaction pooler"). O host termina em
       `.pooler.supabase.com` e a porta é `5432`. O RESEARCH §Pitfall 12 explica: a conexão Direct é
       IPv6-only no plano gratuito e o Transaction pooler (porta 6543) quebra o protocolo `COPY`.
    4. Em **Project Settings → API keys**, copiar a chave `service_role`.
    5. Criar, **na raiz do repositório**, o arquivo `.env.pipeline` (já ignorado pelo git a partir
       desta task) com exatamente três linhas, sem aspas e sem prefixo `VITE_`:
       `SIH_PIPELINE_DB_URL=` (a string de conexão do Session pooler, com a senha embutida),
       `SUPABASE_SERVICE_ROLE_KEY=` e `SUPABASE_URL=https://hmfbxqemububjyhdckrj.supabase.co`.
    6. Confirmar que `git status --short` **não** lista `.env.pipeline`.
  </how-to-verify>
  <resume-signal>Digite "pronto" quando `.env.pipeline` existir e `git status` não o mostrar, ou descreva o que travou</resume-signal>
  <verify>
    <automated>git check-ignore -q .env.pipeline &amp;&amp; grep -c '^\.env\.pipeline$' .gitignore | grep -qx 1 &amp;&amp; test -s .env.pipeline &amp;&amp; grep -q '^SIH_PIPELINE_DB_URL=.*pooler\.supabase\.com:5432' .env.pipeline &amp;&amp; ! grep -q 'VITE_' .env.pipeline &amp;&amp; git ls-files --error-unmatch .env.pipeline 2>/dev/null; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore -q .env.pipeline` sai 0 (o arquivo está ignorado)
    - `git ls-files .env.pipeline` produz saída vazia (o arquivo nunca foi rastreado)
    - `.gitignore` contém, cada uma em sua própria linha: `.env.pipeline`, `pipeline/sih/.venv/`, `pipeline/sih/cache/`, `pipeline/sih/reports/*.log`
    - `grep -c '^pipeline/sih/reports/$' .gitignore` retorna 0 — o diretório inteiro NÃO pode ser ignorado, só os `.log`
    - `.env.pipeline` existe, é não-vazio, e sua linha `SIH_PIPELINE_DB_URL` casa com `pooler\.supabase\.com:5432`
    - `grep -c 'VITE_' .env.pipeline` retorna 0 — nenhuma variável do pipeline pode vazar para o bundle
    - `grep -rn 'SIH_PIPELINE_DB_URL' src/` não retorna nenhuma linha
  </acceptance_criteria>
  <done>
    A credencial do D-17 existe na máquina do operador, é comprovadamente invisível para o git e para
    o bundle, e a conexão escolhida é o Session Pooler (IPv4, porta 5432) — não a Direct nem a
    Transaction.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Ordem de relevância cirúrgica para coleta e upload (D-23)</name>
  <files>scripts/catalog/collection-order.json</files>
  <read_first>
    - `scripts/catalog/extra-diseases.json` (a forma "dado com motivo escrito" a imitar: array de objetos, cada um com um campo `reason` em texto corrido explicando a exceção)
    - `scripts/catalog/diseases.json` (os 331 ids canônicos — toda entrada da ordem precisa referenciar ids que existem aqui)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §"D-23" ("Nenhum agravo é cortado... a preferência fica registrada como ordem de coleta e de upload... nunca como exclusão. Quem define a lista de relevância cirúrgica é julgamento clínico do usuário, não do agente")
    - `.planning/phases/08-taxonomia-can-nica-integridade/08-07-SUMMARY.md` (formato do checkpoint clínico da 08-07, que pegou o `ait` mapeado errado — mesmo formato de apresentação a reusar)
  </read_first>
  <action>
    Preparar a decisão para o usuário em vez de pedir uma lista em branco.

    Ler `scripts/catalog/diseases.json` e montar uma proposta inicial de agrupamento por relevância
    cirúrgica, em **níveis** (`nivel: 1` sobe primeiro), não uma lista plana de 331 itens. Basear a
    proposta nas famílias que a capacitação já exercita e que aparecem nos 10 packs vivos de
    `public/data/catalog/packs/` (vascular: `embolia_e_trombose_arteriais`, `flebite_tromboflebite_embolia_e_trombose_venosa`,
    `outras_doencas_vasculares_perifericas`, `outras_doencas_das_arterias_arteriolas_e_capilares`,
    `amputacao_mmii`, `infarto_cerebral`, `acid_vascular_cerebr_nao_espec_hemorrag_ou_isquem`), mais os
    agravos cirúrgicos de alto volume que o spike já mediu como divergentes na reconciliação
    (apêndice, colelitíase, obstétricos).

    Gravar a decisão aprovada em `scripts/catalog/collection-order.json` no formato: array de objetos
    com `nivel` (inteiro, 1 = primeiro), `label` (nome do grupo em português), `diseaseIds` (array de
    ids canônicos) e `reason` (texto corrido dizendo por que este grupo vem nesta posição). Uma
    entrada final obrigatória, de maior `nivel`, com `label: "Restante do catálogo"` e
    `diseaseIds: []` significando "todos os demais, em ordem alfabética de id" — e um `reason` que
    diga explicitamente que **nenhum agravo é excluído** (D-23), que isto é ordem e não corte.

    Esta é ordem de **coleta agregada e de upload**, não de download: o download é por arquivo
    `RD{UF}{AA}{MM}` e entrega todos os agravos de uma vez (D-12 §reenquadramento). O consumidor da
    ordem é o `upload.py` (plano `09-10`) e a geração de partições (plano `09-09`).
  </action>
  <decision>Qual é a ordem de relevância cirúrgica para agregar e subir os agravos primeiro, enquanto a corrida longa termina?</decision>
  <context>
    O D-23 registra que nenhum agravo é cortado — o teto de 500 MB que forçava a escolha foi removido
    pelo D-20 (município vai para o Storage). A preferência declarada pelo usuário ("prefiro perder
    agravos do que perder anos") sobrevive só como **ordem**: o app fica útil cedo para o que importa
    na aula prática, enquanto o resto do catálogo chega. Quem sabe o que é relevante cirurgicamente
    é o usuário (liga acadêmica de cirurgia vascular), não o agente.
  </context>
  <options>
    <option id="proposta-do-agente">
      <name>Aceitar a proposta de níveis montada pelo agente</name>
      <pros>Zero trabalho; cobre as famílias que os 10 packs vivos já exercitam</pros>
      <cons>É palpite clínico de um agente — exatamente a forma do `KNOWN_BY_CODE` que a Fase 8 rejeitou</cons>
    </option>
    <option id="ajuste-do-usuario">
      <name>Usuário edita os níveis e as razões</name>
      <pros>Julgamento clínico real, registrado com razão escrita por grupo</pros>
      <cons>Custa alguns minutos de leitura da proposta</cons>
    </option>
    <option id="alfabetica">
      <name>Sem ordem: alfabética por id</name>
      <pros>Nenhuma decisão a tomar</pros>
      <cons>Abre mão do ganho do D-23 — o app demora a ficar útil para a aula</cons>
    </option>
  </options>
  <resume-signal>Responda com "proposta-do-agente", "ajuste-do-usuario" (e as correções) ou "alfabetica"</resume-signal>
  <verify>
    <automated>node -e "const o=require('./scripts/catalog/collection-order.json');const d=require('./scripts/catalog/diseases.json');const ids=new Set((Array.isArray(d)?d:d.diseases).map(x=>x.id));if(!Array.isArray(o)||o.length===0)throw new Error('collection-order vazio');for(const g of o){if(typeof g.nivel!=='number')throw new Error('nivel ausente');if(!g.reason||g.reason.length<20)throw new Error('reason ausente ou curto: '+g.label);for(const id of g.diseaseIds){if(!ids.has(id))throw new Error('id fora da taxonomia: '+id);}}if(!o.some(g=>g.diseaseIds.length===0))throw new Error('falta a entrada Restante do catalogo');console.log('collection-order OK:',o.length,'niveis')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/catalog/collection-order.json` existe e é um array JSON não-vazio
    - Todo objeto do array tem `nivel` (number), `label` (string), `diseaseIds` (array) e `reason` (string com ≥ 20 caracteres)
    - Todo id em qualquer `diseaseIds` existe em `scripts/catalog/diseases.json` — o comando acima lança se não existir
    - Existe exatamente uma entrada com `diseaseIds: []` representando "restante do catálogo"
    - O `reason` dessa entrada final contém a palavra `excluído` ou `exclusão` afirmando que nenhum agravo é cortado (D-23)
    - `node -e "..."` acima sai 0
  </acceptance_criteria>
  <done>
    A ordem de relevância cirúrgica existe como dado versionado com razão escrita por grupo, referencia
    apenas ids canônicos, e afirma explicitamente que é ordem e não corte.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → repositório git | A senha do banco e a `service_role` key atravessam a máquina do operador; se cruzarem esta fronteira, viram histórico permanente |
| repositório → bundle Vite | Qualquer variável com prefixo `VITE_` entra no JavaScript servido ao aluno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Elevation of Privilege | `.env.pipeline` (senha do banco + `service_role`) | mitigate | `.gitignore` cobre `.env.pipeline` **antes** do arquivo existir; `git check-ignore` e `git ls-files` são critérios de aceitação, não conferência visual; nomes de variável sem prefixo `VITE_` (verificado por `grep -c 'VITE_' .env.pipeline` = 0) |
| T-09-05b | Information Disclosure | `src/` importando a credencial por engano | mitigate | `grep -rn 'SIH_PIPELINE_DB_URL' src/` precisa retornar vazio — a credencial só é lida por processo Python offline |
| T-09-14 | Configuration | Uso da conexão Direct (IPv6-only) em vez do Session Pooler | mitigate | O critério de aceitação casa a string de conexão contra `pooler\.supabase\.com:5432`; a conexão Direct e o Transaction pooler (6543) falham a asserção por construção (RESEARCH Pitfall 12) |
</threat_model>

<verification>
- `git check-ignore -q .env.pipeline` sai 0 e `git ls-files .env.pipeline` é vazio
- `node -e "..."` de validação de `collection-order.json` sai 0
- `npm run gate` continua verde (nenhum código de produção foi tocado)
</verification>

<success_criteria>
- A credencial Postgres do D-17 existe, é do Session Pooler, e é comprovadamente invisível para git e bundle
- `scripts/catalog/collection-order.json` registra a ordem de relevância cirúrgica com razão escrita por grupo e sem nenhuma exclusão
- O critério de fase pronta está declarado por escrito neste plano e é a referência para `/gsd:verify-work`
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-01-SUMMARY.md` when done
</output>
