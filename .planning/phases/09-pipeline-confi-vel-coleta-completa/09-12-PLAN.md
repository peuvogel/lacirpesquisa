---
phase: 09-pipeline-confi-vel-coleta-completa
plan: 12
type: execute
wave: 7
depends_on: ["09-09", "09-10"]
files_modified:
  - pipeline/sih/src/sih_pipeline/cli.py
  - pipeline/sih/src/sih_pipeline/audit.py
  - pipeline/sih/tests/test_audit.py
  - pipeline/sih/reports/cobertura-final.md
autonomous: false
requirements: [PIPE-02, PIPE-05, DATA-01, DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "As 4 medidas estão presentes para os 331 agravos no grão UF e no grão município, nos dois locais, com taxa_mortalidade derivável em todo o catálogo (SC-5/DATA-01/DATA-02/DATA-03)"
    - "Nenhum agravo foi cortado; a ordem de relevância cirúrgica foi ordem de upload, nunca exclusão (D-23)"
    - "O operador roda um comando e vê se a coleta capturou o que afirma ter capturado, comparando o ledger de arquivo contra a fonte servida (PIPE-05)"
    - "`sih_collection_status` distingue coletado, falhou e nunca_tentado por (agravo, medida, grão, local, ano), e o app consegue lê-lo com a chave anon (PIPE-02/D-13)"
    - "Status `coletado` mais linha de métrica ausente significa zero verdadeiro; `nunca_tentado` ou `falhou` significa ausente — a regra é do ledger, não heurística do consumidor (D-14)"
    - "Um ano ainda incompleto no FTP fica distinguível de um ano completo, porque a chave do ledger inclui o ano (D-13)"
    - "Cada métrica servida carrega a data em que foi derivada e a versão do mapa CID que a produziu (DATA-04/SC-6)"
  artifacts:
    - path: "pipeline/sih/src/sih_pipeline/audit.py"
      provides: "Auditoria de cobertura: ledger de arquivo × ledger de cobertura × fonte servida"
      exports: ["audit_coverage", "CoverageReport"]
    - path: "pipeline/sih/reports/cobertura-final.md"
      provides: "O relatório de cobertura da fase: 331 agravos × 4 medidas × 2 grãos × 2 locais × 13 anos, medido"
  key_links:
    - from: "audit.py"
      to: "sih_collection_status"
      via: "leitura paginada via PostgREST e comparação contra o ledger local"
      pattern: "sih_collection_status"
    - from: "audit.py"
      to: "pipeline/sih/src/sih_pipeline/ledger.py"
      via: "summary() da Camada 1 comparado contra a Camada 2"
      pattern: "FileLedger"
---

<objective>
Fechar a coleta e provar a cobertura. Este é o plano em que o critério de "fase pronta" declarado no
`09-01` se materializa: as 4 medidas para os 331 agravos, nos dois grãos e nos dois locais, servidas —
não um recorte demonstrativo.

Purpose: o produto inteiro existe porque o pipeline anterior registrou 341 agravos como coletados com
dois terços vazios dentro. A diferença entre este pipeline e aquele não é a fonte: é que este consegue
ser auditado, e a auditoria é este plano.
Output: corrida concluída, `sih_collection_status` completo, partições e população carregadas, e o
relatório de cobertura medido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Concluir a corrida, agregar a janela inteira e carregar tudo o que foi decidido</name>
  <read_first>
    - `pipeline/sih/reports/download.log` e o `summary()` do ledger da Camada 1 (o estado real da corrida disparada no 09-04)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §D-23 (a ordem de relevância cirúrgica é ordem de upload, nunca exclusão) e §D-11 (janela 2013–2025)
    - `scripts/catalog/collection-order.json` (a ordem aprovada no checkpoint do 09-01)
    - `pipeline/sih/reports/populacao-dimensionamento.md` e `pipeline/sih/reports/particoes-dimensionamento.md` (as duas decisões do usuário que definem o que sobe e para onde)
    - `pipeline/sih/src/sih_pipeline/upload.py`, `partitions.py`, `population.py`
  </read_first>
  <files>pipeline/sih/reports/cobertura-final.md</files>
  <action>
Conferir o estado da corrida disparada no 09-04. Rodar `pipeline:download` de novo — a retomada é por
arquivo e idempotente (PIPE-03), então reexecutar é seguro e é o caminho normal de fechar pendências.
Para cada arquivo que continuar `falhou` depois de duas tentativas, investigar individualmente e
registrar: um arquivo genuinamente ausente no FTP é falha ruidosa (SC-1) e precisa de decisão, não de
silêncio.

Com os 4.212 arquivos `baixado`, agregar a janela inteira (2013–2025), respeitando a ordem de
`scripts/catalog/collection-order.json` para que o app fique útil cedo: subir nível a nível, e não
esperar o catálogo inteiro para o primeiro upload. Cada nível é um ciclo completo `COPY` → `swap` →
recontagem paginada → liberação de cache (PIPE-04), usando o `upload.py` do 09-10.

Carregar também, conforme as decisões registradas nos dois relatórios de dimensionamento: as tabelas
de população (09-06) e as 27 partições de município no Storage (09-09).

Registrar em `pipeline/sih/reports/cobertura-final.md`, **medidos**: total de arquivos baixados,
total de registros do microdado processados, tempo de parede da corrida do primeiro ao último arquivo,
tempo de parede da agregação, tempo de parede do upload por nível, e `pg_total_relation_size` final
de cada tabela do banco mais o total do bucket de Storage.

**Nenhum agravo é cortado.** Se algum nível ficar sem dado, isso é achado a investigar, não decisão a
tomar — o D-23 é explícito que a preferência do usuário virou ordem e nunca exclusão.
  </action>
  <acceptance_criteria>
    - O `summary()` do ledger da Camada 1 mostra 4.212 entradas `baixado`, ou o relatório lista nominalmente cada arquivo que não pôde ser baixado com a razão e a decisão tomada
    - `select count(distinct disease_id) from sih_metric_uf` devolve 331
    - `select count(distinct local) from sih_metric_uf` devolve 2, e as duas contagens por `local` são não-zero
    - `select min(ano), max(ano) from sih_metric_uf` devolve 2013 e 2025
    - `select count(*) from sih_metric_uf where taxa_mortalidade is not null` é > 0 para mais de 300 `disease_id` distintos (DATA-03 em todo o catálogo, não nos 5 de antes)
    - As 27 partições existem no bucket `sih-municipio` e `GET` anônimo de cada uma devolve 200
    - `pipeline/sih/reports/cobertura-final.md` traz os tempos e tamanhos medidos, incluindo `pg_total_relation_size` por tabela e o total do bucket
    - O total do banco fica abaixo de 500 MB e o total do Storage abaixo de 1 GB — os dois números estão no relatório
  </acceptance_criteria>
  <verify>
    <automated>cd pipeline/sih &amp;&amp; uv run python -c "from sih_pipeline.ledger import FileLedger; s=FileLedger.load().summary(); print(s); assert s['baixado']+s['falhou']+s['nunca_tentado']==4212, s" &amp;&amp; cd .. &amp;&amp; supabase db query --linked --query "select count(distinct disease_id) as agravos, count(distinct local) as locais, min(ano) as ano_min, max(ano) as ano_max from sih_metric_uf"</automated>
  </verify>
  <done>A janela 2013–2025 está agregada e servida para os 331 agravos, nos dois grãos e nos dois locais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ledger Camada 2 completo e a regra de zero verdadeiro (PIPE-02/D-13/D-14)</name>
  <read_first>
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-CONTEXT.md` §D-13 (a chave e por que `ano` é obrigatório) e §D-14 (inteiro — zero verdadeiro contra ausente, e o que foi rejeitado: materializar 48 milhões de zeros, e coluna de status na própria linha de métrica)
    - `.planning/REQUIREMENTS.md` §PIPE-02 e §MAPA-03/MAPA-04 (a Fase 10 recebe daqui uma regra única em vez de heurística)
    - `scripts/catalog/schema-v3.json` (`medidas`, `graos`, `locais`, `statuses`, `anoMin`, `anoMax`)
    - `pipeline/sih/src/sih_pipeline/upload.py` (a escrita de `sih_collection_status`)
    - `09-03-PLAN.md` §`<interfaces>` (as colunas de `sih_collection_status` e o check de proveniência obrigatória)
  </read_first>
  <files>pipeline/sih/src/sih_pipeline/audit.py, pipeline/sih/tests/test_audit.py</files>
  <behavior>
    - `audit_coverage` compara o produto cartesiano completo (331 agravos × 4 medidas × 2 grãos × 2 locais × 13 anos ≈ 69 mil) contra o que existe em `sih_collection_status`, e reporta as combinações faltantes por nome
    - Uma combinação com `status = 'coletado'` e sem linha de métrica é classificada como **zero verdadeiro**, não como ausente
    - Uma combinação com `status` em `('falhou', 'nunca_tentado')` e sem linha de métrica é classificada como **ausente**
    - A mesma combinação nunca aparece nas duas listas
    - `audit_coverage` lê `sih_collection_status` de forma paginada e falha se o total lido não bater com o `content-range` anunciado
    - Um ano com menos arquivos `baixado` do que 27 × 12 é reportado como ano incompleto, mesmo que todos os agravos tenham linha — é o caso do ano mais recente, que o D-13 existe para tornar visível
    - `CoverageReport.ok` é `True` somente quando não há nenhuma combinação `nunca_tentado` nem `falhou`
  </behavior>
  <action>
Implementar `audit.py` com `audit_coverage` conforme os comportamentos declarados, e acrescentar o
subcomando `audit` ao `cli.py` (o nome do script npm já existe desde a 09-02 como parte da família
`pipeline:*`; se não houver entrada dedicada, acrescentar `"pipeline:audit"` seguindo o mesmo padrão).

A classificação zero-verdadeiro × ausente é a entrega conceitual desta task: a Fase 10 vai renderizar
"sem dado coletado", "zero verdadeiro" e "menor balde" como três coisas distintas (MAPA-03/MAPA-04), e
a regra que as distingue precisa nascer aqui, no ledger, e não como heurística no consumidor. Escrever
essa regra como uma função pura, com docstring citando o D-14, para que a Fase 10 possa portá-la ou
consumi-la sem reinterpretar.

A leitura paginada reusa `recount_via_postgrest` do 09-10 — não reimplementar paginação.

Escrever `tests/test_audit.py` cobrindo os sete comportamentos com dados sintéticos pequenos (não a
tabela real de 69 mil linhas). O teste de "mesma combinação nunca nas duas listas" é o que impede a
regra do D-14 de virar ambígua sob refatoração.
  </action>
  <acceptance_criteria>
    - `cd pipeline/sih && uv run pytest tests/test_audit.py -x -q` sai 0 com ≥ 7 testes
    - `cd pipeline/sih && uv run python -m sih_pipeline.cli audit` sai 0 e imprime as contagens por status
    - `select count(*) from sih_collection_status` devolve um número entre 60.000 e 75.000 (a ordem de grandeza do D-13; o valor exato depende de quantas combinações se aplicam)
    - `select count(*) from sih_collection_status where status = 'nunca_tentado'` devolve 0, ou o relatório lista nominalmente as combinações e a razão
    - `select count(*) from sih_collection_status where status = 'coletado' and (derived_at is null or cid_map_version is null)` devolve 0
    - `select count(distinct ano) from sih_collection_status` devolve 13
    - `grep -c 'D-14' pipeline/sih/src/sih_pipeline/audit.py` é ≥ 1 (a regra cita a decisão)
    - `npm run gate` sai 0
  </acceptance_criteria>
  <verify>
    <automated>cd pipeline/sih && uv run pytest tests/test_audit.py -x -q && uv run python -m sih_pipeline.cli audit</automated>
  </verify>
  <done>O ledger de cobertura está completo e a regra que distingue zero verdadeiro de ausente existe como função pura, pronta para a Fase 10.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Conferência humana da cobertura final (PIPE-05)</name>
  <read_first>
    - `pipeline/sih/reports/cobertura-final.md` (Task 1)
    - `.planning/notes/2026-08-04-pysus-microdado-spike.md` §6 (o estado que estamos substituindo: 424 de 654 CSVs com 0 bytes, 212 agravos inteiramente vazios, 84 de 85 trazendo só `internacoes`)
    - `.planning/ROADMAP.md` §"Phase 9" (os 7 critérios de sucesso)
    - `scripts/catalog/collection-order.json` (a ordem que o usuário aprovou no 09-01)
  </read_first>
  <files>pipeline/sih/reports/cobertura-final.md</files>
  <action>
Antes de pausar, montar a comparação lado a lado entre o estado anterior e o atual, no próprio
relatório: agravos com dado (era 115 com qualquer conteúdo, de 331), medidas por agravo (era 1 de 4
em 84 de 85 casos), grãos, locais, anos, e a contagem de arquivos vazios (era 424 de 654). É o número
que torna a fase legível para quem não a acompanhou.

Acrescentar a checagem que o ROADMAP §"Riscos conhecidos" pediu e que ninguém fez ainda: a suposição
de que o TabNet nunca emite célula `0` explícita (ausência de linha *é* o zero) veio do próprio
pipeline aposentado, não de spec oficial. Agora que existe um raspador mínimo (09-05) e uma regra
escrita de zero verdadeiro (Task 2), verificar empiricamente contra o portal um caso em que o
microdado agrega zero e conferir o que o TabNet devolve. Registrar o resultado no relatório — confirma
ou derruba a premissa, e a Fase 10 depende dela.
  </action>
  <what-built>
    A coleta completa da janela 2013–2025 para os 331 agravos, nos dois grãos e nos dois locais, com o
    ledger de cobertura preenchido e auditado, mais a verificação empírica da premissa null-vs-zero
    contra o TabNet.
  </what-built>
  <how-to-verify>
    1. Abrir `pipeline/sih/reports/cobertura-final.md` e conferir a tabela "antes × depois". A pergunta
       é: **os números fazem sentido clínico?** Um agravo raro com dezenas de milhares de internações,
       ou um agravo comum com dezenas, é sinal de faixa CID ainda errada — não de coleta incompleta.
    2. Escolher três agravos que você conhece bem (por exemplo, os da relevância cirúrgica do nível 1
       de `collection-order.json`) e conferir a ordem de grandeza das internações no ano mais recente
       contra a sua expectativa.
    3. Conferir a seção do relatório sobre o ano mais recente: ele está completo no FTP ou ainda em
       revisão? Um ano parcial precisa aparecer como parcial no ledger, não como completo com número
       baixo.
    4. Conferir a seção null-vs-zero: o TabNet devolve célula `0` explícita ou omite a linha? A
       resposta muda como a Fase 10 pinta o mapa.
    5. Aprovar, ou apontar quais agravos parecem errados para voltarem à depuração.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou liste os agravos cujos números parecem errados, com a ordem de grandeza que você esperava</resume-signal>
  <acceptance_criteria>
    - `pipeline/sih/reports/cobertura-final.md` contém a tabela "antes × depois" com os cinco números do estado anterior (115 agravos com conteúdo, 424 CSVs vazios, 1 medida de 4, 1 grão útil, 1 local)
    - O relatório registra se o ano mais recente da janela está completo no FTP, medido pela contagem de arquivos daquele ano no ledger
    - O relatório registra o resultado da verificação empírica null-vs-zero contra o TabNet, com o caso concreto testado
    - A aprovação (ou a lista de agravos a revisar) está registrada no SUMMARY
    - Se o usuário apontar agravos errados, o SUMMARY declara explicitamente que eles voltam ao `09-08` e a fase não é dada por fechada
  </acceptance_criteria>
  <verify>
    <automated>grep -qi 'antes' pipeline/sih/reports/cobertura-final.md &amp;&amp; grep -q '424' pipeline/sih/reports/cobertura-final.md &amp;&amp; grep -qi 'zero' pipeline/sih/reports/cobertura-final.md &amp;&amp; grep -q '331' pipeline/sih/reports/cobertura-final.md</automated>
  </verify>
  <done>Um humano conferiu que os números fazem sentido clínico, e a premissa null-vs-zero deixou de ser herdada do pipeline aposentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger de cobertura → interpretação da Fase 10 | A regra escrita aqui decide se um pixel vazio significa "zero" ou "não sabemos" |
| PostgREST → auditoria de 69 mil linhas | Uma leitura truncada faria a auditoria declarar cobertura que não existe |
| Corrida longa → declaração de completude | "Registrou sucesso com dois terços vazio" é literalmente o estado que a fase substitui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-44 | Repudiation | Declarar cobertura sem prova, repetindo o defeito do pipeline aposentado | mitigate | `audit_coverage` compara o cartesiano completo contra a fonte servida e reporta faltantes por nome; a Task 1 exige contagens SQL absolutas como critério de aceitação (PIPE-05) |
| T-09-11 | Information Disclosure | Auditoria lendo 1000 de 69.000 linhas com HTTP 200 | mitigate | Leitura paginada reusando `recount_via_postgrest`, com falha alta se o total lido não bater com o `content-range` (Pitfall 13) |
| T-09-45 | Repudiation | Ausente virando zero na interpretação da Fase 10 | mitigate | A regra do D-14 nasce como função pura com docstring citando a decisão, e o teste garante que uma combinação nunca cai nas duas listas (MAPA-03/MAPA-04) |
| T-09-46 | Repudiation | Ano incompleto no FTP indistinguível de ano completo | mitigate | A chave do ledger inclui `ano` (D-13) e a auditoria reporta ano incompleto por contagem de arquivos, não por presença de agravo |
| T-09-26 | Denial of Service | Estourar o teto do plano gratuito ao carregar tudo | mitigate | `pg_total_relation_size` por tabela e total do bucket medidos e registrados como critério de aceitação, com os limites de 500 MB e 1 GB explicitados |
</threat_model>

<verification>
- `cd pipeline/sih && uv run python -m sih_pipeline.cli audit` sai 0
- As consultas SQL de contagem devolvem 331 agravos, 2 locais, anos 2013–2025
- `pipeline/sih/reports/cobertura-final.md` traz medições e a comparação antes × depois
- `npm run gate` sai 0
</verification>

<success_criteria>
- SC-5 fechado: 4 medidas × 331 agravos × 2 grãos × 2 locais, com `taxa_mortalidade` em todo o catálogo
- SC-6 fechado: `derived_at` e `cid_map_version` em toda linha coletada
- PIPE-05 fechado: um comando audita a coleta contra a fonte servida
- A regra zero-verdadeiro × ausente existe como função pura, pronta para a Fase 10
- A premissa null-vs-zero foi verificada empiricamente, não herdada
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-12-SUMMARY.md` when done
</output>
