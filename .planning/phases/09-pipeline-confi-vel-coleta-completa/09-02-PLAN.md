---
phase: 09-pipeline-confi-vel-coleta-completa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/sih/pyproject.toml
  - pipeline/sih/.python-version
  - pipeline/sih/uv.lock
  - pipeline/sih/README.md
  - pipeline/sih/src/sih_pipeline/__init__.py
  - pipeline/sih/src/sih_pipeline/paths.py
  - pipeline/sih/tests/conftest.py
  - pipeline/sih/tests/test_paths.py
  - pipeline/sih/tests/test_enumerate.py
  - pipeline/sih/tests/test_ledger.py
  - pipeline/sih/tests/test_matcher.py
  - pipeline/sih/tests/test_aggregate.py
  - pipeline/sih/tests/test_oracle_scrape.py
  - pipeline/sih/tests/test_population.py
  - pipeline/sih/tests/test_reconcile.py
  - pipeline/sih/tests/test_partitions.py
  - pipeline/sih/tests/test_upload.py
  - pipeline/sih/tests/test_reconcile_gate.py
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [PIPE-01, PIPE-03, PIPE-04, DATA-03]

must_haves:
  truths:
    - "O pipeline Python roda em Python 3.11 com pysus pinado em 1.0.1, e o porquê do pin está escrito ao lado do pin"
    - "Nenhuma escrita de arquivo do pipeline resolve caminho fora do diretório de cache configurado"
    - "npm run gate falha se a suíte Python falhar — o pipeline está dentro das garantias da Fase 7, não fora delas"
    - "Todo arquivo de teste que o 09-VALIDATION.md exige existe, e cada um declara qual plano o preenche"
    - "Todo script `pipeline:*` declarado aqui tem um plano dono nominal que entrega o subcomando correspondente antes do fim da fase — nenhum script sobrevive apontando para subcomando inexistente"
  artifacts:
    - path: "pipeline/sih/pyproject.toml"
      provides: "Projeto uv com pysus==1.0.1 e psycopg[binary]==3.3.4 pinados exatos, pytest como dev-dep"
      contains: "pysus==1.0.1"
    - path: "pipeline/sih/src/sih_pipeline/paths.py"
      provides: "Guarda de caminho ASVS V12 — equivalente Python de corpusPath()"
      exports: ["cache_path", "ledger_path", "reports_path", "cache_root"]
    - path: "pipeline/sih/README.md"
      provides: "Documentação do pin pysus==1.0.1 com a razão (defeito do índice de arquivos da 2.x)"
      contains: "2.7.0"
    - path: "package.json"
      provides: "Ponte Node→Python encadeada em test:run, fail-closed"
      contains: "pipeline:test"
    - path: ".github/workflows/ci.yml"
      provides: "uv instalado no CI antes de npm run gate, action pinada por SHA"
      contains: "astral-sh/setup-uv@"
  key_links:
    - from: "package.json test:run"
      to: "pipeline/sih pytest"
      via: "npm run pipeline:test encadeado com &&"
      pattern: "pipeline:test.*vitest run"
    - from: "pipeline/sih/src/sih_pipeline/paths.py"
      to: "SIH_PIPELINE_CACHE_DIR"
      via: "os.environ, com default em ~/.lacir/sih-cache"
      pattern: "SIH_PIPELINE_CACHE_DIR"
---

<objective>
Criar a casa do pipeline Python e conectá-la às garantias que a Fase 7 e a Fase 8 já entregaram, para
que o pipeline novo não repita o destino dos scrapers de `trabalhos datasus/scripts/` — código que
rodava fora de qualquer gate e registrou 341 agravos como coletados com dois terços vazios.

Purpose: `09-VALIDATION.md` §"Wave 0 Requirements" lista exatamente o que precisa existir antes de
qualquer task de implementação ter um `<automated>` real para apontar. Este plano é essa Onda 0.
Output: projeto `uv` em `pipeline/sih/`, guarda de caminho ASVS V12 testada, esqueleto de testes com
dono declarado por arquivo, e `npm run gate` fail-closed sobre a suíte Python.
</objective>

<scope_note>
**Exceção de escopo consciente: 20 arquivos em `files_modified`, acima do limiar usual de 15.**
Dez dos vinte são esqueletos triviais — docstring de módulo mais uma linha de
`pytest.skip(..., allow_module_level=True)` — e três são gerados por ferramenta ou têm uma linha
(`uv.lock`, `.python-version`, `__init__.py`). A complexidade real mora em seis arquivos:
`pyproject.toml`, `paths.py`, `conftest.py`, `README.md`, `package.json` e `ci.yml`.

Os dez esqueletos **não** podem ir para um plano auxiliar sem destruir a razão de existir da Onda 0:
o `09-VALIDATION.md` §"Wave 0 Requirements" exige que todo `<automated>` dos planos seguintes aponte
para um arquivo que já existe, e um plano auxiliar paralelo disputaria o mesmo diretório `tests/`
com este — trocaria uma exceção registrada por um conflito de escrita na mesma onda. A exceção fica
aceita e escrita aqui, em vez de mascarada por um split artificial.
</scope_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-VALIDATION.md
@.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md
@scripts/catalog/paths.mjs
@package.json
@.github/workflows/ci.yml
</context>

<interfaces>
<!-- Contratos que os planos 09-04 a 09-11 vão implementar contra. Definidos aqui, não descobertos lá. -->

`pipeline/sih/src/sih_pipeline/paths.py` exporta:

- `cache_root() -> pathlib.Path` — lê `SIH_PIPELINE_CACHE_DIR`, default `~/.lacir/sih-cache`, cria se não existir, retorna resolvido
- `cache_path(relative: str) -> pathlib.Path` — rejeita caminho absoluto, rejeita `..` após normalização, rejeita string vazia, rejeita qualquer coisa fora de `cache_root()`; usado por todo download/parquet
- `ledger_path() -> pathlib.Path` — `cache_path("ledger/files.json")`
- `reports_path(relative: str) -> pathlib.Path` — resolve dentro de `pipeline/sih/reports/` (versionado no `.gitignore`, não no git)
- `repo_root() -> pathlib.Path` — raiz do repositório, para ler `scripts/catalog/*.json`

Estrutura de diretórios sob `cache_root()` (contrato fixo, não inventar variação):
`parquet/RD{UF}{AA}{MM}/` (diretório parquet que o `pysus` escreve), `dbc/` (temporários),
`ledger/files.json`, `agregados/` (saída do `aggregate.py`), `populacao/` (saída do `population.py`),
`particoes/` (saída do `partitions.py`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Projeto uv em pipeline/sih/ com pysus==1.0.1 pinado e o porquê escrito ao lado</name>
  <files>pipeline/sih/pyproject.toml, pipeline/sih/.python-version, pipeline/sih/uv.lock, pipeline/sih/README.md, pipeline/sih/src/sih_pipeline/__init__.py</files>
  <read_first>
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-RESEARCH.md` §"Standard Stack" (tabela Core + bloco `Installation`) e §"Package Legitimacy Audit" (todos `Approved`, `slopcheck` OK, `pysus` com sinal informativo `NO_REPO` já mitigado)
    - `.planning/notes/2026-08-04-pysus-microdado-spike.md` §1 e §2 — a razão do pin: a 2.7.0 devolve arquivos RJ (AIH rejeitada) e SP (um por procedimento) sob o nome do grupo `RD` pedido
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md` §"No Analog Found" (este é o primeiro projeto `uv` do repositório)
  </read_first>
  <action>
    Criar `pipeline/sih/` com `uv init --python 3.11` e transformá-lo num pacote com layout `src/`:
    `pipeline/sih/src/sih_pipeline/__init__.py` (com `__all__` vazio e um docstring de uma linha).

    Em `pipeline/sih/pyproject.toml`:
    - `[project] name = "sih-pipeline"`, `requires-python = ">=3.11,<3.14"`
    - `dependencies = ["pysus==1.0.1", "psycopg[binary]==3.3.4"]` — **pin exato com `==`**, nunca `>=`
      nem `~=`. `pyarrow`, `pandas`, `dbfread` e `pyreaddbc` entram transitivamente via `pysus`; não
      declarar nenhum deles explicitamente.
    - `[dependency-groups] dev = ["pytest>=8,<9"]`
    - `[tool.pytest.ini_options]` com `testpaths = ["tests"]`, `pythonpath = ["src"]`,
      `addopts = "-q --strict-markers"`
    - `[build-system]` com `hatchling` e `[tool.hatch.build.targets.wheel] packages = ["src/sih_pipeline"]`

    Rodar `uv sync` na pasta para gerar `uv.lock` e **versionar o `uv.lock`** (mesma disciplina do
    `package-lock.json`). O `.venv/` já está no `.gitignore` pelo plano `09-01`.

    Escrever `pipeline/sih/README.md` contendo, no mínimo: (a) um bloco "Por que `pysus==1.0.1` e não
    a 2.x" citando o defeito medido — `list_files("SIH", state, year)` devolve exatamente um arquivo
    por (estado, ano, mês) com o grupo escolhido arbitrariamente, `group="RD"` volta vazio, e
    `RDAC1901.parquet` não é alcançável por caminho nenhum; RJ é AIH rejeitada e SP é uma linha por
    procedimento, então um pipeline que confiasse no índice produziria número plausível e errado;
    (b) a frase explícita **"não atualizar `pysus` sem reproduzir o teste do spike §1"**; (c) os
    comandos de uso (`uv sync`, `uv run pytest`, `uv run python -m sih_pipeline.<módulo>`); (d) a
    tabela das variáveis de ambiente lidas (`SIH_PIPELINE_CACHE_DIR`, `SIH_PIPELINE_DB_URL`,
    `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_URL`) dizendo que vêm de `.env.pipeline`, nunca do git.

    Confirmar a versão de Python disponível antes: usar `python3.11` (a máquina tem 3.11.15); o
    `python3` do sistema é 3.9.6 e **não serve** para `pysus`.
  </action>
  <verify>
    <automated>cd pipeline/sih && uv sync --frozen && uv run python -c "import pysus, sys; from importlib.metadata import version; assert version('pysus')=='1.0.1', version('pysus'); assert sys.version_info[:2]==(3,11), sys.version_info; print('pysus 1.0.1 em py3.11 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/sih/pyproject.toml` contém a string literal `pysus==1.0.1` e a string literal `psycopg[binary]==3.3.4`
    - `grep -c 'pysus>=\|pysus~=\|pysus>' pipeline/sih/pyproject.toml` retorna 0 (nenhum pin frouxo)
    - `pipeline/sih/.python-version` contém exatamente `3.11`
    - `pipeline/sih/uv.lock` existe e está rastreado pelo git (`git ls-files pipeline/sih/uv.lock` não é vazio)
    - `grep -v '^#' pipeline/sih/README.md | grep -c '2\.7\.0'` é ≥ 1 e `grep -c 'não atualizar' pipeline/sih/README.md` é ≥ 1
    - `cd pipeline/sih && uv run python -c "from importlib.metadata import version; print(version('pysus'))"` imprime `1.0.1`
    - `cd pipeline/sih && uv run python -c "import pyarrow, psycopg; print('deps transitivas OK')"` sai 0
  </acceptance_criteria>
  <done>
    O ambiente Python existe, é reprodutível (`uv.lock` versionado), roda em 3.11, tem `pysus` pinado
    exatamente em 1.0.1, e o README impede que alguém "atualize" e reintroduza o defeito do índice.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guarda de caminho ASVS V12 (paths.py) e esqueleto de testes com dono declarado</name>
  <files>pipeline/sih/src/sih_pipeline/paths.py, pipeline/sih/tests/conftest.py, pipeline/sih/tests/test_paths.py, pipeline/sih/tests/test_enumerate.py, pipeline/sih/tests/test_ledger.py, pipeline/sih/tests/test_matcher.py, pipeline/sih/tests/test_aggregate.py, pipeline/sih/tests/test_oracle_scrape.py, pipeline/sih/tests/test_population.py, pipeline/sih/tests/test_reconcile.py, pipeline/sih/tests/test_partitions.py, pipeline/sih/tests/test_upload.py, pipeline/sih/tests/test_reconcile_gate.py</files>
  <read_first>
    - `scripts/catalog/paths.mjs` (a função `corpusPath` inteira, linhas 29-53 — é o guard a reproduzir em Python: rejeita string vazia, rejeita caminho absoluto, normaliza e rejeita `..`, exige prefixo em allowlist, e só então resolve)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md` §"Guarda de path / nunca resolver fora do allowlist (ASVS V12)"
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-VALIDATION.md` §"Wave 0 Requirements" e §"Requirement → Test Map" (os nomes exatos de arquivo e de função de teste que os planos seguintes vão preencher)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-RESEARCH.md` §"Security Domain" linha V12
  </read_first>
  <behavior>
    - `cache_path("parquet/RDAC1901")` retorna um caminho dentro de `cache_root()` e cria os diretórios-pai
    - `cache_path("/etc/passwd")` levanta `ValueError` (caminho absoluto rejeitado)
    - `cache_path("../../fora")` levanta `ValueError` (traversal rejeitado após normalização)
    - `cache_path("")` levanta `ValueError` (string vazia rejeitada)
    - `cache_path("parquet/../../fora")` levanta `ValueError` — o teste precisa cobrir traversal **no meio** do caminho, não só no começo
    - `cache_path("bagunca/x")` levanta `ValueError` — prefixo fora do allowlist (`parquet`, `dbc`, `ledger`, `agregados`, `populacao`, `particoes`)
    - `cache_root()` respeita `SIH_PIPELINE_CACHE_DIR` quando definido (teste usa `monkeypatch.setenv` + `tmp_path`)
    - `ledger_path()` termina em `ledger/files.json` e está dentro de `cache_root()`
    - `reports_path("x.json")` resolve dentro de `pipeline/sih/reports/` e nunca dentro de `cache_root()`
  </behavior>
  <action>
    Escrever `pipeline/sih/tests/test_paths.py` primeiro, cobrindo os nove comportamentos listados em
    `<behavior>`, e só então `pipeline/sih/src/sih_pipeline/paths.py`.

    `paths.py` reproduz a disciplina de `corpusPath()` em Python: constante de módulo
    `ALLOWED_PREFIXES = ("parquet", "dbc", "ledger", "agregados", "populacao", "particoes")`;
    `cache_root()` lê `os.environ.get("SIH_PIPELINE_CACHE_DIR")` com default
    `Path.home() / ".lacir" / "sih-cache"`, chama `.expanduser().resolve()` e `mkdir(parents=True, exist_ok=True)`;
    `cache_path(relative)` valida na ordem — tipo `str` e não-vazio, não `os.path.isabs`,
    `os.path.normpath` e rejeição se o resultado contiver `..` ou começar com `/`, primeiro segmento
    em `ALLOWED_PREFIXES` — e só depois monta `cache_root() / normalizado`, com uma última asserção de
    que o `resolve()` do resultado ainda começa em `cache_root()` (defesa contra symlink).
    Toda rejeição levanta `ValueError` com mensagem em português dizendo qual regra falhou e qual foi
    a entrada, no espírito das mensagens de `corpusPath`.
    `repo_root()` sobe a partir de `Path(__file__)` até encontrar o diretório que contém `package.json`.

    `pipeline/sih/tests/conftest.py`: uma fixture `cache_dir(tmp_path, monkeypatch)` que aponta
    `SIH_PIPELINE_CACHE_DIR` para `tmp_path` e devolve o `Path` — é a fixture que todos os planos
    seguintes usam para nunca escrever no cache real durante teste. Nenhuma fixture do `conftest.py`
    pode tocar rede.

    Criar os dez arquivos de teste restantes como **esqueleto com dono declarado**: cada um contém um
    docstring de módulo dizendo o que ele prova, qual requisito cobre e **qual plano o preenche**,
    seguido de `pytest.skip("preenchido pelo plano 09-NN", allow_module_level=True)`. O mapa de donos
    é fixo e não pode ser inventado pelo executor:
    `test_enumerate.py` → 09-04 (PIPE-01/SC-1) · `test_ledger.py` → 09-04 (PIPE-03/SC-3) ·
    `test_oracle_scrape.py` → 09-05 (D-18/D-04) · `test_population.py` → 09-06 (D-24) ·
    `test_matcher.py` → 09-07 (matcher CID) · `test_aggregate.py` → 09-07 (DATA-03) ·
    `test_reconcile.py` → 09-08 (SC-7/D-02) · `test_partitions.py` → 09-09 (D-20/D-21) ·
    `test_upload.py` → 09-10 (PIPE-04/SC-4) · `test_reconcile_gate.py` → 09-11 (D-06).
    O plano `09-14` tem um gate que falha se algum `allow_module_level=True` sobreviver ao fim da fase.
  </action>
  <verify>
    <automated>cd pipeline/sih && uv run pytest tests/test_paths.py -x -q && uv run pytest -q --collect-only | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd pipeline/sih && uv run pytest tests/test_paths.py -x -q` sai 0 com pelo menos 9 testes coletados
    - `cd pipeline/sih && uv run pytest -q` sai 0 (os dez esqueletos são pulados, nenhum falha)
    - `cd pipeline/sih && uv run python -c "from sih_pipeline.paths import cache_path; cache_path('/etc/passwd')"` sai não-zero com `ValueError`
    - `cd pipeline/sih && uv run python -c "from sih_pipeline.paths import cache_path; cache_path('parquet/../../fora')"` sai não-zero com `ValueError`
    - `grep -l 'allow_module_level=True' pipeline/sih/tests/*.py | wc -l` retorna exatamente 10
    - Cada um dos dez esqueletos contém a string `09-0` ou `09-1` seguida do número do plano dono, no docstring de módulo
    - `grep -rn 'urlopen\|requests\|ftplib\|socket' pipeline/sih/tests/conftest.py` não retorna nada — nenhuma fixture toca rede
  </acceptance_criteria>
  <done>
    O guard V12 existe e é provado por teste contra traversal em três formas distintas; os dez arquivos
    de teste que `09-VALIDATION.md` exige existem, cada um declarando qual plano o preenche.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ponte Node→Python fail-closed em test:run e uv no CI pinado por SHA</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - `package.json` (seção `scripts` atual — `pretest`, `test:run`, `gate` encadeiam com `&&`, nunca com `||`)
    - `.github/workflows/ci.yml` (arquivo inteiro, 29 linhas — o comentário sobre pinar actions por SHA de commit e não por tag móvel é a disciplina a repetir)
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-PATTERNS.md` §"Shared Patterns" → "Encadeamento fail-closed em pretest/test:run/gate" e "CI com actions pinadas por SHA + uv instalado"
    - `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-VALIDATION.md` §"Sampling Rate" (o passo Python precisa caber em ~60 s; a fixture mede ~140–250 KB, nunca os 10 GB)
  </read_first>
  <action>
    Em `package.json`, acrescentar **toda** a família de scripts que os planos seguintes vão invocar —
    declarada aqui de uma vez para que `package.json` tenha um dono único nesta onda e nenhum plano
    posterior precise inventar nome divergente:

    - `"pipeline:test": "cd pipeline/sih && uv run pytest -q"`
    - `"pipeline:reconcile-gate": "cd pipeline/sih && uv run pytest tests/test_reconcile_gate.py -x -q"`
    - `"pipeline:enumerate": "cd pipeline/sih && uv run python -m sih_pipeline.cli enumerate"`
    - `"pipeline:download": "cd pipeline/sih && uv run python -m sih_pipeline.cli download"`
    - `"pipeline:aggregate": "cd pipeline/sih && uv run python -m sih_pipeline.cli aggregate"`
    - `"pipeline:population": "cd pipeline/sih && uv run python -m sih_pipeline.cli population"`
    - `"pipeline:reconcile": "cd pipeline/sih && uv run python -m sih_pipeline.cli reconcile"`
    - `"pipeline:upload": "cd pipeline/sih && uv run python -m sih_pipeline.cli upload"`
    - `"pipeline:partitions": "cd pipeline/sih && uv run python -m sih_pipeline.cli partitions"`
    - `"pipeline:oracle-scrape": "cd pipeline/sih && uv run python -m sih_pipeline.cli oracle-scrape"`
    - `"pipeline:audit": "cd pipeline/sih && uv run python -m sih_pipeline.cli audit"`
    - `"catalog:sih-schema": "node scripts/catalog/generateSihSchemaMigration.mjs"`
    - `"catalog:sih-swap-verify": "node scripts/catalog/generateSihSwapVerify.mjs"`
    - `"catalog:sih-packs": "node scripts/catalog/generateSihPacks.mjs"`

    Os subcomandos de `sih_pipeline.cli` e os três geradores Node ainda não existem nesta onda: os
    nomes ficam declarados de propósito, e os planos `09-03` a `09-14` escrevem contra este contrato
    fixo. Nesta onda só `pipeline:test` é exercitado pelo gate.

    **Regra de propriedade do `cli.py` — única, e válida para a fase inteira.** `package.json`
    declara o contrato; `pipeline/sih/src/sih_pipeline/cli.py` é o despachante que o cumpre, e tem
    **um único dono: o plano `09-04`**, que o cria na Onda 2 como despachante declarativo preguiçoso
    com os nove subcomandos já registrados. Nenhum outro plano da fase edita `cli.py`. Um subcomando
    nasce quando o plano dono do seu módulo entrega `main(argv: list[str]) -> int` no arquivo que ele
    já possui, e o despachante o resolve por `importlib` sem nenhuma edição. Enquanto esse `main` não
    existe, o subcomando sai com código 2 dizendo qual plano o entrega — nunca sai 0 fingindo sucesso.
    O mapa de donos é fixo e não pode ser inventado pelo executor:

    | script npm | subcomando | módulo que entrega `main(argv)` | plano dono |
    |---|---|---|---|
    | `pipeline:enumerate` | `enumerate` | `sih_pipeline/enumerate.py` | 09-04 |
    | `pipeline:download` | `download` | `sih_pipeline/download.py` | 09-04 |
    | `pipeline:oracle-scrape` | `oracle-scrape` | `sih_pipeline/oracle_scrape.py` | 09-05 |
    | `pipeline:aggregate` | `aggregate` | `sih_pipeline/aggregate.py` | 09-07 |
    | `pipeline:population` | `population` | `sih_pipeline/population.py` | 09-06 |
    | `pipeline:reconcile` | `reconcile` | `sih_pipeline/reconcile.py` | 09-08 |
    | `pipeline:partitions` | `partitions` | `sih_pipeline/partitions.py` | 09-09 |
    | `pipeline:upload` | `upload` | `sih_pipeline/upload.py` | 09-10 |
    | `pipeline:audit` | `audit` | `sih_pipeline/audit.py` | 09-12 |

    Os dois scripts restantes (`pipeline:test` e `pipeline:reconcile-gate`) invocam `pytest` direto e
    não passam pelo despachante. Nenhum script desta família pode sobreviver à fase apontando para
    subcomando inexistente: o `09-14` fecha um gate de contrato que falha se algum `pipeline:*` do
    `package.json` não resolver para um `main` chamável. Declarar contrato e nunca implementá-lo é a
    forma exata do defeito que esta fase existe para matar — não vale reproduzi-la no `package.json`.

    Encadear `pipeline:test` **antes** do `vitest run`, com `&&`, nas duas entradas que hoje chamam
    `catalog:validate`:
    `"pretest": "npm run catalog:validate && npm run pipeline:test"` e
    `"test:run": "npm run catalog:validate && npm run pipeline:test && vitest run"`.
    Não tocar em `gate` (ele já é `test:run && build`, portanto herda a mudança). Nunca usar `||`,
    nunca `--passWithNoTests`, nunca `|| true`: se a suíte Python sair não-zero, o `vitest` não roda.

    Em `.github/workflows/ci.yml`, inserir um passo `astral-sh/setup-uv` **entre** `setup-node` e
    `npm ci`. Resolver o SHA de verdade em vez de inventar: rodar
    `gh api repos/astral-sh/setup-uv/releases/latest --jq .tag_name` para obter a tag corrente e
    `gh api repos/astral-sh/setup-uv/git/ref/tags/<tag> --jq '.object.sha,.object.type'`; se o
    `object.type` for `tag` (tag anotada), dereferenciar com
    `gh api repos/astral-sh/setup-uv/git/tags/<sha> --jq .object.sha`. Escrever o passo como
    `uses: astral-sh/setup-uv@<sha-de-commit> # <tag>`, no mesmo formato dos dois `uses` já presentes,
    e com `with: enable-cache: true`. Manter `permissions: contents: read` inalterado.
    Se o `gh` não estiver autenticado, a task falha alto e pede autenticação — **não** cair para
    `@v7` nem para outra tag móvel.
  </action>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts;if(!s['pipeline:test'])throw new Error('pipeline:test ausente');for(const k of ['pretest','test:run']){if(!s[k].includes('npm run pipeline:test'))throw new Error(k+' nao encadeia pipeline:test');if(s[k].includes('||'))throw new Error(k+' usa || (nao fail-closed)');}if(!s['test:run'].indexOf('pipeline:test')<s['test:run'].indexOf('vitest run')){}console.log('ponte npm OK')" && grep -E 'astral-sh/setup-uv@[0-9a-f]{40}' .github/workflows/ci.yml && npm run pipeline:test</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contém a chave de script `pipeline:test` com valor `cd pipeline/sih && uv run pytest -q`
    - `node -e "const s=require('./package.json').scripts; for(const k of ['pipeline:test','pipeline:reconcile-gate','pipeline:enumerate','pipeline:download','pipeline:aggregate','pipeline:population','pipeline:reconcile','pipeline:upload','pipeline:partitions','pipeline:oracle-scrape','pipeline:audit','catalog:sih-schema','catalog:sih-swap-verify','catalog:sih-packs']) if(!s[k]){console.error('faltando',k);process.exit(1)}"` sai 0
    - `package.json` → `scripts["test:run"]` é exatamente `npm run catalog:validate && npm run pipeline:test && vitest run`
    - `package.json` → `scripts["pretest"]` é exatamente `npm run catalog:validate && npm run pipeline:test`
    - Todo script `pipeline:*` que invoca `sih_pipeline.cli` aparece no mapa de donos do `<action>` com um plano nominal; `pipeline:test` e `pipeline:reconcile-gate` são as duas únicas exceções, e chamam `pytest` direto
    - `grep -c '||' package.json` não aumenta em relação ao estado anterior nas linhas de `pretest`/`test:run`/`gate`
    - `.github/workflows/ci.yml` casa com a regex `astral-sh/setup-uv@[0-9a-f]{40}` (SHA de 40 hex, não tag)
    - O passo `setup-uv` aparece antes da linha `- run: npm ci` no arquivo
    - `grep -c 'permissions:' .github/workflows/ci.yml` continua 1 e o bloco continua `contents: read`
    - `npm run pipeline:test` sai 0
    - `npm run gate` sai 0
  </acceptance_criteria>
  <done>
    Uma falha da suíte Python derruba `npm run gate` localmente e no CI. O pipeline novo nasce dentro
    das garantias da Fase 7, não ao lado delas como os scrapers aposentados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → ambiente do pipeline | Dependências de terceiros entram na máquina que tem a credencial do banco |
| nome de arquivo vindo do FTP → filesystem local | `RD{UF}{AA}{MM}` vem de uma listagem remota e é usado para montar caminho de escrita |
| GitHub Actions marketplace → CI | Uma action referenciada por tag móvel pode ser reapontada para outro commit sem mudança neste repositório |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering | `paths.py` — escrita de parquet/ledger a partir de nome remoto | mitigate | `cache_path()` rejeita absoluto, `..` (inclusive no meio do caminho), string vazia e prefixo fora do allowlist de 6 diretórios; a asserção final compara o `resolve()` contra `cache_root()` (defesa contra symlink); provado por `tests/test_paths.py` com 9 casos |
| T-09-09 | Spoofing/Tampering | `pysus` e `psycopg` vindos do PyPI | mitigate | Pin exato com `==` (nunca `>=`/`~=`), `uv.lock` versionado, `slopcheck` já rodado no `09-RESEARCH.md` §"Package Legitimacy Audit" com veredito `Approved` para os dois — nenhum pacote `[ASSUMED]` ou `[SUS]`, portanto sem checkpoint humano de legitimidade |
| T-09-09b | Tampering | Atualização silenciosa de `pysus` para a 2.x defeituosa | mitigate | O README documenta o defeito medido e proíbe a atualização sem reproduzir o teste do spike §1; o critério de aceitação afirma a versão instalada exatamente igual a `1.0.1` |
| T-09-SC | Tampering | Cadeia de suprimentos de CI (`astral-sh/setup-uv`) | mitigate | Action pinada por SHA de commit de 40 hex resolvido ao vivo via `gh api`, com a tag apenas no comentário — mesma disciplina dos dois `uses` já presentes; `permissions: contents: read` preservado |
| T-09-15 | Denial of Service | Suíte Python alongando o loop de commit | accept | `09-VALIDATION.md` fixa o alvo em ~60 s e a fixture do gate em ~140–250 KB; risco residual aceito porque o alternativo (rodar fora do gate) é o modo de falha que a fase existe para matar |
</threat_model>

<verification>
- `cd pipeline/sih && uv run pytest -q` sai 0
- `npm run gate` sai 0 (encadeando `catalog:validate` → `pipeline:test` → `vitest run` → `build`)
- `grep -E 'astral-sh/setup-uv@[0-9a-f]{40}' .github/workflows/ci.yml` casa
- `git status --short` não lista `pipeline/sih/.venv/` nem `pipeline/sih/__pycache__/`
</verification>

<success_criteria>
- O pipeline Python existe, é reprodutível e roda em Python 3.11 com `pysus==1.0.1`
- Toda escrita de arquivo do pipeline passa por um guard V12 provado contra três formas de traversal
- `npm run gate` é fail-closed sobre a suíte Python, local e no CI
- Os dez arquivos de teste da Onda 0 existem, cada um declarando seu plano dono
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-confi-vel-coleta-completa/09-02-SUMMARY.md` when done
</output>
