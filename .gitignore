node_modules/
dist/
.DS_Store
*.log
.env
.env.local
*.tsbuildinfo

# Large reference / generated artifacts (keep out of git unless needed)
jasp-desktop-development/
trabalhos datasus/build/
trabalhos datasus/outputs/
trabalhos datasus/spreadsheet_work/
trabalhos datasus/tools/
trabalhos datasus/Congresso Vascular/

# Editor/agent scratch
.agents/
.cursor/

# Supabase CLI local state (Fase 8 Plan 5) — regenerated by `supabase link`/`db start`,
# never versioned. .local/ is reserved for the production data dump the ensaio local (08-08)
# will produce (centenas de MB) — must never enter git.
supabase/.branches/
supabase/.temp/
.local/
