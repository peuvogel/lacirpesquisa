"""Gate permanente: a reconciliação reproduz o subset do oráculo que AC/2019 prova — invocado
por `npm run gate` via `uv run pytest tests/test_reconcile_gate.py` — SC-7/D-06. Preenchido
pelo plano 09-11.
"""

import pytest

pytest.skip("preenchido pelo plano 09-11", allow_module_level=True)
