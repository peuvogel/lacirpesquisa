"""Prova a geração das partições JSON colunares por UF (gzip) para o Storage — D-20/D-21.
Preenchido pelo plano 09-09.
"""

import pytest

pytest.skip("preenchido pelo plano 09-09", allow_module_level=True)
