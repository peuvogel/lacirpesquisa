"""Prova o matcher CID→categoria contra as quatro formas de valor do lista-morb-cid.json
(prefixo simples, faixa 3 char, exato 4 char, faixa 4 char, composta) — porta testada do
cidmatch.py do spike 2026-08-04. Preenchido pelo plano 09-07.
"""

import pytest

pytest.skip("preenchido pelo plano 09-07", allow_module_level=True)
