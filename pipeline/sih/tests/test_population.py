"""Prova a coleta de população (fonte POPSVS, não POP/POPTCU) por UF e município, total e
estratificada por sexo/idade — D-24. Preenchido pelo plano 09-06.
"""

import pytest

pytest.skip("preenchido pelo plano 09-06", allow_module_level=True)
