"""Prova que reexecutar a coleta após uma interrupção não duplica linhas no ledger local —
PIPE-03/SC-3. Preenchido pelo plano 09-04.
"""

import pytest

pytest.skip("preenchido pelo plano 09-04", allow_module_level=True)
