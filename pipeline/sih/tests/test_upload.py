"""Prova que o cache bruto só é apagado depois que a contagem re-lida do Supabase confere —
PIPE-04/SC-4. Preenchido pelo plano 09-10.
"""

import pytest

pytest.skip("preenchido pelo plano 09-10", allow_module_level=True)
