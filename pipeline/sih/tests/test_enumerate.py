"""Prova que a ausência de um arquivo esperado no FTP é falha ruidosa (saída não-zero),
nunca "OK · 0 linhas" — PIPE-01/SC-1. Preenchido pelo plano 09-04.
"""

import pytest

pytest.skip("preenchido pelo plano 09-04", allow_module_level=True)
