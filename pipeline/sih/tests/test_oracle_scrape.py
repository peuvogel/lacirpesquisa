"""Prova o raspador mínimo do oráculo TabNet (D-18), usado para re-raspar ao vivo a fixture
de reconciliação AC/2019 (D-04). Preenchido pelo plano 09-05.
"""

import pytest

pytest.skip("preenchido pelo plano 09-05", allow_module_level=True)
