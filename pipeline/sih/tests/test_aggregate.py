"""Prova que `taxa_mortalidade` é derivável do campo `MORTE` durante a agregação —
DATA-03. Preenchido pelo plano 09-07.
"""

import pytest

pytest.skip("preenchido pelo plano 09-07", allow_module_level=True)
